---
phase: 07-design-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/design/SKILL.md
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-07
  - DSGN-08
  - DSGN-09

must_haves:
  truths:
    - "Design skill checks for research_complete phase before proceeding"
    - "Design skill reads SCOPE.md and SYNTHESIS.md artifacts to extract DA list and evidence"
    - "Design skill reads override-context.md if it exists and flags affected DAs"
    - "Design skill generates RFC format for engineering intent and PRD format for product intent"
    - "Every DA from scope has a corresponding design decision section in the generated output"
    - "Each design decision references specific evidence from SYNTHESIS.md"
    - "DESIGN.md is written to .expedite/design/DESIGN.md"
  artifacts:
    - path: "skills/design/SKILL.md"
      provides: "Design orchestration Steps 1-5"
      min_lines: 200
  key_links:
    - from: "skills/design/SKILL.md"
      to: ".expedite/scope/SCOPE.md"
      via: "Step 2 reads DA list, evidence requirements, readiness criteria"
    - from: "skills/design/SKILL.md"
      to: ".expedite/research/SYNTHESIS.md"
      via: "Step 2 reads per-DA findings and evidence references"
    - from: "skills/design/SKILL.md"
      to: "skills/design/references/prompt-design-guide.md"
      via: "Step 4 reads design guide as quality reference before generation"
---

<objective>
Replace the design SKILL.md stub with Steps 1-5: prerequisite check, scope+synthesis artifact loading, design state initialization, inline design document generation (RFC for engineering / PRD for product), and DESIGN.md artifact writing.

Purpose: Establish the core design generation pipeline — the skill reads research artifacts, generates a format-correct design document where every DA has an evidence-backed decision, and writes the artifact.
Output: `skills/design/SKILL.md` with Steps 1-5 implemented
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-skill/07-CONTEXT.md
@.planning/phases/07-design-skill/07-RESEARCH.md
@skills/design/references/prompt-design-guide.md
@skills/scope/SKILL.md
@skills/research/SKILL.md
@templates/state.yml.template
@skills/research/references/ref-override-handling.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace design SKILL.md stub with Steps 1-3 (prerequisite, artifact loading, state init)</name>
  <files>skills/design/SKILL.md</files>
  <action>
Replace the design SKILL.md stub body (everything after the frontmatter and `!cat` injection line) with the full design orchestration. Keep the existing frontmatter EXACTLY as-is (name, description, allowed-tools, argument-hint). Keep the `!cat .expedite/state.yml` injection line EXACTLY as-is.

Replace the stub body (`# Design Skill` through the end) with:

**Preamble** (matches scope/research pattern):
```
# Design Skill

You are the Expedite design orchestrator. Your job is to generate a design document that translates research evidence into actionable design decisions. You are the third stage of the contract chain: every decision area and evidence finding from research flows through you to create design decisions that the plan skill must implement. Every design decision must reference the evidence that justifies it — no decision without evidence.

**Interaction model:** Use freeform prompts for revision feedback (design feedback is inherently open-ended — "change DA-3 to use approach B instead"). Use AskUserQuestion only for structured choices (override approval, proceed-to-gate confirmation).

**After completing each step, proceed to the next step automatically.** Do not wait for explicit "next step" instructions from the user between steps unless the step specifically calls for user input.

## Instructions
```

**Step 1: Prerequisite Check** — Look at the injected lifecycle state above.
- Case A: Phase is NOT "research_complete" → Display error: "Error: Research is not complete. Run `/expedite:research` to gather evidence before starting design.\n\nCurrent phase: {phase}" → STOP.
- Case B: Phase IS "research_complete" → Display "Starting design phase..." → Proceed to Step 2.

**Step 2: Read Scope + Research Artifacts** — Read these files:
1. `.expedite/scope/SCOPE.md` — Extract the full list of Decision Areas (DA-1 through DA-N) with their names, depth calibration (Deep/Standard/Light), evidence requirements, and readiness criteria.
2. `.expedite/research/SYNTHESIS.md` — Extract per-DA findings: key findings, trade-offs, contradictions, gaps, evidence file references.
3. `.expedite/state.yml` — Extract `project_name`, `intent`, `lifecycle_id`, `questions` array (for status reference).
4. **If `.expedite/research/override-context.md` exists** — Read it. Extract affected DAs and severity. These DAs need lower-confidence flagging in the design document.

Display artifact loading summary:
```
--- Design Context Loaded ---

Project: {project_name}
Intent: {intent}
Decision Areas: {count} (DA-1 through DA-{N})
{If override context exists:} Override advisory: {severity} severity — {count} affected DAs
```

Verify all DAs from SCOPE.md appear in SYNTHESIS.md. If any DA is missing from synthesis, note it — the design must still address it with an explicit "insufficient evidence" flag and Low confidence.

**Step 3: Initialize Design State** — Update state.yml using the backup-before-write pattern (read current → cp state.yml state.yml.bak → modify → write entire file):
- Set `phase` to `"design_in_progress"`
- Set `last_modified` to current ISO 8601 UTC timestamp

Create the design output directory: `mkdir -p .expedite/design/`

Display: "Design phase initialized. Generating design document..."
  </action>
  <verify>
Read the modified SKILL.md. Verify: (1) frontmatter preserved exactly, (2) !cat injection preserved, (3) Step 1 has both Case A and Case B, (4) Step 2 reads all 4 files (SCOPE.md, SYNTHESIS.md, state.yml, override-context.md), (5) Step 3 uses backup-before-write pattern and transitions to design_in_progress.
  </verify>
  <done>Steps 1-3 exist with prerequisite check (research_complete required), artifact loading (scope + synthesis + optional override context), and state initialization (backup-before-write, design_in_progress transition, mkdir design directory).</done>
</task>

<task type="auto">
  <name>Task 2: Append Steps 4-5 (design generation and DESIGN.md writing)</name>
  <files>skills/design/SKILL.md</files>
  <action>
Append Steps 4-5 after Step 3.

**Step 4: Generate Design Document** — This is inline generation in the main session (DSGN-01). Read `skills/design/references/prompt-design-guide.md` as a quality reference (use Glob with `**/prompt-design-guide.md` if the direct path fails). The design guide defines the required sections, quality criteria, and contract chain enforcement — but is NOT dispatched as a subagent. Its instructions guide the inline generation.

The LLM generates the design document following the correct format for the declared intent:

**If intent is "engineering"** — RFC format with these sections in order:
1. Context and Scope
2. Goals and Non-Goals
3. Design Decisions by DA — For EACH DA from SCOPE.md (DA-1 through DA-N):
   - **Decision:** The specific architectural or implementation choice
   - **Evidence:** Citations to SYNTHESIS.md findings and evidence files (e.g., "evidence-batch-01.md Finding 3")
   - **Alternatives Considered:** Other approaches evaluated with evidence for/against each
   - **Trade-offs:** What we gain and what we sacrifice
   - **Confidence:** High/Medium/Low based on evidence quality. If DA was in override-context.md, mark as Low with note "affected by G2 override"
   Target: 200-400 words per DA (per user decision)
4. Detailed Design
5. Cross-Cutting Concerns
6. Migration/Compatibility (if applicable)
7. Open Questions

**If intent is "product"** — PRD format with these sections in order:
1. Problem Statement
2. Personas
3. User Stories
4. User Flows
5. Design Decisions by DA — Same per-DA structure as RFC but with product-oriented framing:
   - **Decision:** What we decided
   - **Evidence basis:** Citations to evidence files
   - **Trade-offs:** Alternatives considered
   - **Confidence:** High/Medium/Low
   Target: 200-400 words per DA (per user decision)
6. Success Metrics
7. Scope Boundaries
8. Open Questions

**Contract chain enforcement (both intents):**
- Every DA from SCOPE.md MUST have a corresponding `### DA-N: {Name}` section (DSGN-08)
- Each decision MUST cite specific evidence files and findings from SYNTHESIS.md — not just "research showed that..." (DSGN-09)
- If evidence is insufficient for a DA, acknowledge the gap explicitly, state what additional evidence would change the decision, and mark confidence as Low
- Where evidence conflicts, present both perspectives and state which was chosen and why

Include a high-level summary/overview section (3-5 sentences) before the per-DA decisions summarizing the overall design direction.

**Step 5: Write DESIGN.md** — Write the generated design document to `.expedite/design/DESIGN.md`.

Document header (both formats):
```markdown
# {Technical Design | Product Design}: {project_name}
Generated: {ISO 8601 UTC timestamp}
Intent: {Engineering | Product}
Source: SCOPE.md + SYNTHESIS.md
```

After writing, display:
```
Design document written to .expedite/design/DESIGN.md

--- Design Summary ---
Project: {project_name}
Intent: {intent}
Decision Areas covered: {N}/{M} (should be N/N if all DAs addressed)
Format: {RFC | PRD}
{If override context:} Override-affected DAs flagged with Low confidence: {list}
```

If the count of DAs in DESIGN.md does not match the count from SCOPE.md, display a warning: "WARNING: {missing_count} Decision Areas not addressed in design. Missing: {list}. These must be added before G3 gate."
  </action>
  <verify>
Read the modified SKILL.md. Verify: (1) Step 4 reads prompt-design-guide.md with Glob fallback, (2) both RFC and PRD format sections are defined with all required subsections, (3) per-DA structure includes Decision, Evidence, Trade-offs, Confidence fields, (4) 200-400 words per DA guidance present, (5) contract chain enforcement rules are explicit (every DA must have a section, every decision must cite evidence), (6) override-context.md influence on confidence levels is documented, (7) Step 5 writes to .expedite/design/DESIGN.md, (8) DA count validation is performed after writing.
  </verify>
  <done>Steps 4-5 exist with inline design generation (not subagent, DSGN-01), intent-conditional format (RFC for engineering / PRD for product, DSGN-02), per-DA design decisions with evidence citations (DSGN-08, DSGN-09), 200-400 word budget per DA, override context integration, DESIGN.md artifact writing (DSGN-07), and DA count validation.</done>
</task>

</tasks>

<verification>
1. `grep -c "### Step" skills/design/SKILL.md` returns 5 (Steps 1-5)
2. `grep "research_complete" skills/design/SKILL.md` finds prerequisite check
3. `grep "SCOPE.md" skills/design/SKILL.md` finds scope artifact reading
4. `grep "SYNTHESIS.md" skills/design/SKILL.md` finds synthesis artifact reading
5. `grep "override-context.md" skills/design/SKILL.md` finds override context handling
6. `grep "design_in_progress" skills/design/SKILL.md` finds state transition
7. `grep "prompt-design-guide.md" skills/design/SKILL.md` finds design guide reference read
8. `grep "DESIGN.md" skills/design/SKILL.md` finds artifact writing
9. `grep "RFC" skills/design/SKILL.md` AND `grep "PRD" skills/design/SKILL.md` both return matches
10. Design SKILL.md frontmatter is unchanged from Phase 1 stub
</verification>

<success_criteria>
- Design SKILL.md has Steps 1-5 replacing the stub body
- Step 1 blocks on non-research_complete phase
- Step 2 reads SCOPE.md, SYNTHESIS.md, state.yml, and optionally override-context.md
- Step 3 transitions state to design_in_progress with backup-before-write
- Step 4 generates RFC (engineering) or PRD (product) format inline with per-DA decisions citing evidence
- Step 5 writes DESIGN.md and validates DA coverage
- Frontmatter and !cat injection preserved from Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-skill/07-01-SUMMARY.md`
</output>
