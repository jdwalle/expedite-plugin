---
phase: 07-design-skill
plan: 03
type: execute
wave: 1
depends_on:
  - 07-02
files_modified:
  - skills/design/SKILL.md
autonomous: false
requirements:
  - DSGN-06

must_haves:
  truths:
    - "G3 gate has MUST criteria including 'every DA has a decision' and 'decisions reference evidence'"
    - "G3 gate has SHOULD criteria for quality assessment with anti-bias instructions"
    - "Gate outcomes mirror G2: Go, Go-with-advisory, Recycle, Override"
    - "Recycle uses escalation messaging from ref-recycle-escalation.md pattern"
    - "Override writes .expedite/design/override-context.md with affected DAs and severity"
    - "Design completion transitions state to design_complete"
    - "Human verifies the complete design skill reads correctly end-to-end"
  artifacts:
    - path: "skills/design/SKILL.md"
      provides: "Complete design orchestration Steps 1-10"
      min_lines: 400
  key_links:
    - from: "skills/design/SKILL.md Step 8"
      to: ".expedite/design/DESIGN.md"
      via: "G3 gate evaluates the written artifact (not in-memory content)"
    - from: "skills/design/SKILL.md Step 9"
      to: ".expedite/state.yml"
      via: "Gate history recording with backup-before-write"
    - from: "skills/design/SKILL.md Step 9"
      to: ".expedite/design/override-context.md"
      via: "Written on G3 override with affected DAs and severity"
---

<objective>
Append Steps 8-10 to the design SKILL.md: G3 gate evaluation with MUST/SHOULD criteria, gate outcome handling (Go/Go-advisory/Recycle/Override with escalation), and design completion. Then human-verify the complete skill end-to-end.

Purpose: Close the design phase with quality gate validation, proper gate outcome routing, override context propagation for downstream phases, and human verification of the complete 10-step skill.
Output: `skills/design/SKILL.md` complete with Steps 1-10 + human verification
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-skill/07-CONTEXT.md
@.planning/phases/07-design-skill/07-RESEARCH.md
@.planning/phases/07-design-skill/07-01-SUMMARY.md
@.planning/phases/07-design-skill/07-02-SUMMARY.md
@skills/research/SKILL.md
@skills/research/references/ref-recycle-escalation.md
@skills/research/references/ref-override-handling.md
@templates/state.yml.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Steps 8-10 (G3 gate, outcome handling, completion)</name>
  <files>skills/design/SKILL.md</files>
  <action>
Append Steps 8-10 after Step 7 in the design SKILL.md.

**Step 8: G3 Gate Evaluation**

G3 evaluates the design document quality with inline LLM judgment. Unlike G1 (purely structural) and G2 (count-based), G3 requires reading DESIGN.md and evaluating content quality. Use explicit anti-bias instructions: "Evaluate as if someone else produced this design. For each criterion, state the specific evidence from the artifact that supports your pass/fail determination."

Read `.expedite/design/DESIGN.md` and `.expedite/scope/SCOPE.md` (for DA reference).

**MUST criteria (all must pass for Go):**

| # | Criterion | How to Check | Result |
|---|-----------|-------------|--------|
| M1 | Every DA has a design decision | Count DAs in SCOPE.md, count `### DA-` sections in DESIGN.md. State: "Found {N}/{M} DAs covered" | PASS/FAIL |
| M2 | Every decision references evidence | For each DA section, check for evidence citations (evidence file references or SYNTHESIS.md finding references). State: "{N}/{M} decisions cite evidence" | PASS/FAIL |
| M3 | Correct format for intent | Check document structure matches PRD (product) or RFC (engineering) required sections from design guide. State: "Found {N}/{M} required sections" | PASS/FAIL |
| M4 | DESIGN.md exists and is non-empty | File exists and has substantive content (not just headers). State: "DESIGN.md: {line_count} lines" | PASS/FAIL |

**SHOULD criteria (failures produce advisory, not block):**

| # | Criterion | How to Check | Result |
|---|-----------|-------------|--------|
| S1 | Trade-offs articulated for each DA | Check each DA section has trade-offs content (not just "we chose X"). State: "{N}/{M} DAs have trade-offs" | PASS/ADVISORY |
| S2 | Confidence levels assigned per DA | Each DA decision has High/Medium/Low confidence. State: "{N}/{M} DAs have confidence" | PASS/ADVISORY |
| S3 | Open questions section is genuine | Check Open Questions contains genuine uncertainties (not manufactured padding). Use LLM judgment. | PASS/ADVISORY |
| S4 | HANDOFF.md exists (product intent only) | If intent == product, check `.expedite/design/HANDOFF.md` exists with 9 sections. If engineering, auto-PASS. | PASS/ADVISORY |

Display gate results:
```
--- G3 Gate Evaluation ---
(Evaluated as if produced by someone else)

MUST Criteria:
  M1: {PASS|FAIL} — {evidence}
  M2: {PASS|FAIL} — {evidence}
  M3: {PASS|FAIL} — {evidence}
  M4: {PASS|FAIL} — {evidence}

SHOULD Criteria:
  S1: {PASS|ADVISORY} — {evidence}
  S2: {PASS|ADVISORY} — {evidence}
  S3: {PASS|ADVISORY} — {evidence}
  S4: {PASS|ADVISORY} — {evidence}

Gate Outcome: {Go | Go-with-advisory | Recycle}
```

**Gate outcomes:**
- **Go**: All MUST pass AND all SHOULD pass
- **Go-with-advisory**: All MUST pass, one or more SHOULD failures
- **Recycle**: Any MUST criterion fails
- **Override**: Only when user explicitly requests it (not auto-determined)

Proceed to Step 9.

**Step 9: Gate Outcome Handling**

**9a: Record gate history.** Append to `gate_history` in state.yml (backup-before-write):

```yaml
- gate: "G3"
  timestamp: "{ISO 8601 UTC}"
  outcome: "{go|go_advisory|recycle|override}"
  must_passed: {count}
  must_failed: {count}
  should_passed: {count}
  should_failed: {count}
  notes: "{brief summary}"
  overridden: false
```

**9b: Route by outcome:**

**Go** → Display "G3 gate passed. Design is ready for planning." → Proceed to Step 10.

**Go-with-advisory** → Show user which SHOULD criteria failed. Display: "Design passed with advisories. These will be noted in the plan phase." Present via freeform prompt: "1. Proceed with advisory | 2. Revise to address advisories". If proceed → proceed to Step 10. If revise → return to Step 7 (revision cycle).

**Recycle** → Adapt recycle escalation from the research skill's pattern (ref-recycle-escalation.md). Count previous G3 recycle outcomes in gate_history.

- **1st Recycle (recycle_count == 0):** Informational tone. Display which MUST criteria failed with specific evidence. Offer via freeform prompt: "1. Revise (fix the issues above) | 2. Override (proceed with documented gaps)". If revise → return to Step 7 with the gate feedback visible. If override → proceed to 9c.

- **2nd Recycle (recycle_count == 1):** Suggest adjustment. Display: "This is the second G3 recycle. Consider whether the design scope needs adjustment." Show persistently failing criteria. Offer: "1. Revise | 2. Override (recommended if same criteria keep failing)". If revise → return to Step 7. If override → proceed to 9c.

- **3rd+ Recycle (recycle_count >= 2):** Recommend override. Display: "This is recycle #{recycle_count + 1}. Recommend overriding the gate. Remaining issues may require scope adjustment rather than design revision." Offer: "1. Override (recommended) | 2. One more attempt (not recommended)". If override → 9c. If attempt → return to Step 7.

**9c: Override handling.** Follow the override pattern from ref-override-handling.md adapted for G3:

1. Update gate_history entry: set `overridden: true`, update `outcome` to `"override"`.
2. Determine severity based on MUST failures: low (0 MUST failures — user-initiated override), medium (1 MUST failure), high (2+ MUST failures).
3. Write `.expedite/design/override-context.md`:

```markdown
# G3 Override Context

Timestamp: {ISO 8601 UTC}
Severity: {low|medium|high}
Recycle count: {N}

## Overridden Issues

{For each failed MUST criterion:}
- {criterion_id}: {criterion description}
  Evidence: {what the gate found}
  Impact: {which DAs are affected}

## Plan Phase Advisory

The following design quality issues were overridden. The plan phase should
account for these gaps when creating tasks.
{List affected criteria and recommendations}
```

4. Proceed to Step 10.

**Step 10: Design Completion**

Update state.yml (backup-before-write): set `phase` to `"design_complete"`, update `last_modified`.

Display design completion summary:

```
## Design Complete

Project: {project_name}
Intent: {intent}

### Artifacts Produced
- Design: .expedite/design/DESIGN.md
{If product intent:} - Handoff: .expedite/design/HANDOFF.md
{If override:} - Override context: .expedite/design/override-context.md

### Gate Results
G3 outcome: {outcome}
MUST: {passed}/{total} | SHOULD: {passed}/{total}
{If advisory:} Advisories: {list SHOULD failures}
{If override:} Override severity: {severity}

### Contract Chain
Scope → Research → Design ✓
Decision Areas: {N} designed | Evidence citations: {count}

### Next Step
Run `/expedite:plan` to generate an implementation plan from the design.
```
  </action>
  <verify>
Read the modified SKILL.md. Verify: (1) Step 8 has 4 MUST + 4 SHOULD criteria with evidence requirements, (2) anti-bias instruction present ("evaluate as if someone else produced this"), (3) gate outcomes match G2 pattern (Go/Go-advisory/Recycle/Override), (4) Step 9 records gate history in state.yml with backup-before-write, (5) recycle escalation has 3 tiers (informational/suggest/recommend), (6) override writes .expedite/design/override-context.md with severity, (7) Step 10 transitions to design_complete, (8) completion summary includes artifacts, gate results, contract chain status, and next step.
  </verify>
  <done>Steps 8-10 exist with G3 gate (4 MUST + 4 SHOULD criteria, anti-bias, per-criterion evidence), gate outcome routing matching G2 pattern, escalating recycle messaging, override context propagation to plan phase, and design completion with state transition to design_complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete design skill</name>
  <files>skills/design/SKILL.md</files>
  <action>
Present the complete design SKILL.md for human review. The skill should have:
- Preserved frontmatter and !cat injection from Phase 1
- 10 coherent steps covering the full orchestration flow:
  1. Prerequisite check (research_complete required)
  2. Scope + synthesis + override context artifact loading
  3. State initialization (design_in_progress)
  4. Inline design generation (RFC for engineering, PRD for product)
  5. DESIGN.md writing with DA coverage validation
  6. HANDOFF.md generation (product intent only, 9 sections)
  7. Freeform revision cycle with change summaries
  8. G3 gate with 4 MUST + 4 SHOULD criteria and anti-bias
  9. Gate outcome handling (Go/Go-advisory/Recycle/Override with escalation)
  10. Design completion (design_complete transition)
- Per-DA design decisions citing evidence (contract chain enforcement)
- Override context propagation (read from research, write for plan phase)
- No hard revision round limit

Optionally invoke `/expedite:design` to verify discoverability (will error on prerequisite check since no research exists, which is correct).
  </action>
  <verify>Human types "approved" confirming the skill reads correctly end-to-end</verify>
  <done>Human has verified the complete 10-step design orchestration skill</done>
</task>

</tasks>

<verification>
1. `grep -c "### Step" skills/design/SKILL.md` returns 10 (Steps 1-10)
2. `grep "G3" skills/design/SKILL.md` finds gate evaluation and history recording
3. `grep "MUST" skills/design/SKILL.md` finds MUST criteria (M1-M4)
4. `grep "SHOULD" skills/design/SKILL.md` finds SHOULD criteria (S1-S4)
5. `grep "override-context.md" skills/design/SKILL.md` finds both reading (Step 2) and writing (Step 9c)
6. `grep "design_complete" skills/design/SKILL.md` finds completion transition
7. `grep "evaluate as if someone else" skills/design/SKILL.md` finds anti-bias instruction
8. `grep "recycle" skills/design/SKILL.md` finds escalation handling
9. `wc -l skills/design/SKILL.md` returns 400+ lines
10. Frontmatter unchanged, !cat injection intact
</verification>

<success_criteria>
- G3 gate has M1 (DA coverage), M2 (evidence references), M3 (format correctness), M4 (artifact exists) as MUST criteria
- SHOULD criteria cover trade-offs, confidence levels, open questions quality, HANDOFF.md
- Gate outcomes mirror G2: Go, Go-with-advisory, Recycle, Override
- Recycle escalation adapts ref-recycle-escalation.md for design context
- Override writes .expedite/design/override-context.md for plan phase consumption
- Design completion transitions state to design_complete
- Human confirms the complete 10-step skill reads correctly
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-skill/07-03-SUMMARY.md`
</output>
