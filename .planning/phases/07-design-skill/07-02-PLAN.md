---
phase: 07-design-skill
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - skills/design/SKILL.md
autonomous: true
requirements:
  - DSGN-03
  - DSGN-04
  - DSGN-05

must_haves:
  truths:
    - "Product-intent lifecycles produce HANDOFF.md with 9 sections referencing DESIGN.md"
    - "HANDOFF.md is a distillation, not a standalone duplicate of DESIGN.md"
    - "User can provide freeform revision feedback to modify the design document"
    - "Each revision summarizes changes before rewriting DESIGN.md"
    - "Revision loop offers both 'revise' and 'proceed to gate' options every iteration"
    - "No hard limit enforced on revision rounds"
  artifacts:
    - path: "skills/design/SKILL.md"
      provides: "Design orchestration Steps 6-7 appended"
      min_lines: 300
  key_links:
    - from: "skills/design/SKILL.md Step 6"
      to: ".expedite/design/HANDOFF.md"
      via: "Write HANDOFF.md for product intent only"
    - from: "skills/design/SKILL.md Step 7"
      to: ".expedite/design/DESIGN.md"
      via: "Rewrite DESIGN.md on each revision with change summary"
---

<objective>
Append Steps 6-7 to the design SKILL.md: HANDOFF.md generation for product intent, and freeform revision cycle with change summaries.

Purpose: Complete the interactive design workflow — product-intent gets an engineer handoff document, and all users can iteratively refine the design before gate evaluation.
Output: `skills/design/SKILL.md` with Steps 6-7 appended
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-design-skill/07-CONTEXT.md
@.planning/phases/07-design-skill/07-RESEARCH.md
@.planning/phases/07-design-skill/07-01-SUMMARY.md
@skills/design/references/prompt-design-guide.md
@skills/scope/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Step 6 (HANDOFF.md generation for product intent)</name>
  <files>skills/design/SKILL.md</files>
  <action>
Append Step 6 after Step 5 in the design SKILL.md.

**Step 6: Generate HANDOFF.md (Product Intent Only)**

Check the `intent` value from state.yml (loaded in Step 2).

**If intent is "engineering":** Skip this step entirely. Display: "Engineering intent — no HANDOFF.md needed. Proceeding to revision cycle." → Proceed to Step 7.

**If intent is "product":** Generate HANDOFF.md as a distillation of DESIGN.md (per user decision: HANDOFF.md references DESIGN.md for deeper rationale — it is NOT a standalone document).

HANDOFF.md has 9 sections targeting individual engineers as the audience:

1. **Problem Statement** — Compressed from DESIGN.md Problem Statement section. 100-150 words. End with: "See DESIGN.md § Problem Statement for full evidence basis."
2. **Key Decisions** — List each DA's decision as a one-liner with confidence level. These are LOCKED constraints from the design. Format: "DA-N: {decision summary} (Confidence: {High/Medium/Low})". End with: "See DESIGN.md § Design Decisions for rationale and evidence."
3. **Scope Boundaries** — In/out of scope, compressed from DESIGN.md. 50-100 words.
4. **Success Metrics** — Observable, measurable metrics from DESIGN.md. Bulleted list.
5. **User Flows** — Compressed from DESIGN.md User Flows. Key flows only, not exhaustive.
6. **Acceptance Criteria** — Testable Given/When/Then format derived from design decisions. Each criterion traces to a DA.
7. **Assumptions and Constraints** — Technical implications of product decisions. Include any implementation suggestions where research evidence supports them (per discretion recommendation).
8. **Suggested Engineering Questions** — Seed questions for an engineering lifecycle if one follows. Derived from Open Questions in DESIGN.md plus any gaps flagged during design.
9. **Priority Ranking for Trade-offs** — Guidance on what to prioritize when trade-offs arise during implementation. Ordered list.

Each section should be concise (100-200 words per section, ~1500 words total). Include explicit cross-references to DESIGN.md sections (e.g., "See DESIGN.md § Design Decisions by DA for full rationale").

Document header:
```markdown
# Product Handoff: {project_name}
Generated: {ISO 8601 UTC timestamp}
Intent: Product → Engineering Handoff
Source: DESIGN.md
Full design: .expedite/design/DESIGN.md
```

Write to `.expedite/design/HANDOFF.md`.

Display: "HANDOFF.md written to .expedite/design/HANDOFF.md ({word_count} words, 9 sections)"

If HANDOFF.md is longer than DESIGN.md, display warning: "WARNING: HANDOFF.md ({handoff_words} words) is longer than DESIGN.md ({design_words} words). HANDOFF.md should be a concise distillation. Consider trimming."

Proceed to Step 7.
  </action>
  <verify>
Read the modified SKILL.md. Verify: (1) Step 6 checks intent before generating, (2) engineering intent skips HANDOFF.md, (3) all 9 sections are defined with names matching prompt-design-guide.md, (4) each section has word budget guidance (100-200 words), (5) cross-references to DESIGN.md are required ("See DESIGN.md §..."), (6) HANDOFF.md written to .expedite/design/HANDOFF.md, (7) length comparison warning exists.
  </verify>
  <done>Step 6 generates HANDOFF.md for product intent only with 9 sections, cross-references to DESIGN.md, concise word budgets, and length validation. Engineering intent cleanly skips the step.</done>
</task>

<task type="auto">
  <name>Task 2: Append Step 7 (freeform revision cycle)</name>
  <files>skills/design/SKILL.md</files>
  <action>
Append Step 7 after Step 6 in the design SKILL.md.

**Step 7: Revision Cycle**

Present the design document to the user for review. This is a freeform revision loop with no hard round limit (per user decision — the "up to 2 rounds" in DSGN-03 is a soft expectation, not enforced).

**7a: Present design for review.** Display:

```
--- Design Review ---

The design document has been written to .expedite/design/DESIGN.md
{If product intent:} HANDOFF.md has been written to .expedite/design/HANDOFF.md

Review the design document above. You can:
- **revise** — describe changes you'd like (e.g., "change DA-3 to use approach B", "strengthen the caching rationale in DA-5")
- **proceed** — run G3 gate evaluation on the current design

What would you like to do?
```

**7b: On "revise" (user provides feedback):**

1. Parse the user's freeform feedback. Identify which DAs or sections they want changed.
2. Apply the changes to the in-memory design document.
3. **Summarize changes before rewriting** (per user decision). Display a change summary:

```
Changes applied:
- DA-{N}: {brief description of change}
- DA-{M}: {brief description of change}
- {Section}: {brief description of change}
```

4. Rewrite `.expedite/design/DESIGN.md` with the updated content.
5. If product intent AND changes affect sections mirrored in HANDOFF.md, also rewrite `.expedite/design/HANDOFF.md` with corresponding updates.
6. Re-validate DA coverage: verify every DA from SCOPE.md still has a section in the updated DESIGN.md.
7. Return to 7a — present the revision prompt again.

**7c: On "proceed" (user wants gate evaluation):**

Display: "Proceeding to G3 gate evaluation..."

Proceed to Step 8.

**Key behaviors:**
- No round counter displayed. No "you have N revisions remaining" messaging.
- Every iteration of the loop presents both "revise" and "proceed" options.
- Interpret "looks good", "done", "approved", "no changes", "yes", "lgtm" as "proceed" — do NOT ask for more revisions when the user signals satisfaction.
- Interpret "skip" or "skip to gate" as "proceed".
- If user feedback is ambiguous, ask for clarification rather than guessing.
  </action>
  <verify>
Read the modified SKILL.md. Verify: (1) Step 7 has subsections 7a, 7b, 7c, (2) revision prompt offers both "revise" and "proceed" options, (3) change summary is generated before rewriting (per user decision), (4) DESIGN.md is rewritten on each revision, (5) HANDOFF.md updated if product intent and affected, (6) DA coverage re-validated after each revision, (7) no hard round limit or counter, (8) "looks good"/"done"/"lgtm" mapped to "proceed", (9) loop returns to 7a after each revision.
  </verify>
  <done>Step 7 implements freeform revision cycle (DSGN-03, DSGN-04) with change summaries before rewriting (per user decision), both "revise" and "proceed" options on every iteration, no hard round limit, HANDOFF.md co-update for product intent, DA coverage re-validation, and natural language mapping for "proceed" signals.</done>
</task>

</tasks>

<verification>
1. `grep -c "### Step" skills/design/SKILL.md` returns 7 (Steps 1-7)
2. `grep "HANDOFF.md" skills/design/SKILL.md` finds HANDOFF.md generation and co-update
3. `grep "9 sections" skills/design/SKILL.md` OR individual section names found in Step 6
4. `grep "revise" skills/design/SKILL.md` finds revision prompt
5. `grep "proceed" skills/design/SKILL.md` finds proceed-to-gate option
6. `grep "Changes applied" skills/design/SKILL.md` finds change summary pattern
7. `grep "product" skills/design/SKILL.md` finds intent check for HANDOFF.md
8. No "round limit" or "revision count" enforcement found in Step 7
</verification>

<success_criteria>
- Step 6 generates HANDOFF.md with 9 sections for product intent only, referencing DESIGN.md
- Step 7 implements freeform revision cycle with change summaries, no hard round limit
- User can "revise" or "proceed" at every iteration
- HANDOFF.md updated when product-intent revisions affect shared content
- Natural language signals ("looks good", "lgtm", "done") correctly mapped to "proceed"
</success_criteria>

<output>
After completion, create `.planning/phases/07-design-skill/07-02-SUMMARY.md`
</output>
