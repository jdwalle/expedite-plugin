---
phase: 18-gate-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/research/SKILL.md
  - skills/design/SKILL.md
autonomous: true
requirements:
  - GATE-01
  - GATE-02

must_haves:
  truths:
    - "G2 gate evaluation checks that every DA readiness criterion is marked MET in SYNTHESIS.md"
    - "G2 M5 failure triggers Recycle (MUST criterion)"
    - "G3 gate evaluation checks that evidence citations in DESIGN.md address DA-specific readiness criteria from SCOPE.md"
    - "G3 M5 failure triggers Recycle (MUST criterion)"
    - "Previously passing G2 and G3 gates continue to pass (no regression)"
  artifacts:
    - path: "skills/research/SKILL.md"
      provides: "G2 gate with M5 DA readiness criterion"
      contains: "M5"
    - path: "skills/design/SKILL.md"
      provides: "G3 gate with M5 evidence-readiness criterion"
      contains: "M5"
  key_links:
    - from: "skills/research/SKILL.md G2 M5"
      to: "SYNTHESIS.md readiness status lines"
      via: "Pattern match on 'Readiness status:' per DA section"
      pattern: "Readiness status.*MET"
    - from: "skills/design/SKILL.md G3 M5"
      to: "SCOPE.md readiness criteria + DESIGN.md evidence citations"
      via: "Cross-reference readiness criterion text against cited evidence"
      pattern: "readiness criter"
---

<objective>
Add MUST criteria M5 to both G2 (research) and G3 (design) quality gates for DA readiness enforcement.

Purpose: Close the gap between "evidence exists" and "evidence addresses what was asked for" at the two gates where failure should trigger Recycle.
Output: Updated research/SKILL.md (G2 M5) and design/SKILL.md (G3 M5)
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-gate-enforcement/18-RESEARCH.md

Key source files:
@skills/research/SKILL.md (G2 gate at Step 14, lines 669-717)
@skills/design/SKILL.md (G3 gate at Step 8, lines 435-508)
@skills/research/references/prompt-research-synthesizer.md (line 110: readiness status format)
</context>

<interfaces>
<!-- Current G2 gate criteria format (research/SKILL.md lines 682-691) - list format, NOT table: -->

**MUST criteria (all must pass for Go):**
- M1: Every question assessed (covered + partial + not_covered + unavailable = total)
- M2: Majority COVERED (covered_count > total / 2)
- M3: All P0 questions COVERED or PARTIAL (p0_not_covered == 0)
- M4: No unresolved UNAVAILABLE-SOURCE (unresolved_unavailable == 0)

**SHOULD criteria (failures produce advisory, not block):**
- S1: All questions COVERED (covered_count == total)
- S2: No PARTIAL ratings remain (partial_count == 0)
- S3: All evidence requirements MET (no PARTIALLY MET in evaluator output)

<!-- Current G3 gate criteria format (design/SKILL.md lines 448-464) - table format: -->

| # | Criterion | How to Check | Result |
|---|-----------|-------------|--------|
| M1 | Every DA has a design decision | ... | PASS/FAIL |
| M2 | Every decision references evidence | ... | PASS/FAIL |
| M3 | Correct format for intent | ... | PASS/FAIL |
| M4 | DESIGN.md exists and is non-empty | ... | PASS/FAIL |

| S1 | Trade-offs articulated | ... | PASS/ADVISORY |
| S2 | Confidence levels assigned | ... | PASS/ADVISORY |
| S3 | Open questions genuine | ... | PASS/ADVISORY |
| S4 | HANDOFF.md exists (product) | ... | PASS/ADVISORY |

<!-- SYNTHESIS.md readiness format (from prompt-research-synthesizer.md line 110): -->
Readiness status: [MET / NOT MET]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add M5 DA readiness criterion to G2 gate (research/SKILL.md)</name>
  <files>skills/research/SKILL.md</files>
  <action>
In `skills/research/SKILL.md`, locate Step 14 G2 gate section (around line 669).

1. **Add a new instruction paragraph** after the "Compute counts" paragraph (line 680) and before the MUST criteria list. Insert:

```
Read `.expedite/research/SYNTHESIS.md`. For each `## Decision Area:` section, find the `Readiness status:` line. Count how many say "MET" vs "NOT MET" (case-insensitive, whitespace-tolerant). If a DA section is missing a readiness status line, treat it as NOT MET. Store as `da_total`, `da_met_count`.
```

2. **Add M5 to the MUST criteria list** after M4 (line 686). Append:

```
- M5: Every DA readiness criterion MET (da_met_count == da_total). State: "{da_met_count}/{da_total} DA readiness criteria MET{. NOT MET: DA-2, DA-5 if any}"
```

3. **Add a clarifying note** below M5 to distinguish from existing question-level checks:

```
Note: M1-M4 evaluate question-level coverage from state.yml. M5 evaluates DA-level readiness from SYNTHESIS.md. Both must pass.
```

IMPORTANT: G2 uses a simple dash-list format for criteria, NOT markdown tables. Match the existing format exactly.

IMPORTANT: The existing "Display gate results" instruction (line 699) already says "MUST and SHOULD with pass/fail + evidence for each" -- M5 will naturally appear in the output. No display format change needed.

Pitfall avoidance (from research P2): The synthesizer prompt always produces readiness status lines (prompt-research-synthesizer.md line 110), so well-formed lifecycles will pass M5. Making the check case-insensitive and handling missing lines as NOT MET protects against format variance.
  </action>
  <verify>
Read skills/research/SKILL.md lines 678-700. Confirm:
- "SYNTHESIS.md" read instruction exists before MUST criteria
- M5 line exists in MUST criteria list matching the dash-list format of M1-M4
- M5 references da_met_count and da_total
- Clarifying note distinguishes M1-M4 (question-level) from M5 (DA-level)
- Existing M1-M4 and S1-S3 are unchanged
- Gate outcomes section is unchanged
- Telemetry logging section is unchanged
  </verify>
  <done>
G2 gate has 5 MUST criteria (M1-M5). M5 checks every DA readiness criterion is MET in SYNTHESIS.md. Failure triggers Recycle. Existing criteria and gate infrastructure untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add M5 evidence-readiness criterion to G3 gate (design/SKILL.md)</name>
  <files>skills/design/SKILL.md</files>
  <action>
In `skills/design/SKILL.md`, locate Step 8 G3 gate section (around line 435).

1. **Add M5 row to the MUST criteria table** after the M4 row (line 455). Insert a new table row:

```markdown
| M5 | Evidence citations address DA readiness criteria | For each DA, read its readiness criterion from SCOPE.md. Check that evidence cited in DESIGN.md's DA section addresses what the readiness criterion requires (not just any evidence -- evidence relevant to the specific readiness criterion). State: "{N}/{M} DAs cite evidence addressing their readiness criteria{. Gaps: DA-3 readiness asks for benchmarks but citations only reference API docs}" | PASS/FAIL |
```

2. **Add a clarifying note** between the MUST table and the SHOULD table to distinguish M5 from M2:

```
Note: M2 checks that evidence citations *exist* for each DA. M5 checks that the cited evidence *addresses the specific readiness criterion* from SCOPE.md. Both must pass. Example: if a readiness criterion asks for "benchmark data comparing approaches" and the design cites evidence that only contains API documentation, M2 passes (citation exists) but M5 fails (citation does not address the readiness criterion).
```

3. **Update the display format block** (lines 466-484). Add M5 line in the MUST section:

```
  M5: {PASS|FAIL} -- {evidence}
```

This goes after the M4 display line and before the blank line separating MUST from SHOULD.

IMPORTANT: G3 uses markdown table format for criteria. Match exactly with pipe-delimited columns.

IMPORTANT: G3 already reads SCOPE.md (line 444: "Read `.expedite/design/DESIGN.md` and `.expedite/scope/SCOPE.md`") so no new file read instruction is needed.

Pitfall avoidance (from research P3): The M5 description explicitly distinguishes from M2 ("not just any evidence -- evidence relevant to the specific readiness criterion") to prevent conflation.
  </action>
  <verify>
Read skills/design/SKILL.md lines 448-485. Confirm:
- M5 row exists in MUST criteria table after M4
- M5 text references SCOPE.md readiness criteria and DESIGN.md evidence citations
- Clarifying note distinguishes M2 (citations exist) from M5 (citations address readiness)
- Display format block includes M5 line
- Existing M1-M4 and S1-S4 rows are unchanged
- Gate outcomes section is unchanged
- Telemetry logging section is unchanged
  </verify>
  <done>
G3 gate has 5 MUST criteria (M1-M5). M5 cross-references SCOPE.md readiness criteria against DESIGN.md evidence citations. Failure triggers Recycle. Existing criteria and gate infrastructure untouched.
  </done>
</task>

</tasks>

<verification>
1. Read skills/research/SKILL.md -- G2 section has M1-M5 MUST + S1-S3 SHOULD
2. Read skills/design/SKILL.md -- G3 section has M1-M5 MUST + S1-S4 SHOULD
3. No other sections of either SKILL.md have been modified
4. Gate outcome routing (Go/Go-with-advisory/Recycle/Override) is unchanged in both files
5. Telemetry logging format is unchanged in both files (must_passed/must_failed counts naturally include M5)
6. Gate history schema is unchanged (counts update naturally)
</verification>

<success_criteria>
- G2 gate in research/SKILL.md evaluates M5: DA readiness criterion check against SYNTHESIS.md
- G3 gate in design/SKILL.md evaluates M5: evidence-readiness cross-reference against SCOPE.md + DESIGN.md
- Both M5 criteria are MUST (failure triggers Recycle)
- Clarifying notes prevent confusion with existing criteria (M2 in G3, M1-M4 in G2)
- No regressions: existing criteria, display format, outcome routing, telemetry, and gate history are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/18-gate-enforcement/18-01-SUMMARY.md`
</output>
