---
phase: 15-step-level-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/state.yml.template
  - skills/status/SKILL.md
autonomous: true
requirements:
  - STEP-01
  - STEP-02
  - STEP-09

must_haves:
  truths:
    - "state.yml.template contains a current_step field with documented sub-keys (skill, step, label)"
    - "New lifecycles initialized from the template have current_step: null"
    - "Status skill displays 'Current Step: {skill}: step {N} of {total} -- {label}' when current_step is present in state"
    - "Status skill omits the Current Step line entirely when current_step is null or absent"
    - "Existing v1.0 lifecycles without current_step cause no errors in the status skill"
  artifacts:
    - path: "templates/state.yml.template"
      provides: "current_step field definition and schema documentation"
      contains: "current_step"
    - path: "skills/status/SKILL.md"
      provides: "Step position display logic with skill-to-total lookup table"
      contains: "current_step"
  key_links:
    - from: "templates/state.yml.template"
      to: ".expedite/state.yml"
      via: "Template copied during scope Step 3 initialization"
      pattern: "current_step"
    - from: "skills/status/SKILL.md"
      to: ".expedite/state.yml"
      via: "Dynamic context injection reads state.yml, status parses current_step"
      pattern: "current_step"
---

<objective>
Add the `current_step` field to the state schema template and update the status skill to display step position.

Purpose: Establishes the schema foundation (STEP-01, STEP-02) and the user-facing display (STEP-09) so that once skills write `current_step`, users see their position. This plan defines both ends of the contract; plans 15-02 through 15-04 implement the writers.

Output: Updated state.yml.template with documented `current_step` field; updated status SKILL.md with step position display logic.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-step-level-tracking/15-RESEARCH.md
@templates/state.yml.template
@skills/status/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add current_step field to state.yml.template</name>
  <files>templates/state.yml.template</files>
  <action>
Add a `current_step` field to `templates/state.yml.template`. Place it AFTER the `current_task: null` line and BEFORE the `imported_from: null` line (in the "Current position" section).

Add these exact lines:

```yaml
# Current position within active skill (set on step entry, cleared on skill completion)
# Sub-keys: skill (string), step (integer), label (string)
# Example: {skill: "scope", step: 5, label: "Interactive Questioning (Round 2: Adaptive Refinement)"}
current_step: null
```

This adds the field with full schema documentation as YAML comments, following the same commenting pattern used for `questions` and `gate_history` (schema documented inline).

Do NOT modify any other fields in the template. The `current_step` field defaults to `null` which means no step is active (new lifecycle, between skills, or completed lifecycle).
  </action>
  <verify>
    <automated>grep -A3 "current_step" templates/state.yml.template | head -6</automated>
  </verify>
  <done>
- state.yml.template contains `current_step: null` with 3 lines of YAML comment documenting sub-keys, type, and example
- Field is positioned in the "Current position" section between `current_task` and `imported_from`
- No other fields modified
  </done>
</task>

<task type="auto">
  <name>Task 2: Add step position display to status SKILL.md</name>
  <files>skills/status/SKILL.md</files>
  <action>
Update the status skill to display current step position when `current_step` is present in state.yml.

**Step 1: Add current_step to the field extraction list.**

In instruction 2 ("Parse state fields"), add `current_step` to the list of fields to extract:
```
   - `current_step` -- current position within active skill (may be null or absent)
```

**Step 2: Add a new instruction between current instruction 2 and instruction 3.**

Insert a new instruction (renumber subsequent instructions accordingly). Add it as instruction 3, and renumber existing 3-7 to 4-8:

```
3. **Parse current_step (if present).** If the injected state contains a `current_step` field that is not null:
   - Extract `skill`, `step`, and `label` sub-keys
   - Look up total steps for the skill using this table:
     - scope: 10, research: 18, design: 10, plan: 9, spike: 9, execute: 7
   - Format the display string: `{skill}: step {step} of {total} -- {label}`
   - If the skill name is not in the lookup table, display without total: `{skill}: step {step} -- {label}`
   - If `current_step` is null or absent, skip this entirely (no placeholder, no error)
```

**Step 3: Add Current Step line to the display format.**

In the display format section (instruction 6, which becomes instruction 7), add a `**Current Step:**` line AFTER the `**Phase:**` line:

Change from:
```
**Phase:** {phase} ({human-readable description})
```

To:
```
**Phase:** {phase} ({human-readable description})
**Current Step:** {skill}: step {step} of {total} -- {label}
```

Add a comment after the Current Step line:
```
(Only show Current Step line when current_step is present and not null. Omit entirely otherwise.)
```

Do NOT change any other display sections (Questions, Gate History, Recovery Info). Do NOT modify the read-only constraint in instruction 7 (which becomes instruction 8).
  </action>
  <verify>
    <automated>grep -c "current_step" skills/status/SKILL.md</automated>
  </verify>
  <done>
- Status skill extracts `current_step` from injected state
- New instruction parses current_step with skill-to-total lookup table (scope:10, research:18, design:10, plan:9, spike:9, execute:7)
- Display format includes "Current Step" line after "Phase" line
- Current Step line is only shown when current_step is present and not null
- When current_step is null/absent, no placeholder or error appears
- All existing instructions renumbered correctly
- Read-only constraint preserved
  </done>
</task>

</tasks>

<verification>
1. `grep "current_step" templates/state.yml.template` shows the field definition and comments
2. `grep -c "current_step" skills/status/SKILL.md` returns 4+ (field extraction, parsing instruction, display format, condition note)
3. The template field is between `current_task` and `imported_from`
4. The status skill has a lookup table with all 6 skill names and correct step counts
5. The status skill instruction numbering is sequential (1-8)
</verification>

<success_criteria>
- state.yml.template defines `current_step: null` with schema documentation in the Current position section
- Status skill parses `current_step` and displays "Current Step: {skill}: step {N} of {total} -- {label}" when present
- Status skill silently omits Current Step line when field is null or absent (backward compatible with v1.0 lifecycles)
</success_criteria>

<output>
After completion, create `.planning/phases/15-step-level-tracking/15-01-SUMMARY.md`
</output>
