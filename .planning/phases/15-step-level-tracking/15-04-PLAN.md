---
phase: 15-step-level-tracking
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/spike/SKILL.md
  - skills/execute/SKILL.md
autonomous: true
requirements:
  - STEP-07
  - STEP-08

must_haves:
  truths:
    - "Every numbered step in spike SKILL.md (Steps 1-9) writes current_step on entry"
    - "Every numbered step in execute SKILL.md (Steps 1-7) writes current_step on entry"
    - "Steps that already write state.yml fold current_step into existing writes (no double I/O)"
    - "Steps that do not already write state.yml add a standalone backup-before-write cycle"
    - "Spike Step 9 and Execute Step 7 clear current_step to null on completion"
    - "Execute Step 6 clears current_step to null when a non-final phase completes"
    - "Labels in current_step match the exact step heading text from each SKILL.md"
  artifacts:
    - path: "skills/spike/SKILL.md"
      provides: "Step tracking writes for all 9 spike steps"
      contains: "current_step"
    - path: "skills/execute/SKILL.md"
      provides: "Step tracking writes for all 7 execute steps"
      contains: "current_step"
  key_links:
    - from: "skills/spike/SKILL.md"
      to: ".expedite/state.yml"
      via: "Each step writes current_step field on entry"
      pattern: "current_step"
    - from: "skills/execute/SKILL.md"
      to: ".expedite/state.yml"
      via: "Each step writes current_step field on entry"
      pattern: "current_step"
---

<objective>
Add step-level tracking to the spike and execute skills so every numbered step writes `current_step` to state.yml on entry.

Purpose: Spike (9 steps) and execute (7 steps) complete the step tracking rollout across all 6 stateful skills. After this plan, users always know their exact position in any skill.

Output: Updated spike SKILL.md (9 step tracking writes) and execute SKILL.md (7 step tracking writes).
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-step-level-tracking/15-RESEARCH.md
@skills/spike/SKILL.md
@skills/execute/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add step tracking to spike SKILL.md (9 steps)</name>
  <files>skills/spike/SKILL.md</files>
  <action>
Add `current_step` tracking to EVERY numbered step in `skills/spike/SKILL.md`. Insert the tracking instruction at the VERY TOP of each step, immediately after the `### Step N:` heading line (before any existing content).

**Two patterns depending on whether the step already writes state.yml:**

### STANDALONE pattern (steps that do NOT already write state.yml: Steps 1, 2, 3, 4, 5, 6, 8, 9)

Insert this block immediately after the `### Step N:` heading:

```markdown

**Step tracking:** Update `current_step` in state.yml (backup-before-write):
1. Read `.expedite/state.yml`
2. Backup: `cp .expedite/state.yml .expedite/state.yml.bak` (via Bash)
3. Set `current_step` to `{skill: "spike", step: N, label: "LABEL"}`
4. Set `last_modified` to current timestamp
5. Write the entire file back
```

### FOLD-IN pattern (steps that ALREADY write state.yml: Step 7)

For Step 7, find the existing state.yml write instructions and add ONE line to the existing in-memory update list:

```markdown
   - Set `current_step` to `{skill: "spike", step: 7, label: "G5 Structural Gate"}`
```

### Clearing current_step on completion:

**Step 9 (Display Summary):** Spike does NOT transition phase (it stays at `plan_complete`). However, Step 9 is the final step and the spike is done. Add a standalone backup-before-write at entry that sets `current_step` AND then at the END of Step 9, add a final state write that clears it:

At step entry (standard tracking):
```markdown
**Step tracking:** Update `current_step` in state.yml (backup-before-write):
1. Read `.expedite/state.yml`
2. Backup: `cp .expedite/state.yml .expedite/state.yml.bak` (via Bash)
3. Set `current_step` to `{skill: "spike", step: 9, label: "Display Summary"}`
4. Set `last_modified` to current timestamp
5. Write the entire file back
```

At step END (after displaying the summary):
```markdown
**Clear step tracking:** Update state.yml to clear `current_step` (backup-before-write):
1. Read `.expedite/state.yml`
2. Backup: `cp .expedite/state.yml .expedite/state.yml.bak` (via Bash)
3. Set `current_step` to null
4. Set `last_modified` to current timestamp
5. Write the entire file back
```

### Exact labels for each step (use EXACTLY these):

| Step | Label (from heading) |
|------|---------------------|
| 1 | Prerequisite Check |
| 2 | Parse Phase Argument |
| 3 | Read Plan Artifacts |
| 4 | Extract Phase Definition |
| 5 | Resolve Tactical Decisions |
| 6 | Generate Implementation Steps |
| 7 | G5 Structural Gate |
| 8 | Write SPIKE.md |
| 9 | Display Summary |
  </action>
  <verify>
    <automated>grep -c "current_step" skills/spike/SKILL.md</automated>
  </verify>
  <done>
- All 9 steps have `current_step` tracking at step entry
- Steps 1, 2, 3, 4, 5, 6, 8, 9 use standalone backup-before-write pattern
- Step 7 folds into existing state write
- Step 9 clears `current_step` to null at step end
- All labels match the exact step heading text
- grep returns 14+ occurrences of "current_step" in the file
  </done>
</task>

<task type="auto">
  <name>Task 2: Add step tracking to execute SKILL.md (7 steps)</name>
  <files>skills/execute/SKILL.md</files>
  <action>
Add `current_step` tracking to EVERY numbered step in `skills/execute/SKILL.md`. Insert the tracking instruction at the VERY TOP of each step, immediately after the `### Step N:` heading line (before any existing content).

**Two patterns depending on whether the step already writes state.yml:**

### STANDALONE pattern (steps that do NOT already write state.yml: Steps 1, 2, 4)

Insert this block immediately after the `### Step N:` heading:

```markdown

**Step tracking:** Update `current_step` in state.yml (backup-before-write):
1. Read `.expedite/state.yml`
2. Backup: `cp .expedite/state.yml .expedite/state.yml.bak` (via Bash)
3. Set `current_step` to `{skill: "execute", step: N, label: "LABEL"}`
4. Set `last_modified` to current timestamp
5. Write the entire file back
```

### FOLD-IN pattern (steps that ALREADY write state.yml: Steps 3, 5, 6, 7)

For these steps, find the existing state.yml write instructions and add ONE line to the existing in-memory update list:

```markdown
   - Set `current_step` to `{skill: "execute", step: N, label: "LABEL"}`
```

### Clearing current_step:

**Step 5 (Task Execution Loop):** This step iterates over tasks, writing `current_task` on each iteration. Fold `current_step` into the FIRST write of the loop iteration (when `current_task` is updated). The `current_step` value stays the same throughout the loop (`step: 5, label: "Task Execution Loop"`) -- only `current_task` changes per iteration.

**Step 6 (Phase Completion):** This step handles completion of a single execution phase (not the whole lifecycle). When a phase completes and the lifecycle continues (more phases to execute), set `current_step` to null in the existing write. This is because the user returns to plan/spike for the next phase.

In the same write that marks phase completion:
```markdown
   - Set `current_step` to null
```

**Step 7 (Lifecycle Completion):** This step writes `phase: "complete"`. In the SAME write, also set `current_step: null`.

### Exact labels for each step (use EXACTLY these):

| Step | Label (from heading) |
|------|---------------------|
| 1 | Prerequisite Check |
| 2 | Read Plan + Spike Artifacts |
| 3 | Initialize Execute State |
| 4 | Determine Starting Point |
| 5 | Task Execution Loop |
| 6 | Phase Completion |
| 7 | Lifecycle Completion |

### Special cases:

- **Step 3 (Initialize Execute State):** Writes phase transition (`execute_in_progress`) and initializes task state. Fold `current_step` into that same write.

- **Step 5 (Task Execution Loop):** This step has an inner loop with repeated state writes (updating `current_task`). Fold `current_step` into EACH iteration's state write so that the step tracking is always current. The label stays "Task Execution Loop" throughout.

- **Step 6 (Phase Completion):** Clear `current_step` to null. The user is done with execute for this phase.

- **Step 7 (Lifecycle Completion):** Clear `current_step` to null with `phase: "complete"`.
  </action>
  <verify>
    <automated>grep -c "current_step" skills/execute/SKILL.md</automated>
  </verify>
  <done>
- All 7 steps have `current_step` tracking at step entry
- Steps 1, 2, 4 use standalone backup-before-write pattern
- Steps 3, 5, 6, 7 fold into existing state writes
- Step 5 folds `current_step` into each loop iteration's state write
- Steps 6 and 7 clear `current_step` to null on completion
- All labels match the exact step heading text
- grep returns 12+ occurrences of "current_step" in the file
  </done>
</task>

</tasks>

<verification>
1. `grep -c "current_step" skills/spike/SKILL.md` returns 14+ (9 step tracking writes + clear + labels)
2. `grep -c "current_step" skills/execute/SKILL.md` returns 12+ (7 step tracking writes + clears + labels)
3. Every `### Step` heading is followed by a tracking instruction (either standalone or folded)
4. Spike Step 9 clears `current_step` to null at step end
5. Execute Steps 6 and 7 clear `current_step` to null on completion
6. No duplicate backup-before-write cycles in steps that already write state.yml
</verification>

<success_criteria>
- Every numbered step in spike (9 steps) writes `current_step` on entry with correct skill name and label
- Every numbered step in execute (7 steps) writes `current_step` on entry with correct skill name and label
- Steps with existing state writes fold `current_step` in (no double I/O)
- Completion steps clear `current_step` to null
- Execute Step 5 maintains step tracking across task iterations
</success_criteria>

<output>
After completion, create `.planning/phases/15-step-level-tracking/15-04-SUMMARY.md`
</output>
