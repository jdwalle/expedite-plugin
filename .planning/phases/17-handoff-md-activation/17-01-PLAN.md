---
phase: 17-handoff-md-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/design/SKILL.md
  - skills/status/SKILL.md
autonomous: true
requirements: [HAND-03]

must_haves:
  truths:
    - "Product-intent lifecycle that crashes between Steps 5-6 resumes at Step 6 (HANDOFF.md generation), not Step 7"
    - "Engineering-intent lifecycle resume behavior is unchanged (still resumes at Step 7 when DESIGN.md exists)"
    - "Status skill reports missing HANDOFF.md as inconsistency for product-intent lifecycles at design_complete or later"
    - "Status skill does NOT flag missing HANDOFF.md for engineering-intent lifecycles"
  artifacts:
    - path: "skills/design/SKILL.md"
      provides: "Case B2 resume logic with HANDOFF.md awareness for product intent"
      contains: "HANDOFF.md"
    - path: "skills/status/SKILL.md"
      provides: "Artifact cross-reference with intent-conditional HANDOFF.md check"
      contains: "HANDOFF.md"
  key_links:
    - from: "skills/design/SKILL.md Case B2"
      to: ".expedite/design/HANDOFF.md"
      via: "intent check + file existence check"
      pattern: "intent.*product.*HANDOFF"
    - from: "skills/status/SKILL.md Step 5"
      to: ".expedite/state.yml intent field"
      via: "conditional artifact expectation"
      pattern: "intent.*product.*HANDOFF"
---

<objective>
Fix two identified gaps in existing SKILL.md files to fully support HANDOFF.md for product-intent lifecycles.

Purpose: The HANDOFF.md generation logic already exists in the design skill (Steps 6, 7, 8) but two gaps prevent reliable operation: (1) the resume logic (Case B2) skips HANDOFF.md generation after a crash between Steps 5-6, and (2) the status skill's artifact cross-reference does not check for HANDOFF.md. These gaps must be closed before verification testing in Plan 03.

Output: Updated design SKILL.md with HANDOFF.md-aware resume logic, updated status SKILL.md with intent-conditional HANDOFF.md artifact check.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-handoff-md-activation/17-RESEARCH.md

<interfaces>
<!-- Key code sections the executor needs to modify. Extracted from codebase. -->

From skills/design/SKILL.md Case B2 (lines 92-94) -- the resume logic to update:
```markdown
2. Check for `.expedite/design/DESIGN.md`:
   - If DESIGN.md exists: Step 5 completed. Resume at Step 7 (revision cycle). Display: "Found in-progress design with DESIGN.md already generated. Resuming at revision cycle..."
   - If DESIGN.md does not exist: Resume at Step 2 (read artifacts, then generate). Display: "Found in-progress design, but no DESIGN.md yet. Resuming from artifact loading..."
```

From skills/status/SKILL.md Step 5 (lines 60-76) -- the artifact cross-reference mapping:
```markdown
   2. Use this mapping to determine which artifacts SHOULD exist:
      - `scope_complete` and later: `.expedite/scope/SCOPE.md`
      - `research_complete` and later: `.expedite/research/SYNTHESIS.md`
      - `design_complete` and later: `.expedite/design/DESIGN.md`
      - `plan_complete` and later: `.expedite/plan/PLAN.md`
```

From skills/status/SKILL.md Step 2 (line 33) -- intent is already parsed:
```markdown
   - `intent` -- "product" or "engineering"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix design SKILL.md Case B2 resume logic for HANDOFF.md awareness</name>
  <files>skills/design/SKILL.md</files>
  <action>
Update the Case B2 resume logic in Step 1 (currently at lines 92-94) to check for HANDOFF.md when the intent is "product".

Replace the current two-branch check:
```
2. Check for `.expedite/design/DESIGN.md`:
   - If DESIGN.md exists: Step 5 completed. Resume at Step 7...
   - If DESIGN.md does not exist: Resume at Step 2...
```

With a three-branch check:
```
2. Check for `.expedite/design/DESIGN.md`:
   - If DESIGN.md exists AND (intent is "engineering" OR `.expedite/design/HANDOFF.md` exists):
     Step 6 completed. Resume at Step 7 (revision cycle).
     Display: "Found in-progress design with DESIGN.md already generated. Resuming at revision cycle..."
   - If DESIGN.md exists AND intent is "product" AND `.expedite/design/HANDOFF.md` does NOT exist:
     Step 5 completed but Step 6 not yet done. Resume at Step 6 (HANDOFF.md generation).
     Display: "Found in-progress design with DESIGN.md generated but HANDOFF.md missing. Resuming at HANDOFF.md generation..."
   - If DESIGN.md does not exist:
     Resume at Step 2 (read artifacts, then generate).
     Display: "Found in-progress design, but no DESIGN.md yet. Resuming from artifact loading..."
```

IMPORTANT: The intent value must be read from state.yml (it is already available from the injected lifecycle state at the top of SKILL.md). Keep the surrounding Case B2 structure intact -- only modify the DESIGN.md check section (item 2 in the numbered list). Do NOT modify any other steps.

Also update the corresponding resume step reference in item 4 ("Proceed directly to the resume step") -- it currently says "Do NOT re-run Step 3" which remains correct. Ensure the new Step 6 resume path is included in the "Resume point: Step {2 or 7}" display (item 3) -- update to "Resume point: Step {2, 6, or 7}".
  </action>
  <verify>
    <automated>cd /Users/jaredwallenfels/Desktop/Projects/expedite-plugin && grep -n "HANDOFF" skills/design/SKILL.md | head -30 && echo "---" && grep -c "HANDOFF" skills/design/SKILL.md && echo "---" && grep -A2 "intent.*product.*HANDOFF" skills/design/SKILL.md</automated>
  </verify>
  <done>Case B2 has three branches: (1) DESIGN.md exists + engineering or HANDOFF.md exists -> Step 7, (2) DESIGN.md exists + product + no HANDOFF.md -> Step 6, (3) no DESIGN.md -> Step 2. Display text updated to show Step 6 as possible resume point. No other steps modified.</done>
</task>

<task type="auto">
  <name>Task 2: Add HANDOFF.md to status skill artifact cross-reference</name>
  <files>skills/status/SKILL.md</files>
  <action>
Update Step 5 (Artifact Cross-Reference) in the status SKILL.md to include HANDOFF.md as a conditionally expected artifact for product-intent lifecycles.

In the artifact mapping list (currently 4 items), add a 5th entry AFTER the `design_complete` line:
```
      - `design_complete` and later, if intent is "product": `.expedite/design/HANDOFF.md`
```

This entry must be gated on intent. The intent value is already extracted in Step 2 (parsed state fields include `intent`). The status skill must NOT flag missing HANDOFF.md for engineering-intent lifecycles.

Also add a note to Step 5 item 3 ("For each expected artifact, check existence") clarifying that the HANDOFF.md check requires reading the intent from the parsed state in Step 2.

Do NOT modify any other steps in the status SKILL.md. The check must remain read-only (Step 10 constraint).
  </action>
  <verify>
    <automated>cd /Users/jaredwallenfels/Desktop/Projects/expedite-plugin && grep -n "HANDOFF" skills/status/SKILL.md && echo "---" && grep -B1 -A1 "intent.*product" skills/status/SKILL.md</automated>
  </verify>
  <done>Status skill Step 5 artifact mapping includes HANDOFF.md with intent-conditional check. Engineering-intent lifecycles are unaffected. Read-only constraint preserved.</done>
</task>

</tasks>

<verification>
1. Design SKILL.md Case B2 contains three resume branches (engineering/HANDOFF exists -> Step 7, product/no HANDOFF -> Step 6, no DESIGN.md -> Step 2)
2. Status SKILL.md Step 5 mapping includes 5 artifact entries (SCOPE.md, SYNTHESIS.md, DESIGN.md, HANDOFF.md conditional, PLAN.md)
3. Both files parse as valid Markdown with no broken formatting
4. No changes to any other steps in either SKILL.md
</verification>

<success_criteria>
- Design SKILL.md Case B2 has HANDOFF.md-aware resume logic with three branches
- Status SKILL.md artifact cross-reference includes intent-conditional HANDOFF.md check
- Engineering-intent behavior unchanged in both skills
- Total line changes: ~10-15 lines across both files
</success_criteria>

<output>
After completion, create `.planning/phases/17-handoff-md-activation/17-01-SUMMARY.md`
</output>
