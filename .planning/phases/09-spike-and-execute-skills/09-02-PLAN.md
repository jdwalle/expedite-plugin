---
phase: 09-spike-and-execute-skills
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/execute/SKILL.md
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-06]

must_haves:
  truths:
    - "User can run /expedite:execute <phase> and have that specific phase's tasks implemented sequentially with verification"
    - "Execute reads that phase's SPIKE.md if available, falls back to PLAN.md tasks for unspiked execution"
    - "If no SPIKE.md and phase has needs-spike TDs, user receives a non-blocking nudge to run spike first"
    - "Per-task verification uses prompt-task-verifier.md to validate contract chain end-to-end"
    - "Execute populates state.yml tasks array scoped to the current phase only"
    - "checkpoint.yml and PROGRESS.md are per-phase, stored in .expedite/plan/phases/{slug}/"
  artifacts:
    - path: "skills/execute/SKILL.md"
      provides: "Execute skill orchestration Steps 1-5 (prerequisite through task execution loop)"
      min_lines: 250
  key_links:
    - from: "skills/execute/SKILL.md"
      to: ".expedite/plan/PLAN.md"
      via: "Read in Step 2"
      pattern: "PLAN\\.md"
    - from: "skills/execute/SKILL.md"
      to: ".expedite/plan/phases/{slug}/SPIKE.md"
      via: "Read in Step 2 (if exists)"
      pattern: "SPIKE\\.md"
    - from: "skills/execute/SKILL.md"
      to: "skills/execute/references/prompt-task-verifier.md"
      via: "Read inline reference for verification"
      pattern: "prompt-task-verifier"
    - from: "skills/execute/SKILL.md"
      to: ".expedite/plan/phases/{slug}/checkpoint.yml"
      via: "Write task tracking state in Step 3, update in Step 5"
      pattern: "checkpoint\\.yml"
    - from: "skills/execute/SKILL.md"
      to: ".expedite/plan/phases/{slug}/PROGRESS.md"
      via: "Append via cat >> in Step 5"
      pattern: "PROGRESS\\.md"
    - from: "skills/execute/SKILL.md"
      to: ".expedite/state.yml"
      via: "Write task tracking state in Step 3"
      pattern: "state\\.yml"
---

<objective>
Replace the execute SKILL.md stub (29 lines) with the core execution pipeline: Steps 1-5 covering prerequisite validation, artifact loading with dual-mode detection (spiked vs unspiked), state initialization with task tracking population, checkpoint setup, and the task execution loop with per-task verification.

Execute operates per-phase: `/expedite:execute 1` executes Phase 1 only, `/expedite:execute 2` executes Phase 2 only. This mirrors the same sequential pattern as GSD (plan-phase then execute-phase, one at a time). All execution artifacts (checkpoint.yml, PROGRESS.md) are stored per-phase in `.expedite/plan/phases/{slug}/`.

Purpose: Execute is the final stage of the contract chain. It implements plan tasks, verifies each against design decisions, and validates the contract chain end-to-end. This plan builds the core pipeline; Plan 09-03 adds phase completion and lifecycle finalization.

Output: `skills/execute/SKILL.md` with Steps 1-5 (~250-350 lines).
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-spike-and-execute-skills/09-RESEARCH.md

Key prior patterns:
- Plan SKILL.md (skills/plan/SKILL.md, 481 lines) -- Steps 1-5 pattern to mirror for prerequisite/artifact/state/generate/write
- Design SKILL.md (skills/design/SKILL.md, 475 lines) -- inline generation pattern
- prompt-task-verifier.md (skills/execute/references/prompt-task-verifier.md, 133 lines) -- already exists, used for per-task verification

<interfaces>
From skills/execute/SKILL.md (existing stub -- preserve frontmatter):
```yaml
name: execute
description: >
  This skill should be used when the user wants to "execute the plan",
  "run the plan", "implement", "build it", "start building",
  or needs to execute implementation plan tasks sequentially in the current session.
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch]
```
Note: Execute does NOT have Task in allowed-tools (it implements directly, does not dispatch subagents).

From templates/state.yml.template (execute state fields):
```yaml
# Execution state (populated by execute skill, scoped to current phase)
tasks: []
current_wave: null
current_task: null
# Task item schema:
#   id: "t01"
#   title: "task title"
#   wave: 1
#   status: "pending"    # pending | in_progress | complete | failed | skipped
```

From state.yml.template (valid transitions):
```yaml
# plan_complete -> execute_in_progress
# execute_in_progress -> complete
```

From skills/execute/references/prompt-task-verifier.md (133 lines, already exists):
- Receives: {{project_name}}, {{intent}}, {{design_file}}, {{plan_file}}, {{current_task_yaml}}, {{task_details}}, {{code_changes}}
- Produces: Verification status (VERIFIED|PARTIAL|FAILED|NEEDS REVIEW) with contract chain trace
- Intent-conditional: product checks Given/When/Then, engineering checks technical criteria
- Has anti-bias quality gate

Per-phase directory structure (USER DECISION -- mandatory):
```
.expedite/plan/phases/{slug}/
  SPIKE.md           # from spike skill (if spiked)
  checkpoint.yml     # execute checkpoint for THIS phase
  PROGRESS.md        # execute progress log for THIS phase
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace execute SKILL.md stub with Steps 1-3 (prerequisite, artifact loading, state init)</name>
  <files>skills/execute/SKILL.md</files>
  <action>
Replace the entire execute SKILL.md stub with the new orchestration script. PRESERVE the existing YAML frontmatter exactly (name, description, allowed-tools). Replace only the body below the closing `---`.

**!cat injection line** (established pattern):
```
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`
```

**Role preamble:**
"You are the Expedite execute orchestrator. Your job is to implement plan tasks sequentially, verify each task against design decisions, and maintain the contract chain from scope through execution. You operate on ONE PHASE (wave/epic) at a time. Each invocation of `/expedite:execute <phase>` executes that specific phase's tasks only. Execution artifacts (checkpoint, progress log) are stored per-phase."

**Interaction model:** "Use freeform prompts for micro-interactions (yes / pause / review) and error recovery (retry / skip / pause). Do NOT use AskUserQuestion (60-second timeout constraint)."

"After completing each step, proceed to the next step automatically. Do not wait for explicit 'next step' instructions unless the step specifically calls for user input."

**Step 1: Prerequisite Check**
Look at injected lifecycle state.

Case A: Phase is "plan_complete" -- fresh execution start. Display: "Starting execution..." Proceed to Step 2.

Case B: Phase is "execute_in_progress" -- resume scenario.
1. Parse the phase argument first (same logic as Step 2).
2. Determine the slug for the requested phase.
3. Check for `.expedite/plan/phases/{slug}/checkpoint.yml` (use Read, handle missing file gracefully)
4. If checkpoint exists AND status is "paused" or "in_progress": display "Resuming execution of {Wave/Epic} {N} from checkpoint..." Skip to Step 4 (resume logic).
5. If no checkpoint for this phase: display "Starting execution of {Wave/Epic} {N}..." Proceed to Step 2.

Case C: Anything else -- display error with current phase, suggest running preceding skills. If phase is "plan_recycled", suggest `--override`. STOP.

**Step 2: Read Plan + Spike Artifacts**
Parse the phase argument (same flexible parsing as spike skill):
- Bare number: "1", "2", "3"
- "wave N" / "Wave N" (engineering)
- "epic N" / "Epic N" (product)
- If no argument and only one phase exists in PLAN.md: auto-select
- If no argument and multiple phases exist: display available phases and ask user to choose (do NOT auto-chain to "next" phase -- the user explicitly chooses which phase to execute)

Determine slug: `wave-{N}` for engineering, `epic-{N}` for product.

Read the following files:
1. `.expedite/plan/PLAN.md` -- extract the target phase definition (heading, tasks, tactical decisions, design decision references, acceptance criteria)
2. `.expedite/plan/phases/{slug}/SPIKE.md` -- if exists, this is SPIKED MODE. Extract resolved tactical decisions and implementation steps.
3. `.expedite/design/DESIGN.md` -- extract design decisions for verification reference
4. `.expedite/scope/SCOPE.md` -- extract DA definitions for contract chain
5. `.expedite/state.yml` -- extract project_name, intent, lifecycle_id
6. If `.expedite/plan/override-context.md` exists -- read it, note affected DAs
7. **Prior phase context:** If executing Phase N where N > 1, check for prior phase's PROGRESS.md to understand what was already implemented.

**Determine execution mode:**
- If SPIKE.md exists for this phase: **Spiked mode** -- follow SPIKE.md implementation steps
- If no SPIKE.md: **Unspiked mode** -- follow PLAN.md tasks directly

**Spike nudge (EXEC-02):**
If unspiked mode AND the phase's tactical decision table contains any "needs-spike" entries:
Display non-blocking nudge:
```
Note: This phase has {N} unresolved tactical decision(s). Consider running
`/expedite:spike {phase_number}` first to investigate them.

Proceeding with unspiked execution...
```
This is informational only -- do NOT block execution.

Display artifact loading summary:
```
--- Execute Context Loaded ---

Project: {project_name}
Intent: {intent}
Phase: {Wave/Epic} {N} - {description}
Mode: {Spiked (SPIKE.md found) | Unspiked (direct from PLAN.md)}
Tasks: {count}
{If override context:} Override advisory: {severity} severity
```

Error handling: If PLAN.md cannot be read, display error and STOP.

**Step 3: Initialize Execute State**
Update state.yml using the backup-before-write pattern:
1. Read `.expedite/state.yml`
2. Copy to backup: `cp .expedite/state.yml .expedite/state.yml.bak` (via Bash)
3. Update the in-memory representation:
   - Set `phase` to `"execute_in_progress"` (only if currently plan_complete; skip if already execute_in_progress from prior phase execution)
   - Set `current_wave` to the phase number being executed
   - Set `current_task` to the first task ID in THIS phase
   - Populate `tasks` array: enumerate tasks/stories from THIS PHASE ONLY (not all phases), each with id, title, wave number, and status: "pending"
   - Set `last_modified` to current ISO 8601 UTC timestamp
4. Write the entire file back to `.expedite/state.yml`

Create the per-phase execute output directory:
```bash
mkdir -p .expedite/plan/phases/{slug}/
```

Create initial checkpoint.yml at `.expedite/plan/phases/{slug}/checkpoint.yml`:
```yaml
current_task: "{first_task_id}"
current_wave: {wave_number}
last_completed_task: null
last_completed_at: null
tasks_completed: 0
tasks_total: {total_task_count_for_this_phase}
status: "in_progress"
continuation_notes: "Execution started for {Wave/Epic} {N}."
```

Write initial PROGRESS.md header via Bash (append-only from here on):
```bash
cat > .expedite/plan/phases/{slug}/PROGRESS.md << 'PROGRESS_EOF'
# Execution Progress: {project_name}
Started: {ISO 8601 UTC timestamp}
Phase: {Wave/Epic} {N}
Mode: {Spiked | Unspiked}

PROGRESS_EOF
```
Note: This initial header uses `cat >` (create). ALL subsequent writes MUST use `cat >>` (append). Include an explicit comment in the SKILL.md: "NEVER use Write tool for PROGRESS.md after this point. Use `cat >>` via Bash only."

Display: "Execute state initialized for {Wave/Epic} {N}. Beginning task execution..."
  </action>
  <verify>
Verify skills/execute/SKILL.md has YAML frontmatter preserved from stub. Verify Steps 1, 2, and 3 headings are present. Verify Step 1 handles plan_complete and execute_in_progress. Verify Step 2 checks for SPIKE.md and determines spiked/unspiked mode. Verify Step 2 includes the non-blocking spike nudge for needs-spike TDs. Verify Step 2 does NOT auto-chain to next phase (user explicitly chooses). Verify Step 3 populates tasks array scoped to current phase only (not all phases). Verify checkpoint.yml and PROGRESS.md paths are per-phase: `.expedite/plan/phases/{slug}/`. Verify PROGRESS.md initial header uses `cat >` with explicit note about `cat >>` for subsequent writes.
  </verify>
  <done>
Execute SKILL.md stub replaced with Steps 1-3: prerequisite check with per-phase resume detection, artifact loading with spiked/unspiked mode detection and non-blocking spike nudge, and state initialization with per-phase task tracking, per-phase checkpoint creation, and per-phase PROGRESS.md header. All paths use `.expedite/plan/phases/{slug}/` for execution artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Steps 4-5 to execute SKILL.md (resume logic and task execution loop with verification)</name>
  <files>skills/execute/SKILL.md</files>
  <action>
Append Steps 4-5 to the execute SKILL.md created in Task 1. These steps handle resume from checkpoint and the core task execution loop with per-task verification.

**Step 4: Determine Starting Point**

**4a: Fresh start (came from Step 3):**
Start from the first task in the current phase. Set current_task to the first task ID.

**4b: Resume from checkpoint (came from Step 1 Case B):**
1. Read `.expedite/plan/phases/{slug}/checkpoint.yml`
2. Extract `last_completed_task` and `current_wave`
3. Verify the checkpoint's `current_wave` matches the requested phase number
4. Find the next task after `last_completed_task` in the task order
5. Display: "Resuming from task {next_task_id}: {task_title}. ({completed}/{total} tasks completed)"
6. Read `.expedite/plan/phases/{slug}/PROGRESS.md` to load context of what was already done

**4c: Checkpoint reconstruction fallback:**
If checkpoint.yml is missing or corrupted but PROGRESS.md exists in the phase directory:
1. Parse PROGRESS.md to find the last completed task ID (look for `## t{NN}:` headings)
2. Find the next task in sequence
3. Display: "Checkpoint missing. Reconstructed from PROGRESS.md. Resuming from task {next_task_id}."
4. Create a new checkpoint.yml in `.expedite/plan/phases/{slug}/` reflecting the reconstructed state

Proceed to Step 5 with the determined starting task.

**Step 5: Task Execution Loop**

This is the core loop. For each task starting from the determined starting point:

**5a: Display current task.**

For **spiked mode** (from SPIKE.md):
```
--- Task {task_id}: {task_title} ({current}/{total}) ---
Traces to: {TD -> DA chain from SPIKE.md}
Files: {file list}

Implementation steps:
{numbered steps from SPIKE.md}
```

For **unspiked mode** (from PLAN.md):
```
--- Task {task_id}: {task_title} ({current}/{total}) ---
Design decision: {DA-X: brief}
Files: {file list}
Acceptance criteria:
{criteria list from PLAN.md}
```

**5b: Implement the task.**
This is where actual code changes happen. The execute skill is running in the main session with full tool access (Read, Write, Edit, Glob, Grep, Bash, WebSearch, WebFetch).

In spiked mode: follow the implementation steps from SPIKE.md. Each step has specific sub-steps and file targets.

In unspiked mode: read the task definition from PLAN.md, understand the acceptance criteria, and implement directly. Use the design decision from DESIGN.md as guidance.

**5c: Per-task verification (EXEC-06).**
After implementing, run verification using the prompt-task-verifier.md reference:

1. Read `skills/execute/references/prompt-task-verifier.md` (use Glob with `**/prompt-task-verifier.md` if direct path fails)
2. This is an INLINE reference (not a subagent dispatch). Apply its verification process in the current session:
   a. Read the task definition (acceptance criteria, DA reference)
   b. Read the referenced design decision from DESIGN.md
   c. For each acceptance criterion: check pass/fail AND design decision alignment (YES/PARTIAL/NO)
   d. Check for disconnected criteria
   e. Produce verification status: VERIFIED | PARTIAL | FAILED | NEEDS REVIEW
3. Display the verification summary

**5d: Update checkpoint.**
Update `.expedite/plan/phases/{slug}/checkpoint.yml` (full rewrite, not append):
```yaml
current_task: "{next_task_id or null if last task}"
current_wave: {wave_number}
last_completed_task: "{completed_task_id}"
last_completed_at: "{ISO 8601 UTC}"
tasks_completed: {count}
tasks_total: {total}
status: "in_progress"
continuation_notes: "{brief: what was done, what's next}"
```

Update state.yml (backup-before-write): set `current_task` to next task ID, update task status to "complete" (or "failed"/"partial" per verification), set `last_modified`.

**5e: Append to PROGRESS.md via Bash.**
NEVER use the Write tool for PROGRESS.md. Use `cat >>` via Bash:

```bash
cat >> .expedite/plan/phases/{slug}/PROGRESS.md << 'PROGRESS_EOF'

## {task_id}: {task_title}
- Status: {complete|partial|failed|needs_review}
- Wave: {wave_number}
- Design decision: {DA-X}: {brief}
- Files modified: {list}
- Verification: {VERIFIED|PARTIAL|FAILED|NEEDS REVIEW}
- Contract chain: {DA-X} -> {evidence citation} -> {design decision} -> {task ID} -> {files}
- Completed: {ISO 8601 UTC timestamp}
PROGRESS_EOF
```

**5f: Micro-interaction (EXEC-05).**
Display task completion and verification status, then prompt:

```
Task {task_id} complete ({completed}/{total}).
Verification: {VERIFIED|PARTIAL|FAILED|NEEDS REVIEW}
{If FAILED: "Contract chain broken at: {stage}. Review recommended."}

Continue?
> yes / pause / review
```

Handle responses:
- **yes**, **continue**, **next**, **go**, **keep going**: proceed to next task (loop back to 5a)
- **pause**, **stop**, **wait**, **hold**: update checkpoint.yml status to "paused", display "Execution paused at task {task_id}. Resume with `/expedite:execute {phase_number}`.", STOP.
- **review**: display PROGRESS.md contents (read the file and display), then re-prompt

**5g: Error handling during implementation.**
If a task fails (build error, test failure, code error):
Display the error and prompt:
```
Task {task_id} encountered an error:
{error details}

> retry / skip / pause
```
- **retry**: re-attempt the implementation (loop back to 5b)
- **skip**: mark task status as "skipped" in state.yml and checkpoint, append "Status: skipped" to PROGRESS.md, proceed to next task
- **pause**: save checkpoint with error context in continuation_notes, STOP

After the last task in the phase: proceed to Step 6 (implemented in Plan 09-03).
Display: "All tasks in {Wave/Epic} {N} complete. Proceeding to phase completion..."
  </action>
  <verify>
Verify Steps 4 and 5 headings are present in SKILL.md. Verify Step 4 handles fresh start, resume from checkpoint, and checkpoint reconstruction fallback. Verify all checkpoint and PROGRESS.md paths use `.expedite/plan/phases/{slug}/` (per-phase). Verify Step 5 has all sub-steps: 5a display, 5b implement, 5c verify (references prompt-task-verifier.md), 5d checkpoint update, 5e PROGRESS.md append via `cat >>`, 5f micro-interaction, 5g error handling. Verify `cat >>` is used for PROGRESS.md (not Write tool). Verify "NEVER use Write tool for PROGRESS.md" instruction is present. Count total SKILL.md lines: should be 280+ lines.
  </verify>
  <done>
Execute SKILL.md has Steps 1-5: prerequisite check, artifact loading with dual-mode detection, state initialization with per-phase checkpoint and progress, resume logic with per-phase checkpoint reconstruction, and complete task execution loop (display, implement, verify via prompt-task-verifier.md reference, per-phase checkpoint, per-phase progress append, micro-interaction, error handling). EXEC-01, EXEC-02, and EXEC-06 all addressed. All execution artifacts stored per-phase in `.expedite/plan/phases/{slug}/`.
  </done>
</task>

</tasks>

<verification>
1. `skills/execute/SKILL.md` exists with preserved YAML frontmatter from stub
2. SKILL.md has Steps 1-5 headings
3. Step 1 handles plan_complete (fresh) and execute_in_progress (resume with per-phase checkpoint)
4. Step 2 detects spiked vs unspiked mode by checking for per-phase SPIKE.md
5. Step 2 displays non-blocking nudge when needs-spike TDs detected in unspiked mode
6. Step 2 does NOT auto-chain to next phase -- user explicitly selects which phase to execute
7. Step 3 populates tasks array scoped to current phase only, creates per-phase checkpoint.yml and PROGRESS.md
8. Step 4 handles resume from per-phase checkpoint with per-phase PROGRESS.md reconstruction fallback
9. Step 5 includes per-task verification using prompt-task-verifier.md inline reference
10. Step 5 uses `cat >>` for per-phase PROGRESS.md (explicit "NEVER Write tool" instruction present)
11. Step 5 includes micro-interaction (yes/pause/review) and error handling (retry/skip/pause)
12. All checkpoint.yml and PROGRESS.md paths use `.expedite/plan/phases/{slug}/` (not `.expedite/execute/`)
</verification>

<success_criteria>
- Execute SKILL.md stub replaced with complete Steps 1-5 (~280-380 lines)
- Dual-mode execution works: spiked (SPIKE.md steps) and unspiked (PLAN.md tasks)
- Per-task verification references existing prompt-task-verifier.md (no duplication)
- State.yml tasks array populated by execute, scoped to current phase only
- All execution artifacts (checkpoint.yml, PROGRESS.md) stored per-phase in `.expedite/plan/phases/{slug}/`
- No auto-chaining between phases -- user explicitly runs `/expedite:execute <N>` for each phase
- All micro-interaction uses freeform prompts (no AskUserQuestion)
</success_criteria>

<output>
After completion, create `.planning/phases/09-spike-and-execute-skills/09-02-SUMMARY.md`
</output>
