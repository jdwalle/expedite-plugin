---
phase: 09-spike-and-execute-skills
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - skills/execute/SKILL.md
autonomous: false
requirements: [EXEC-03, EXEC-04, EXEC-05]

must_haves:
  truths:
    - "checkpoint.yml tracks execution position for pause/resume across sessions"
    - "PROGRESS.md is written exclusively via cat >> Bash (never Write tool rewrite)"
    - "Micro-interaction between tasks uses freeform yes/pause/review prompt"
    - "Wave/epic transitions prompt user before starting next phase"
    - "Execution completion sets phase to complete and displays lifecycle summary"
  artifacts:
    - path: "skills/execute/SKILL.md"
      provides: "Complete execute skill with Steps 1-7"
      min_lines: 350
  key_links:
    - from: "skills/execute/SKILL.md Step 6"
      to: ".expedite/execute/checkpoint.yml"
      via: "Update between waves"
      pattern: "checkpoint\\.yml"
    - from: "skills/execute/SKILL.md Step 7"
      to: ".expedite/state.yml"
      via: "Set phase to complete"
      pattern: "phase.*complete"
---

<objective>
Append Steps 6-7 to the execute SKILL.md: wave/epic transitions between phases and execution completion with lifecycle summary. Then perform a human verification of the complete execute SKILL.md as a coherent orchestration script.

Purpose: Steps 6-7 complete the execute skill by handling multi-wave execution and lifecycle finalization. The checkpoint verify ensures the full skill (Steps 1-7) reads as a coherent, complete orchestration script.

Output: Complete `skills/execute/SKILL.md` with all 7 steps (~350-450 lines).
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-spike-and-execute-skills/09-RESEARCH.md
@.planning/phases/09-spike-and-execute-skills/09-02-SUMMARY.md

<interfaces>
From checkpoint.yml schema (design decision #18):
```yaml
current_task: "t03"
current_wave: 2
last_completed_task: "t02"
last_completed_at: "2026-02-26T14:30:00Z"
tasks_completed: 2
tasks_total: 12
status: "in_progress"   # in_progress | paused | complete
continuation_notes: "Completed auth middleware. Next: rate limiting."
```

From state.yml.template transitions:
```yaml
# execute_in_progress -> complete
# (any) -> archived
```

From PROGRESS.md append format (design decision #20):
```markdown
## {task_id}: {task_title}
- Status: {complete|partial|failed|needs_review}
- Wave: {wave_number}
- Design decision: {DA-X}: {brief}
- Files modified: {list}
- Verification: {VERIFIED|PARTIAL|FAILED|NEEDS REVIEW}
- Completed: {ISO 8601 UTC timestamp}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Steps 6-7 to execute SKILL.md (wave transition and execution completion)</name>
  <files>skills/execute/SKILL.md</files>
  <action>
Append Steps 6-7 to the execute SKILL.md from Plan 09-02. These steps handle transitions between waves/epics and lifecycle completion.

**Step 6: Wave/Epic Transition**

After all tasks in the current wave/epic are complete (Step 5 loop exhausted):

**6a: Update checkpoint for wave completion.**
Update checkpoint.yml: set status to "in_progress", update continuation_notes to "Wave/Epic {N} complete. Transitioning to next phase."

**6b: Check if more waves/epics remain.**
Read PLAN.md and check if there are additional waves/epics after the current one.

If **more phases remain**:
Display:
```
--- {Wave/Epic} {N} Complete ---

Tasks completed: {count}/{total} in this phase
{Display summary of verification statuses: X VERIFIED, Y PARTIAL, Z FAILED}

Next: {Wave/Epic} {N+1} - {description}
Tasks: {count}
{If Wave N+1 has needs-spike TDs and no SPIKE.md:} Unresolved tactical decisions: {count} (consider running /expedite:spike {N+1} first)

Continue to {Wave/Epic} {N+1}?
> yes / spike first / pause
```

Handle responses:
- **yes**, **continue**: update current_wave in state.yml and checkpoint.yml. Re-enter Step 2 with the next phase number (re-read PLAN.md for new phase, check for SPIKE.md, etc.). This naturally re-enters the task execution loop.
- **spike first**: display "Run `/expedite:spike {N+1}` then resume with `/expedite:execute {N+1}`.", update checkpoint to paused, STOP.
- **pause**: update checkpoint to paused, STOP.

If **no more phases** (this was the last wave/epic):
Proceed to Step 7.

**Step 7: Execution Completion**

**7a: Update state.yml (backup-before-write):**
- Set `phase` to `"complete"`
- Set `current_task` to null
- Set all remaining task statuses to their final status
- Set `last_modified` to current ISO 8601 UTC timestamp

**7b: Update checkpoint.yml:**
```yaml
current_task: null
current_wave: {last_wave}
last_completed_task: "{final_task_id}"
last_completed_at: "{ISO 8601 UTC}"
tasks_completed: {total}
tasks_total: {total}
status: "complete"
continuation_notes: "Execution complete. All waves/epics finished."
```

**7c: Append completion entry to PROGRESS.md via Bash:**
```bash
cat >> .expedite/execute/PROGRESS.md << 'PROGRESS_EOF'

---

## Execution Complete
- Total tasks: {total}
- Verified: {count}
- Partial: {count}
- Failed: {count}
- Skipped: {count}
- Completed: {ISO 8601 UTC timestamp}
PROGRESS_EOF
```

**7d: Display lifecycle completion summary:**
```
## Execution Complete

Project: {project_name}
Intent: {intent}

### Artifacts Produced
- Progress log: .expedite/execute/PROGRESS.md
- Checkpoint: .expedite/execute/checkpoint.yml (status: complete)

### Execution Summary
Total waves/epics: {count}
Total tasks: {total}
  - Verified: {count}
  - Partial: {count}
  - Failed: {count}
  - Skipped: {count}

### Contract Chain Summary
Scope -> Research -> Design -> Plan -> Execute (complete)
Decision Areas: {N} | Design Decisions: {count} | Tasks Executed: {total}
Verification rate: {verified_count}/{total} tasks fully verified

### Lifecycle Status
Phase: complete
{If any FAILED tasks:} WARNING: {count} tasks had FAILED verification. Review PROGRESS.md for details.

### Next Steps
- Review .expedite/execute/PROGRESS.md for detailed task outcomes
- Run `/expedite:scope` to start a new lifecycle
{If product intent:} - Review .expedite/design/HANDOFF.md for engineer handoff documentation
```

Note: Do NOT archive the lifecycle. Archival is Phase 10 functionality (ARTF-03).
  </action>
  <verify>
Verify Steps 6 and 7 headings are present in SKILL.md. Verify Step 6 handles wave transition with yes/spike-first/pause options. Verify Step 7 sets phase to "complete" in state.yml. Verify Step 7 appends completion entry to PROGRESS.md via `cat >>`. Verify lifecycle summary includes contract chain statistics. Count total SKILL.md lines: should be 350+ lines.
  </verify>
  <done>
Execute SKILL.md has all 7 steps: prerequisite check, artifact loading, state initialization, resume logic, task execution loop, wave transition, and execution completion with lifecycle summary. Phase transitions to "complete" on final task. Contract chain validated end-to-end.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete spike and execute skills</name>
  <files>skills/spike/SKILL.md, skills/execute/SKILL.md</files>
  <action>
Present both complete SKILL.md files for human review. The user reads each file end-to-end and verifies they are coherent orchestration scripts ready for end-to-end use.

What was built: Complete execute SKILL.md (7 steps, ~350-450 lines) and spike SKILL.md (8 steps, ~250-350 lines) -- both skills implementing the final two stages of the Expedite lifecycle.

How to verify:
1. Read skills/execute/SKILL.md end-to-end. Verify it reads as a coherent orchestration script:
   - Steps flow logically (prerequisite -> artifacts -> state -> resume -> loop -> transition -> complete)
   - Spiked vs unspiked mode detection is clear
   - Per-task verification references prompt-task-verifier.md correctly
   - PROGRESS.md uses cat >> exclusively (never Write tool)
   - Micro-interaction uses freeform prompts (never AskUserQuestion)
   - Error handling covers retry/skip/pause
   - Wave transitions offer spike-first option
   - Lifecycle completion sets phase to "complete"

2. Read skills/spike/SKILL.md end-to-end. Verify it reads as a coherent orchestration script:
   - Steps flow logically (prerequisite -> parse -> artifacts -> extract -> resolve -> generate -> write -> summary)
   - Flexible phase argument parsing (bare numbers, "wave N", "epic N")
   - Inline resolve vs Task() research dispatch for needs-spike TDs
   - SPIKE.md output format has full traceability
   - No state.yml transitions (spike is stateless)

3. Verify both skills appear in /expedite: autocomplete (they should auto-discover from skills/{name}/SKILL.md)
  </action>
  <verify>User types "approved" or describes issues to fix.</verify>
  <done>Human has reviewed both spike and execute SKILL.md files and confirmed they are coherent, complete orchestration scripts.</done>
</task>

</tasks>

<verification>
1. `skills/execute/SKILL.md` has all 7 step headings (Step 1 through Step 7)
2. Step 6 handles wave/epic transition with yes/spike-first/pause options
3. Step 7 sets phase to "complete" in state.yml (lifecycle completion)
4. Step 7 uses `cat >>` for final PROGRESS.md append
5. Total SKILL.md line count is 350-450 lines
6. Execute skill covers EXEC-01 through EXEC-06
7. Spike skill covers SPIKE-01 through SPIKE-05
8. Both skills auto-discoverable via /expedite: namespace
9. Human verified both skills read as coherent orchestration scripts
</verification>

<success_criteria>
- Complete execute SKILL.md with all 7 steps (~350-450 lines)
- Wave transition supports spike-first workflow
- Lifecycle completion with contract chain statistics
- Human-verified: both spike and execute skills are coherent and complete
- All 11 phase requirements (SPIKE-01 through SPIKE-05, EXEC-01 through EXEC-06) addressed
</success_criteria>

<output>
After completion, create `.planning/phases/09-spike-and-execute-skills/09-03-SUMMARY.md`
</output>
