---
phase: 09-spike-and-execute-skills
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - skills/execute/SKILL.md
autonomous: false
requirements: [EXEC-03, EXEC-04, EXEC-05]

must_haves:
  truths:
    - "Per-phase checkpoint.yml tracks execution position for pause/resume across sessions"
    - "Per-phase PROGRESS.md is written exclusively via cat >> Bash (never Write tool rewrite)"
    - "Micro-interaction between tasks uses freeform yes/pause/review prompt"
    - "Phase completion updates per-phase checkpoint status to complete and summarizes outcomes"
    - "Lifecycle completion (phase -> complete in state.yml) only occurs when user executes the final phase"
    - "Execute does NOT auto-chain to next phase -- user explicitly runs /expedite:execute <N> for each"
  artifacts:
    - path: "skills/execute/SKILL.md"
      provides: "Complete execute skill with Steps 1-7"
      min_lines: 350
  key_links:
    - from: "skills/execute/SKILL.md Step 6"
      to: ".expedite/plan/phases/{slug}/checkpoint.yml"
      via: "Set status to complete for current phase"
      pattern: "checkpoint\\.yml"
    - from: "skills/execute/SKILL.md Step 7"
      to: ".expedite/state.yml"
      via: "Set phase to complete (only on final phase)"
      pattern: "phase.*complete"
---

<objective>
Append Steps 6-7 to the execute SKILL.md: per-phase completion summary and lifecycle completion detection. Then perform a human verification of the complete execute SKILL.md as a coherent orchestration script.

Execute operates per-phase: Step 6 completes ONE phase (not all phases). Step 7 detects whether this was the LAST phase and, only then, sets the lifecycle to "complete". There is NO auto-chaining -- between phases, the user manually runs `/expedite:spike <N>` and `/expedite:execute <N>`.

Purpose: Steps 6-7 complete the execute skill by handling per-phase finalization and lifecycle-level completion detection. The checkpoint verify ensures the full skill (Steps 1-7) reads as a coherent, complete orchestration script.

Output: Complete `skills/execute/SKILL.md` with all 7 steps (~350-450 lines).
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-spike-and-execute-skills/09-RESEARCH.md
@.planning/phases/09-spike-and-execute-skills/09-02-SUMMARY.md

<interfaces>
From per-phase checkpoint.yml schema (design decision #18):
```yaml
# Located at .expedite/plan/phases/{slug}/checkpoint.yml
current_task: "t03"
current_wave: 2
last_completed_task: "t02"
last_completed_at: "2026-02-26T14:30:00Z"
tasks_completed: 2
tasks_total: 5           # tasks in THIS phase only
status: "in_progress"    # in_progress | paused | complete
continuation_notes: "Completed auth middleware. Next: rate limiting."
```

From state.yml.template transitions:
```yaml
# execute_in_progress -> complete   (only when ALL phases done)
# (any) -> archived
```

From per-phase PROGRESS.md append format (design decision #20):
```markdown
## {task_id}: {task_title}
- Status: {complete|partial|failed|needs_review}
- Wave: {wave_number}
- Design decision: {DA-X}: {brief}
- Files modified: {list}
- Verification: {VERIFIED|PARTIAL|FAILED|NEEDS REVIEW}
- Completed: {ISO 8601 UTC timestamp}
```

Per-phase execution model (USER DECISION -- mandatory):
```
User workflow:
  /expedite:spike 1  -> SPIKE.md in .expedite/plan/phases/wave-1/
  /expedite:execute 1 -> checkpoint.yml + PROGRESS.md in .expedite/plan/phases/wave-1/
  /expedite:spike 2  -> SPIKE.md in .expedite/plan/phases/wave-2/
  /expedite:execute 2 -> checkpoint.yml + PROGRESS.md in .expedite/plan/phases/wave-2/

NO auto-chaining between phases. Execute completes ONE phase per invocation.
Lifecycle "complete" only when the final phase is executed.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Steps 6-7 to execute SKILL.md (phase completion and lifecycle completion detection)</name>
  <files>skills/execute/SKILL.md</files>
  <action>
Append Steps 6-7 to the execute SKILL.md from Plan 09-02. These steps handle per-phase completion and lifecycle-level completion detection.

**Step 6: Phase Completion**

After all tasks in the current phase are complete (Step 5 loop exhausted):

**6a: Update per-phase checkpoint to complete.**
Update `.expedite/plan/phases/{slug}/checkpoint.yml`:
```yaml
current_task: null
current_wave: {wave_number}
last_completed_task: "{final_task_id}"
last_completed_at: "{ISO 8601 UTC}"
tasks_completed: {total}
tasks_total: {total}
status: "complete"
continuation_notes: "{Wave/Epic} {N} execution complete."
```

**6b: Append phase completion entry to PROGRESS.md via Bash.**
```bash
cat >> .expedite/plan/phases/{slug}/PROGRESS.md << 'PROGRESS_EOF'

---

## Phase Complete: {Wave/Epic} {N}
- Total tasks: {total}
- Verified: {count}
- Partial: {count}
- Failed: {count}
- Skipped: {count}
- Completed: {ISO 8601 UTC timestamp}
PROGRESS_EOF
```

**6c: Display phase completion summary.**
```
--- {Wave/Epic} {N} Complete ---

Project: {project_name}
Phase: {Wave/Epic} {N} - {description}

Tasks completed: {count}/{total}
  - Verified: {count}
  - Partial: {count}
  - Failed: {count}
  - Skipped: {count}

Artifacts:
  - Progress log: .expedite/plan/phases/{slug}/PROGRESS.md
  - Checkpoint: .expedite/plan/phases/{slug}/checkpoint.yml (status: complete)

Contract Chain for this phase:
  Scope DA -> Design Decision -> Plan Task -> {Spike Step (if spiked) ->} Code Change -> Verified
```

**6d: Check if this was the final phase.**
Read PLAN.md and determine if there are any phases AFTER the current one.

If **more phases remain** (NOT the last phase):
Display:
```
### Next Steps
Remaining phases: {list remaining phase numbers and descriptions}
{If next phase has needs-spike TDs and no SPIKE.md:}
Recommended: Run `/expedite:spike {next_N}` to investigate tactical decisions before execution.
{Else:}
Run `/expedite:spike {next_N}` (optional) or `/expedite:execute {next_N}` to continue.
```

Update state.yml (backup-before-write): keep `phase` as `"execute_in_progress"`, set `current_wave` to null, set `current_task` to null, set `last_modified`. Do NOT set phase to "complete" -- there are more phases.

STOP. (User explicitly invokes the next phase.)

If **no more phases** (this was the last phase):
Proceed to Step 7.

**Step 7: Lifecycle Completion**

This step only runs when the FINAL phase has been executed.

**7a: Update state.yml (backup-before-write):**
- Set `phase` to `"complete"`
- Set `current_task` to null
- Set `current_wave` to null
- Clear `tasks` array (phase-level tracking complete)
- Set `last_modified` to current ISO 8601 UTC timestamp

**7b: Display lifecycle completion summary:**
```
## Lifecycle Complete

Project: {project_name}
Intent: {intent}

### Execution Summary
Total phases executed: {count}
{For each phase:}
  - {Wave/Epic} {N}: {tasks_completed}/{tasks_total} tasks ({verified_count} verified)

### Per-Phase Artifacts
{For each phase:}
  - .expedite/plan/phases/{slug}/PROGRESS.md
  - .expedite/plan/phases/{slug}/checkpoint.yml (status: complete)
{If any phases were spiked:}
  - .expedite/plan/phases/{slug}/SPIKE.md

### Contract Chain Summary
Scope -> Research -> Design -> Plan -> {Spike (where used) ->} Execute (complete)
Decision Areas: {N} | Design Decisions: {count} | Phases: {count} | Tasks: {total across all phases}

### Lifecycle Status
Phase: complete
{If any FAILED tasks across any phase:} WARNING: {count} tasks had FAILED verification. Review per-phase PROGRESS.md files for details.

### Next Steps
- Review per-phase PROGRESS.md files for detailed task outcomes
- Run `/expedite:scope` to start a new lifecycle
{If product intent:} - Review .expedite/design/HANDOFF.md for engineer handoff documentation
```

Note: Do NOT archive the lifecycle. Archival is Phase 10 functionality (ARTF-03).
  </action>
  <verify>
Verify Steps 6 and 7 headings are present in SKILL.md. Verify Step 6 completes ONE phase (updates per-phase checkpoint, appends to per-phase PROGRESS.md). Verify Step 6d checks if more phases remain and STOPs if so (no auto-chaining). Verify Step 6d suggests next spike/execute commands. Verify Step 7 ONLY runs on the final phase. Verify Step 7 sets phase to "complete" in state.yml. Verify Step 7 uses `cat >>` for final PROGRESS.md append. Verify lifecycle summary aggregates across all phases. Count total SKILL.md lines: should be 350+ lines.
  </verify>
  <done>
Execute SKILL.md has all 7 steps: prerequisite check, artifact loading, state initialization, resume logic, task execution loop, per-phase completion (with stop -- no auto-chaining), and lifecycle completion (only on final phase). Phase transitions to "complete" only when the last phase finishes. All execution artifacts stored per-phase.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete spike and execute skills</name>
  <files>skills/spike/SKILL.md, skills/execute/SKILL.md</files>
  <action>
Present both complete SKILL.md files for human review. The user reads each file end-to-end and verifies they are coherent orchestration scripts ready for end-to-end use.

What was built: Complete execute SKILL.md (7 steps, ~350-450 lines) and spike SKILL.md (8 steps, ~250-350 lines) -- both skills implementing the final two stages of the Expedite lifecycle.

How to verify:
1. Read skills/execute/SKILL.md end-to-end. Verify it reads as a coherent orchestration script:
   - Steps flow logically (prerequisite -> artifacts -> state -> resume -> loop -> phase-complete -> lifecycle-complete)
   - Execute operates per-phase: one invocation = one phase
   - No auto-chaining between phases (user explicitly runs `/expedite:execute <N>`)
   - Spiked vs unspiked mode detection is clear
   - Per-task verification references prompt-task-verifier.md correctly
   - Per-phase PROGRESS.md uses cat >> exclusively (never Write tool)
   - Per-phase checkpoint.yml stored in `.expedite/plan/phases/{slug}/`
   - Micro-interaction uses freeform prompts (never AskUserQuestion)
   - Error handling covers retry/skip/pause
   - Phase completion stops and tells user what to run next
   - Lifecycle completion only triggers on the final phase
   - Lifecycle completion sets phase to "complete"

2. Read skills/spike/SKILL.md end-to-end. Verify it reads as a coherent orchestration script:
   - Steps flow logically (prerequisite -> parse -> artifacts -> extract -> resolve -> generate -> write -> summary)
   - Spike operates per-phase: one invocation = one phase
   - Flexible phase argument parsing (bare numbers, "wave N", "epic N")
   - Prior-phase context loaded when spiking Phase N > 1
   - Inline resolve vs Task() research dispatch for needs-spike TDs
   - SPIKE.md output format has full traceability
   - No state.yml transitions (spike is stateless)

3. Verify both skills appear in /expedite: autocomplete (they should auto-discover from skills/{name}/SKILL.md)
  </action>
  <verify>User types "approved" or describes issues to fix.</verify>
  <done>Human has reviewed both spike and execute SKILL.md files and confirmed they are coherent, complete orchestration scripts with per-phase execution architecture.</done>
</task>

</tasks>

<verification>
1. `skills/execute/SKILL.md` has all 7 step headings (Step 1 through Step 7)
2. Step 6 handles per-phase completion: updates per-phase checkpoint.yml, appends to per-phase PROGRESS.md, displays summary, and STOPs (no auto-chaining)
3. Step 6 suggests next spike/execute commands for remaining phases
4. Step 7 ONLY triggers when the final phase is executed
5. Step 7 sets phase to "complete" in state.yml (lifecycle completion)
6. Step 7 aggregates results across all phases in lifecycle summary
7. Total SKILL.md line count is 350-450 lines
8. Execute skill covers EXEC-01 through EXEC-06
9. Spike skill covers SPIKE-01 through SPIKE-05
10. Both skills operate per-phase with no auto-chaining
11. All execution artifacts stored per-phase in `.expedite/plan/phases/{slug}/`
12. Both skills auto-discoverable via /expedite: namespace
13. Human verified both skills read as coherent orchestration scripts
</verification>

<success_criteria>
- Complete execute SKILL.md with all 7 steps (~350-450 lines)
- Per-phase execution: each invocation completes one phase, then stops
- No auto-chaining between phases -- user controls the cadence
- Lifecycle completion only on final phase
- Human-verified: both spike and execute skills are coherent and complete
- All 11 phase requirements (SPIKE-01 through SPIKE-05, EXEC-01 through EXEC-06) addressed
</success_criteria>

<output>
After completion, create `.planning/phases/09-spike-and-execute-skills/09-03-SUMMARY.md`
</output>
