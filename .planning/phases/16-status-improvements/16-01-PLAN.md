---
phase: 16-status-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/status/SKILL.md
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03]

must_haves:
  truths:
    - "When log.yml exceeds 50KB, status output includes a visible Diagnostics section with a log size warning"
    - "When log.yml is absent or under 50KB, no log size warning appears"
    - "When state.yml says design_complete but DESIGN.md is missing, status output reports the inconsistency"
    - "When all expected artifacts exist for the current phase, no artifact inconsistencies appear"
    - "Status skill never writes to state.yml or any other file"
    - "The Diagnostics section is omitted entirely when there are no warnings or inconsistencies"
  artifacts:
    - path: "skills/status/SKILL.md"
      provides: "Complete status skill with log size check, artifact cross-reference, and read-only enforcement"
      contains: "wc -c"
  key_links:
    - from: "skills/status/SKILL.md Step 4"
      to: ".expedite/log.yml"
      via: "Bash wc -c size check"
      pattern: "wc -c.*log\\.yml"
    - from: "skills/status/SKILL.md Step 5"
      to: ".expedite/scope/SCOPE.md, .expedite/research/SYNTHESIS.md, .expedite/design/DESIGN.md, .expedite/plan/PLAN.md"
      via: "Glob or Read existence check"
      pattern: "SCOPE\\.md.*SYNTHESIS\\.md.*DESIGN\\.md.*PLAN\\.md"
---

<objective>
Add log size warning and artifact cross-referencing to the status skill, reinforcing its read-only constraint.

Purpose: Users get proactive diagnostics about lifecycle health -- oversized logs and missing artifacts -- without the status skill ever modifying state.
Output: Updated `skills/status/SKILL.md` with 2 new steps (log size check, artifact cross-reference), a Diagnostics output section, and reinforced read-only guard.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-status-improvements/16-RESEARCH.md

<interfaces>
<!-- Current status SKILL.md structure (121 lines, 8 steps) -->
<!-- The executor modifies THIS file. Key structure: -->

From skills/status/SKILL.md:
```yaml
---
name: status
allowed-tools: [Read, Glob, Bash]
---
```

Current step structure:
- Step 1: Check for active lifecycle
- Step 2: Parse state fields (extracts phase, questions, gate_history, current_step, etc.)
- Step 3: Parse current_step (Phase 15 addition -- lookup table, conditional display)
- Step 4: Map phase to human-readable description (11 phase mappings)
- Step 5: Determine next action (11 phase-to-action mappings)
- Step 6: Count question statuses
- Step 7: Display formatted output (exact format template with # Expedite Lifecycle Status)
- Step 8: Do NOT modify any files (read-only guard)

After modification:
- Steps 1-3: unchanged
- Step 4: NEW -- Log size check (STAT-01)
- Step 5: NEW -- Artifact cross-reference (STAT-02)
- Steps 6-10: renumbered from current 4-8
- Step 9: Output format gains ## Diagnostics section
- Step 10: Read-only guard reinforced (STAT-03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add log size check and artifact cross-reference steps to status SKILL.md</name>
  <files>skills/status/SKILL.md</files>
  <action>
Read the current `skills/status/SKILL.md` (121 lines, 8 steps). Modify it to add 2 new steps and update the output format. The file structure after modification should have 10 steps total (~185 lines).

**New Step 4: Log Size Check (STAT-01)**

Insert after current Step 3 (Parse current_step). Exact content:

```
### Step 4: Log Size Check

Check if `.expedite/log.yml` exists and exceeds 50KB (51200 bytes).

1. Run via Bash: `wc -c < .expedite/log.yml 2>/dev/null`
2. If the command fails (file does not exist), skip this step entirely -- no warning needed.
3. If the byte count exceeds 51200, store a warning:
   "log.yml is {size_kb}KB -- consider archiving the current lifecycle to reset it"
   Where {size_kb} is the byte count divided by 1024, rounded to nearest integer.
4. This warning will be displayed in the Diagnostics section of the output (Step 9).
```

**New Step 5: Artifact Cross-Reference (STAT-02)**

Insert after the new Step 4. Exact content:

```
### Step 5: Artifact Cross-Reference

Cross-reference expected artifact files against the current phase from state.yml.

1. Read the `phase` value from the parsed state (Step 2).
2. Use this mapping to determine which artifacts SHOULD exist:
   - `scope_complete` and later: `.expedite/scope/SCOPE.md`
   - `research_complete` and later: `.expedite/research/SYNTHESIS.md`
   - `design_complete` and later: `.expedite/design/DESIGN.md`
   - `plan_complete` and later: `.expedite/plan/PLAN.md`
   - `scope_in_progress`: no artifacts expected yet
   - `archived`: skip check entirely (artifacts may have been moved to archive/)

   "Later" means any phase that comes AFTER in the lifecycle sequence.
   The full phase ordering is:
   scope_in_progress < scope_complete < research_in_progress < research_complete < design_in_progress < design_complete < plan_in_progress < plan_complete < execute_in_progress < complete

3. For each expected artifact, check existence using Glob or Read.
4. Collect any missing artifacts as inconsistencies:
   - Format: "State says {phase} but {artifact_path} not found"
5. These inconsistencies will be displayed in the Diagnostics section (Step 9).
6. If phase is `scope_in_progress` or `archived`, or if no inconsistencies found, store nothing.

**Do NOT attempt to fix inconsistencies.** This is a read-only diagnostic.
```

**Renumber existing steps:**
- Current Step 4 (Map phase) becomes Step 6
- Current Step 5 (Determine next action) becomes Step 7
- Current Step 6 (Count question statuses) becomes Step 8
- Current Step 7 (Display formatted output) becomes Step 9
- Current Step 8 (Do NOT modify) becomes Step 10

**Update Step 9 output format (formerly Step 7):**

Add a `## Diagnostics` section to the output format template. Place it AFTER the `## Recovery Info` section. The section should only appear when there are warnings or inconsistencies to report:

```
## Diagnostics
{Only display this section if there are warnings or inconsistencies from Steps 4-5.
 If no issues found, omit this section entirely.}

**Warnings:**
- {log size warning from Step 4, if applicable}

**Artifact Inconsistencies:**
- {inconsistency message from Step 5, if applicable}

(Status is read-only. Run the relevant skill to fix inconsistencies.)
```

Within the Diagnostics section, only include the **Warnings** sub-heading if there is a log size warning. Only include the **Artifact Inconsistencies** sub-heading if there are inconsistencies. If both exist, show both.

**Update Step 10 (formerly Step 8) -- Read-Only Reinforcement (STAT-03):**

Expand the existing read-only guard to explicitly mention the new diagnostic steps:

```
### Step 10: Read-Only Constraint

Do NOT modify any files. This is a read-only skill. Do not write to state.yml, log.yml, or any other file. If inconsistencies are found in Step 5, REPORT them in the output -- do not attempt to fix them.
```

**Important:** Update ALL internal step references within the file. The output format instructions in Step 9 that previously said "step 4" etc. must reference the new step numbers. The "next action" routing was in Step 5 and is now Step 7 -- verify no cross-references break.
  </action>
  <verify>
    <automated>grep -c "^###\? Step" skills/status/SKILL.md | grep -q "10" && grep -q "wc -c" skills/status/SKILL.md && grep -q "Artifact Cross-Reference" skills/status/SKILL.md && grep -q "Diagnostics" skills/status/SKILL.md && grep -q "Do NOT modify any files" skills/status/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - skills/status/SKILL.md has exactly 10 numbered steps
    - Step 4 checks log.yml size using `wc -c` with 51200 byte threshold
    - Step 5 cross-references artifacts against phase using the full phase-to-artifact mapping table
    - Step 9 output format includes conditional Diagnostics section after Recovery Info
    - Step 10 explicitly says read-only and mentions Step 5 inconsistencies
    - All internal step references are updated (no stale references to old step numbers)
    - Frontmatter and Steps 1-3 are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate SKILL.md structure and step numbering integrity</name>
  <files>skills/status/SKILL.md</files>
  <action>
Read the modified `skills/status/SKILL.md` and verify structural integrity:

1. **Step count:** Confirm exactly 10 steps, numbered sequentially 1-10.
2. **Step content verification:**
   - Step 1: "Check for active lifecycle" (unchanged)
   - Step 2: "Parse state fields" (unchanged)
   - Step 3: "Parse current_step" (unchanged)
   - Step 4: Contains "wc -c" and "51200" and "log.yml"
   - Step 5: Contains "Artifact Cross-Reference" and the phase ordering sequence and "Do NOT attempt to fix"
   - Step 6: Contains "Map phase to human-readable description" and all 11 phase mappings
   - Step 7: Contains "Determine next action" and all 11 phase-to-action mappings
   - Step 8: Contains "Count question statuses"
   - Step 9: Contains the output format template with "# Expedite Lifecycle Status" AND "## Diagnostics"
   - Step 10: Contains "Do NOT modify any files" and "read-only"
3. **No stale references:** Search for any reference to "step 4" that means the old step 4 (phase mapping) or "step 5" that means the old step 5 (next action). All step references must point to their new numbers.
4. **Frontmatter unchanged:** `allowed-tools` still lists exactly `Read`, `Glob`, `Bash` (no `Write`).
5. **Diagnostics section conditional:** The output format instructions clearly state to omit the Diagnostics section entirely when no issues exist.

If any issue found, fix it in-place. This is a validation+fixup task.
  </action>
  <verify>
    <automated>grep -n "Step [0-9]" skills/status/SKILL.md | head -20 && echo "---" && grep "allowed-tools" -A 5 skills/status/SKILL.md | head -6 && echo "---" && grep -c "Write" skills/status/SKILL.md | grep -q "^0$" && echo "NO_WRITE_TOOL: PASS" || echo "NO_WRITE_TOOL: FAIL"</automated>
  </verify>
  <done>
    - 10 steps numbered sequentially 1-10 with no gaps or duplicates
    - All step content matches expected purpose (no step accidentally received wrong content)
    - No stale step-number cross-references
    - allowed-tools frontmatter does NOT include Write
    - Diagnostics section only shown conditionally
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `skills/status/SKILL.md` exists with 10 numbered steps
2. Step 4 implements STAT-01 (log size warning at 50KB/51200 bytes)
3. Step 5 implements STAT-02 (artifact cross-reference with full phase-to-artifact mapping)
4. Step 10 reinforces STAT-03 (read-only constraint, explicitly mentions not fixing inconsistencies)
5. Output format includes conditional Diagnostics section
6. No Write tool in allowed-tools (enforces STAT-03 at the tool level)
7. All step references are consistent (no stale numbers from pre-renumbering)
</verification>

<success_criteria>
- STAT-01: Status skill contains a step that checks `.expedite/log.yml` size against 51200 bytes using `wc -c` and produces a warning when exceeded
- STAT-02: Status skill contains a step that maps phases to expected artifacts and reports missing ones as inconsistencies
- STAT-03: Status skill's allowed-tools excludes Write, and final step explicitly forbids file modification including for "fixing" inconsistencies
- Output format has a conditional Diagnostics section that only appears when there are issues to report
</success_criteria>

<output>
After completion, create `.planning/phases/16-status-improvements/16-01-SUMMARY.md`
</output>
