---
phase: 36-spike-g5-integration-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/spike/SKILL.md
  - hooks/lib/gate-checks.js
  - hooks/lib/fsm.js
autonomous: true
requirements: [GATE-07, THIN-04]
gap_closure: true

must_haves:
  truths:
    - "Spike skill writes SPIKE.md before G5 gate evaluates it"
    - "G5 gate result writes to gates.yml succeed during spike_in_progress lifecycle phase"
    - "After spike completion, FSM permits transition from spike_complete to execute_in_progress"
    - "Spike skill writes spike_in_progress at start and spike_complete at end, consistent with all other skills"
  artifacts:
    - path: "skills/spike/SKILL.md"
      provides: "Fixed step ordering (write SPIKE.md before G5) and phase transition writes"
      contains: "spike_in_progress"
    - path: "hooks/lib/gate-checks.js"
      provides: "Unchanged GATE_PHASE_MAP (G5 already maps to spike_in_progress/spike_complete)"
    - path: "hooks/lib/fsm.js"
      provides: "Unchanged FSM (already has spike_in_progress -> spike_complete -> execute_in_progress)"
  key_links:
    - from: "skills/spike/SKILL.md"
      to: "hooks/lib/fsm.js"
      via: "spike skill writes spike_in_progress and spike_complete phases matching FSM transitions"
      pattern: "spike_in_progress.*spike_complete"
    - from: "skills/spike/SKILL.md"
      to: "hooks/lib/gate-checks.js"
      via: "G5 gate write happens during spike_in_progress, which is in GATE_PHASE_MAP"
      pattern: "G5.*spike_in_progress"
    - from: "skills/spike/SKILL.md Step 8 (write SPIKE.md)"
      to: "skills/spike/SKILL.md Step 9 (G5 gate)"
      via: "SPIKE.md exists on disk before g5-spike.js reads it"
      pattern: "Write SPIKE.md.*G5 Gate"
---

<objective>
Fix 3 interrelated breaks preventing the spike -> G5 gate -> execute flow from working end-to-end.

Purpose: Close GATE-07 (G5 dual-layer gate unsatisfied) and THIN-04 (gated transitions broken for G5). The spike skill currently (1) runs G5 before writing SPIKE.md, (2) operates during plan_complete which the GATE_PHASE_MAP denies for G5 writes, and (3) never writes spike_in_progress/spike_complete so the FSM path to execute is dead. All three breaks are fixed by one coherent design: spike writes its own phase transitions like every other skill.

Output: Updated skills/spike/SKILL.md with correct step ordering and phase transition writes
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v3.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From hooks/lib/fsm.js (UNCHANGED -- already correct):
```javascript
var TRANSITIONS = {
  'plan_complete':          { to: 'spike_in_progress',    gate: null },
  'spike_in_progress':      { to: 'spike_complete',       gate: 'G5' },
  'spike_complete':         { to: 'execute_in_progress',  gate: null },
};
```

From hooks/lib/gate-checks.js (UNCHANGED -- already correct):
```javascript
var GATE_PHASE_MAP = {
  'G5': ['spike_in_progress', 'spike_complete'],
};
```

From hooks/validate-state-write.js HOOK-04 logic (lines 218-266):
- When gates.yml is written with a new history entry, HOOK-04 reads state.yml to get current phase
- Calls gateChecks.validateGateForPhase(entry.gate, stateObj.phase)
- If current phase is NOT in GATE_PHASE_MAP[gate], write is denied
- So G5 gate write is allowed during spike_in_progress and spike_complete ONLY

From skills/spike/SKILL.md current step ordering (BROKEN):
- Step 7: G5 Gate Evaluation (reads SPIKE.md which doesn't exist yet)
- Step 8: Write SPIKE.md (creates the artifact G5 needs)
- Line 166: "NOTE: Spike does NOT write phase transitions"

Design direction per audit: Have spike write its own phase transitions (spike_in_progress at start, spike_complete at end). This naturally fixes all 3 breaks because:
- FSM already has plan_complete -> spike_in_progress (no gate required)
- GATE_PHASE_MAP already allows G5 during spike_in_progress
- FSM already has spike_complete -> execute_in_progress (no gate required)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phase transition writes to spike skill</name>
  <files>skills/spike/SKILL.md</files>
  <action>
Modify skills/spike/SKILL.md to add phase transition writes, matching the pattern used by all other skills (scope, research, design, plan all write their own _in_progress and _complete phases).

**Step 1 changes (Prerequisite Check):**
In Step 1, after the prerequisite check passes (Case A or Case B), add a phase transition write to spike_in_progress. Insert BEFORE "Proceed to Step 2":

For Case A (plan_complete): After "Starting spike..." add: Write state.yml setting `phase: "spike_in_progress"` using backup-before-write (read state.yml, cp .expedite/state.yml .expedite/state.yml.bak, update phase to spike_in_progress, update last_modified, write back). This uses the existing FSM transition plan_complete -> spike_in_progress (no gate required).

For Case B (execute_in_progress): Do NOT write spike_in_progress. This case is a re-spike during execution -- the phase should stay as execute_in_progress. Add a note: "Phase stays execute_in_progress (re-spike during execution)."

**Step 9 changes (Display Summary, currently the final step):**
At the end of Step 9, after writing the completion checkpoint and before the STOP, add: Write state.yml setting `phase: "spike_complete"` using backup-before-write. Then display: "Next: `/expedite:execute {N}`."

Note: The spike_in_progress -> spike_complete transition requires G5 passage per the FSM. By Step 9, G5 has already been evaluated and passed (Step 7/8 in the new ordering), so the hook will allow this write.

**Remove the NOTE on line 166:** Delete or replace the line that says "NOTE: Spike does NOT write phase transitions (no spike_in_progress/spike_complete phases). It operates within plan_complete or execute_in_progress. Gate outcomes are recorded in gates.yml." Replace with: "NOTE: Spike writes spike_in_progress at Step 1 and spike_complete at Step 9 (Case A only). When re-spiking during execution (Case B), phase stays execute_in_progress."

Do NOT modify the GATE_PHASE_MAP in hooks/lib/gate-checks.js or the FSM in hooks/lib/fsm.js -- they already have the correct entries for spike_in_progress and spike_complete.
  </action>
  <verify>
grep -c "spike_in_progress" skills/spike/SKILL.md should return at least 1.
grep -c "spike_complete" skills/spike/SKILL.md should return at least 1.
grep "does NOT write phase transitions" skills/spike/SKILL.md should return 0 results (old NOTE removed).
  </verify>
  <done>Spike skill writes spike_in_progress at Step 1 (Case A) and spike_complete at Step 9. Old NOTE about not writing phase transitions is replaced. FSM and GATE_PHASE_MAP remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fix step ordering -- write SPIKE.md before G5 gate evaluation</name>
  <files>skills/spike/SKILL.md</files>
  <action>
Swap Steps 7 and 8 in skills/spike/SKILL.md so that SPIKE.md is written to disk BEFORE the G5 gate evaluates it.

**Current (broken) ordering:**
- Step 7: G5 Gate Evaluation (Dual-Layer) -- reads SPIKE.md which doesn't exist yet
- Step 8: Write SPIKE.md -- creates the artifact

**New (fixed) ordering:**
- Step 7: Write SPIKE.md -- create the artifact first (was Step 8)
- Step 8: G5 Gate Evaluation (Dual-Layer) -- reads SPIKE.md which now exists (was Step 7)

Specifically:
1. Read the current SKILL.md
2. The current "### Step 7: G5 Gate Evaluation (Dual-Layer)" section becomes "### Step 8: G5 Gate Evaluation (Dual-Layer)"
3. The current "### Step 8: Write SPIKE.md" section becomes "### Step 7: Write SPIKE.md"
4. In the G5 gate section (now Step 8): Update "**On Go:** Proceed to Step 8." to "**On Go:** Proceed to Step 9." since the next step after gate evaluation is now Step 9 (Display Summary).
5. In the G5 gate section: Any references to looping back to "relevant step" for recycle are fine as-is (they reference earlier steps by description, not number).
6. Update checkpoint step/label references within both sections to match their new step numbers.

IMPORTANT: Preserve ALL content within each step section exactly -- only change the step numbers (7<->8) and the cross-references between them. Do not modify the gate evaluation logic, the SPIKE.md writing logic, or any other step content.
  </action>
  <verify>
Read skills/spike/SKILL.md and verify:
- "### Step 7" heading contains "Write SPIKE.md"
- "### Step 8" heading contains "G5 Gate Evaluation"
- Step 8 says "Proceed to Step 9" (not "Proceed to Step 8")
- The G5 gate section references reading SPIKE.md (which now exists because Step 7 wrote it)
  </verify>
  <done>Step ordering fixed: SPIKE.md is written at Step 7, G5 gate evaluates at Step 8. The gate script (g5-spike.js) will find SPIKE.md on disk when it runs M1 check. Cross-references between steps are correct.</done>
</task>

</tasks>

<verification>
All 3 breaks from the v3.0 audit are resolved by these 2 tasks:

- **BREAK-1 (Step ordering):** Task 2 swaps Steps 7/8 so SPIKE.md exists before G5 reads it.
- **BREAK-2 (Gate-phase map mismatch):** Task 1 makes spike write spike_in_progress, which is already in GATE_PHASE_MAP for G5. No GATE_PHASE_MAP changes needed.
- **BREAK-3 (FSM dead-end):** Task 1 makes spike write spike_in_progress at start and spike_complete at end. FSM already has spike_complete -> execute_in_progress.

Verification commands:
1. `grep -n "spike_in_progress\|spike_complete" skills/spike/SKILL.md` -- should show phase transition writes
2. `grep -n "Step 7.*Write SPIKE\|Step 8.*G5 Gate" skills/spike/SKILL.md` -- should confirm correct ordering
3. `grep "does NOT write phase transitions" skills/spike/SKILL.md` -- should return nothing (old NOTE removed)
4. `node -e "var g=require('./hooks/lib/gate-checks');console.log(g.GATE_PHASE_MAP.G5)"` -- should show ['spike_in_progress', 'spike_complete'] (unchanged)
5. `node -e "var f=require('./hooks/lib/fsm');console.log(f.validateTransition('plan_complete','spike_in_progress'))"` -- should show {valid:true, gate:null}
6. `node -e "var f=require('./hooks/lib/fsm');console.log(f.validateTransition('spike_complete','execute_in_progress'))"` -- should show {valid:true, gate:null}
</verification>

<success_criteria>
1. SPIKE.md is written to disk (Step 7) before G5 gate evaluates it (Step 8) -- BREAK-1 closed
2. Spike skill writes spike_in_progress at Step 1, allowing G5 gate writes during that phase -- BREAK-2 closed
3. Spike skill writes spike_complete at Step 9, enabling FSM path to execute_in_progress -- BREAK-3 closed
4. hooks/lib/gate-checks.js and hooks/lib/fsm.js are NOT modified (they already have correct entries)
5. Re-spike during execute_in_progress (Case B) does NOT write spike_in_progress (avoids FSM violation)
</success_criteria>

<output>
After completion, create `.planning/phases/36-spike-g5-integration-fix/36-01-SUMMARY.md`
</output>
