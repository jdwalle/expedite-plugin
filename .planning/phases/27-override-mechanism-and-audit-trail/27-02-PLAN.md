---
phase: 27-override-mechanism-and-audit-trail
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - skills/shared/ref-override-protocol.md
  - skills/scope/SKILL.md
  - skills/research/SKILL.md
  - skills/design/SKILL.md
  - skills/plan/SKILL.md
  - skills/spike/SKILL.md
  - skills/execute/SKILL.md
  - skills/status/SKILL.md
autonomous: true
requirements: [OVRD-06]

must_haves:
  truths:
    - "Every skill includes an override protocol section that the LLM reads BEFORE it encounters any hook denial"
    - "The override protocol instructions match the exact denial message format from Plan 01 (same gate, timestamp, outcome, override_reason fields)"
    - "The LLM has two reinforcing sources of truth for the override flow: skill preamble + denial reason message"
  artifacts:
    - path: "skills/shared/ref-override-protocol.md"
      provides: "Single-source-of-truth override protocol reference injected into all skills"
      min_lines: 20
    - path: "skills/scope/SKILL.md"
      provides: "Override protocol cat injection before main heading"
      contains: "ref-override-protocol"
    - path: "skills/research/SKILL.md"
      provides: "Override protocol cat injection before main heading"
      contains: "ref-override-protocol"
    - path: "skills/design/SKILL.md"
      provides: "Override protocol cat injection before main heading"
      contains: "ref-override-protocol"
    - path: "skills/plan/SKILL.md"
      provides: "Override protocol cat injection before main heading"
      contains: "ref-override-protocol"
    - path: "skills/spike/SKILL.md"
      provides: "Override protocol cat injection before main heading"
      contains: "ref-override-protocol"
    - path: "skills/execute/SKILL.md"
      provides: "Override protocol cat injection before main heading"
      contains: "ref-override-protocol"
    - path: "skills/status/SKILL.md"
      provides: "Override protocol cat injection before main heading"
      contains: "ref-override-protocol"
  key_links:
    - from: "skills/*/SKILL.md"
      to: "skills/shared/ref-override-protocol.md"
      via: "!cat injection line"
      pattern: "cat skills/shared/ref-override-protocol"
    - from: "skills/shared/ref-override-protocol.md"
      to: "hooks/validate-state-write.js denial messages"
      via: "Identical override record format (gate, timestamp, outcome, override_reason)"
      pattern: "override_reason|gates.yml|immediately retry"
---

<objective>
Add override protocol section to all skill preambles so the LLM knows the deny-override-retry flow before encountering any denial

Purpose: Research estimates ~70-85% reliability for the two-step override round-trip (write override, retry state write) because the LLM may "move on" after step 1. Adding the protocol to every skill preamble gives the LLM prior knowledge before it encounters a denial, creating two reinforcing signals (preamble + denial reason). This is one of the three required mitigations from the product design.

Output: A shared override protocol reference file and !cat injection in all 7 skills
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-override-mechanism-and-audit-trail/27-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

Current skill frontmatter injection pattern (all 7 skills follow this):
```
---
name: {skill}
description: ...
allowed-tools: [...]
---

Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`

Checkpoint:
!`cat .expedite/checkpoint.yml 2>/dev/null || echo "No checkpoint"`

{skill-specific injections like questions.yml or gates.yml}

# {Skill} Skill
```

The override protocol injection goes between the last !cat injection and the `# {Skill} Skill` heading.

Existing shared reference (only one currently):
- skills/shared/ref-state-recovery.md

Override denial message format from Plan 01 (the protocol MUST match this):
```
To override: Write gates.yml with a new history entry:
{gate: "<GATE>", timestamp: "<ISO 8601>", outcome: "overridden",
override_reason: "<your justification>"}.
Then immediately retry this exact Write to state.yml.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared override protocol reference and inject into all skills</name>
  <files>skills/shared/ref-override-protocol.md, skills/scope/SKILL.md, skills/research/SKILL.md, skills/design/SKILL.md, skills/plan/SKILL.md, skills/spike/SKILL.md, skills/execute/SKILL.md, skills/status/SKILL.md</files>
  <action>
**A. Create skills/shared/ref-override-protocol.md** (~30-40 lines). This is an LLM instruction set, not human documentation. It must be concise and directive.

Content:

```markdown
# Override Protocol

When you attempt to write a state file (.expedite/*.yml) and the write is **denied by a hook**, follow the instructions in the denial reason exactly.

## Gate Override Flow

When a state transition is denied because a gate has not passed:

1. **Read the denial reason carefully.** It names the gate (e.g., G1) and provides the override record format.
2. **Write an override record to gates.yml.** Add a new entry to the history array:
   ```yaml
   history:
     # ... existing entries ...
     - gate: "G1"
       timestamp: "2026-01-01T00:00:00Z"
       outcome: "overridden"
       override_reason: "User requested: <justification>"
   ```
3. **Immediately retry the exact same Write to state.yml** that was denied. Do NOT proceed to other work between steps 2 and 3. The hook will recognize the override and allow the write.

## Important Rules

- **Do not skip the retry step.** After writing the override record, you MUST retry the state.yml write immediately.
- **Override records bypass gate-phase validation.** You can write an override for any gate regardless of current phase.
- **FSM transitions cannot be overridden via gates.yml.** If the denial is about an invalid phase transition (not a missing gate), the only bypass is `EXPEDITE_HOOKS_DISABLED=true`.
- **After 3 failed attempts**, the denial message will suggest manual intervention. Follow those instructions.

## Emergency Bypass

If enforcement is preventing all progress, the user can set:
```
EXPEDITE_HOOKS_DISABLED=true
```
This disables all hooks. Use only for debugging.
```

**B. Inject the reference into all 7 skills.** In each SKILL.md, add this block after the last `!cat .expedite/*.yml` injection line and BEFORE the `# {Skill} Skill` heading:

```
Override protocol:
!`cat skills/shared/ref-override-protocol.md 2>/dev/null || echo "No override protocol found"`
```

Add a blank line before and after the injection block for readability.

The 7 skills to modify: scope, research, design, plan, spike, execute, status.

For each skill, the injection point is:
- **scope**: After line `!cat .expedite/questions.yml...`, before `# Scope Skill`
- **research**: After the last `!cat .expedite/...` line, before `# Research Skill`
- **design**: After the last `!cat .expedite/...` line, before `# Design Skill`
- **plan**: After the last `!cat .expedite/...` line, before `# Plan Skill`
- **spike**: After the last `!cat .expedite/...` line, before `# Spike Skill`
- **execute**: After the last `!cat .expedite/...` line, before `# Execute Skill`
- **status**: After the last `!cat .expedite/...` line, before `# Status Skill`

**Important:** Use `!cat skills/shared/ref-override-protocol.md` (the plugin's skills/ directory), NOT `.expedite/`. The file lives in the plugin source, not in the project's state directory.

**Verify placement order** in each SKILL.md after editing:
1. Frontmatter (---/---)
2. State file cat injections (state.yml, checkpoint.yml, etc.)
3. Override protocol cat injection (NEW)
4. `# {Skill} Skill` heading and main content
  </action>
  <verify>
```bash
cd /Users/jaredwallenfels/Desktop/Projects/expedite-plugin && \
echo "=== File exists ===" && \
test -f skills/shared/ref-override-protocol.md && echo "ref-override-protocol.md: EXISTS" || echo "MISSING" && \
echo "" && \
echo "=== Injection in all 7 skills ===" && \
for skill in scope research design plan spike execute status; do \
  if grep -q "ref-override-protocol" "skills/$skill/SKILL.md"; then \
    echo "$skill: INJECTED"; \
  else \
    echo "$skill: MISSING INJECTION"; \
  fi; \
done && \
echo "" && \
echo "=== Content alignment with Plan 01 denial format ===" && \
grep -c "override_reason" skills/shared/ref-override-protocol.md | xargs -I{} echo "override_reason mentions: {}" && \
grep -c "immediately retry" skills/shared/ref-override-protocol.md | xargs -I{} echo "retry instruction mentions: {}" && \
grep -c "EXPEDITE_HOOKS_DISABLED" skills/shared/ref-override-protocol.md | xargs -I{} echo "env bypass mentions: {}" && \
echo "Content alignment: PASS"
```
  </verify>
  <done>
  - skills/shared/ref-override-protocol.md exists with gate override flow, retry instructions, and emergency bypass (~30-40 lines)
  - All 7 skills (scope, research, design, plan, spike, execute, status) include the override protocol cat injection
  - Override protocol placement is AFTER state file injections and BEFORE the main skill heading
  - Override record format matches the denial message format from Plan 01 (gate, timestamp, outcome, override_reason)
  - LLM now has two reinforcing sources: preamble protocol (read at skill load) + denial reason (read on deny) (OVRD-06)
  </done>
</task>

</tasks>

<verification>
1. `test -f skills/shared/ref-override-protocol.md && echo "EXISTS"` -- reference file exists
2. `grep -l "ref-override-protocol" skills/*/SKILL.md | wc -l` -- should be 7
3. `grep "override_reason" skills/shared/ref-override-protocol.md` -- format matches Plan 01
4. `grep "immediately retry" skills/shared/ref-override-protocol.md` -- retry instruction present
5. `grep "EXPEDITE_HOOKS_DISABLED" skills/shared/ref-override-protocol.md` -- emergency bypass documented
</verification>

<success_criteria>
- All 7 skill SKILL.md files inject the override protocol reference via !cat before the main skill heading
- The override protocol describes the complete gate override flow: read denial, write override record, immediately retry
- The override record format in the protocol matches the denial message format from hooks/validate-state-write.js
- The LLM encounters the override protocol BEFORE any state writes, providing prior knowledge of the deny-override-retry flow (OVRD-06)
</success_criteria>

<output>
After completion, create `.planning/phases/27-override-mechanism-and-audit-trail/27-02-SUMMARY.md`
</output>
