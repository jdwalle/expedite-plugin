---
phase: 02-plan-phase-retrospective-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/plan-decomposer.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Plan-decomposer agent produces plans with TD-N references in tasks that address tactical decisions"
    - "Plan-decomposer agent includes a Tactical Decisions table in generated PLAN.md"
    - "Plan-decomposer agent produces effort estimates calibrated for agent execution (minutes, not hours)"
    - "G4 M4 check passes on plans generated by the updated agent prompt"
  artifacts:
    - path: "agents/plan-decomposer.md"
      provides: "Plan-decomposer agent prompt with G4 TD-N format requirements and agent-calibrated effort estimates"
      contains: "TD-\\d+"
  key_links:
    - from: "agents/plan-decomposer.md"
      to: "gates/g4-plan.js"
      via: "TD-N format requirements matching G4 M4 regex"
      pattern: "TD-\\d+|tactical\\s+decision"
    - from: "agents/plan-decomposer.md"
      to: "plan SKILL.md"
      via: "agent dispatch output consumed by plan skill"
      pattern: "effort|estimate|agent"
---

<objective>
Add G4 tactical decision format requirements and agent-calibrated effort estimates to the plan-decomposer agent prompt.

Purpose: The plan-decomposer produced correct plans but omitted TD-N references because it did not know the G4 gate checks for them (M4 regex `/TD-\d+/`). Separately, effort estimates were calibrated for human developers (2-8 hours) instead of agent execution time (15min-2hr). Both issues are prompt additions to the same file.

Output: Updated agents/plan-decomposer.md with G4-aware TD format requirements and agent-calibrated effort guidance.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/plan-decomposer.md
@gates/g4-plan.js
</context>

<interfaces>
<!-- G4 M4 check from gates/g4-plan.js (lines 179-211) that the agent's output must satisfy -->
```javascript
// What G4 M4 actually checks:
var tdMatches = planContent.match(/TD-\d+/gi);
// Falls back to:
var hasTacticalText = /tactical\s+decision/i.test(planContent);
```

<!-- Current plan-decomposer effort guidance (line 131) -->
```markdown
- Effort estimates are realistic (2-8 hours per task is typical; larger tasks should be split)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add G4 tactical decision format requirements to plan-decomposer</name>
  <files>agents/plan-decomposer.md</files>
  <action>
Four targeted additions to agents/plan-decomposer.md. All changes are additive (no existing content is removed or rewritten).

**Addition 1: Tactical decision surfacing in `<instructions>` step 2 (line 61-64 area).**

After the existing step 2 ("Map design decisions to tasks"), add a new bullet or sub-step:

```
   - For design decisions that involve multiple viable implementation approaches (e.g., library choice, data structure, caching strategy), surface these as **tactical decisions (TDs)** with sequential IDs per phase: TD-1, TD-2, etc. Mark each as `resolved` (design already specifies exact approach) or `needs-spike` (direction set but implementation details require investigation). Every task referencing a TD must include `(TD-N)` in its title or description.
```

**Addition 2: Tactical Decisions table requirement in format templates.**

In the engineering format section (after the `### Task t02: ...` line, around line 119), add before the "Key rules:" line:

```markdown
## Tactical Decisions

| ID | Decision | Status | Phase |
|----|----------|--------|-------|
| TD-1 | [description] | resolved / needs-spike | Wave N |
| TD-2 | [description] | resolved / needs-spike | Wave N |
```

In the product format section (after the `### Story 1.2: ...` line, around line 89), add the same Tactical Decisions table before the "Key rules:" line.

**Addition 3: G4 M4 awareness in `<downstream_consumer>` section.**

Update item 3 in the `<downstream_consumer>` section (line 52-53) to mention the M4 check specifically. Change:

```
3. The G4 gate, which validates that every design decision has at least one corresponding task and that acceptance criteria cite design decisions.
```

To:

```
3. The G4 gate, which validates that every design decision has at least one corresponding task, that acceptance criteria cite design decisions, and that tactical decisions are referenced using TD-N identifiers (M4 check uses regex `/TD-\d+/` or the text "tactical decision").
```

**Addition 4: TD self-check in `<quality_gate>` section.**

Add a new checklist item in the `<quality_gate>` section (after line 175, in the common checks area before the if_intent blocks):

```
- [ ] Every task that addresses a tactical decision includes a TD-N reference in its title or description
- [ ] A "Tactical Decisions" table exists listing each TD with ID, description, status (resolved/needs-spike), and phase
```
  </action>
  <verify>
Read agents/plan-decomposer.md and confirm:
1. Step 2 mentions "tactical decisions (TDs)" with "TD-1, TD-2" sequential IDs
2. Both format templates (engineering and product) include a "Tactical Decisions" table
3. `<downstream_consumer>` item 3 mentions "TD-N identifiers" and "M4 check"
4. `<quality_gate>` has TD-N reference and Tactical Decisions table checklist items
  </verify>
  <done>Plan-decomposer agent knows about G4's M4 check and will produce plans with TD-N references and a Tactical Decisions table.</done>
</task>

<task type="auto">
  <name>Task 2: Calibrate effort estimates for agent execution time</name>
  <files>agents/plan-decomposer.md</files>
  <action>
Two changes to agents/plan-decomposer.md to recalibrate effort estimates from human developer time to agent execution time.

**Change 1: Update the effort estimate guidance in the engineering format Key rules section.**

Find the line (currently line 131):
```
- Effort estimates are realistic (2-8 hours per task is typical; larger tasks should be split)
```

Replace with:
```
- Effort estimates reflect agent execution time, not human developer time. Agents execute with full codebase context, no context-switching, and parallel file access. A task that would take a human developer 4 hours typically completes in 30-60 minutes via agent dispatch. Typical range: 15 minutes to 2 hours per task. Larger tasks should be split.
```

**Change 2: Update the quality gate effort estimate check.**

Find the line in `<quality_gate>` (currently line 184):
```
- [ ] Effort estimates are in the 2-8 hour range (larger tasks are split)
```

Replace with:
```
- [ ] Effort estimates are in the 15min-2hr range for agent execution (larger tasks are split)
```
  </action>
  <verify>
Read agents/plan-decomposer.md and confirm:
1. Engineering Key rules section mentions "agent execution time" with "15 minutes to 2 hours" range
2. Quality gate checklist mentions "15min-2hr range for agent execution"
3. No remaining references to "2-8 hours" as the effort estimate range
  </verify>
  <done>Effort estimates calibrated for agent execution (15min-2hr) instead of human developer time (2-8 hours).</done>
</task>

</tasks>

<verification>
1. Read agents/plan-decomposer.md -- TD-N format requirements present in instructions, format templates, downstream consumer, and quality gate
2. Grep for "TD-\d+" in agents/plan-decomposer.md -- confirms TD pattern references
3. Grep for "tactical decision" in agents/plan-decomposer.md -- confirms TD terminology
4. Grep for "2-8 hour" in agents/plan-decomposer.md -- should return NO matches (replaced with agent-calibrated estimates)
5. Grep for "agent execution" in agents/plan-decomposer.md -- confirms new estimate calibration
</verification>

<success_criteria>
- Plan-decomposer instructions explain TD-N surfacing with sequential IDs and resolved/needs-spike status
- Both format templates (engineering and product) include a Tactical Decisions table
- Downstream consumer section references G4 M4 check with TD-N regex
- Quality gate includes TD-N reference and table checklist items
- Effort estimates calibrated for 15min-2hr agent execution, not 2-8hr human development
- No remaining "2-8 hour" references in the file
</success_criteria>

<output>
After completion, create `.planning/phases/02-plan-phase-retrospective-fixes/02-01-SUMMARY.md`
</output>
