---
phase: 35-worktree-git-workflow
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - skills/execute/SKILL.md
  - skills/execute/references/ref-git-commit.md
autonomous: true
requirements: [DEVW-01, DEVW-02, DEVW-03, DEVW-04, DEVW-05, DEVW-06]

must_haves:
  truths:
    - "After each verified task, the execute skill creates an atomic git commit"
    - "Commit message follows conventional format: {type}(DA-{N}): {task_description}"
    - "Only files explicitly listed in the task definition are staged (never git add . or git add -A)"
    - "User can disable auto-commits via auto_commit: false in state.yml or --no-commit invocation flag"
    - "A task that fails verification does not auto-commit; user is prompted with commit/skip options"
    - "A git error (staging failure, commit failure) pauses execution with instructions -- no auto-resolution"
  artifacts:
    - path: "skills/execute/references/ref-git-commit.md"
      provides: "Git commit reference with format, staging rules, opt-out logic, error handling"
      min_lines: 40
    - path: "skills/execute/SKILL.md"
      provides: "Execute skill with git commit sub-step after verification"
      contains: "ref-git-commit"
  key_links:
    - from: "skills/execute/SKILL.md"
      to: "skills/execute/references/ref-git-commit.md"
      via: "Read reference for git commit protocol"
      pattern: "ref-git-commit"
    - from: "skills/execute/SKILL.md"
      to: "state.yml"
      via: "Read auto_commit flag"
      pattern: "auto_commit"
    - from: "skills/execute/references/ref-git-commit.md"
      to: "git add/commit via Bash"
      via: "Selective staging and conventional commit"
      pattern: "git add.*git commit"
---

<objective>
Implement per-task atomic git commits in the execute skill with conventional format, selective staging, opt-out mechanism, failed-task protection, and git error pausing.

Purpose: Each verified task produces a traceable git commit linking code changes to design decisions via the DA chain. This gives users a clean, meaningful git history that mirrors the lifecycle structure. The opt-out and error handling ensure the feature is safe and non-intrusive.

Output: New reference file `skills/execute/references/ref-git-commit.md` with the full git commit protocol. Updated `skills/execute/SKILL.md` with a new sub-step 5c-git between verification (5c) and state update (5d).
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-worktree-git-workflow/35-01-SUMMARY.md (worktree isolation just added)

<interfaces>
<!-- Key contracts from Plan 01 output and existing codebase -->

From skills/execute/SKILL.md Step 5 loop (post Plan 01):
```markdown
### Step 5: Task Execution Loop

For each task from starting point:

**5a: Display task.**
**5b: Dispatch task-implementer agent (worktree-isolated).** [includes merge-back]
**5c: Per-task verification (inline, not agent).** Status: VERIFIED | PARTIAL | FAILED | NEEDS REVIEW.
[NEW: 5c-git goes here]
**5d: Update state.**
**5e: Append to PROGRESS.md via Bash.**
**5f: Micro-interaction.**
**5g: Error handling.**
```

From .planning/research/STACK.md (commit format):
```
Commit format (adapted from GSD pattern per DEFER-14 analysis):
- Type: feat, fix, refactor, test, docs (conventional commits)
- Scope: DA-{N} (decision area that the task traces to)
- Description: task title from PLAN.md or SPIKE.md
- Example: feat(DA-3): implement PKCE OAuth2 flow
```

From .planning/research/FEATURES.md (opt-out mechanism):
```
Opt-out: A flag (--no-commit on execute invocation, or a field in state.yml like auto_commit: false)
disables auto-commits. User can also respond "skip" at each individual commit prompt.
```

Existing reference file pattern (from skills/execute/references/):
- prompt-task-verifier.md (already exists -- verification reference)
- Reference files are read by the skill at runtime via Read tool
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create git commit reference file</name>
  <files>skills/execute/references/ref-git-commit.md</files>
  <action>
Create `skills/execute/references/ref-git-commit.md` containing the complete git commit protocol. This reference is read by the execute skill at Step 5c-git. It must cover:

**1. Opt-out check (first thing):**
```markdown
## Opt-Out Check

Before any git operation, check whether auto-commits are disabled:
1. Read state.yml -- if `auto_commit: false` is set, skip all git operations for this task. Log "Auto-commit disabled, skipping git commit" and return.
2. Check if the user passed `--no-commit` in the execute invocation (parsed in Step 1 or Step 2). If so, skip.
3. If neither opt-out is active, proceed with git commit.
```

**2. Verification gate:**
```markdown
## Verification Gate

Only commit if the task verification status from Step 5c is VERIFIED or PARTIAL:
- **VERIFIED:** Proceed to staging and commit.
- **PARTIAL:** Proceed to staging and commit (partial work is still valuable to preserve).
- **FAILED:** Do NOT auto-commit. Display: "Task {task_id} failed verification. Options: commit-anyway / skip / pause". Wait for user response.
  - commit-anyway: proceed to staging and commit (user accepts risk)
  - skip: skip git commit for this task, continue to 5d
  - pause: save checkpoint, STOP
- **NEEDS REVIEW:** Same behavior as FAILED -- do not auto-commit, prompt user.
```

**3. Selective staging:**
```markdown
## Selective Staging

Stage ONLY the files listed in the task definition (from PLAN.md task file list or SPIKE.md implementation steps). Never use `git add .` or `git add -A`.

For each file in the task's file list:
```bash
git add {file_path}
```

If a `git add` fails (file not found, permission error): display the error and prompt: "Git staging failed for {file_path}. Options: skip-file / skip-commit / pause". Do NOT proceed to commit with partial staging unless user confirms.

After staging, verify something was staged:
```bash
git diff --cached --stat
```
If nothing staged (all files unmodified): log "No changes to commit for task {task_id}" and skip commit.
```

**4. Commit format:**
```markdown
## Commit Message Format

Format: `{type}(DA-{N}): {task_description}`

**Type derivation** from task nature:
- `feat` -- new functionality, new files, new capabilities
- `fix` -- bug fixes, corrections to existing behavior
- `refactor` -- restructuring without behavior change
- `test` -- test additions or modifications
- `docs` -- documentation-only changes

If task traces to multiple DAs, use the primary DA (first listed).

**Extended body** (via heredoc for multi-line):
```bash
git commit -m "$(cat <<'EOF'
{type}(DA-{N}): {task_description}

Task: {task_id}
Phase: {phase_slug}
DA: DA-{N}: {DA_name}
Verification: {status}
EOF
)"
```

**Track commit hash:** After successful commit, capture the hash:
```bash
git rev-parse --short HEAD
```
Include the commit hash in the PROGRESS.md append (Step 5e) for traceability.
```

**5. Git error handling:**
```markdown
## Git Error Handling

If `git commit` fails for any reason (no staged changes after filtering, hook failure, lock file):
- Display the full git error output
- Display: "Git commit failed for task {task_id}. Options: retry / skip / pause"
- Do NOT attempt auto-resolution (no `git reset`, no force flags, no stash)
- retry: re-run the git add + git commit sequence
- skip: skip commit, continue to Step 5d
- pause: save checkpoint with substep `git_error_{task_id}`, STOP
```

The reference file should be self-contained (the skill reads it and follows the protocol). Target ~60-80 lines.
  </action>
  <verify>
    <automated>test -f skills/execute/references/ref-git-commit.md && grep -c "Opt-Out Check" skills/execute/references/ref-git-commit.md | grep -q "1" && grep -c "Verification Gate" skills/execute/references/ref-git-commit.md | grep -q "1" && grep -c "Selective Staging" skills/execute/references/ref-git-commit.md | grep -q "1" && grep -c "Commit Message Format" skills/execute/references/ref-git-commit.md | grep -q "1" && grep -c "Git Error Handling" skills/execute/references/ref-git-commit.md | grep -q "1" && echo "PASS: all 5 sections present" || echo "FAIL: missing sections"</automated>
    Also verify: `grep "git add -A" skills/execute/references/ref-git-commit.md` returns NO matches (selective staging only). `grep "auto_commit" skills/execute/references/ref-git-commit.md` returns a match (opt-out check).
  </verify>
  <done>Reference file exists with all 5 sections: opt-out check, verification gate, selective staging, commit format, git error handling. Contains no `git add .` or `git add -A` patterns. Includes auto_commit opt-out flag check and --no-commit invocation flag check.</done>
</task>

<task type="auto">
  <name>Task 2: Add git commit sub-step 5c-git to execute skill</name>
  <files>skills/execute/SKILL.md</files>
  <action>
Add a new sub-step `5c-git` to the execute skill's Step 5 task execution loop, positioned between the existing 5c (verification) and 5d (state update). This sub-step orchestrates the git commit protocol by reading the reference file.

Insert the following after the current Step 5c and before Step 5d:

```markdown
**5c-git: Per-task git commit (optional).** Read `skills/execute/references/ref-git-commit.md` for the full protocol. In summary:
1. Check opt-out: if `auto_commit: false` in state.yml or `--no-commit` was passed, skip entirely.
2. Check verification status: VERIFIED/PARTIAL proceed; FAILED/NEEDS REVIEW prompt user (commit-anyway / skip / pause).
3. Stage only task-specific files via `git add {file}` for each file in the task definition. Verify staged changes exist.
4. Commit with message `{type}(DA-{N}): {task_description}` and extended body with traceability metadata.
5. Capture commit hash via `git rev-parse --short HEAD` for PROGRESS.md logging.
6. On any git error: display error, prompt (retry / skip / pause). Do NOT auto-resolve.
```

Also update Step 3 (Initialize Execute State) to parse the `--no-commit` flag:
- After the existing state initialization, add: "Parse `--no-commit` flag from user invocation if present. Store as `no_commit: true` in execution context for Step 5c-git opt-out check."

Also update Step 5e (PROGRESS.md append) to include commit hash when available:
- In the list of items to record, add: "commit hash (if auto-commit was active and succeeded)"

IMPORTANT constraints:
- Keep the skill under 200 lines total. The current file (post Plan 01) should be ~135 lines. This adds ~8-10 lines net to the skill body (the heavy logic lives in the reference file).
- Do NOT modify Steps 1, 2, 4, 5a, 5b, 6, or 7.
- The `--no-commit` flag is parsed from the user's invocation of `/expedite:execute` (e.g., `/expedite:execute phase 1 --no-commit`).
- The `auto_commit` field in state.yml is a per-lifecycle setting the user can set manually or via a future config command.
  </action>
  <verify>
    <automated>grep -c "5c-git" skills/execute/SKILL.md | grep -q "1" && grep -c "ref-git-commit" skills/execute/SKILL.md | grep -q "1" && grep -c "no_commit" skills/execute/SKILL.md | grep -q "1" && grep -c "commit hash" skills/execute/SKILL.md | grep -q "1" && echo "PASS: git commit sub-step integrated" || echo "FAIL: git commit integration incomplete"</automated>
    Also verify: `wc -l skills/execute/SKILL.md` remains under 200 lines. Step ordering should be 5a, 5b, 5c, 5c-git, 5d, 5e, 5f, 5g.
  </verify>
  <done>Execute skill contains sub-step 5c-git referencing the git commit protocol. Step 3 parses --no-commit flag. Step 5e includes commit hash in PROGRESS.md logging. Skill remains under 200 lines. Step ordering is correct (5c-git between 5c and 5d).</done>
</task>

</tasks>

<verification>
1. `test -f skills/execute/references/ref-git-commit.md` passes (reference file exists)
2. `grep "5c-git" skills/execute/SKILL.md` returns a match (sub-step exists)
3. `grep "ref-git-commit" skills/execute/SKILL.md` returns a match (skill references the file)
4. `grep "auto_commit" skills/execute/references/ref-git-commit.md` returns a match (opt-out mechanism)
5. `grep "no_commit" skills/execute/SKILL.md` returns a match (invocation flag parsed)
6. `grep "git add -A\|git add \." skills/execute/references/ref-git-commit.md skills/execute/SKILL.md` returns NO matches (selective staging enforced)
7. `grep "auto-resolve\|--force\|git reset" skills/execute/references/ref-git-commit.md` returns NO matches (no auto-resolution)
8. `wc -l skills/execute/SKILL.md` is under 200 lines
9. Sections in ref-git-commit.md: Opt-Out Check, Verification Gate, Selective Staging, Commit Message Format, Git Error Handling -- all 5 present
</verification>

<success_criteria>
- New reference file `ref-git-commit.md` contains complete git commit protocol (opt-out, verification gate, selective staging, conventional format, error handling)
- Execute skill has 5c-git sub-step that reads the reference and applies the protocol
- Opt-out works via `auto_commit: false` in state.yml OR `--no-commit` invocation flag
- Failed/needs-review tasks prompt user instead of auto-committing
- Git errors pause with instructions; no auto-resolution attempted
- Commit format is `{type}(DA-{N}): {task_description}` with extended body
- Only task-specific files are staged (no `git add .` or `git add -A`)
- Commit hash tracked in PROGRESS.md for traceability
- Execute skill remains under 200 lines
</success_criteria>

<output>
After completion, create `.planning/phases/35-worktree-git-workflow/35-02-SUMMARY.md`
</output>
