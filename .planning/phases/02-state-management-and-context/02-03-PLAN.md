---
phase: 02-state-management-and-context
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - skills/status/SKILL.md
autonomous: false
requirements:
  - CTX-01
  - CTX-02
  - CTX-03

must_haves:
  truths:
    - "skills/status/SKILL.md contains full orchestration logic that reads and parses state.yml"
    - "Status skill displays: project name, intent, current phase with human-readable description, next recommended action"
    - "Status skill displays question summary table (counts by status: covered, partial, not_covered, pending)"
    - "Status skill displays gate history table (gate name, outcome, timestamp)"
    - "Status skill handles missing .expedite/state.yml gracefully with 'No active lifecycle' message and guidance to run /expedite:scope"
    - "Status skill handles missing/empty questions and gate_history arrays gracefully"
    - "Status skill provides phase-aware next-action recommendation matching the SessionStart hook routing"
    - "All 6 SKILL.md files still contain the !cat .expedite/state.yml context injection line (CTX-01 verification)"
    - "Context reconstruction works via two independent layers: !cat injection (Layer 1) + /expedite:status (Layer 2)"
  artifacts:
    - path: "skills/status/SKILL.md"
      provides: "Full status skill implementation with state.yml parsing and formatted display"
      contains: "Expedite Lifecycle Status"
  key_links:
    - from: "skills/status/SKILL.md"
      to: ".expedite/state.yml"
      via: "Read tool + YAML parsing by LLM at invocation time"
      pattern: "state\\.yml"
    - from: "skills/status/SKILL.md (CTX-01)"
      to: "All 6 SKILL.md files"
      via: "!cat .expedite/state.yml injection line present in all skills"
      pattern: "cat .expedite/state.yml"
---

<objective>
Replace the status skill stub with full orchestration logic that reads state.yml and renders a formatted lifecycle overview.

Purpose: `/expedite:status` is the second fallback layer for context reconstruction. After `/clear`, if a user needs to understand where they are in the lifecycle, they can invoke `/expedite:status` to see the full picture: project name, intent, current phase, question status, gate history, and the recommended next action. This skill reads `.expedite/state.yml` (which is also injected via `!cat` at the top of every SKILL.md) and presents a human-readable summary. Additionally, this plan verifies that CTX-01 (the `!cat` injection in all SKILL.md files) is still intact from Phase 1.

Output: Updated `skills/status/SKILL.md` with full orchestration logic. No new files created.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-management-and-context/02-RESEARCH.md
@.planning/phases/02-state-management-and-context/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace status skill stub with full orchestration logic</name>
  <files>
    skills/status/SKILL.md
  </files>
  <action>
Replace the ENTIRE content of `skills/status/SKILL.md` with the content below. The frontmatter stays the same (already correct from Phase 1). The body is replaced with full orchestration instructions.

**IMPORTANT:** The SKILL.md body IS the prompt. It is not documentation -- it is instructions that Claude follows when the skill is invoked. Write it as imperative instructions to the LLM.

```markdown
---
name: status
description: >
  This skill should be used when the user wants to "check lifecycle status",
  "where am I", "show status", "lifecycle status", "what phase am I on",
  or needs to see the current lifecycle state, phase, progress, and recommended next action.
allowed-tools:
  - Read
  - Glob
  - Bash
---

Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`

# Status Skill

You are the Expedite status display. Your job is to read the lifecycle state and present a clear, formatted overview to the user.

## Instructions

1. **Check for active lifecycle.** Look at the injected state above. If it says "No active lifecycle", respond with:
   ```
   No active Expedite lifecycle.
   Run /expedite:scope to start a new lifecycle.
   ```
   Then stop. Do not proceed further.

2. **Parse state fields.** Extract from the injected state.yml content:
   - `project_name` -- the lifecycle name
   - `intent` -- "product" or "engineering"
   - `phase` -- the current lifecycle phase
   - `questions` -- the list of research questions (may be empty)
   - `gate_history` -- the list of gate evaluations (may be empty)
   - `research_rounds` -- number of research rounds completed
   - `current_task` -- current task ID during execute phase (may be null)
   - `current_wave` -- current wave during execute phase (may be null)

3. **Map phase to human-readable description.** Use this mapping:
   - `scope_in_progress` -> "Scope: defining questions and intent"
   - `scope_complete` -> "Scope: complete, ready for research"
   - `research_in_progress` -> "Research: gathering evidence"
   - `research_complete` -> "Research: complete, ready for design"
   - `research_recycled` -> "Research: gaps identified, needs re-research"
   - `design_in_progress` -> "Design: generating design document"
   - `design_complete` -> "Design: complete, ready for planning"
   - `design_recycled` -> "Design: needs revision"
   - `plan_in_progress` -> "Plan: generating implementation plan"
   - `plan_complete` -> "Plan: complete, ready for execution"
   - `plan_recycled` -> "Plan: needs revision"
   - `execute_in_progress` -> "Execute: implementing tasks"
   - `complete` -> "Lifecycle complete"
   - `archived` -> "Lifecycle archived"

4. **Determine next action.** Use this routing (same as SessionStart hook):
   - `scope_in_progress` -> "Continue with `/expedite:scope`"
   - `scope_complete` -> "Run `/expedite:research` to begin evidence gathering"
   - `research_in_progress` -> "Continue with `/expedite:research`"
   - `research_complete` -> "Run `/expedite:design` to generate the design"
   - `research_recycled` -> "Re-run `/expedite:research` to address gaps (or use `--override` to proceed)"
   - `design_in_progress` -> "Continue with `/expedite:design`"
   - `design_complete` -> "Run `/expedite:plan` to generate the plan"
   - `design_recycled` -> "Re-run `/expedite:design` to address gaps (or use `--override` to proceed)"
   - `plan_in_progress` -> "Continue with `/expedite:plan`"
   - `plan_complete` -> "Run `/expedite:execute` to begin implementation"
   - `plan_recycled` -> "Re-run `/expedite:plan` to address gaps (or use `--override` to proceed)"
   - `execute_in_progress` -> "Resume with `/expedite:execute`"
   - `complete` -> "Lifecycle complete. Run `/expedite:scope` for a new lifecycle."
   - `archived` -> "Lifecycle archived. Run `/expedite:scope` for a new lifecycle."

5. **Count question statuses.** If `questions` is non-empty, count:
   - Total questions
   - Questions with status `covered`
   - Questions with status `partial`
   - Questions with status `not_covered`
   - Questions with status `pending`
   - Questions with status `unavailable_source`

6. **Display formatted output.** Use EXACTLY this format:

```
# Expedite Lifecycle Status

**Project:** {project_name}
**Intent:** {intent}
**Phase:** {phase} ({human-readable description})

## Next Action
{phase-aware recommendation from step 4}

## Questions ({total} total)
| Status | Count |
|--------|-------|
| Covered | {n} |
| Partial | {n} |
| Not Covered | {n} |
| Pending | {n} |
| Unavailable Source | {n} |

(Only show rows with count > 0. If no questions exist, display "No questions defined yet.")

## Gate History
| Gate | Outcome | Timestamp |
|------|---------|-----------|
| {gate} | {outcome} | {timestamp} |

(If no gate history, display "No gates evaluated yet.")

## Recovery Info
State file: .expedite/state.yml
Backup: .expedite/state.yml.bak
Research rounds: {research_rounds}
```

7. **Do NOT modify any files.** This is a read-only skill. Do not write to state.yml or any other file.
```

**Key constraints:**
- The frontmatter MUST remain identical to the Phase 1 version (name, description, allowed-tools)
- The `!cat` injection line MUST remain as the first line after the frontmatter delimiter
- The body is imperative instructions to the LLM, NOT documentation
- Phase-to-description and phase-to-action mappings MUST cover ALL valid phase values
- The skill MUST handle empty questions/gate_history gracefully
- The skill MUST handle missing state.yml gracefully
- The skill is READ-ONLY -- it MUST NOT write to any files
- Do NOT use "arc" anywhere
  </action>
  <verify>
    <automated>
FILE="skills/status/SKILL.md"
test -f "$FILE" && \
grep -q "^name: status$" "$FILE" && \
grep -q 'cat .expedite/state.yml' "$FILE" && \
grep -q 'Expedite Lifecycle Status' "$FILE" && \
grep -q 'scope_in_progress' "$FILE" && \
grep -q 'scope_complete' "$FILE" && \
grep -q 'research_in_progress' "$FILE" && \
grep -q 'research_complete' "$FILE" && \
grep -q 'research_recycled' "$FILE" && \
grep -q 'design_in_progress' "$FILE" && \
grep -q 'design_complete' "$FILE" && \
grep -q 'design_recycled' "$FILE" && \
grep -q 'plan_in_progress' "$FILE" && \
grep -q 'plan_complete' "$FILE" && \
grep -q 'plan_recycled' "$FILE" && \
grep -q 'execute_in_progress' "$FILE" && \
grep -q 'No active Expedite lifecycle' "$FILE" && \
grep -q '/expedite:scope' "$FILE" && \
grep -q 'Gate History' "$FILE" && \
grep -q 'Recovery Info' "$FILE" && \
grep -q 'read-only' "$FILE" && \
! grep -qi 'Under development' "$FILE" && \
! grep -qi '\barc\b' "$FILE" && \
echo "PASS: status SKILL.md valid"
    </automated>
  </verify>
  <done>
    - skills/status/SKILL.md replaced with full orchestration logic
    - Frontmatter unchanged from Phase 1 (name, description, allowed-tools)
    - !cat injection line present after frontmatter
    - Handles "No active lifecycle" case with guidance to run /expedite:scope
    - All 14 phase values mapped to human-readable descriptions
    - All 14 phase values mapped to next-action recommendations
    - Question status counting logic included
    - Gate history display logic included
    - Recovery info section included
    - Read-only constraint explicitly stated
    - No "arc" references
    - "Under development" stub removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify CTX-01 -- all SKILL.md files have !cat injection</name>
  <files>
    skills/scope/SKILL.md
    skills/research/SKILL.md
    skills/design/SKILL.md
    skills/plan/SKILL.md
    skills/execute/SKILL.md
    skills/status/SKILL.md
  </files>
  <action>
Verify that CTX-01 is satisfied: every SKILL.md file includes the `!cat .expedite/state.yml` dynamic context injection line. This was implemented in Phase 1 (plan 01-02) but must be confirmed as still present after the status skill update in Task 1.

Run this verification command:
```bash
for skill in scope research design plan execute status; do
  FILE="skills/$skill/SKILL.md"
  if grep -q 'cat .expedite/state.yml' "$FILE"; then
    echo "PASS: $FILE has !cat injection"
  else
    echo "FAIL: $FILE missing !cat injection"
    exit 1
  fi
done
echo "CTX-01 VERIFIED: All 6 SKILL.md files have dynamic context injection"
```

This is a verification-only task. Do NOT modify any files unless the verification reveals a missing injection line (which would be a regression from Phase 1).
  </action>
  <verify>
    <automated>
for skill in scope research design plan execute status; do
  FILE="skills/$skill/SKILL.md"
  if ! grep -q 'cat .expedite/state.yml' "$FILE"; then
    echo "FAIL: $FILE missing !cat injection"
    exit 1
  fi
done
echo "PASS: CTX-01 verified -- all 6 SKILL.md files have !cat injection"
    </automated>
  </verify>
  <done>
    - All 6 SKILL.md files confirmed to contain `!cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"` injection line
    - CTX-01 requirement verified as satisfied
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify status skill and context reconstruction in Claude Code</name>
  <files>
    skills/status/SKILL.md
  </files>
  <action>
**What was built:** Complete state management infrastructure for the Expedite plugin:
- State schema template (`templates/state.yml.template`) defining all lifecycle fields
- Gitignore and source config templates (`templates/gitignore.template`, `templates/sources.yml.template`)
- SessionStart hook (`hooks/hooks.json` + `scripts/session-start.sh`) for automatic context injection
- Full status skill implementation (`skills/status/SKILL.md`) with state parsing and formatted display

**Human verification steps:**

1. **Create a test state file.** In the project root, create a temporary `.expedite/` directory with a test `state.yml`:
   ```bash
   mkdir -p .expedite
   cat > .expedite/state.yml << 'EOF'
   version: "1"
   last_modified: "2026-02-28T20:00:00Z"
   project_name: "test-project"
   intent: "engineering"
   lifecycle_id: "test-project-20260228"
   phase: "scope_complete"
   current_task: null
   imported_from: null
   constraints: []
   research_rounds: 0
   questions:
     - id: "q01"
       text: "How should auth be implemented?"
       priority: "P0"
       decision_area: "authentication"
       source_hints: ["web", "codebase"]
       status: "pending"
       source: "original"
       gap_details: null
       evidence_files: []
   gate_history:
     - gate: "G1"
       timestamp: "2026-02-28T19:55:00Z"
       outcome: "go"
       must_passed: 4
       must_failed: 0
       should_passed: 3
       should_failed: 0
       notes: null
       overridden: false
   tasks: []
   current_wave: null
   EOF
   ```

2. **Open Claude Code with plugin:** `claude --plugin-dir .`

3. **Invoke `/expedite:status`** and verify the output includes:
   - Project name: "test-project"
   - Intent: "engineering"
   - Phase: "scope_complete" with human-readable description
   - Next action recommendation: "Run `/expedite:research` to begin evidence gathering"
   - Questions section showing 1 pending question
   - Gate history showing G1 with "go" outcome
   - Recovery info with state file path

4. **Test context reconstruction after /clear:**
   - Type `/clear` to clear the conversation
   - Invoke `/expedite:status` again
   - Verify the same output appears (the `!cat` injection rebuilds context)

5. **Clean up test state:**
   ```bash
   rm -rf .expedite
   ```

6. **Verify no-lifecycle handling:** Invoke `/expedite:status` without `.expedite/` directory and confirm it shows "No active Expedite lifecycle" message.

**Expected result:** Status skill displays formatted lifecycle overview matching the output format in the SKILL.md instructions. Context reconstruction works after `/clear`.

**Resume signal:** Type "approved" if the status skill displays correctly in all scenarios (with state, after /clear, without state), or describe what is wrong.
  </action>
  <verify>
    <automated>echo "CHECKPOINT: Requires human verification in Claude Code -- run 'claude --plugin-dir .' and test /expedite:status with and without .expedite/state.yml"</automated>
  </verify>
  <done>
    - Human confirms /expedite:status displays formatted lifecycle overview with test state.yml
    - Human confirms context reconstruction works after /clear
    - Human confirms "No active lifecycle" message when no .expedite/ directory exists
    - Human confirms the status output includes project name, intent, phase, next action, questions, and gate history
  </done>
</task>

</tasks>

<verification>
- [ ] `skills/status/SKILL.md` has full orchestration logic (not a stub)
- [ ] Status skill handles all 14 phase values with descriptions and next-action routing
- [ ] Status skill handles missing state.yml with "No active lifecycle" guidance
- [ ] Status skill handles empty questions and gate_history gracefully
- [ ] Status skill is read-only (does not modify any files)
- [ ] All 6 SKILL.md files contain `!cat .expedite/state.yml` injection (CTX-01)
- [ ] Human confirms status skill works in Claude Code with test state file
- [ ] Human confirms context reconstruction after `/clear`
- [ ] No references to "arc" in the status skill
</verification>

<success_criteria>
The `/expedite:status` skill displays a complete, formatted lifecycle overview when invoked. It handles all lifecycle phases with correct descriptions and next-action recommendations. It gracefully handles the case where no lifecycle exists. Combined with the `!cat` injection in all SKILL.md files (CTX-01, verified) and the SessionStart hook (02-02), the plugin has three independent layers of context reconstruction that work across sessions and after `/clear`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-state-management-and-context/02-03-SUMMARY.md`
</output>
