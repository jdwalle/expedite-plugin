---
phase: 04-scope-skill
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - skills/scope/SKILL.md
autonomous: false
requirements:
  - SCOPE-07
  - GATE-01
  - GATE-02
  - GATE-06

must_haves:
  truths:
    - "Step 9 evaluates G1 gate with 6 MUST criteria and 3 SHOULD criteria, all structural/deterministic (no LLM judgment)"
    - "MUST criteria: SCOPE.md exists and non-empty, at least 3 questions, intent declared, at least 1 success criterion, every question has evidence_requirements, every DA has readiness criterion and depth calibration"
    - "SHOULD criteria: questions have source_hints, questions span at least 2 DAs, no more than 15 questions"
    - "Gate evaluation requires per-criterion evidence (not just 'all pass') -- state the specific count or field for each check"
    - "All MUST pass = Go: write phase='scope_complete' to state.yml, record G1 in gate_history"
    - "Any MUST fail = Hold: display which criteria failed, offer to fix inline (scope is interactive)"
    - "SHOULD failures produce advisory notes recorded in gate_history notes field"
    - "Gate history entry includes: gate, timestamp, outcome, must_passed, must_failed, should_passed, should_failed, notes, overridden"
    - "Gate outcome uses go/go_advisory distinction: all MUST pass + all SHOULD pass = go; all MUST pass + some SHOULD fail = go_advisory"
    - "State.yml update uses backup-before-write pattern"
    - "Human verification checkpoint confirms the full scope skill works end-to-end"
    - "All paths use .expedite/ namespace, all commands use /expedite: namespace"
  artifacts:
    - path: "skills/scope/SKILL.md"
      provides: "Complete scope skill with all 9 steps (Steps 1-9)"
      contains: "Gate G1"
  key_links:
    - from: "skills/scope/SKILL.md Step 9"
      to: ".expedite/state.yml (runtime)"
      via: "Backup-before-write sets phase to scope_complete and appends G1 to gate_history"
      pattern: "scope_complete"
    - from: "skills/scope/SKILL.md Step 9"
      to: ".expedite/scope/SCOPE.md (runtime)"
      via: "Reads SCOPE.md to verify MUST criteria (file existence, DA metadata, success criteria)"
      pattern: "SCOPE.md"
    - from: "skills/scope/SKILL.md (complete)"
      to: "skills/research/SKILL.md (Phase 5)"
      via: "After G1 pass, phase=scope_complete enables research skill entry"
      pattern: "scope_complete"
---

<objective>
Append Step 9 (G1 gate evaluation) to the scope SKILL.md, completing the full 9-step orchestration flow. Then verify the complete skill works via human testing.

Purpose: The G1 gate is the quality checkpoint that ensures the scope output is structurally complete before any research tokens are spent. It validates that the contract chain origin is well-formed: every question has evidence requirements, every DA has readiness criteria, intent is declared. G1 is purely structural (GATE-06) -- no LLM judgment needed. This makes it deterministic and reliable, unlike G2/G3 which require judgment.

Output: Complete skills/scope/SKILL.md with all 9 steps. Human-verified working skill.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scope-skill/04-RESEARCH.md
@.planning/phases/04-scope-skill/04-01-SUMMARY.md
@.planning/phases/04-scope-skill/04-02-SUMMARY.md
@skills/scope/SKILL.md
@templates/state.yml.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Step 9 (G1 gate evaluation) to scope SKILL.md</name>
  <files>
    skills/scope/SKILL.md
  </files>
  <action>
Append the following content to the END of `skills/scope/SKILL.md` (after the existing "Proceed to Step 9." line from Plan 04-02). Do NOT modify any existing content -- only append.

```markdown

### Step 9: Gate G1 Evaluation

Evaluate the G1 gate. This is a **structural gate** -- all checks are deterministic (counts, field existence, string matching). Do NOT apply LLM judgment. For each criterion, state the specific evidence from the artifacts.

**Read the artifacts for evaluation:**
1. Read `.expedite/scope/SCOPE.md`
2. Read `.expedite/state.yml`

**Evaluate MUST criteria (all must pass for Go):**

| # | Criterion | How to Check | Result |
|---|-----------|-------------|--------|
| M1 | SCOPE.md exists and is non-empty | Check that `.expedite/scope/SCOPE.md` exists and has content | PASS/FAIL |
| M2 | At least 3 questions defined | Count entries in state.yml `questions` array. State the count: "Found {N} questions" | PASS/FAIL |
| M3 | Intent declared (product or engineering) | Check `intent` field in state.yml is "product" or "engineering", not null | PASS/FAIL |
| M4 | At least one success criterion defined | Check SCOPE.md "Success Criteria" section has at least one bullet point | PASS/FAIL |
| M5 | Every question has evidence_requirements defined | Check each question in state.yml has a non-null, non-empty `evidence_requirements` field. State: "Checked {N} questions, all have evidence_requirements" or "{N} missing" | PASS/FAIL |
| M6 | Every DA has a readiness criterion and depth calibration | Check SCOPE.md "Decision Areas" section: each DA heading includes a depth (Deep/Standard/Light) and has a "Readiness criterion:" line | PASS/FAIL |

**Evaluate SHOULD criteria (failures produce advisory, do not block):**

| # | Criterion | How to Check | Result |
|---|-----------|-------------|--------|
| S1 | Questions have source_hints | Check each question in state.yml has non-empty `source_hints`. State: "{N}/{M} questions have source_hints" | PASS/ADVISORY |
| S2 | Questions span at least 2 decision areas | Count unique `decision_area` values in state.yml questions. State: "Found {N} distinct DAs" | PASS/ADVISORY |
| S3 | No more than 15 questions | Count questions array. State: "Found {N} questions" | PASS/ADVISORY |

**Determine gate outcome:**

- **All MUST pass + All SHOULD pass → "go"**
  Write `phase: "scope_complete"` to state.yml.
  Display: "Gate G1: **PASS** -- All criteria met. Scope is complete."

- **All MUST pass + Some SHOULD fail → "go_advisory"**
  Write `phase: "scope_complete"` to state.yml.
  Display: "Gate G1: **PASS with advisory** -- All required criteria met. Advisory notes: {list which SHOULD criteria failed and why}."

- **Any MUST fail → "hold"**
  Do NOT write `phase: "scope_complete"`.
  Display: "Gate G1: **HOLD** -- {N} required criteria failed:"
  List each failed MUST criterion with what specifically is missing.
  Then display: "Would you like to fix these issues now? (yes / no)"
  - If "yes": Guide the user through fixing each issue (e.g., add missing evidence requirements, add a success criterion). After fixes, re-run the gate evaluation from the beginning of Step 9.
  - If "no": Display: "Scope is incomplete. Run `/expedite:scope` again when ready to continue." Then STOP.

**Record gate result in state.yml:**

Using the backup-before-write pattern:
1. Read `.expedite/state.yml`
2. Backup: `cp .expedite/state.yml .expedite/state.yml.bak` (via Bash)
3. Update:
   - If outcome is "go" or "go_advisory": set `phase: "scope_complete"`
   - Set `last_modified` to current timestamp
   - Append to `gate_history` array:
     ```yaml
     - gate: "G1"
       timestamp: "{current timestamp ISO 8601}"
       outcome: "{go/go_advisory/hold}"
       must_passed: {count of passed MUST criteria}
       must_failed: {count of failed MUST criteria}
       should_passed: {count of passed SHOULD criteria}
       should_failed: {count of failed SHOULD criteria}
       notes: {null if all pass, or description of SHOULD failures for go_advisory, or MUST failures for hold}
       overridden: false
     ```
4. Write the entire file back to `.expedite/state.yml`

**After successful gate (go or go_advisory):**

Display:
```
Scope complete! Here is a summary:

Project: {project_name}
Intent: {intent}
Questions: {N} across {M} decision areas
Estimated research batches: {K}

Next step: Run `/expedite:research` to begin evidence gathering.
```

STOP. Do not proceed to any other step.
```

**Critical implementation notes:**
- This content is APPENDED after the existing "Proceed to Step 9." line. Do not modify anything above.
- G1 is STRUCTURAL (GATE-06). Every check is a count, field existence, or string match. No LLM judgment.
- The gate evaluation MUST state per-criterion evidence (not just "all pass"). This prevents the self-grading bias pitfall.
- The "hold" outcome offers inline fix because scope is interactive. This is unique to G1 -- G2/G3 gates in later phases use Recycle instead.
- Gate history uses go/go_advisory distinction per GATE-02.
- After this append, the SKILL.md is COMPLETE (all 9 steps).
  </action>
  <verify>
    <automated>
FILE="skills/scope/SKILL.md"
test -f "$FILE" && \
grep -q 'Step 9: Gate G1 Evaluation' "$FILE" && \
grep -q 'MUST criteria' "$FILE" && \
grep -q 'SHOULD criteria' "$FILE" && \
grep -q 'scope_complete' "$FILE" && \
grep -q 'gate_history' "$FILE" && \
grep -q 'go_advisory' "$FILE" && \
grep -q 'hold' "$FILE" && \
grep -q 'must_passed' "$FILE" && \
grep -q 'must_failed' "$FILE" && \
grep -q 'should_passed' "$FILE" && \
grep -q 'should_failed' "$FILE" && \
grep -q 'overridden: false' "$FILE" && \
grep -q 'SCOPE.md exists and is non-empty' "$FILE" && \
grep -q 'At least 3 questions' "$FILE" && \
grep -q 'evidence_requirements' "$FILE" && \
grep -q 'readiness criterion' "$FILE" && \
grep -q 'backup-before-write' "$FILE" && \
grep -q '/expedite:research' "$FILE" && \
grep -q 'Step 1: Lifecycle Check' "$FILE" && \
grep -q 'Step 6: Question Plan Generation' "$FILE" && \
! grep -qi '\.arc/' "$FILE" && \
! grep -qi '/arc:' "$FILE" && \
echo "PASS: SKILL.md Step 9 valid and complete (all 9 steps present)"
    </automated>
  </verify>
  <done>
    - Step 9 (G1 gate evaluation) appended to skills/scope/SKILL.md
    - 6 MUST criteria: SCOPE.md exists, 3+ questions, intent declared, success criteria, evidence requirements, DA metadata
    - 3 SHOULD criteria: source_hints, 2+ DAs, max 15 questions
    - Per-criterion evidence required (prevents self-grading bias)
    - Gate outcomes: go, go_advisory, hold (no recycle for G1)
    - Hold offers inline fix (unique to scope's interactive nature)
    - Gate history recorded in state.yml with full metadata
    - Backup-before-write for state.yml update
    - All .expedite/ namespace, no .arc/ references
    - SKILL.md is now COMPLETE with all 9 steps
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The complete scope SKILL.md with all 9 orchestration steps:
1. Lifecycle check (existing/resume/fresh detection)
2. Connected flow import (v2 placeholder)
3. Lifecycle initialization (directory creation, template copying)
4. Round 1 context questions (project name, intent, description)
5. Round 2 refinement questions (intent-specific: product vs engineering)
6. Question plan generation (using questioning guide reference, self-check, preview format)
7. Source configuration (read-only display)
8. Artifact writing (SCOPE.md + state.yml update)
9. G1 gate evaluation (6 MUST + 3 SHOULD structural criteria)
  </what-built>
  <how-to-verify>
1. **Invoke the skill:** Run `/expedite:scope` in a test project directory (not the plugin development directory).

2. **Fresh start test:** Verify it creates `.expedite/` directory, copies templates, and starts asking questions.

3. **Answer the questions:** Provide a project name, intent (try "engineering"), and description. Answer the refinement questions.

4. **Check question plan:** Verify:
   - Plan uses "--- Question Plan Preview ---" format
   - Each DA has depth calibration and readiness criterion
   - Each question has evidence requirements
   - Self-check quality criteria are met

5. **Test modification:** When presented with the plan, type "modify" and request a change. Verify the plan is updated and re-presented.

6. **Approve the plan:** Type "yes" to approve.

7. **Check source configuration:** Verify sources are displayed as a checklist.

8. **Check artifacts:** After completion, verify:
   - `.expedite/scope/SCOPE.md` exists with full question plan, DA metadata, success criteria
   - `.expedite/state.yml` has `phase: "scope_complete"`, questions array populated, gate_history with G1 entry
   - `.expedite/state.yml.bak` exists (backup was created)

9. **Check G1 gate output:** Verify the gate displayed per-criterion evaluation results.

10. **Resume test (optional):** Start a new session, run `/expedite:scope` while state shows `scope_in_progress` (manually set phase back), verify resume detection works.
  </how-to-verify>
  <resume-signal>Type "approved" if the scope skill works correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- [ ] Step 9 is present in skills/scope/SKILL.md
- [ ] All 9 steps (1-9) are present with correct headings
- [ ] G1 gate has 6 MUST and 3 SHOULD criteria
- [ ] Gate evaluation requires per-criterion evidence
- [ ] Gate outcomes handle go, go_advisory, and hold correctly
- [ ] Hold offers inline fix (scope-specific behavior)
- [ ] Gate history is recorded in state.yml with proper schema
- [ ] Phase transitions to scope_complete on go/go_advisory
- [ ] Backup-before-write pattern used for state.yml update
- [ ] No references to .arc/ or /arc: anywhere in the file
- [ ] Human verification confirms the skill works end-to-end
</verification>

<success_criteria>
The scope SKILL.md is complete with all 9 orchestration steps. A user can run `/expedite:scope`, answer interactive questions, review and modify a question plan with typed evidence requirements, and pass through the G1 structural gate. The resulting artifacts (SCOPE.md and state.yml) contain all the data needed for the research phase to begin: questions with evidence requirements, DAs with readiness criteria and depth calibration, and the gate history proving structural completeness. Human verification confirms the full flow works.
</success_criteria>

<output>
After completion, create `.planning/phases/04-scope-skill/04-03-SUMMARY.md`
</output>
