---
phase: 12-audit-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/spike/SKILL.md
  - skills/scope/references/prompt-scope-questioning.md
  - skills/design/references/prompt-design-guide.md
  - skills/plan/references/prompt-plan-guide.md
  - skills/execute/references/prompt-task-verifier.md
autonomous: true
requirements: [GATE-01, TMPL-01]

must_haves:
  truths:
    - "G5 gate outcome appears in state.yml gate_history (status skill can display it)"
    - "Spike Step 9 NOTE clarifies that spike writes gate_history but not phase transitions"
    - "All 4 inline reference templates have zero {{placeholder}} syntax"
    - "Subagent templates (6 files in references/) are NOT modified"
  artifacts:
    - path: "skills/spike/SKILL.md"
      provides: "G5 gate_history recording after log.yml write"
      contains: "gate_history"
    - path: "skills/scope/references/prompt-scope-questioning.md"
      provides: "Placeholder-free inline guide"
    - path: "skills/design/references/prompt-design-guide.md"
      provides: "Placeholder-free inline guide"
    - path: "skills/plan/references/prompt-plan-guide.md"
      provides: "Placeholder-free inline guide"
    - path: "skills/execute/references/prompt-task-verifier.md"
      provides: "Placeholder-free inline guide"
  key_links:
    - from: "skills/spike/SKILL.md"
      to: "gate_history in state.yml"
      via: "G5 gate outcome append in Step 7"
      pattern: "gate.*G5"
    - from: "skills/spike/SKILL.md"
      to: "skills/status/SKILL.md"
      via: "gate_history array consumed by status display"
      pattern: "gate_history"
---

<objective>
Add G5 gate_history recording to spike skill (INT-05) and clean up {{placeholder}} syntax in 4 inline reference templates (INT-06).

Purpose: G5 is the only gate not recorded in state.yml gate_history, so status skill cannot display it. The 4 inline guide templates contain {{placeholder}} syntax that is never filled since they are read as quality references, not dispatched as subagents. Both are cosmetic/completeness issues with straightforward fixes.

Output: 5 modified files -- spike SKILL.md with gate_history append, 4 inline templates with placeholders converted to bracketed descriptions.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-audit-tech-debt-cleanup/12-RESEARCH.md
</context>

<interfaces>
<!-- Spike SKILL.md Step 7 G5 gate -- current log.yml write (lines 295-308) -->

From skills/spike/SKILL.md:
```markdown
**Log gate outcome to telemetry** (after evaluation, before outcome routing):
```bash
cat >> .expedite/log.yml << 'LOG_EOF'
---
event: gate_outcome
timestamp: "{ISO 8601 UTC}"
lifecycle_id: "{lifecycle_id}"
gate: "G5"
outcome: "{go|go_advisory|recycle|override}"
must_passed: {N}
must_failed: {N}
should_passed: {N}
should_failed: {N}
LOG_EOF
```
```

From skills/spike/SKILL.md (line 452-454, Step 9 NOTE):
```markdown
NOTE: Do NOT update state.yml. Spike does not have a lifecycle phase -- it operates within plan_complete or execute_in_progress. The output file (SPIKE.md) is the only indicator of spike completion. This is deliberate: spike is optional and phase-scoped, so lifecycle-level state tracking would add unnecessary complexity.
```

<!-- Gate history schema from state.yml.template -->

From templates/state.yml.template (lines 52-63):
```yaml
# Gate history (flat list -- each item is a flat object, max 2 nesting levels)
gate_history: []
# Gate history item schema:
#   gate: "G1"
#   timestamp: "2026-01-01T00:00:00Z"
#   outcome: "go"            # go | go_advisory | hold | recycle | override
#   must_passed: 0
#   must_failed: 0
#   should_passed: 0
#   should_failed: 0
#   notes: null
#   overridden: false
```

<!-- Placeholder locations in 4 inline templates -->

prompt-scope-questioning.md: 3 placeholders
  Line 6: {{project_name}}
  Line 7: {{intent}}
  Line 156: {{user_context}}

prompt-design-guide.md: 12 placeholders
  Lines 6,7,9,10,13: {{project_name}}, {{intent}}, {{scope_file}}, {{synthesis_file}}, {{decision_areas_yaml}}
  Lines 112,113,117,118,155,159,160: output format placeholders
  Lines 219,220: {{scope_content}}, {{synthesis_content}}

prompt-plan-guide.md: 10 placeholders
  Lines 6,7,9,10,13: {{project_name}}, {{intent}}, {{design_file}}, {{scope_file}}, {{decision_areas_yaml}}
  Lines 49,50,80,81,125,135: output format placeholders
  Lines 169,170: {{design_content}}, {{scope_content}}

prompt-task-verifier.md: 8 placeholders
  Lines 6,7,9,10,13: {{project_name}}, {{intent}}, {{design_file}}, {{plan_file}}, {{current_task_yaml}}
  Lines 90: {{task_id}}, {{task_title}}
  Lines 131,132: {{task_details}}, {{code_changes}}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add G5 gate_history recording to spike SKILL.md Step 7</name>
  <files>skills/spike/SKILL.md</files>
  <action>
In skills/spike/SKILL.md Step 7, after the existing `cat >> .expedite/log.yml` telemetry block (after line 308, before the override log block), add a gate_history append instruction:

```markdown
**Record gate history in state.yml** (backup-before-write):

1. Read `.expedite/state.yml`
2. Copy to backup: `cp .expedite/state.yml .expedite/state.yml.bak` (via Bash)
3. Append to `gate_history`:
   ```yaml
   - gate: "G5"
     timestamp: "{ISO 8601 UTC}"
     outcome: "{go|go_advisory|recycle|override}"
     must_passed: {N}
     must_failed: {N}
     should_passed: {N}
     should_failed: {N}
     notes: "Spike phase: {slug}"
     overridden: false
   ```
4. Write the entire file back to `.expedite/state.yml`
```

Also update the Step 9 NOTE (currently at line 452-454). Replace the current NOTE with:

```markdown
NOTE: Do NOT write **phase transitions** to state.yml. Spike does not have a lifecycle phase (no `spike_in_progress` or `spike_complete`) -- it operates within `plan_complete` or `execute_in_progress`. The output file (SPIKE.md) is the only indicator of spike completion. This is deliberate: spike is optional and phase-scoped, so lifecycle-level state tracking would add unnecessary complexity. However, G5 gate outcomes ARE recorded in gate_history (Step 7) since gate outcomes are events, not phase transitions, and the status skill needs them for the complete G1-G5 gate chain display.
```

IMPORTANT: Do NOT modify any other part of spike SKILL.md. The existing G5 gate evaluation logic, recycle handling, and telemetry logging are all correct.
  </action>
  <verify>
Run: `grep -c "gate_history" skills/spike/SKILL.md` -- should return at least 3 (the new append instruction references).
Run: `grep "Do NOT write.*phase transitions" skills/spike/SKILL.md` -- should return 1 match (updated NOTE).
Run: `grep "Do NOT update state.yml" skills/spike/SKILL.md` -- should return 0 matches (old NOTE replaced).
  </verify>
  <done>
Spike Step 7 appends G5 gate outcome to gate_history in state.yml using backup-before-write pattern. Step 9 NOTE clarifies that spike writes gate_history entries (events) but not phase transitions (state). Status skill will now show G5 outcomes in the gate history table.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove {{placeholder}} syntax from 4 inline reference templates</name>
  <files>skills/scope/references/prompt-scope-questioning.md, skills/design/references/prompt-design-guide.md, skills/plan/references/prompt-plan-guide.md, skills/execute/references/prompt-task-verifier.md</files>
  <action>
For each of the 4 inline reference templates, replace all `{{placeholder}}` instances with bracketed plain-text descriptions. Keep all 8 XML sections intact (TMPL-01 compliance). Do NOT modify the `<role>`, `<downstream_consumer>`, `<instructions>`, or `<quality_gate>` sections (they have no placeholders).

**prompt-scope-questioning.md** (3 replacements):
- Line 6: `{{project_name}}` -> `[current project name from state.yml]`
- Line 7: `{{intent}}` -> `[current intent from state.yml — product or engineering]`
- Line 156: `{{user_context}}` -> `[User's responses to Round 1 and Round 2 questions, loaded by the scope skill at invocation time]`

**prompt-design-guide.md** (12 replacements):
- `<context>` section:
  - `{{project_name}}` -> `[current project name from state.yml]`
  - `{{intent}}` -> `[current intent from state.yml]`
  - `{{scope_file}}` -> `.expedite/scope/SCOPE.md`
  - `{{synthesis_file}}` -> `.expedite/research/SYNTHESIS.md`
  - `{{decision_areas_yaml}}` -> `[Decision areas extracted from SCOPE.md by the design skill at invocation time]`
- `<output_format>` section:
  - `{{design_output_file}}` -> `.expedite/design/DESIGN.md`
  - `{{handoff_output_file}}` -> `.expedite/design/HANDOFF.md`
  - `{{project_name}}` in headers -> `[project name]`
  - `{{timestamp}}` -> `[current ISO 8601 UTC timestamp]`
- `<input_data>` section:
  - Replace the entire `{{scope_content}}` / `{{synthesis_content}}` block with: `[Scope and synthesis content are loaded by the design skill at invocation time. The skill reads SCOPE.md and SYNTHESIS.md directly -- this section documents what data the guide expects to be available in context.]`

**prompt-plan-guide.md** (10 replacements):
- `<context>` section:
  - `{{project_name}}` -> `[current project name from state.yml]`
  - `{{intent}}` -> `[current intent from state.yml]`
  - `{{design_file}}` -> `.expedite/design/DESIGN.md`
  - `{{scope_file}}` -> `.expedite/scope/SCOPE.md`
  - `{{decision_areas_yaml}}` -> `[Decision areas extracted from SCOPE.md by the plan skill at invocation time]`
- `<output_format>` section:
  - `{{plan_output_file}}` -> `.expedite/plan/PLAN.md`
  - `{{project_name}}` in headers -> `[project name]`
  - `{{timestamp}}` -> `[current ISO 8601 UTC timestamp]`
- `<input_data>` section:
  - Replace the entire `{{design_content}}` / `{{scope_content}}` block with: `[Design and scope content are loaded by the plan skill at invocation time. The skill reads DESIGN.md and SCOPE.md directly -- this section documents what data the guide expects to be available in context.]`

**prompt-task-verifier.md** (8 replacements):
- `<context>` section:
  - `{{project_name}}` -> `[current project name from state.yml]`
  - `{{intent}}` -> `[current intent from state.yml]`
  - `{{design_file}}` -> `.expedite/design/DESIGN.md`
  - `{{plan_file}}` -> `.expedite/plan/PLAN.md`
  - `{{current_task_yaml}}` -> `[Current task definition loaded by the execute skill at verification time]`
- `<output_format>` section:
  - `{{task_id}}` -> `[task ID]`
  - `{{task_title}}` -> `[task title]`
- `<input_data>` section:
  - Replace `{{task_details}}` / `{{code_changes}}` block with: `[Task details and code changes are loaded by the execute skill at verification time. The skill provides the current task definition and code diff -- this section documents what data the verifier expects to be available in context.]`

CRITICAL: Do NOT touch these subagent templates (they legitimately use {{placeholders}} filled at dispatch time):
- skills/research/references/prompt-web-researcher.md
- skills/research/references/prompt-codebase-analyst.md
- skills/research/references/prompt-mcp-researcher.md
- skills/research/references/prompt-sufficiency-evaluator.md
- skills/research/references/prompt-research-synthesizer.md
- skills/spike/references/prompt-spike-researcher.md

After all replacements, run `grep -rn '{{' skills/scope/references/prompt-scope-questioning.md skills/design/references/prompt-design-guide.md skills/plan/references/prompt-plan-guide.md skills/execute/references/prompt-task-verifier.md` to confirm zero matches.
  </action>
  <verify>
Run: `grep -rn '{{' skills/scope/references/prompt-scope-questioning.md skills/design/references/prompt-design-guide.md skills/plan/references/prompt-plan-guide.md skills/execute/references/prompt-task-verifier.md` -- should return zero matches.
Run: `grep -rn '{{' skills/research/references/ skills/spike/references/` -- should return matches (subagent templates correctly preserved).
Run: `grep -c '<context>\|<input_data>\|<output_format>\|<role>\|<instructions>\|<quality_gate>\|<downstream_consumer>\|<intent_lens>' skills/scope/references/prompt-scope-questioning.md` -- should return at least 7 (8-section structure preserved).
  </verify>
  <done>
All 33 {{placeholder}} instances across 4 inline reference templates replaced with bracketed plain-text descriptions. 8-section XML structure preserved per TMPL-01. Subagent templates (6 files) untouched and still contain their legitimate {{placeholders}}.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn '{{' skills/scope/references/prompt-scope-questioning.md skills/design/references/prompt-design-guide.md skills/plan/references/prompt-plan-guide.md skills/execute/references/prompt-task-verifier.md` returns zero matches
2. `grep -rn '{{' skills/research/references/ skills/spike/references/` returns matches (subagent templates preserved)
3. `grep -c "gate_history" skills/spike/SKILL.md` returns at least 3
4. `grep "Do NOT write.*phase transitions" skills/spike/SKILL.md` returns 1 match
5. `grep "Do NOT update state.yml" skills/spike/SKILL.md` returns 0 matches
</verification>

<success_criteria>
- G5 gate outcome recorded in state.yml gate_history (status skill can now display full G1-G5 chain)
- Spike NOTE clarifies gate_history vs phase transitions distinction
- All 4 inline templates have zero {{placeholder}} syntax
- All 6 subagent templates are untouched
- 8-section XML structure preserved in all modified templates
</success_criteria>

<output>
After completion, create `.planning/phases/12-audit-tech-debt-cleanup/12-02-SUMMARY.md`
</output>
