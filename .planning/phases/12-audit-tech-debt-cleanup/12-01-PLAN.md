---
phase: 12-audit-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/design/SKILL.md
  - skills/plan/SKILL.md
  - skills/execute/SKILL.md
  - skills/status/SKILL.md
  - templates/state.yml.template
autonomous: true
requirements: [STATE-01, GATE-04, TELE-01, TELE-02, TELE-03, TELE-04, TELE-05]

must_haves:
  truths:
    - "Cross-session override re-entry works: user who exits mid-recycle can re-enter with --override and proceed to the next skill"
    - "Design Case B accepts research_in_progress + --override + G2 recycle in gate_history (not research_recycled)"
    - "Plan Case B accepts design_in_progress + --override + G3 recycle in gate_history (not design_recycled)"
    - "state.yml.template transition diagram annotates *_recycled as conceptual-only"
    - "TELE-01..05 checkboxes are [x] in REQUIREMENTS.md"
  artifacts:
    - path: "skills/design/SKILL.md"
      provides: "Fixed Case B override re-entry for design skill"
      contains: "research_in_progress"
    - path: "skills/plan/SKILL.md"
      provides: "Fixed Case B override re-entry for plan skill"
      contains: "design_in_progress"
    - path: "skills/execute/SKILL.md"
      provides: "Updated error hint for dead plan_recycled reference"
    - path: "skills/status/SKILL.md"
      provides: "Retained *_recycled display mappings (documentation value)"
    - path: "templates/state.yml.template"
      provides: "Annotated transition diagram documenting *_recycled as conceptual"
  key_links:
    - from: "skills/design/SKILL.md"
      to: "gate_history in state.yml"
      via: "Case B checks for G2 recycle entry"
      pattern: 'gate.*G2.*recycle'
    - from: "skills/plan/SKILL.md"
      to: "gate_history in state.yml"
      via: "Case B checks for G3 recycle entry"
      pattern: 'gate.*G3.*recycle'
---

<objective>
Fix dead *_recycled override re-entry paths (INT-04 + FLOW-03) and verify TELE checkboxes (already complete).

Purpose: Cross-session override re-entry is currently unreachable dead code. Users who exit mid-recycle and re-enter with --override hit an error instead of proceeding. This plan adjusts Case B checks in design and plan skills to accept *_in_progress + --override + gate_history evidence, updates the state.yml.template transition diagram, and cleans up the execute Case C hint.

Output: 5 modified files with working cross-session override re-entry path.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-audit-tech-debt-cleanup/12-RESEARCH.md
</context>

<interfaces>
<!-- Current Case B patterns that need changing -->

From skills/design/SKILL.md (lines 39-61):
```markdown
**Case B: Phase is "research_recycled" AND `--override` flag is present**

The user's research did not pass G2 but they want to proceed to design with known gaps. This is the override entry path from the original design.

1. Read `.expedite/research/override-context.md` (must exist after a G2 override/recycle). If it doesn't exist, display error: "Override requested but no override-context.md found. Run `/expedite:research` first." -> STOP.
2. Record the override entry in state.yml gate_history (backup-before-write):
   - gate: "G2-design-entry"
   - timestamp: "{ISO 8601 UTC}"
   - outcome: "override"
   - notes: "Entered design via --override with research_recycled phase"
   - overridden: true
3. Display warning about override entry.

**Case C: Phase is anything else (not "research_complete", not "research_recycled" with --override)**
```

From skills/plan/SKILL.md (lines 39-61):
```markdown
**Case B: Phase is "design_recycled" AND `--override` flag is present**

(Same pattern as design, checking design_recycled + G3)

**Case C: Phase is anything else (not "design_complete", not "design_recycled" with --override)**
```

From skills/execute/SKILL.md (line 61):
```markdown
If phase is "plan_recycled", additionally display: "Plan was recycled. Use `/expedite:plan` to revise and pass the G4 gate before executing."
```

From templates/state.yml.template (lines 17-24):
```yaml
#   research_in_progress -> research_complete | research_recycled
#   research_recycled -> research_in_progress
#   design_in_progress -> design_complete | design_recycled
#   design_recycled -> design_in_progress
#   plan_in_progress -> plan_complete | plan_recycled
#   plan_recycled -> plan_in_progress
```

From skills/status/SKILL.md:
```markdown
- `research_recycled` -> "Research: gaps identified, needs re-research"
- `design_recycled` -> "Design: needs revision"
- `plan_recycled` -> "Plan: needs revision"

- `research_recycled` -> "Re-run `/expedite:research` to address gaps (or use `--override` to proceed)"
- `design_recycled` -> "Re-run `/expedite:design` to address gaps (or use `--override` to proceed)"
- `plan_recycled` -> "Re-run `/expedite:plan` to address gaps (or use `--override` to proceed)"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix Case B override re-entry in design and plan skills</name>
  <files>skills/design/SKILL.md, skills/plan/SKILL.md</files>
  <action>
In skills/design/SKILL.md Step 1, replace Case B (currently lines 39-61):

**BEFORE:** Case B checks `Phase is "research_recycled" AND --override flag is present`
**AFTER:** Case B checks `Phase is "research_in_progress" AND --override flag is present AND gate_history contains at least one G2 recycle entry`

The new Case B content should be:

```markdown
**Case B: Phase is "research_in_progress" AND `--override` flag is present AND gate_history contains at least one G2 recycle entry**

The user's research gate recycled, they exited the session, and are now re-entering with --override to proceed to design with known gaps.

1. Verify gate_history contains at least one entry where `gate: "G2"` and `outcome: "recycle"`.
   If no G2 recycle found, display error: "Override requested but no G2 recycle found in gate history. Run `/expedite:research` first." -> STOP.
2. Read `.expedite/research/override-context.md` (must exist after a G2 override/recycle). If it doesn't exist, display error: "Override requested but no override-context.md found. Run `/expedite:research` first." -> STOP.
3. Record the override entry in state.yml gate_history (backup-before-write):
   ```yaml
   - gate: "G2-design-entry"
     timestamp: "{ISO 8601 UTC}"
     outcome: "override"
     notes: "Entered design via --override with research_in_progress phase and G2 recycle evidence"
     overridden: true
   ```
4. Display:
   ```
   Starting design phase with --override...

   WARNING: Research gate (G2) was not passed. Override context will be injected.
   Affected DAs will be flagged with Low confidence in the design document.
   ```

Proceed to Step 2. (Step 2 already reads override-context.md if it exists and flags affected DAs.)
```

Also update Case C to reference the new condition:
```markdown
**Case C: Phase is anything else (not "research_complete", not matching Case B conditions)**

Display:
```
Error: Research is not complete. Run `/expedite:research` to gather evidence before starting design.

Current phase: {phase}
```
If phase is "research_in_progress" and `--override` flag is NOT present, additionally check gate_history for G2 recycle entries. If found, display: "Research was recycled in a prior session. Use `--override` flag to proceed with known gaps: `/expedite:design --override`"
```

Apply the SAME pattern to skills/plan/SKILL.md Step 1:
- Case B: Change from `design_recycled` to `design_in_progress` + `--override` + gate_history contains G3 recycle entry
- Add gate_history verification step (check for `gate: "G3"` and `outcome: "recycle"`)
- Check for `.expedite/design/override-context.md` instead of research
- Record as `G3-plan-entry` in gate_history
- Update notes to reference `design_in_progress` instead of `design_recycled`
- Update Case C similarly: check gate_history for G3 recycle entries when phase is `design_in_progress` without --override
  </action>
  <verify>
Run: `grep -n "research_recycled\|design_recycled" skills/design/SKILL.md skills/plan/SKILL.md` -- should return zero matches.
Run: `grep -c "research_in_progress.*override\|G2 recycle" skills/design/SKILL.md` -- should return at least 2 matches.
Run: `grep -c "design_in_progress.*override\|G3 recycle" skills/plan/SKILL.md` -- should return at least 2 matches.
  </verify>
  <done>
Design Case B accepts research_in_progress + --override + G2 gate_history evidence. Plan Case B accepts design_in_progress + --override + G3 gate_history evidence. Neither skill references *_recycled phases. Case C provides helpful guidance when gate_history shows recycle evidence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update state.yml.template, execute Case C hint, status mappings, and verify TELE checkboxes</name>
  <files>templates/state.yml.template, skills/execute/SKILL.md, skills/status/SKILL.md, .planning/REQUIREMENTS.md</files>
  <action>
**state.yml.template (lines 17-24):** Update the transition diagram comments. Replace the `*_recycled` transition lines with annotated versions:

```yaml
#   research_in_progress -> research_complete
#   # Note: research_recycled is conceptual. Cross-session override re-entry
#   # uses research_in_progress + --override + gate_history G2 recycle evidence.
#   research_complete -> design_in_progress
#   design_in_progress -> design_complete
#   # Note: design_recycled is conceptual. Cross-session override re-entry
#   # uses design_in_progress + --override + gate_history G3 recycle evidence.
#   design_complete -> plan_in_progress
#   plan_in_progress -> plan_complete
#   # Note: plan_recycled is conceptual. Cross-session override re-entry
#   # uses plan_in_progress + --override + gate_history G4 recycle evidence.
#   plan_complete -> execute_in_progress
```

Remove the `research_recycled -> research_in_progress`, `design_recycled -> design_in_progress`, and `plan_recycled -> plan_in_progress` lines since these transitions never occur.

Also update the gate_history outcome schema comment (line 57) to include `go_advisory`:
```yaml
#   outcome: "go"            # go | go_advisory | hold | recycle | override
```
(This is already correct per the current file -- verify it matches.)

**skills/execute/SKILL.md (line 61):** The `plan_recycled` hint in Case C is unreachable since `plan_recycled` is never written. Update the hint to match the new pattern:
```markdown
If phase is "plan_in_progress" and `--override` flag is NOT present, check gate_history for G4 recycle entries. If found, display: "Plan was recycled in a prior session. Use `/expedite:plan` to revise and pass the G4 gate, or re-run with `/expedite:plan --override` to proceed with known gaps."
```
Remove the `plan_recycled` specific check entirely.

**skills/status/SKILL.md:** KEEP all 3 `*_recycled` display mappings and action mappings unchanged. They serve as documentation and would handle manual state.yml edits correctly. No changes needed to status SKILL.md.

**TELE checkbox verification:** Read .planning/REQUIREMENTS.md and confirm TELE-01 through TELE-05 all show `[x]` (not `[ ]`). If already correct (expected per commit 81fc0b5), no changes needed -- just confirm in verification output. If any are still `[ ]`, update them to `[x]`.
  </action>
  <verify>
Run: `grep -n "research_recycled\|design_recycled\|plan_recycled" templates/state.yml.template` -- should show only the "Note:" annotation comment lines, not transition arrow lines.
Run: `grep "plan_recycled" skills/execute/SKILL.md` -- should return zero matches.
Run: `grep -c "TELE-0[1-5].*\[x\]" .planning/REQUIREMENTS.md` -- should return 5.
  </verify>
  <done>
state.yml.template transition diagram annotates *_recycled as conceptual with explanation of the actual override mechanism. Execute Case C no longer references unreachable plan_recycled state. Status SKILL.md retained unchanged (display mappings serve as documentation). TELE-01..05 confirmed as [x] in REQUIREMENTS.md.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "research_recycled\|design_recycled\|plan_recycled" skills/design/SKILL.md skills/plan/SKILL.md skills/execute/SKILL.md` returns zero matches (dead references removed)
2. `grep -rn "research_recycled\|design_recycled\|plan_recycled" templates/state.yml.template` returns only annotation comments (not transition arrows)
3. `grep -rn "research_recycled\|design_recycled\|plan_recycled" skills/status/SKILL.md` returns 6 matches (3 display + 3 action mappings retained)
4. `grep -c "research_in_progress.*override" skills/design/SKILL.md` returns at least 1 (new Case B)
5. `grep -c "design_in_progress.*override" skills/plan/SKILL.md` returns at least 1 (new Case B)
6. `grep -c "TELE-0[1-5].*\[x\]" .planning/REQUIREMENTS.md` returns 5
</verification>

<success_criteria>
- Cross-session override re-entry path is reachable: design accepts research_in_progress + --override + G2 evidence, plan accepts design_in_progress + --override + G3 evidence
- No skill checks for *_recycled phases that are never written
- state.yml.template documents the actual override mechanism
- Status SKILL.md retains *_recycled mappings for documentation
- TELE checkboxes verified complete
</success_criteria>

<output>
After completion, create `.planning/phases/12-audit-tech-debt-cleanup/12-01-SUMMARY.md`
</output>
