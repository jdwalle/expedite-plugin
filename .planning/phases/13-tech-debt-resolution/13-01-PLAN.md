---
phase: 13-tech-debt-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/research/SKILL.md
  - skills/design/SKILL.md
  - skills/plan/SKILL.md
autonomous: true
requirements: [STATE-06]

must_haves:
  truths:
    - "Re-invoking /expedite:research during research_in_progress resumes at the correct step instead of rejecting with a misleading error"
    - "Re-invoking /expedite:design during design_in_progress (without --override) resumes at the correct step instead of rejecting"
    - "Re-invoking /expedite:plan during plan_in_progress (without --override) resumes at the correct step instead of rejecting"
    - "Existing override re-entry (Case B with --override) still works unchanged in design and plan"
  artifacts:
    - path: "skills/research/SKILL.md"
      provides: "Resume case for research_in_progress in Step 1"
      contains: "research_in_progress"
    - path: "skills/design/SKILL.md"
      provides: "Resume case for design_in_progress without --override in Step 1"
      contains: "design_in_progress"
    - path: "skills/plan/SKILL.md"
      provides: "Resume case for plan_in_progress without --override in Step 1"
      contains: "plan_in_progress"
  key_links:
    - from: "skills/research/SKILL.md Step 1"
      to: "state.yml research_round + artifact existence"
      via: "decision tree for resume point"
      pattern: "research_in_progress.*resume"
    - from: "skills/design/SKILL.md Step 1"
      to: ".expedite/design/DESIGN.md existence"
      via: "artifact check for resume point"
      pattern: "design_in_progress.*without.*override"
    - from: "skills/plan/SKILL.md Step 1"
      to: ".expedite/plan/PLAN.md existence"
      via: "artifact check for resume point"
      pattern: "plan_in_progress.*without.*override"
---

<objective>
Add mid-phase crash resume logic to research, design, and plan SKILL.md files so that re-invoking these skills during `*_in_progress` state resumes at the correct step instead of rejecting with a misleading error.

Purpose: Closes TD-1 from v1.0 milestone audit. Currently, if a user's session crashes during research, design, or plan, re-invoking the skill tells them to run the *previous* skill (e.g., "Run /expedite:scope" when research was already in progress). Scope and execute already handle resume correctly -- this fix brings the remaining 3 skills into parity.

Output: Updated Step 1 in research/design/plan SKILL.md with new resume cases
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tech-debt-resolution/13-RESEARCH.md

Source of truth patterns to replicate:
@skills/scope/SKILL.md (Step 1 Case B -- resume pattern)
@skills/execute/SKILL.md (Step 1 Case B -- checkpoint-based resume, for reference)
</context>

<interfaces>
<!-- Key patterns the executor needs to replicate -->

From skills/scope/SKILL.md Step 1 Case B (the model resume pattern):
```markdown
**Case B: Active lifecycle with `phase: "scope_in_progress"` AND `project_name` is set (not null)**
This is a resume scenario. Display:
[progress summary]
Use AskUserQuestion: Continue / Start over
- Continue: Skip to the step that corresponds to the current progress.
  If questions exist, skip to Step 6. If intent and description are set but no questions, skip to Step 5. If only project_name is set, skip to Step 4.
- Start over: Execute the archival flow below, then proceed to Step 3.
```

From skills/design/SKILL.md Step 1 current structure (Case B is override re-entry):
```markdown
Case A: Phase is "research_complete" -> normal entry
Case B: Phase is "research_in_progress" AND --override AND gate_history G2 recycle -> override re-entry
Case C: Everything else -> error/STOP
```

From skills/plan/SKILL.md Step 1 current structure (same pattern as design):
```markdown
Case A: Phase is "design_complete" -> normal entry
Case B: Phase is "design_in_progress" AND --override AND gate_history G3 recycle -> override re-entry
Case C: Everything else -> error/STOP
```

From skills/research/SKILL.md Step 1 current structure (simpler, no override):
```markdown
Case A: Phase is NOT "scope_complete" -> error/STOP
Case B: Phase IS "scope_complete" -> proceed (with gap-fill round check)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add crash resume Case B2 to research SKILL.md Step 1</name>
  <files>skills/research/SKILL.md</files>
  <action>
Edit research SKILL.md Step 1 to add a new case that handles `research_in_progress` re-invocation. The current structure has Case A (not scope_complete -> error) and Case B (scope_complete -> proceed). Restructure to:

**Case A: Phase is "scope_complete"** (normal entry -- unchanged, move existing Case B content here)
- If research_round is 0: initial run. Display "Starting research phase..."
- If research_round > 0: gap-fill re-entry. Display "Resuming research (round {research_round + 1})..."
- Proceed to Step 2.

**Case B: Phase is "research_in_progress"** (NEW -- crash resume)
This is a resume scenario. The research skill was running when the session ended.

1. Read `.expedite/state.yml` to get `research_round` and question statuses.
2. Check artifact existence to determine resume point using this decision tree:
   - `research_round == 0`: Crashed before Step 3 initialized state. Resume at Step 2 (read scope artifacts).
   - `research_round >= 1` AND no evidence files in `.expedite/research/`: Crashed during batching/dispatch (Steps 4-9). Resume at Step 4 (re-form batches).
   - `research_round >= 1` AND evidence files exist AND no `.expedite/research/sufficiency-results.yml`: Crashed during collection or before assessment. Resume at Step 12 (assess with available evidence).
   - `research_round >= 1` AND `sufficiency-results.yml` exists AND no `.expedite/research/SYNTHESIS.md`: Crashed during gate/synthesis. Resume at Step 14 (re-run gate).
   - `research_round >= 1` AND `SYNTHESIS.md` exists: Crashed during completion. Resume at Step 18 (finalize).

3. Display progress summary:
```
Found in-progress research for "{project_name}".

Progress detected:
- Research round: {research_round}
- Evidence files: {count or "none"}
- Sufficiency assessed: {yes/no}
- Synthesis written: {yes/no}
- Resume point: Step {N}

Continue from where you left off?
```

4. Use AskUserQuestion (matching scope pattern):
```
header: "Resume"
question: "Continue from where you left off?"
options:
  - label: "Continue"
    description: "Resume from Step {N}"
  - label: "Start over"
    description: "Reset research state and start from Step 2"
multiSelect: false
```

- Continue: Skip to the determined step. Do NOT re-run Step 3 (state transition) since state is already research_in_progress.
- Start over: Reset research_round to 0, clear question statuses to "pending", proceed to Step 2.

**Case C: Phase is anything else** (error -- move existing Case A content here, updated)
Display the existing error message: "Error: Scope is not complete. Run `/expedite:scope`..."
Then STOP.

CRITICAL: Do NOT touch any content below Step 1. The rest of the research SKILL.md (Steps 2-18) must remain exactly as-is.

PITFALL AVOIDANCE: Research resume is more complex than design/plan because research has 18 steps with multiple intermediate artifacts and a multi-round structure. The decision tree above uses multiple indicators (research_round, evidence files, sufficiency-results.yml, SYNTHESIS.md) to correctly identify the resume point. Do NOT simplify to a single artifact check.

PITFALL AVOIDANCE: The resume case must NOT attempt a state transition (Step 3 writes research_in_progress). The state is ALREADY research_in_progress -- that is why we are resuming. The resume path skips Step 3 entirely.
  </action>
  <verify>
Read skills/research/SKILL.md Step 1 section and confirm:
1. Three cases exist: Case A (scope_complete), Case B (research_in_progress resume), Case C (error)
2. Case B contains a 5-branch decision tree for resume point detection
3. Case B uses AskUserQuestion with Continue/Start over options
4. Case B explicitly states to skip Step 3
5. No content below Step 1 was modified (Step 2 header still present at the same position relative to end of Step 1)
  </verify>
  <done>Research SKILL.md Step 1 handles research_in_progress re-invocation with a decision tree that determines the correct resume step based on research_round value and artifact existence, offers Continue/Start over via AskUserQuestion</done>
</task>

<task type="auto">
  <name>Task 2: Add crash resume Case B2 to design and plan SKILL.md Step 1</name>
  <files>skills/design/SKILL.md, skills/plan/SKILL.md</files>
  <action>
Edit both design and plan SKILL.md Step 1 to add a new resume case. Both follow the same pattern. The existing Case B (override re-entry) MUST remain intact and unchanged.

**For design SKILL.md**, restructure Step 1 to:

**Case A: Phase is "research_complete"** (normal entry -- unchanged)

**Case B: Phase is "research_in_progress" AND `--override` flag is present AND gate_history contains at least one G2 recycle entry** (override re-entry -- unchanged, keep ALL existing content verbatim)

**Case B2: Phase is "design_in_progress" AND `--override` flag is NOT present** (NEW -- crash resume)
This is a resume scenario. The design skill was running when the session ended.

1. Check for `.expedite/design/DESIGN.md`:
   - If DESIGN.md exists: Step 5 completed. Resume at Step 7 (revision cycle). Display: "Found in-progress design with DESIGN.md already generated. Resuming at revision cycle..."
   - If DESIGN.md does not exist: Resume at Step 2 (read artifacts, then generate). Display: "Found in-progress design, but no DESIGN.md yet. Resuming from artifact loading..."

2. Display:
```
Found in-progress design for "{project_name}".

Design document: {exists/not yet generated}
Resume point: Step {2 or 7}
```

3. Proceed directly to the resume step. Do NOT re-run Step 3 (state transition) since state is already design_in_progress. No AskUserQuestion needed -- just resume (matching execute pattern for simpler artifact-centric skills).

**Case C: Phase is anything else** (error -- unchanged, but ensure it does NOT match design_in_progress without --override since that is now handled by Case B2)

Update the Case C text: the existing Case C already has a special sub-check for "design_in_progress without --override" that checks gate_history for G2 recycle entries and suggests using --override. This sub-check should remain for the case where design_in_progress + no --override + HAS G2 recycle (user probably wants override, not resume). Add a condition: if design_in_progress + no --override + NO G2 recycle evidence, that is a crash resume and should be handled by Case B2 above.

Simplest approach: Case B2 triggers on `design_in_progress` + no `--override`. Inside Case B2, first check gate_history for G2 recycle evidence. If found, display BOTH options: "It appears your design gate was recycled. You can resume the design revision, or use --override to proceed with known gaps." If no G2 recycle, proceed with pure crash resume as described above.

---

**For plan SKILL.md**, apply the identical pattern with these substitutions:
- "research_complete" -> "design_complete" (Case A)
- "research_in_progress" -> "design_in_progress" (Case B override)
- "G2" -> "G3" (gate reference)
- "design_in_progress" -> "plan_in_progress" (Case B2 resume)
- `.expedite/design/DESIGN.md` -> `.expedite/plan/PLAN.md` (artifact check)
- Step 5 -> Step 5 (same step number for plan)
- Step 7 -> Step 6 (plan revision cycle starts at Step 6)
- Step 2 -> Step 2 (same step number for plan)
- Step 3 -> Step 3 (same state transition step to skip)

CRITICAL: The existing Case B (override re-entry with --override + gate_history recycle) must remain EXACTLY as-is in both files. The --override flag is the discriminator between Case B (override re-entry) and Case B2 (crash resume). Do NOT merge, modify, or remove Case B.

PITFALL AVOIDANCE: Design and plan have a simpler resume than research because they have a single primary artifact (DESIGN.md / PLAN.md) that clearly marks progress. Do NOT add a complex decision tree -- just check if the artifact exists.

PITFALL AVOIDANCE: The resume case must NOT attempt a state transition. The state is already *_in_progress.
  </action>
  <verify>
Read skills/design/SKILL.md Step 1 and skills/plan/SKILL.md Step 1 and confirm:
1. Four cases exist in each: Case A (normal), Case B (override re-entry with --override), Case B2 (crash resume without --override), Case C (error)
2. Case B is unchanged from current content (verify --override + gate_history check is intact)
3. Case B2 checks for primary artifact existence (DESIGN.md / PLAN.md)
4. Case B2 explicitly skips Step 3 (state transition)
5. No content below Step 1 was modified in either file
  </verify>
  <done>Design and plan SKILL.md Step 1 each handle *_in_progress re-invocation (without --override) as crash resume, checking artifact existence to determine resume point, while preserving the existing override re-entry Case B exactly as-is</done>
</task>

</tasks>

<verification>
For each modified skill, verify:
1. `grep -n "research_in_progress" skills/research/SKILL.md` shows the new resume case in Step 1
2. `grep -n "design_in_progress" skills/design/SKILL.md` shows BOTH override re-entry (Case B) and crash resume (Case B2)
3. `grep -n "plan_in_progress" skills/plan/SKILL.md` shows BOTH override re-entry (Case B) and crash resume (Case B2)
4. `grep -c "override" skills/design/SKILL.md` count is >= the original count (override logic preserved, not removed)
5. `grep -c "override" skills/plan/SKILL.md` count is >= the original count (override logic preserved, not removed)
</verification>

<success_criteria>
- Research SKILL.md Step 1 has a resume case for research_in_progress with 5-branch decision tree
- Design SKILL.md Step 1 has a resume case for design_in_progress (without --override) with artifact existence check
- Plan SKILL.md Step 1 has a resume case for plan_in_progress (without --override) with artifact existence check
- All existing override re-entry logic (Case B with --override) is preserved unchanged in design and plan
- No content below Step 1 was modified in any of the 3 files
</success_criteria>

<output>
After completion, create `.planning/phases/13-tech-debt-resolution/13-01-SUMMARY.md`
</output>
