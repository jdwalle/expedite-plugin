---
phase: 13-tech-debt-resolution
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - skills/status/SKILL.md
  - skills/research/SKILL.md
autonomous: true
requirements: [STATE-06]

must_haves:
  truths:
    - "Status SKILL.md no longer maps *_recycled phases that are never written to state.yml"
    - "Research inline reference files include Glob fallback parenthetical consistent with the 10 prompt templates"
  artifacts:
    - path: "skills/status/SKILL.md"
      provides: "Status display and action mappings without dead *_recycled entries"
    - path: "skills/research/SKILL.md"
      provides: "Glob fallback parentheticals on all 3 ref-* file references"
      contains: "use Glob with"
  key_links:
    - from: "skills/status/SKILL.md"
      to: "state.yml phase values"
      via: "phase-to-description and phase-to-action mappings"
      pattern: "scope_in_progress|scope_complete|research_in_progress"
    - from: "skills/research/SKILL.md"
      to: "skills/research/references/ref-*.md"
      via: "Read with Glob fallback"
      pattern: "use Glob with.*ref-"
---

<objective>
Remove dead `*_recycled` status mappings from status SKILL.md and add Glob fallback parenthetical to 3 research inline reference file paths.

Purpose: Closes TD-2 and TD-3 from v1.0 milestone audit. The *_recycled phase values are never written to state.yml by any skill, so the status mappings are dead code. The 3 research ref-* files lack the Glob fallback that all 10 prompt templates have, creating an inconsistency.

Output: Cleaner status SKILL.md without dead mappings, consistent Glob fallback pattern across all reference file reads
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-tech-debt-resolution/13-RESEARCH.md
@.planning/phases/13-tech-debt-resolution/13-01-SUMMARY.md
</context>

<interfaces>
<!-- Current status SKILL.md lines to remove (6 lines total) -->

From skills/status/SKILL.md Step 3 (display mappings):
```
Line 44: - `research_recycled` -> "Research: gaps identified, needs re-research"
Line 47: - `design_recycled` -> "Design: needs revision"
Line 50: - `plan_recycled` -> "Plan: needs revision"
```

From skills/status/SKILL.md Step 4 (action mappings):
```
Line 60: - `research_recycled` -> "Re-run `/expedite:research` to address gaps (or use `--override` to proceed)"
Line 63: - `design_recycled` -> "Re-run `/expedite:design` to address gaps (or use `--override` to proceed)"
Line 66: - `plan_recycled` -> "Re-run `/expedite:plan` to address gaps (or use `--override` to proceed)"
```

<!-- Current research SKILL.md lines to modify (3 lines) -->

From skills/research/SKILL.md (ref-* reads without Glob fallback):
```
Line 621: Read `skills/research/references/ref-recycle-escalation.md` for escalation messaging...
Line 626: Read `skills/research/references/ref-override-handling.md` and follow its instructions...
Line 650: Read `skills/research/references/ref-gapfill-dispatch.md` for detailed batch structure...
```

<!-- Established Glob fallback pattern (from same file, prompt template reads) -->
```
Read `skills/research/references/prompt-web-researcher.md`
  (use Glob with `**/prompt-web-researcher.md` if the direct path fails)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead *_recycled mappings from status SKILL.md</name>
  <files>skills/status/SKILL.md</files>
  <action>
Remove exactly 6 lines from status SKILL.md:

In Step 3 (phase-to-description mapping), remove these 3 lines:
- `research_recycled` -> "Research: gaps identified, needs re-research"
- `design_recycled` -> "Design: needs revision"
- `plan_recycled` -> "Plan: needs revision"

In Step 4 (phase-to-action routing), remove these 3 lines:
- `research_recycled` -> "Re-run `/expedite:research` to address gaps (or use `--override` to proceed)"
- `design_recycled` -> "Re-run `/expedite:design` to address gaps (or use `--override` to proceed)"
- `plan_recycled` -> "Re-run `/expedite:plan` to address gaps (or use `--override` to proceed)"

NOTE: Phase 12 decision (12-01) retained these "for documentation value." Phase 13 roadmap success criterion 2 explicitly requires their removal: "Status SKILL.md no longer maps `*_recycled` phases that are never written to state.yml." The roadmap takes precedence -- this is exactly what tech debt resolution means.

Do NOT modify any other lines. The remaining 11 phase mappings in Step 3 and 11 action mappings in Step 4 must remain exactly as-is.
  </action>
  <verify>
Run: `grep -c "recycled" skills/status/SKILL.md` -- should return 0 (no *_recycled references remain).
Run: `grep -c "scope_in_progress\|scope_complete\|research_in_progress\|research_complete\|design_in_progress\|design_complete\|plan_in_progress\|plan_complete\|execute_in_progress\|complete\|archived" skills/status/SKILL.md` -- should return 22 (11 display + 11 action mappings for the valid phases).
  </verify>
  <done>Status SKILL.md has exactly 11 phase-to-description mappings and 11 phase-to-action mappings with zero *_recycled entries</done>
</task>

<task type="auto">
  <name>Task 2: Add Glob fallback parenthetical to 3 research ref-* file reads</name>
  <files>skills/research/SKILL.md</files>
  <action>
Add Glob fallback parenthetical to the 3 inline reference file reads in research SKILL.md. Each read instruction should include the fallback on the same line or the line immediately following, matching the established pattern used by all prompt-* template reads in the same file.

Modify line 621 from:
```
Read `skills/research/references/ref-recycle-escalation.md` for escalation messaging...
```
To:
```
Read `skills/research/references/ref-recycle-escalation.md` (use Glob with `**/ref-recycle-escalation.md` if the direct path fails) for escalation messaging...
```

Modify line 626 from:
```
Read `skills/research/references/ref-override-handling.md` and follow its instructions...
```
To:
```
Read `skills/research/references/ref-override-handling.md` (use Glob with `**/ref-override-handling.md` if the direct path fails) and follow its instructions...
```

Modify line 650 from:
```
Read `skills/research/references/ref-gapfill-dispatch.md` for detailed batch structure...
```
To:
```
Read `skills/research/references/ref-gapfill-dispatch.md` (use Glob with `**/ref-gapfill-dispatch.md` if the direct path fails) for detailed batch structure...
```

Insert the parenthetical immediately after the backtick-quoted path, before the rest of the instruction text. This matches the existing pattern used by prompt-* template reads.

Do NOT modify any other content. The line numbers may have shifted due to Plan 13-01 edits to Step 1, so locate these lines by searching for the ref-* filenames rather than relying on exact line numbers.
  </action>
  <verify>
Run: `grep "ref-.*use Glob" skills/research/SKILL.md` -- should return exactly 3 matches (one for each ref-* file).
Run: `grep "ref-" skills/research/SKILL.md | grep -v "use Glob"` -- should return 0 matches (all ref-* reads now have Glob fallback). Note: if ref- appears in other contexts (not file reads), filter to just the Read instruction lines.
  </verify>
  <done>All 3 research inline reference file reads include Glob fallback parenthetical, consistent with the 10 prompt template reads across the codebase</done>
</task>

</tasks>

<verification>
1. `grep -c "recycled" skills/status/SKILL.md` returns 0
2. `grep "ref-.*use Glob" skills/research/SKILL.md` returns exactly 3 matches
3. `grep "use Glob" skills/research/SKILL.md` count is now 13 (10 prompt templates + 3 ref files)
4. Status SKILL.md still has all 11 valid phase mappings (scope_in_progress through archived)
</verification>

<success_criteria>
- Status SKILL.md contains zero *_recycled references
- Status SKILL.md retains all 11 valid phase-to-description and 11 phase-to-action mappings
- All 3 research ref-* file reads include Glob fallback parenthetical
- The Glob fallback pattern matches the established format: `(use Glob with **/{filename} if the direct path fails)`
</success_criteria>

<output>
After completion, create `.planning/phases/13-tech-debt-resolution/13-02-SUMMARY.md`
</output>
