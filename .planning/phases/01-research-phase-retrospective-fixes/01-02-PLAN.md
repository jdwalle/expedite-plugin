---
phase: 01-research-phase-retrospective-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/research/SKILL.md
  - agents/sufficiency-evaluator.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Questions with source_hints containing 'codebase' route to the codebase batch, not the web batch"
    - "Web batch only receives questions where web is the sole or primary source hint"
    - "Direct source code reads (reading the actual function/class) count as strong single-source evidence that does not require corroboration"
  artifacts:
    - path: "skills/research/SKILL.md"
      provides: "Corrected source routing logic in Step 4"
      contains: "codebase"
    - path: "agents/sufficiency-evaluator.md"
      provides: "Source code read exception for corroboration requirement"
      contains: "source code"
  key_links:
    - from: "skills/research/SKILL.md"
      to: "agents/codebase-researcher"
      via: "batch routing by source_hints"
      pattern: "codebase.*batch"
    - from: "agents/sufficiency-evaluator.md"
      to: "corroboration dimension"
      via: "exception for direct code reads"
      pattern: "direct.*source.*code|single.*source.*exception"
---

<objective>
Fix source routing logic in the research skill and add a corroboration exception for direct code reads in the sufficiency evaluator.

Purpose: Questions with codebase source hints incorrectly route to the web batch instead of the codebase batch, sending the wrong agent to gather evidence. Additionally, the sufficiency evaluator penalizes questions answered by directly reading source code for lacking corroboration, even though reading the literal implementation is definitive evidence.

Output: Corrected routing rules in skills/research/SKILL.md Step 4 and a direct-code-read exception in agents/sufficiency-evaluator.md.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/research/SKILL.md
@agents/sufficiency-evaluator.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix primary source routing in research skill Step 4</name>
  <files>skills/research/SKILL.md</files>
  <action>
In skills/research/SKILL.md, modify the Step 4 (Evidence Planning) section. The current batching rule (line 71) says:

```
Group questions by `source_hints` into batches (web, codebase, mcp). Rules: skip `"covered"` questions; assign to first enabled source; fallback to web.
```

The problem: "assign to first enabled source" is ambiguous. In practice, questions with `source_hints: ["codebase", "web"]` ended up in the web batch because the ordering or interpretation was inconsistent.

Replace the batching rules with explicit, unambiguous routing logic:

```
Group questions by `source_hints` into batches (codebase, web, mcp). Rules:
- Skip questions with status `"covered"`.
- Route by source_hints priority: if `"codebase"` is in source_hints, assign to codebase batch. Else if `"mcp"` is in source_hints, assign to mcp batch. Else assign to web batch.
- Fallback: questions with no source_hints or empty source_hints go to web batch.
- Max 5 batches. Sort by priority within each.
```

This ensures codebase takes priority when present (since the question was scoped with codebase as a source hint, the evidence is expected to come from code). Web is the fallback, not the default when multiple hints exist.

Keep the rest of Step 4 unchanged (present batch plan for approval, validate DA coverage, cancel handling).
  </action>
  <verify>
Read skills/research/SKILL.md and confirm Step 4 contains the explicit routing priority: codebase > mcp > web. Confirm the old "assign to first enabled source" wording is gone.
  </verify>
  <done>Questions with source_hints including "codebase" route to codebase batch. Web batch only receives questions where web is the only/default source. Routing priority is explicit: codebase > mcp > web.</done>
</task>

<task type="auto">
  <name>Task 2: Add corroboration exception for direct source code reads</name>
  <files>agents/sufficiency-evaluator.md</files>
  <action>
In agents/sufficiency-evaluator.md, modify the corroboration assessment instructions (currently instruction 4, around lines 83-85) to add an exception for direct source code reads.

Current instruction 4:
```
4. **Assess Corroboration:** Do multiple independent sources agree?
   - **Strong:** 3+ independent sources converge on the same finding.
   - **Adequate:** 2 sources agree with consistent evidence.
   - **Weak:** Single source only, or partial disagreement between sources.
   - **None:** Sources contradict or no relevant sources found.
   (Calibrate by DA depth: Deep DAs require Strong/Adequate. Standard DAs require at least Adequate. Light DAs accept Adequate/Weak.)
```

Add a new paragraph after the calibration note but before instruction 5:

```
   **Direct source code exception:** When evidence comes from reading the actual source code implementation (the literal function, class, configuration, or data structure being asked about -- not documentation *about* the code), this counts as authoritative primary-source evidence. A direct code read is self-corroborating: the code IS the ground truth. Rate corroboration as **Adequate** (not Weak) for a single direct code read, regardless of DA depth. This exception applies ONLY to readings of the actual artifact in question, not to code examples from other projects or documentation excerpts.
```

Also update the quality gate checklist item on line 151 ("No single-source finding is rated as Strong corroboration") to clarify:
```
- [ ] No single-source finding is rated as Strong corroboration (direct code reads may be rated Adequate per the source code exception, but not Strong)
```
  </action>
  <verify>
Read agents/sufficiency-evaluator.md and confirm:
1. A "Direct source code exception" paragraph exists in instruction 4
2. The exception specifies corroboration rated as "Adequate" for direct code reads
3. The quality gate checklist item about single-source corroboration is updated to reference the exception
  </verify>
  <done>Direct source code reads (reading the actual implementation being asked about) count as Adequate corroboration, preventing PARTIAL ratings on questions definitively answered by reading the source. The exception is scoped to actual artifact reads only, not code examples from other projects.</done>
</task>

</tasks>

<verification>
1. Read skills/research/SKILL.md Step 4 -- routing priority is codebase > mcp > web
2. Read agents/sufficiency-evaluator.md instruction 4 -- "Direct source code exception" paragraph present
3. Grep for "first enabled source" in skills/research/SKILL.md -- should NOT be found (old wording removed)
4. Grep for "source code exception" in agents/sufficiency-evaluator.md -- should be found
</verification>

<success_criteria>
- Research skill Step 4 routes codebase-hinted questions to codebase batch (not web)
- Sufficiency evaluator treats direct source code reads as Adequate corroboration
- Both changes are scoped and do not affect other functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-phase-retrospective-fixes/01-02-SUMMARY.md`
</output>
