---
phase: 01-research-phase-retrospective-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gates/g2-structural.js
  - agents/web-researcher.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "G2 gate counts evidence files in subdirectories (e.g., round-2/) not just the research root"
    - "Web researcher agent prioritizes web sources and only uses codebase tools for supplemental context, not primary research"
    - "Web researcher writes a skeleton evidence file after first batch of findings to prevent total output loss on turn limit"
  artifacts:
    - path: "gates/g2-structural.js"
      provides: "Recursive evidence file counting in listEvidenceFiles"
      contains: "readdirSync.*recursive"
    - path: "agents/web-researcher.md"
      provides: "Prompt guardrails prioritizing web research + write-early pattern"
      contains: "guardrails"
  key_links:
    - from: "gates/g2-structural.js"
      to: ".expedite/research/"
      via: "recursive directory scan"
      pattern: "readdirSync|recursive"
    - from: "agents/web-researcher.md"
      to: "guardrails block"
      via: "prompt instructions"
      pattern: "guardrails|web-first"
---

<objective>
Fix three issues in the G2 gate script and web researcher agent discovered during the first real-world research phase run.

Purpose: The G2 structural gate undercounts evidence files (missing subdirectories), the web researcher wastes turns exploring local codebase instead of doing web research, and the web researcher loses all output if it hits the turn limit.

Output: Corrected gates/g2-structural.js with recursive scan, hardened agents/web-researcher.md with codebase tool restrictions and write-early instructions.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@gates/g2-structural.js
@agents/web-researcher.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix G2 gate recursive evidence file counting</name>
  <files>gates/g2-structural.js</files>
  <action>
Modify the `listEvidenceFiles(dir)` function in gates/g2-structural.js (currently lines 50-59) to recursively scan subdirectories under `.expedite/research/`.

Current behavior: `fs.readdirSync(dir)` only lists files in the research root directory, missing evidence files in subdirectories like `round-2/`.

Fix: Change the function to walk directories recursively. Use `fs.readdirSync(dir, { withFileTypes: true })` to get directory entries, then for each entry: if it is a file matching `evidence-*.md`, add it; if it is a directory, recurse into it. Return the full list of matching filenames across all subdirectories.

Important constraints:
- Keep ES5-compatible style (var, not const/let) to match the existing codebase style throughout this file.
- The function should still return an array of filename strings (not full paths) since the count is what matters for M3 and S2 checks.
- Actually, return full relative paths from researchDir (e.g., "evidence-web-01.md", "round-2/evidence-web-02.md") so the count is accurate and the detail messages are informative.
- Handle the case where readdirSync with `withFileTypes` is not available (Node < 10.10) by falling back to the current non-recursive behavior.

Update the M3 and S2 detail messages to mention "research directory (including subdirectories)" so operators know the count includes nested files.
  </action>
  <verify>
Run: `node -e "var g2 = require('./gates/g2-structural.js'); console.log('syntax ok')" 2>&1 || node -c gates/g2-structural.js`

Verify the listEvidenceFiles function handles recursive scanning by reading the file and confirming the recursive logic is present.
  </verify>
  <done>listEvidenceFiles recursively scans subdirectories, evidence files in round-N/ directories are counted in M3 and S2 checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add web researcher guardrails and write-early pattern</name>
  <files>agents/web-researcher.md</files>
  <action>
Two changes to agents/web-researcher.md:

**Change 1: Add prompt guardrails for web-first research behavior.**

Do NOT modify the disallowedTools frontmatter — the agent may need Read/Grep/Glob for supplemental codebase context. Instead, add a guardrails block in the `<instructions>` section, immediately before instruction 1:

```
<guardrails>
You are a WEB research agent. Your PRIMARY information sources are WebSearch and WebFetch.
- Spend your turn budget on web research (WebSearch, WebFetch). This is your core job.
- You MAY use Read/Grep/Glob for brief supplemental codebase context (e.g., checking a config format, confirming an API signature), but these should be quick spot-checks, not extended exploration.
- Do NOT spend multiple turns reading through codebase files, exploring directory structures, or tracing code paths. If a question requires deep codebase analysis, note that in your output and move on to web research.
- If a question mentions codebase concepts, research them via web sources (official docs, GitHub repos, Stack Overflow) as your primary approach.
</guardrails>
```

Place this block immediately before instruction 1 inside `<instructions>`.

**Change 2: Add write-early instructions.**

Add a new instruction between current instruction 4 (Read promising sources) and instruction 5 (Evaluate evidence quality). Number it as instruction 5, and renumber the rest accordingly. The new instruction:

```
5. **Write early, append often.** After completing your first question's findings (or after your first 3-5 significant findings if working on a single question), immediately write a skeleton evidence file to `{{output_file}}` with whatever findings you have so far. Use the full output format structure but mark incomplete sections with "[RESEARCH IN PROGRESS]". As you gather more findings, rewrite the file with accumulated results. This prevents total output loss if you reach the turn limit. The final write should have all "[RESEARCH IN PROGRESS]" markers removed.
```

After the new instruction 5, the existing instructions 5-8 become 6-9.
  </action>
  <verify>
Read agents/web-researcher.md and confirm:
1. disallowedTools is UNCHANGED from original (Bash, Edit, EnterWorktree only — no Read/Grep/Glob)
2. A guardrails block exists directing web-first behavior while allowing supplemental codebase reads
3. A "write early" instruction exists mentioning skeleton evidence file
4. Instructions are numbered 1-9 (was 1-8, now 1-9 with the new insert)
  </verify>
  <done>Web researcher has prompt guardrails directing web-first behavior (codebase tools available for supplemental context only), and writes skeleton output early to prevent output loss on turn limit.</done>
</task>

</tasks>

<verification>
1. `node -c gates/g2-structural.js` -- syntax check passes
2. Read agents/web-researcher.md -- guardrails block present with web-first directive
3. Grep for "recursive" in gates/g2-structural.js -- confirms recursive scan logic
4. Grep for "Write early" or "skeleton" in agents/web-researcher.md -- confirms write-early pattern
5. Grep for "guardrails" in agents/web-researcher.md -- confirms codebase prohibition
</verification>

<success_criteria>
- G2 gate evidence file count includes files in research subdirectories (round-N/)
- Web researcher agent has prompt guardrails directing web-first behavior (codebase tools allowed for supplemental context only)
- Web researcher agent guardrails block present before instruction 1
- Web researcher agent writes skeleton output after first batch of findings
</success_criteria>

<output>
After completion, create `.planning/phases/01-research-phase-retrospective-fixes/01-01-SUMMARY.md`
</output>
