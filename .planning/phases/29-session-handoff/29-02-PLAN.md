---
phase: 29-session-handoff
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - skills/scope/SKILL.md
  - skills/research/SKILL.md
  - skills/design/SKILL.md
  - skills/plan/SKILL.md
  - skills/spike/SKILL.md
  - skills/execute/SKILL.md
  - skills/status/SKILL.md
autonomous: true
requirements: [SESS-04, STATE-03]

must_haves:
  truths:
    - "Every skill's frontmatter includes session-summary.md for next-session context"
    - "session-summary.md is loaded via !cat with silent fallback when file does not exist"
    - "session-summary.md provides narrative orientation when resuming across session boundaries"
  artifacts:
    - path: "skills/scope/SKILL.md"
      provides: "Frontmatter injection of session-summary.md"
      contains: "session-summary.md"
    - path: "skills/research/SKILL.md"
      provides: "Frontmatter injection of session-summary.md"
      contains: "session-summary.md"
    - path: "skills/design/SKILL.md"
      provides: "Frontmatter injection of session-summary.md"
      contains: "session-summary.md"
    - path: "skills/plan/SKILL.md"
      provides: "Frontmatter injection of session-summary.md"
      contains: "session-summary.md"
    - path: "skills/spike/SKILL.md"
      provides: "Frontmatter injection of session-summary.md"
      contains: "session-summary.md"
    - path: "skills/execute/SKILL.md"
      provides: "Frontmatter injection of session-summary.md"
      contains: "session-summary.md"
    - path: "skills/status/SKILL.md"
      provides: "Frontmatter injection of session-summary.md"
      contains: "session-summary.md"
  key_links:
    - from: "skills/*/SKILL.md frontmatter"
      to: ".expedite/session-summary.md"
      via: "!cat injection in YAML frontmatter"
      pattern: "session-summary\\.md"
    - from: "hooks/session-stop.js (29-01)"
      to: "skills/*/SKILL.md frontmatter"
      via: "Stop hook writes the file that frontmatter reads on next session"
      pattern: "session-summary"
---

<objective>
Add session-summary.md frontmatter injection to all 7 skills so that session context from the previous session is automatically loaded when any skill is invoked.

Purpose: This is the read side of session handoff. The Stop and PreCompact hooks (from 29-01) write session-summary.md. This plan adds the frontmatter injection that loads it into every skill's context. When a developer starts a new session and invokes any skill, they see the session summary as part of the skill's context -- providing narrative orientation like "Resuming research, step 7 of 18: Dispatch web researchers. 3 of 5 dispatched."

Output: All 7 skill SKILL.md files updated with session-summary.md frontmatter injection.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/29-session-handoff/29-01-SUMMARY.md
@.planning/phases/25-state-splitting-and-hook-infrastructure/25-03-SUMMARY.md

@.planning/research/agent-harness-architecture/design/PRODUCT-DESIGN.md (lines 265, 379 — frontmatter injection patterns including session-summary.md)
@.planning/research/agent-harness-architecture/design/PRODUCT-DESIGN.md (lines 756-776 — scoped injection examples with session-summary.md)
</context>

<interfaces>
<!-- Current frontmatter pattern (from PRODUCT-DESIGN.md lines 756-776) -->

Each skill's SKILL.md frontmatter currently has injections like:
```
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`

Checkpoint:
!`cat .expedite/checkpoint.yml 2>/dev/null || echo "No checkpoint"`
```

The session-summary.md injection to add (per PRODUCT-DESIGN.md design):
```
!`cat .expedite/session-summary.md 2>/dev/null`
```

Note: session-summary.md uses bare `!cat` without a fallback echo. When the file does
not exist, the injection produces empty output (no "No session summary" noise). This is
intentional -- session-summary.md is optional context, not required state. Its absence
should be silent.

<!-- Consumption matrix (from PRODUCT-DESIGN.md line 379-383) -->
All skills consume session-summary.md via frontmatter injection.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add session-summary.md frontmatter injection to all 7 skills</name>
  <files>
    skills/scope/SKILL.md
    skills/research/SKILL.md
    skills/design/SKILL.md
    skills/plan/SKILL.md
    skills/spike/SKILL.md
    skills/execute/SKILL.md
    skills/status/SKILL.md
  </files>
  <action>
Add a session-summary.md injection line to each skill's frontmatter section, after the existing state file injections and before the override protocol injection.

The injection line to add to ALL 7 skills:

```
Session context:
!`cat .expedite/session-summary.md 2>/dev/null`
```

**Placement in each skill:** Add this block AFTER the last state file injection (checkpoint, questions, tasks, or gates depending on the skill) and BEFORE the override protocol injection line.

**For each skill, the frontmatter order should be:**

1. `Current lifecycle state:` + state.yml injection (existing)
2. `Checkpoint:` + checkpoint.yml injection (existing)
3. Skill-specific state files (existing -- questions.yml for scope/research/design/plan, tasks.yml for execute, gates.yml for status)
4. **NEW: `Session context:` + session-summary.md injection** <-- add here
5. `Override protocol:` + ref-override-protocol.md injection (existing, except status skill which has no override protocol)

**For the status skill specifically:** The status skill does not have an override protocol line. Add the session-summary.md injection after the gates.yml injection, before the skill body (`# Status Skill`).

Important notes:
- Use `2>/dev/null` with NO fallback `|| echo ...`. When session-summary.md does not exist, the output should be empty/silent. This is different from other injections that have fallback text.
- The label is `Session context:` (not "Session summary:" -- the skill sees it as context, not a separate section).
- Do NOT modify any other part of the skill files. Only add the one injection block.

Example of what scope/SKILL.md frontmatter should look like after this change:

```
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`

Checkpoint:
!`cat .expedite/checkpoint.yml 2>/dev/null || echo "No checkpoint"`

Questions:
!`cat .expedite/questions.yml 2>/dev/null || echo "No questions"`

Session context:
!`cat .expedite/session-summary.md 2>/dev/null`

Override protocol:
!`cat skills/shared/ref-override-protocol.md 2>/dev/null || echo "No override protocol found"`
```

Example of what execute/SKILL.md frontmatter should look like:

```
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`

Checkpoint:
!`cat .expedite/checkpoint.yml 2>/dev/null || echo "No checkpoint"`

Tasks:
!`cat .expedite/tasks.yml 2>/dev/null || echo "No tasks"`

Session context:
!`cat .expedite/session-summary.md 2>/dev/null`

Override protocol:
!`cat skills/shared/ref-override-protocol.md 2>/dev/null || echo "No override protocol found"`
```
  </action>
  <verify>
    grep -c "session-summary.md" skills/scope/SKILL.md skills/research/SKILL.md skills/design/SKILL.md skills/plan/SKILL.md skills/spike/SKILL.md skills/execute/SKILL.md skills/status/SKILL.md
    Expected: Each file shows 1 match (7 files total)
  </verify>
  <done>All 7 skills inject session-summary.md via frontmatter. The injection uses silent fallback (no echo on missing file). Placement is consistent: after state file injections, before override protocol.</done>
</task>

</tasks>

<verification>
1. All 7 skill SKILL.md files contain `!cat .expedite/session-summary.md 2>/dev/null` in their frontmatter
2. The injection uses `2>/dev/null` with NO fallback echo (silent when file missing)
3. The injection is labeled `Session context:` in all skills
4. Placement is after state file injections and before override protocol in all skills
5. No other changes were made to the skill files
</verification>

<success_criteria>
- Each skill's frontmatter includes session-summary.md for next-session context (SESS-04)
- session-summary.md is created at session end and loaded by all skill frontmatter as the primary orientation mechanism (STATE-03)
- When session-summary.md exists, skills see narrative context on invocation
- When session-summary.md does not exist, skills see no noise (silent fallback)
</success_criteria>

<output>
After completion, create `.planning/phases/29-session-handoff/29-02-SUMMARY.md`
</output>
