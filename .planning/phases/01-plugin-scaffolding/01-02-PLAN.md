---
phase: 01-plugin-scaffolding
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - skills/scope/SKILL.md
  - skills/research/SKILL.md
  - skills/design/SKILL.md
  - skills/plan/SKILL.md
  - skills/execute/SKILL.md
  - skills/status/SKILL.md
autonomous: false
requirements:
  - FOUND-02
  - FOUND-04
  - FOUND-05

must_haves:
  truths:
    - "All 6 SKILL.md files exist at skills/{name}/SKILL.md with valid YAML frontmatter"
    - "Each SKILL.md has name, description, allowed-tools, and argument-hint (where applicable) in frontmatter"
    - "Each description contains quoted trigger phrases enabling auto-invocation"
    - "Each SKILL.md body includes dynamic context injection line for state.yml"
    - "Running /expedite: in Claude Code shows all 6 skills in autocomplete"
  artifacts:
    - path: "skills/scope/SKILL.md"
      provides: "Scope skill entry point with trigger phrases for lifecycle start"
      contains: "name: scope"
    - path: "skills/research/SKILL.md"
      provides: "Research skill entry point with trigger phrases for evidence gathering"
      contains: "name: research"
    - path: "skills/design/SKILL.md"
      provides: "Design skill entry point with trigger phrases for design generation"
      contains: "name: design"
    - path: "skills/plan/SKILL.md"
      provides: "Plan skill entry point with trigger phrases for plan generation"
      contains: "name: plan"
    - path: "skills/execute/SKILL.md"
      provides: "Execute skill entry point with trigger phrases for task execution"
      contains: "name: execute"
    - path: "skills/status/SKILL.md"
      provides: "Status skill entry point with trigger phrases for lifecycle overview"
      contains: "name: status"
  key_links:
    - from: "skills/{name}/SKILL.md"
      to: "Claude Code skill auto-discovery"
      via: "Convention-based scanning of skills/ subdirectories"
      pattern: "skills/[a-z]+/SKILL\\.md"
    - from: "SKILL.md description field"
      to: "Claude Code auto-invocation"
      via: "Trigger phrase matching in quoted strings"
      pattern: "\"[^\"]+\""
---

<objective>
Create all 6 SKILL.md files with valid YAML frontmatter, trigger phrases, and stub bodies so Claude Code auto-discovers them under the `/expedite:` namespace.

Purpose: Claude Code scans `skills/` for subdirectories containing `SKILL.md`. Each SKILL.md's frontmatter defines the skill's metadata, and quoted trigger phrases in the description enable auto-invocation. Without these files, no skills appear in the `/expedite:` namespace.

Output: 6 SKILL.md files (scope, research, design, plan, execute, status), each with frontmatter + context injection + placeholder body. Verified via human test in Claude Code.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-scaffolding/01-RESEARCH.md
@.planning/phases/01-plugin-scaffolding/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 6 SKILL.md files with frontmatter and stub bodies</name>
  <files>
    skills/scope/SKILL.md
    skills/research/SKILL.md
    skills/design/SKILL.md
    skills/plan/SKILL.md
    skills/execute/SKILL.md
    skills/status/SKILL.md
  </files>
  <action>
Create each SKILL.md with three sections: YAML frontmatter, dynamic context injection, and stub body. The frontmatter is the critical part — it determines how Claude Code discovers and invokes the skill.

**General rules for ALL SKILL.md files:**
- `name` field MUST match the directory name exactly (e.g., `scope` in `skills/scope/`)
- `description` MUST use third person: "This skill should be used when the user wants to..."
- Trigger phrases MUST be in double quotes within the description
- `allowed-tools` MUST use YAML array format (dash-prefixed list, not flow sequence)
- Body starts with the dynamic context injection line (inert until Phase 2 creates state.yml)
- Body includes a stub message indicating which phase implements the full skill
- Do NOT write full orchestration logic — stubs only. SKILL.md files should not exceed 30 lines.
- Do NOT use "arc" anywhere — use "expedite" for all naming

**Dynamic context injection line (same for ALL skills):**
```markdown
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`
```

**Create skills/scope/SKILL.md:**
```yaml
---
name: scope
description: >
  This skill should be used when the user wants to "start a new project lifecycle",
  "define project scope", "scope out research questions", "begin expedite",
  "new expedite project", "what should we research", or needs to decompose a
  high-level goal into structured decision areas with evidence requirements
  before beginning research.
allowed-tools:
  - Read
  - Write
  - Glob
  - Bash
  - AskUserQuestion
argument-hint: "[project-description]"
---
```
Stub body: "# Scope Skill\n\n> Under development. Full orchestration in Phase 4.\n\n**Purpose:** Start a new expedite lifecycle. Define intent, research questions, and decision areas.\n\n**Next:** After scope, run `/expedite:research`."

**Create skills/research/SKILL.md:**
```yaml
---
name: research
description: >
  This skill should be used when the user wants to "research the questions",
  "gather evidence", "investigate questions", "run research", "start research phase",
  or needs to dispatch parallel research agents to gather evidence for scoped questions.
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - Task
  - AskUserQuestion
  - WebSearch
  - WebFetch
---
```
Stub body: "# Research Skill\n\n> Under development. Core orchestration in Phase 5, quality/synthesis in Phase 6.\n\n**Purpose:** Dispatch parallel subagents to gather evidence for scoped questions.\n\n**Requires:** Completed scope (`/expedite:scope`).\n\n**Next:** After research, run `/expedite:design`."

**Create skills/design/SKILL.md:**
```yaml
---
name: design
description: >
  This skill should be used when the user wants to "generate a design",
  "create design document", "design phase", "write RFC", "write PRD",
  or needs to synthesize research evidence into an implementation or product design.
  Supports --override flag to proceed despite gate warnings.
allowed-tools:
  - Read
  - Write
  - Glob
  - Bash
argument-hint: "[--override]"
---
```
Stub body: "# Design Skill\n\n> Under development. Full orchestration in Phase 7.\n\n**Purpose:** Generate RFC (engineering) or PRD (product) design from research evidence.\n\n**Requires:** Completed research (`/expedite:research`).\n\n**Next:** After design, run `/expedite:plan`."

**Create skills/plan/SKILL.md:**
```yaml
---
name: plan
description: >
  This skill should be used when the user wants to "generate a plan",
  "create implementation plan", "plan phase", "decompose tasks",
  "break down into tasks", or needs to decompose a design into an ordered
  implementation plan. Supports --override flag.
allowed-tools:
  - Read
  - Write
  - Glob
  - Bash
argument-hint: "[--override]"
---
```
Stub body: "# Plan Skill\n\n> Under development. Full orchestration in Phase 8.\n\n**Purpose:** Decompose design into wave-ordered tasks (engineering) or epics/stories (product).\n\n**Requires:** Completed design (`/expedite:design`).\n\n**Next:** After plan, run `/expedite:execute`."

**Create skills/execute/SKILL.md:**
```yaml
---
name: execute
description: >
  This skill should be used when the user wants to "execute the plan",
  "run the plan", "implement", "build it", "start building",
  or needs to execute implementation plan tasks sequentially in the current session.
allowed-tools:
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Bash
  - WebSearch
  - WebFetch
---
```
Stub body: "# Execute Skill\n\n> Under development. Full orchestration in Phase 9.\n\n**Purpose:** Execute plan tasks sequentially with checkpoint/resume support.\n\n**Requires:** Completed plan (`/expedite:plan`).\n\n**Next:** Lifecycle complete after execution."

**Create skills/status/SKILL.md:**
```yaml
---
name: status
description: >
  This skill should be used when the user wants to "check lifecycle status",
  "where am I", "show status", "lifecycle status", "what phase am I on",
  or needs to see the current lifecycle state, phase, progress, and recommended next action.
allowed-tools:
  - Read
  - Glob
  - Bash
---
```
Stub body: "# Status Skill\n\n> Under development. Full orchestration in Phase 2.\n\n**Purpose:** Display current lifecycle phase, question status, gate history, and next action.\n\n**Requires:** An active lifecycle (any phase)."
  </action>
  <verify>
    <automated>
for skill in scope research design plan execute status; do
  FILE="skills/$skill/SKILL.md"
  if [ ! -f "$FILE" ]; then echo "FAIL: $FILE missing"; exit 1; fi
  if ! head -1 "$FILE" | grep -q "^---$"; then echo "FAIL: $FILE missing frontmatter start"; exit 1; fi
  if ! grep -q "^name: $skill$" "$FILE"; then echo "FAIL: $FILE name mismatch"; exit 1; fi
  if ! grep -q "This skill should be used when" "$FILE"; then echo "FAIL: $FILE missing trigger pattern"; exit 1; fi
  if ! grep -q 'allowed-tools:' "$FILE"; then echo "FAIL: $FILE missing allowed-tools"; exit 1; fi
  if ! grep -q 'cat .expedite/state.yml' "$FILE"; then echo "FAIL: $FILE missing context injection"; exit 1; fi
  echo "PASS: $FILE"
done
echo "PASS: All 6 SKILL.md files valid"
    </automated>
  </verify>
  <done>
    - All 6 SKILL.md files exist at skills/{name}/SKILL.md
    - Each has valid YAML frontmatter with name matching directory, third-person description with quoted trigger phrases, and allowed-tools array
    - Each body contains dynamic context injection line (!`cat .expedite/state.yml ...`)
    - Each body has a stub message indicating which phase provides full implementation
    - No SKILL.md exceeds 30 lines
    - No "arc" references anywhere
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify plugin discovery in Claude Code</name>
  <files>skills/scope/SKILL.md, skills/research/SKILL.md, skills/design/SKILL.md, skills/plan/SKILL.md, skills/execute/SKILL.md, skills/status/SKILL.md</files>
  <action>
**What was built:** Complete Expedite plugin structure — manifest at .claude-plugin/plugin.json, 6 SKILL.md files in skills/{name}/ directories, references/ subdirectories, and templates/ directory. All files follow Claude Code plugin conventions for auto-discovery.

**Human verification steps:**
1. Open a NEW Claude Code session in this project directory (the plugin source repo)
2. Run with plugin directory flag: `claude --plugin-dir .` (or `cc --plugin-dir .`)
3. Type `/expedite:` and check autocomplete — all 6 skills should appear:
   - `/expedite:scope`
   - `/expedite:research`
   - `/expedite:design`
   - `/expedite:plan`
   - `/expedite:execute`
   - `/expedite:status`
4. Invoke `/expedite:status` — should display the stub message (not an error)
5. Verify no `/arc:` namespace appears anywhere

**Expected result:** All 6 skills visible in autocomplete, each invocable without errors, stub messages displayed correctly.

**If skills do NOT appear:** Check that `.claude-plugin/plugin.json` exists (not `plugin.json` at root), that each SKILL.md is at `skills/{name}/SKILL.md` (not directly in `skills/`), and that YAML frontmatter has no syntax errors.

**Resume signal:** Type "approved" if all 6 skills appear and are invocable, or describe which skills are missing/broken.
  </action>
  <verify>
    <automated>echo "CHECKPOINT: Requires human verification in Claude Code — run 'claude --plugin-dir .' and type /expedite: to check autocomplete"</automated>
  </verify>
  <done>Human confirms all 6 skills (scope, research, design, plan, execute, status) appear in /expedite: autocomplete and at least one is invocable without errors.</done>
</task>

</tasks>

<verification>
- [ ] All 6 SKILL.md files have valid YAML frontmatter (parseable, correct fields)
- [ ] `name` field matches directory name in every SKILL.md
- [ ] Every description uses third-person format with quoted trigger phrases
- [ ] Every SKILL.md has the `!cat .expedite/state.yml` context injection line
- [ ] `allowed-tools` uses dash-prefixed array format in every file
- [ ] No SKILL.md exceeds 30 lines (stubs only, no orchestration logic)
- [ ] Human confirms all 6 skills appear in `/expedite:` autocomplete in Claude Code
- [ ] Human confirms at least one skill is invocable without errors
</verification>

<success_criteria>
Claude Code recognizes Expedite as an installed plugin. Typing `/expedite:` shows all 6 skills (scope, research, design, plan, execute, status) in autocomplete. Each skill can be invoked and displays its stub message. The plugin directory structure is complete and ready for subsequent phases to add full orchestration logic to each SKILL.md.
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-scaffolding/01-02-SUMMARY.md`
</output>
