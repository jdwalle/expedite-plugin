---
phase: 19-state-recovery
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - skills/scope/SKILL.md
  - skills/research/SKILL.md
  - skills/design/SKILL.md
  - skills/plan/SKILL.md
  - skills/spike/SKILL.md
  - skills/execute/SKILL.md
  - skills/status/SKILL.md
autonomous: true
requirements: [RESL-01, RESL-02, RESL-03, RESL-04]

must_haves:
  truths:
    - "Every skill checks for missing state.yml and attempts recovery before routing"
    - "Scope skill treats missing-state-with-artifacts differently from missing-state-without-artifacts"
    - "Non-scope skills attempt recovery then re-route using recovered state values"
    - "Status skill attempts recovery before displaying 'No active lifecycle' error"
    - "Recovery preamble instructs Claude to re-read state.yml after recovery (overriding stale injection)"
  artifacts:
    - path: "skills/scope/SKILL.md"
      provides: "Scope-specific recovery preamble in Step 1"
      contains: "ref-state-recovery.md"
    - path: "skills/research/SKILL.md"
      provides: "Recovery preamble in Step 1"
      contains: "ref-state-recovery.md"
    - path: "skills/design/SKILL.md"
      provides: "Recovery preamble in Step 1"
      contains: "ref-state-recovery.md"
    - path: "skills/plan/SKILL.md"
      provides: "Recovery preamble in Step 1"
      contains: "ref-state-recovery.md"
    - path: "skills/spike/SKILL.md"
      provides: "Recovery preamble in Step 1"
      contains: "ref-state-recovery.md"
    - path: "skills/execute/SKILL.md"
      provides: "Recovery preamble in Step 1"
      contains: "ref-state-recovery.md"
    - path: "skills/status/SKILL.md"
      provides: "Recovery preamble before 'No active lifecycle' response"
      contains: "ref-state-recovery.md"
  key_links:
    - from: "skills/*/SKILL.md (Step 1 preamble)"
      to: "skills/shared/ref-state-recovery.md"
      via: "Read tool invocation from preamble instructions"
      pattern: "ref-state-recovery\\.md"
    - from: "skills/*/SKILL.md (Step 1 preamble)"
      to: ".expedite/state.yml"
      via: "Re-read after recovery writes state"
      pattern: "Re-read.*state\\.yml|Read.*newly written"
---

<objective>
Add recovery preambles to all 7 skills so they invoke the centralized recovery protocol when state.yml is missing.

Purpose: The recovery protocol (Plan 01) exists but is never invoked. This plan wires it into every skill's entry point so that missing state.yml triggers automatic recovery instead of an error. Scope has different semantics from the other 6 skills.

Output: All 7 skill files modified with recovery preambles. Status skill additionally gains recovery attempt before its "No active lifecycle" response.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-state-recovery/19-CONTEXT.md
@.planning/phases/19-state-recovery/19-RESEARCH.md
@.planning/phases/19-state-recovery/19-01-SUMMARY.md

@skills/shared/ref-state-recovery.md
@skills/scope/SKILL.md
@skills/research/SKILL.md
@skills/design/SKILL.md
@skills/plan/SKILL.md
@skills/spike/SKILL.md
@skills/execute/SKILL.md
@skills/status/SKILL.md
</context>

<interfaces>
<!-- The recovery reference file created in Plan 01 -->
From skills/shared/ref-state-recovery.md:
- Invoked when `!cat .expedite/state.yml` injection returns "No active lifecycle"
- On success: writes .expedite/state.yml, displays inline notice
- On failure (no artifacts): displays "No recovery source found. Run /expedite:scope to start a new lifecycle." and instructs STOP
- After recovery: caller must re-read .expedite/state.yml for routing (stale injection override)

<!-- Existing Step 1 patterns across all skills -->
All skills share the same injection:
```
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`
```

Non-scope skills Step 1: "Look at the injected lifecycle state above." then Case A/B/C routing.
Scope skill Step 1: Case A is "No active lifecycle" -> proceed to Step 3 (fresh start). Case C is "Active lifecycle with any other phase" -> ask about archiving.
Status skill: Step 1 equivalent is instruction 1: "If it says 'No active lifecycle', respond with 'No active Expedite lifecycle...'"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add recovery preamble to 6 non-scope skills</name>
  <files>
    skills/research/SKILL.md
    skills/design/SKILL.md
    skills/plan/SKILL.md
    skills/spike/SKILL.md
    skills/execute/SKILL.md
    skills/status/SKILL.md
  </files>
  <action>
For each of the 5 lifecycle skills (research, design, plan, spike, execute), add a **Step 1 Preamble** block immediately after the Step 1 heading and step tracking instructions, BEFORE the "Look at the injected lifecycle state above." line. The preamble goes between the step tracking block and the Case routing.

**Preamble text for research, design, plan, spike, execute (identical for all 5):**

```markdown
**State Recovery Preamble**

Look at the injected lifecycle state above. If the injection shows "No active lifecycle" (meaning state.yml is missing):

1. Follow the recovery protocol in `skills/shared/ref-state-recovery.md` (use Glob with `**/ref-state-recovery.md` if the direct path fails).
2. If recovery succeeds: Re-read `.expedite/state.yml` to get the recovered state values. Use these recovered values -- not the original "No active lifecycle" injection -- for ALL subsequent Case routing below.
3. If recovery fails (no artifacts found): Display "No recovery source found. Run /expedite:scope to start a new lifecycle." Then STOP.

If the injection shows actual state content (not "No active lifecycle"), skip this preamble entirely and proceed to the Case routing below.
```

**Placement in each skill:**

- **research/SKILL.md**: Insert after the step tracking block (lines ~34-39) and before "Look at the injected lifecycle state above." (line ~41). The existing "Look at the injected lifecycle state above." line that precedes Case A should be REMOVED since the preamble now handles looking at the injection.
- **design/SKILL.md**: Same pattern -- after step tracking, before the existing "Look at" line. Remove the redundant "Look at" line.
- **plan/SKILL.md**: Same pattern.
- **spike/SKILL.md**: Same pattern.
- **execute/SKILL.md**: Same pattern.

**For status/SKILL.md** -- different structure since it uses numbered instructions, not Steps:

Modify instruction 1 (currently around line 22-27). Replace:
```
1. **Check for active lifecycle.** Look at the injected state above. If it says "No active lifecycle", respond with:
   ```
   No active Expedite lifecycle.
   Run /expedite:scope to start a new lifecycle.
   ```
   Then stop. Do not proceed further.
```

With:
```
1. **Check for active lifecycle.** Look at the injected state above. If it says "No active lifecycle" (state.yml is missing):

   a. Attempt recovery: Follow the protocol in `skills/shared/ref-state-recovery.md` (use Glob with `**/ref-state-recovery.md` if the direct path fails).
   b. If recovery succeeds: Re-read `.expedite/state.yml` and use the recovered state values for all subsequent steps. Continue to instruction 2.
   c. If recovery fails (no artifacts found): Respond with:
      ```
      No active Expedite lifecycle.
      Run /expedite:scope to start a new lifecycle.
      ```
      Then stop. Do not proceed further.

   If the injected state contains actual lifecycle content (not "No active lifecycle"), proceed to instruction 2.
```

**Critical detail (Pitfall 2 from research):** The preamble MUST instruct Claude to re-read `.expedite/state.yml` after recovery succeeds. The `!cat` injection is evaluated once at skill load time. After recovery writes state.yml, the injected text at the top of the prompt is still "No active lifecycle." The preamble must override this stale injection.

**What NOT to do:**
- Do NOT modify any Case A/B/C routing logic -- only add the preamble BEFORE the cases
- Do NOT add .bak references, sentinel checks, or corruption detection
- Do NOT change step tracking instructions or step numbering
  </action>
  <verify>
For each of the 6 files, verify the preamble was added:
```bash
for skill in research design plan spike execute status; do
  echo "=== $skill ===" && grep -c "ref-state-recovery.md" skills/$skill/SKILL.md
done
```
Each should return 1 (one reference to the recovery protocol).

Also verify no .bak references were introduced:
```bash
grep -r "state.yml.bak" skills/*/SKILL.md
```
Should return nothing (or only existing non-recovery references if any).

Verify step numbering is unchanged:
```bash
for skill in research design plan spike execute; do
  echo "=== $skill ===" && grep -c "^### Step" skills/$skill/SKILL.md
done
```
Counts should match pre-modification values: research=18, design=10, plan=9, spike=9, execute=7 (step heading counts, not total steps since substeps may vary).
  </verify>
  <done>
All 6 non-scope skills (research, design, plan, spike, execute, status) contain a recovery preamble that invokes `skills/shared/ref-state-recovery.md` when state.yml is missing, instructs re-reading state.yml after recovery, and falls through to existing Case routing with recovered values. Step numbering unchanged. No .bak/sentinel references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add scope-specific recovery preamble</name>
  <files>skills/scope/SKILL.md</files>
  <action>
Read `skills/scope/SKILL.md`. The scope skill has DIFFERENT recovery semantics from all other skills (Pitfall 1 from research):

- Scope is the ONLY skill where "No active lifecycle" is a VALID starting state (Case A: user wants to create a new lifecycle)
- For scope: "No active lifecycle" AND artifacts exist -> recover state, fall through to Case C (active lifecycle with phase != scope_in_progress, which asks about archiving)
- For scope: "No active lifecycle" AND no artifacts -> skip recovery, let scope proceed to Case A (fresh start)

**Add preamble after the step tracking block and BEFORE the existing "Look at the injected lifecycle state above." line (around line 40):**

```markdown
**State Recovery Preamble**

Look at the injected lifecycle state above. If the injection shows "No active lifecycle" (meaning state.yml is missing):

1. Check whether recovery artifacts exist: Follow the artifact scan in `skills/shared/ref-state-recovery.md` (use Glob with `**/ref-state-recovery.md` if the direct path fails).
2. If recovery succeeds (artifacts found and state.yml reconstructed): Re-read `.expedite/state.yml` to get the recovered state values. The recovered state will have a phase like `scope_complete` or later -- fall through to **Case C** below (active lifecycle with phase other than scope_in_progress).
3. If no artifacts found: This is a genuine fresh start. Proceed to **Case A** below.

If the injection shows actual state content (not "No active lifecycle"), skip this preamble entirely and proceed to the Case routing below.
```

**Remove the redundant "Look at the injected lifecycle state above." line** that currently precedes Case A, since the preamble now handles this.

**Critical difference from non-scope skills:**
- Non-scope skills: no artifacts -> STOP with error message
- Scope skill: no artifacts -> proceed to Case A (fresh start, which is the normal scope entry point)

**What NOT to do:**
- Do NOT modify Case A, B, or C logic
- Do NOT make recovery interactive (no user confirmation per locked decision)
- Do NOT add .bak references or sentinel checks
- Do NOT change step tracking or step numbering
  </action>
  <verify>
```bash
echo "=== scope ===" && grep -c "ref-state-recovery.md" skills/scope/SKILL.md
```
Should return 1.

Verify the scope preamble routes differently from non-scope skills:
```bash
grep -A5 "no artifacts found" skills/scope/SKILL.md | head -3
```
Should contain "genuine fresh start" and "Case A" (not "STOP" or "No recovery source").

Verify step count unchanged:
```bash
grep -c "^### Step" skills/scope/SKILL.md
```
Should match pre-modification count (10 step headings for scope).
  </verify>
  <done>
Scope skill has a recovery preamble with scope-specific semantics: missing state + artifacts -> recover and route to Case C (existing lifecycle), missing state + no artifacts -> Case A (fresh start). Recovery protocol reference is wired. Step numbering unchanged.
  </done>
</task>

</tasks>

<verification>
1. All 7 skills reference `skills/shared/ref-state-recovery.md` in their Step 1 / instruction 1
2. Scope skill routes to Case A (fresh start) when no artifacts found, unlike other skills which STOP
3. Non-scope skills display error and STOP when no artifacts found
4. All preambles instruct re-reading state.yml after recovery (stale injection override)
5. No step numbering changes in any skill
6. No .bak file references, sentinel fields, or corruption detection in any preamble
7. Status skill attempts recovery before displaying "No active lifecycle" message
</verification>

<success_criteria>
- All 7 skill files contain recovery preambles referencing the centralized protocol
- Scope skill has distinct preamble semantics (no-artifacts -> fresh start, not error)
- Step numbering preserved across all skills
- Stale injection override is explicitly addressed in every preamble
</success_criteria>

<output>
After completion, create `.planning/phases/19-state-recovery/19-02-SUMMARY.md`
</output>
