---
phase: 41-aud029-go-advisory-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gate-verifier.md
  - skills/plan/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "gate-verifier.md uses go_advisory consistently — no Go-with-advisory variants remain"
    - "plan SKILL.md G4 outcome routing uses go_advisory — no Go-with-advisory variants remain"
    - "Zero occurrences of go-with-advisory (any case) across all production files"
    - "gate-utils.computeOutcome output (go_advisory) matches plan skill G4 routing labels exactly"
  artifacts:
    - path: "agents/gate-verifier.md"
      provides: "Gate verifier agent instructions with standardized go_advisory naming"
      contains: "go_advisory"
    - path: "skills/plan/SKILL.md"
      provides: "Plan skill with correct G4 outcome routing labels"
      contains: "go_advisory"
  key_links:
    - from: "gates/lib/gate-utils.js"
      to: "skills/plan/SKILL.md"
      via: "G4 outcome string matching"
      pattern: "go_advisory"
---

<objective>
Replace 5 remaining Go-with-advisory strings with go_advisory in gate-verifier.md and plan SKILL.md, completing the AUD-029 naming standardization.

Purpose: Align gate-verifier prose and plan skill G4 routing with the canonical go_advisory form emitted by gate-utils.computeOutcome, ensuring Claude pattern-matches outcome branches correctly.
Output: Two updated Markdown files with zero go-with-advisory occurrences remaining.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-aud029-go-advisory-cleanup/41-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Go-with-advisory with go_advisory in gate-verifier and plan skill</name>
  <files>agents/gate-verifier.md, skills/plan/SKILL.md</files>
  <action>
Make 5 targeted string replacements using the Edit tool. The canonical form is `go_advisory` (all lowercase, underscore) per gate-utils.js line 96.

**File 1: agents/gate-verifier.md** (3 replacements)

Line 91 — Change:
`Your overall outcome (Go/Go-with-advisory/Recycle) must be definitive`
To:
`Your overall outcome (go/go_advisory/recycle) must be definitive`

Line 157 — Change:
`The overall outcome follows the score thresholds (Go: all 4+, Go-with-advisory: all 3+, Recycle: any below 3)`
To:
`The overall outcome follows the score thresholds (go: all 4+, go_advisory: all 3+, recycle: any below 3)`

Line 160 — Change:
`You did not hedge the outcome -- it is a definitive Go, Go-with-advisory, or Recycle`
To:
`You did not hedge the outcome -- it is a definitive go, go_advisory, or recycle`

IMPORTANT: Do NOT touch line 133 (`outcome: "{go | go_advisory | recycle}"`) — it is already correct.

**File 2: skills/plan/SKILL.md** (2 replacements)

Line 117 — Change:
`**Outcomes:** Go, Go-with-advisory, Recycle -- same routing as Step 8.`
To:
`**Outcomes:** go, go_advisory, recycle -- same routing as Step 8.`

Line 123 — Change:
`**Go-with-advisory** -- Show SHOULD failures.`
To:
`**go_advisory** -- Show SHOULD failures.`
  </action>
  <verify>
    <automated>grep -rni "go-with-advisory" agents/ skills/ gates/ hooks/ templates/ 2>/dev/null; test $? -eq 1 && echo "PASS: zero occurrences" || echo "FAIL: go-with-advisory still found"</automated>
  </verify>
  <done>All 5 Go-with-advisory occurrences replaced with go_advisory. Case-insensitive grep across all production directories returns zero matches. gate-verifier.md line 133 unchanged.</done>
</task>

</tasks>

<verification>
1. `grep -rni "go-with-advisory" agents/ skills/ gates/ hooks/ templates/` returns zero matches
2. `grep -n "go_advisory" agents/gate-verifier.md` shows lines 91, 133, 157, 160 all using go_advisory
3. `grep -n "go_advisory" skills/plan/SKILL.md` shows lines 117, 123 using go_advisory
4. `grep -n "go_advisory" gates/lib/gate-utils.js` still shows line 96 canonical form unchanged
</verification>

<success_criteria>
- Zero occurrences of go-with-advisory (any case) in production files
- gate-verifier.md lines 91, 157, 160 updated; line 133 unchanged
- plan SKILL.md lines 117, 123 updated
- gate-utils.computeOutcome output matches plan skill G4 routing labels
</success_criteria>

<output>
After completion, create `.planning/phases/41-aud029-go-advisory-cleanup/41-01-SUMMARY.md`
</output>
