---
phase: 30-agent-formalization
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - skills/research/SKILL.md
  - skills/spike/SKILL.md
autonomous: false
requirements:
  - AGNT-02
  - AGNT-08

must_haves:
  truths:
    - "At least one skill dispatches an agent by name via the Agent tool instead of Task() with inline prompt assembly"
    - "A dispatched agent executes with its frontmatter-specified model and tool restrictions"
    - "A dispatched agent cannot use a tool listed in its disallowedTools"
    - "Agent dispatch by name returns results to the skill without manual wiring"
  artifacts:
    - path: "skills/research/SKILL.md"
      provides: "Research skill with Agent tool dispatch replacing Task() for at least web-researcher"
      contains: "Agent"
    - path: "skills/spike/SKILL.md"
      provides: "Spike skill with Agent tool dispatch replacing Task() for spike-researcher"
      contains: "Agent"
  key_links:
    - from: "skills/research/SKILL.md"
      to: "agents/web-researcher.md"
      via: "Agent tool dispatch by name"
      pattern: "agent.*web-researcher"
    - from: "skills/spike/SKILL.md"
      to: "agents/spike-researcher.md"
      via: "Agent tool dispatch by name"
      pattern: "agent.*spike-researcher"
---

<objective>
Update skills to dispatch agents by name via the Agent tool (replacing Task() with inline prompt assembly) and validate that tool restrictions work correctly.

Purpose: Prove the agent formalization is functional end-to-end -- skills can dispatch agents by name, agents execute with their frontmatter config, and tool restrictions are enforced. This is the validation that makes the 8 agent files from Plan 30-01 operational rather than just definitions.

Output: Updated skill files with Agent tool dispatch pattern; validated tool restriction behavior via a human-verified test.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-agent-formalization/30-01-SUMMARY.md
@.planning/research/agent-harness-architecture/design/PRODUCT-DESIGN.md

<interfaces>
<!-- Agent Dispatch Pattern from PRODUCT-DESIGN.md -->

Skills dispatch agents via the standard Claude Code Agent tool, referencing agents by name:
1. Skill reads checkpoint.yml to determine current step (already in context from frontmatter)
2. Skill assembles context for the agent: task description, relevant artifact paths, output expectations
3. Skill invokes agent by name via Agent tool
4. Agent executes with its own frontmatter (model, tools, maxTurns)
5. Agent produces artifacts on disk and returns summary to skill
6. Skill verifies expected artifacts exist (file-existence check)
7. Skill updates checkpoint.yml with completed step

<!-- Current Task() dispatch pattern (to be replaced) -->

Current pattern in research skill (Step 5):
```
Task(
  prompt: {assembled_prompt},
  description: "Research batch: {batch_id}",
  subagent_type: "general-purpose"
)
```

New pattern (Agent tool dispatch by name):
The Agent tool dispatches a named agent from agents/*.md.
The skill passes the task context as the prompt argument.
The agent's frontmatter (model, tools, maxTurns) is automatically applied.

<!-- Tool Restriction Validation -->

To validate AGNT-08 (tool restriction test), dispatch an agent with disallowedTools
and verify it cannot use those tools. The web-researcher has Bash, Edit, EnterWorktree
in its disallowedTools. If dispatched and asked to run a Bash command, the agent
should be unable to do so.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update research and spike skills to use Agent tool dispatch</name>
  <files>
    skills/research/SKILL.md
    skills/spike/SKILL.md
  </files>
  <action>
Update the dispatch instructions in two skills to reference the Agent tool with named agents instead of Task() with inline prompt assembly. This is a TARGETED update -- only change the dispatch mechanism, not the overall skill flow. The full skill thinning (reducing skills to step-sequencer + agent-dispatcher) happens in Phase 31.

**Research skill (skills/research/SKILL.md):**

Find every `Task()` dispatch call in the research skill. There are several:
1. **Step 5 (Research Agent Dispatch):** Web researcher and codebase analyst dispatch (~line 587-605). Change from `Task(prompt: {assembled_prompt}, description: "...", subagent_type: "general-purpose")` to dispatch by agent name. The skill should still assemble the context prompt (filling in placeholders from the prompt template), but dispatch via the Agent tool referencing `web-researcher` or `codebase-researcher` by name. The agent's model and tool restrictions come from its frontmatter automatically.
2. **Step 8 (Sufficiency Evaluator):** (~line 789-801). Change Task() dispatch to reference a named agent. NOTE: The sufficiency evaluator is currently dispatched as a subagent but is NOT one of the 8 core agents in the Agent Catalog. For now, keep the existing Task() dispatch for the sufficiency evaluator unchanged -- it will be addressed in Phase 31 (skill thinning). Add a `<!-- TODO Phase 31: Convert sufficiency evaluator to named agent or inline logic -->` comment.
3. **Step 9 (Research Synthesizer):** (~line 1092-1102). Change Task() dispatch to reference `research-synthesizer` by name.

For each changed dispatch, update the instructions to say:
```
Dispatch the {agent-name} agent via the Agent tool. Pass the assembled context prompt.
The agent's model, tool restrictions, and maxTurns are defined in its frontmatter at agents/{agent-name}.md.
```

**Spike skill (skills/spike/SKILL.md):**

Find the Task() dispatch in Step 5 for spike research (~line 332-338). Change from Task() to Agent tool dispatch referencing `spike-researcher` by name.

**IMPORTANT constraints:**
- Do NOT change the prompt assembly logic (the skill still fills placeholders and builds the prompt)
- Do NOT change the result handling logic (skill still reads condensed return and checks artifacts)
- Do NOT remove the existing prompt template references (skills still read `prompt-web-researcher.md` etc. to assemble prompts) -- this will change in Phase 31 when skills become pure dispatchers
- ONLY change the dispatch mechanism from Task() to Agent tool with named agent
- Keep the agent logging (log.yml agent_completion entries) unchanged
- Keep the failure handling unchanged
  </action>
  <verify>
    <automated>
grep -c "Task(" skills/research/SKILL.md && echo "Remaining Task() calls in research (should be 1 for sufficiency evaluator)" && grep -c "Agent" skills/research/SKILL.md | head -5 && grep -c "Task(" skills/spike/SKILL.md && echo "Remaining Task() calls in spike (should be 0)" && grep "TODO Phase 31" skills/research/SKILL.md && echo "Phase 31 TODO comment present"
    </automated>
  </verify>
  <done>
Research skill dispatches web-researcher, codebase-researcher, and research-synthesizer by name via Agent tool. Sufficiency evaluator retains Task() dispatch with Phase 31 TODO comment. Spike skill dispatches spike-researcher by name via Agent tool. Prompt assembly and result handling logic unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validate agent dispatch and tool restriction</name>
  <what-built>
Agent dispatch by name was wired in the research and spike skills. The 8 agent definition files from Plan 30-01 have frontmatter with model tiering and tool restrictions.
  </what-built>
  <how-to-verify>
**Test 1: Agent dispatch by name (AGNT-02)**
1. Start a fresh Expedite lifecycle: run `/expedite:scope` with a simple test project
2. Complete scope (or use an existing lifecycle at research_complete or later phase)
3. Run `/expedite:research` and observe whether the web-researcher agent is dispatched by name
4. Verify in the output that the agent header shows "web-researcher" (not "general-purpose")
5. Verify the agent produces evidence files as expected

If you do not have a lifecycle ready to test, alternatively:
- Ask Claude: "Use the web-researcher agent to research what Claude Code agent frontmatter fields are available"
- Verify the agent is dispatched by name and runs with model: sonnet

**Test 2: Tool restriction enforcement (AGNT-08)**
1. Ask Claude: "Use the web-researcher agent to run `ls` in the terminal"
2. The web-researcher has `Bash` in its disallowedTools
3. Verify that the agent CANNOT execute Bash commands (should fail or refuse)
4. This confirms tool restrictions from frontmatter are enforced

**Test 3: Model tier verification (AGNT-05/AGNT-06)**
1. Ask Claude: "Use the research-synthesizer agent to summarize what it can do"
2. Observe the agent header in the output -- it should show model: opus (or claude-opus-*)
3. Ask Claude: "Use the web-researcher agent to summarize what it can do"
4. Observe the agent header -- it should show model: sonnet (or claude-sonnet-*)

**Expected outcomes:**
- Test 1: Agent dispatched by name, evidence file produced
- Test 2: Bash command blocked for web-researcher
- Test 3: Different model tiers visible in agent execution headers
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 tests pass, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
After both tasks complete:

1. Research skill dispatches web-researcher, codebase-researcher, and research-synthesizer by Agent tool name
2. Spike skill dispatches spike-researcher by Agent tool name
3. Sufficiency evaluator retains Task() dispatch (Phase 31 TODO)
4. Human-verified: agent dispatch by name succeeds
5. Human-verified: tool restriction (disallowedTools) is enforced
6. Human-verified: model tiering works (Opus vs Sonnet)
</verification>

<success_criteria>
- Skills dispatch agents by name via Agent tool (AGNT-02)
- Tool restriction test confirms dispatched agent cannot use a restricted tool (AGNT-08)
- Agent dispatch succeeds without requiring manual wiring
- Model tiering visible in execution (Opus agents use Opus, Sonnet agents use Sonnet)
</success_criteria>

<output>
After completion, create `.planning/phases/30-agent-formalization/30-02-SUMMARY.md`
</output>
