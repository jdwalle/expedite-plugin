---
phase: 04-scope-skill
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - skills/scope/SKILL.md
autonomous: true
requirements:
  - SCOPE-03
  - SCOPE-04
  - SCOPE-05
  - SCOPE-06
  - SCOPE-08
  - SCOPE-09
  - SCOPE-10
  - SCOPE-11
  - ARTF-01
  - ARTF-02

must_haves:
  truths:
    - "Step 6 instructs the LLM to read the references/prompt-scope-questioning.md guide BEFORE generating the question plan"
    - "Step 6 generates a question plan with DAs (ID, name, depth, readiness criterion) and questions (ID, text, priority, decision_area, source_hints, evidence_requirements)"
    - "Step 6 presents the plan in the exact '--- Question Plan Preview ---' format with DA grouping, depth calibration, readiness criteria, and per-question evidence requirements"
    - "Step 6 implements a review loop: user can approve (yes), modify, or add questions -- loop until approved"
    - "Step 6 includes self-check instructions: verify against quality criteria in prompt-scope-questioning.md before presenting to user"
    - "Step 7 displays configured sources from .expedite/sources.yml as a checklist and asks 'Use these? (yes)' -- read-only display, full edit deferred to Phase 10"
    - "Step 8 writes .expedite/scope/SCOPE.md with full question plan, DA metadata (readiness, depth), project context, success criteria, intent, source configuration"
    - "Step 8 updates state.yml with questions array (id, text, priority, decision_area, source_hints, evidence_requirements, status:pending, source:original, gap_details:null, evidence_files:[]) -- DA-level metadata lives in SCOPE.md only, NOT in state.yml"
    - "Step 8 uses backup-before-write pattern for state.yml update"
    - "Evidence requirements for each question are concrete and checkable (not vague like 'thorough understanding')"
    - "Each DA has depth calibration (Deep/Standard/Light) that sets evidence count expectations for downstream research"
    - "All paths use .expedite/ namespace, all commands use /expedite: namespace"
  artifacts:
    - path: "skills/scope/SKILL.md"
      provides: "Scope skill Steps 6-8 appended: question plan generation, source config, artifact writing"
      contains: "Question Plan Preview"
    - path: ".expedite/scope/SCOPE.md (runtime)"
      provides: "Human-readable scope artifact with full question plan and DA metadata"
      contains: "Decision Areas"
  key_links:
    - from: "skills/scope/SKILL.md Step 6"
      to: "skills/scope/references/prompt-scope-questioning.md"
      via: "SKILL.md instructs LLM to Read the reference before generating the plan"
      pattern: "prompt-scope-questioning.md"
    - from: "skills/scope/SKILL.md Step 8"
      to: ".expedite/scope/SCOPE.md (runtime)"
      via: "Write tool creates the scope artifact after user approval"
      pattern: "SCOPE.md"
    - from: "skills/scope/SKILL.md Step 8"
      to: ".expedite/state.yml (runtime)"
      via: "Backup-before-write updates questions array with full question metadata"
      pattern: "questions"
    - from: ".expedite/scope/SCOPE.md (runtime)"
      to: "skills/research/SKILL.md (Phase 5)"
      via: "Research skill reads SCOPE.md to get question plan, evidence requirements, and DA metadata"
      pattern: "Evidence Requirements"
---

<objective>
Append Steps 6-8 to the scope SKILL.md: question plan generation using the inline reference, the "Terraform plan-apply" review loop, simplified source configuration, and artifact writing (SCOPE.md + state.yml update).

Purpose: This is where the contract chain is forged. Step 6 transforms raw user context into structured decision areas with typed evidence requirements. The quality of these requirements determines whether research agents can find the right evidence, whether the sufficiency evaluator can assess it mechanically, and whether design decisions will be grounded in real evidence. The prompt-scope-questioning.md reference (created in Phase 3) defines WHAT a well-formed plan looks like; these steps define HOW the interactive flow produces one.

Output: Steps 6-8 appended to skills/scope/SKILL.md. At runtime, these steps produce .expedite/scope/SCOPE.md and update .expedite/state.yml.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scope-skill/04-RESEARCH.md
@.planning/phases/04-scope-skill/04-01-SUMMARY.md
@skills/scope/SKILL.md
@skills/scope/references/prompt-scope-questioning.md
@templates/sources.yml.template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Steps 6-8 to scope SKILL.md</name>
  <files>
    skills/scope/SKILL.md
  </files>
  <action>
Append the following content to the END of `skills/scope/SKILL.md` (after the existing "Proceed to Step 6." line from Plan 04-01). Do NOT modify any existing content -- only append.

```markdown

### Step 6: Question Plan Generation and Review

This is the most important step. The question plan defines the contract chain for the entire lifecycle.

**6a. Load the questioning guide.**
Read the file `skills/scope/references/prompt-scope-questioning.md` (use Glob with `**/prompt-scope-questioning.md` if the direct path fails). This guide defines the structure, quality criteria, and intent-specific guidance for the question plan. You MUST read it before generating the plan.

**6b. Generate the question plan.**
Using ALL collected context (project_name, intent, description, and the Round 2 refinement answers), generate a question plan following the structure defined in the questioning guide:

- Group questions into **Decision Areas (DAs)**. Each DA gets:
  - An ID: DA-1, DA-2, ... DA-N
  - A descriptive name
  - A **depth calibration**: Deep (high-stakes, hard to reverse -- requires 2+ corroborating sources per evidence requirement) | Standard (moderate reversibility -- requires at least 2 sources for key requirements) | Light (low-stakes, easily reversible -- single credible source acceptable)
  - A **readiness criterion**: A concrete, checkable statement of what evidence is needed to make a design decision for this DA. NOT "enough evidence" -- instead something like "at least 2 implementation examples with benchmarks comparing approaches"

- Within each DA, define **questions**. Each question gets:
  - An ID: q01, q02, ... qNN (sequential across ALL DAs)
  - The question text (phrased as a question, not a task)
  - A priority: P0 (blocking -- design cannot proceed without), P1 (important -- quality degrades without), P2 (supplementary)
  - Source hints: which sources are most likely to have the answer (web, codebase, mcp)
  - **Evidence requirements**: What SPECIFIC evidence would constitute a sufficient answer. These must be concrete and checkable:
    - GOOD: "At least 2 implementation examples with performance benchmarks"
    - GOOD: "API documentation confirming capability X with code samples"
    - BAD: "Thorough understanding of the topic" (uncheckable)
    - BAD: "Sufficient evidence" (circular)
    - BAD: "Research the approach" (task, not requirement)

Intent-specific guidance:
- **Product intent:** Include at least 1 DA addressing value risk and 1 addressing usability risk. P0 questions should include user validation evidence requirements. After the plan, include a "Risks Addressed" summary mapping DAs/questions to Cagan's four risks (value, usability, feasibility, viability).
- **Engineering intent:** Include at least 1 DA addressing architecture and 1 addressing implementation. P0 questions should include feasibility evidence requirements. After the plan, include a "Concerns Addressed" summary mapping DAs/questions to engineering concerns (architecture, implementation, performance, migration/integration).

**6c. Self-check before presenting.**
Before presenting the plan to the user, verify it against the quality criteria from the questioning guide:
- [ ] At least 3 questions total (usually 5-15)
- [ ] At least 1 P0 question exists
- [ ] At least 2 decision areas exist
- [ ] Every question has evidence requirements (no blanks)
- [ ] Every DA has a readiness criterion (concrete, checkable)
- [ ] Every DA has a depth calibration
- [ ] No more than 15 questions
- [ ] Questions are phrased as questions, not tasks
- [ ] Evidence requirements are specific and checkable

If any check fails, revise the plan before presenting. Do NOT present a plan that fails self-check.

**6d. Present the plan.**
Display the plan in this exact format:

```
--- Question Plan Preview ---

DA-1: [Name] ([Depth])
Readiness: [concrete readiness criterion]
  [P0] q01: [question text] ([source hints])
    Evidence needed: [specific evidence requirements]
  [P1] q02: [question text] ([source hints])
    Evidence needed: [specific evidence requirements]

DA-2: [Name] ([Depth])
Readiness: [concrete readiness criterion]
  [P0] q03: [question text] ([source hints])
    Evidence needed: [specific evidence requirements]
  ...

--- [N] questions across [M] DAs, estimated [K] research batches ---
Proceed? (yes / modify / add questions)
```

Estimate research batches by counting how many distinct source-type groups the questions split into (typically 2-3 batches for web, codebase, and MCP).

If product intent, also display the "Risks Addressed" summary after the plan.
If engineering intent, also display the "Concerns Addressed" summary after the plan.

**6e. Review loop.**
Wait for user response:
- **"yes"** or approval: Proceed to Step 7.
- **"modify"** + description of changes: Apply the requested modifications to the plan (add/remove/reprioritize questions, change DAs, adjust evidence requirements). Re-run the self-check (6c). Re-present the updated plan (6d). Loop until approved.
- **"add"** + new question descriptions: Add the new questions to the appropriate DAs (or create new DAs if needed). Assign IDs sequentially. Re-run self-check (6c). Re-present (6d). Loop until approved.
- If the user's response is ambiguous, ask for clarification: "I want to make sure I get this right. Would you like to (a) approve the plan as-is, (b) modify existing questions, or (c) add new questions?"

### Step 7: Source Configuration

Read `.expedite/sources.yml` and display the configured sources as a checklist:

```
Configured sources:
  [x] Web (WebSearch, WebFetch) - always available
  [x] Codebase (Grep, Read, Glob, Bash) - always available
```

If sources.yml contains additional MCP sources that are enabled, also show them:
```
  [x] GitHub (mcp__github__*) - configured
```

If MCP sources are listed but not enabled (commented out), show them as unchecked:
```
  [ ] GitHub (mcp__github__*) - not configured
```

Display: "Use these sources for research? (yes)"

Wait for user response:
- "yes" or any approval: Proceed to Step 8.
- If the user asks to edit sources: Respond with "Source editing will be available in a future update. For now, you can manually edit `.expedite/sources.yml` to add or remove MCP sources. Proceeding with current configuration."

### Step 8: Write Artifacts

Now that the user has approved the question plan and confirmed sources, write the artifacts.

**8a. Write SCOPE.md.**

Create `.expedite/scope/SCOPE.md` (create the `scope/` directory if it does not exist) with this structure:

```markdown
# Expedite Scope: {project_name}

**Created:** {current timestamp ISO 8601}
**Intent:** {product/engineering}
**Lifecycle ID:** {lifecycle_id from state.yml}

## Project Context

{User's description from Round 1, Question 3}

### Refinement Context

{Summary of Round 2 answers, organized by the questions asked}

## Success Criteria

{Extract success criteria from the user's responses. For product intent, these come from the "What does success look like?" answer. For engineering intent, these come from the "What is the desired end state?" answer. Present as a bulleted list of concrete, measurable criteria.}

## Decision Areas

### DA-1: {Name} ({Depth})
**Readiness criterion:** {concrete, checkable statement}

| ID | Priority | Question | Sources | Evidence Requirements |
|----|----------|----------|---------|----------------------|
| q01 | P0 | {question text} | {web, codebase} | {specific requirements} |
| q02 | P1 | {question text} | {codebase} | {specific requirements} |

### DA-2: {Name} ({Depth})
**Readiness criterion:** {concrete, checkable statement}

| ID | Priority | Question | Sources | Evidence Requirements |
|----|----------|----------|---------|----------------------|
| q03 | P0 | {question text} | {web} | {specific requirements} |

{Repeat for all DAs}

## {Risks Addressed / Concerns Addressed}

{For product intent: map DAs/questions to Cagan's four risks}
{For engineering intent: map DAs/questions to engineering concerns}

## Source Configuration

{Checklist from Step 7, showing enabled/disabled sources}

## Metadata

- Questions: {N} across {M} DAs
- Estimated research batches: {K}
- Intent: {product/engineering}
- Created: {timestamp}
```

**8b. Update state.yml with questions.**

Using the backup-before-write pattern:
1. Read `.expedite/state.yml`
2. Backup: `cp .expedite/state.yml .expedite/state.yml.bak` (via Bash)
3. Update the in-memory representation:
   - Set `last_modified` to current timestamp
   - Set `questions` array with one entry per question from the approved plan. Each question entry:
     ```yaml
     - id: "q01"
       text: "{question text}"
       priority: "P0"
       decision_area: "{DA name}"
       source_hints: ["{web}", "{codebase}"]
       evidence_requirements: "{specific requirements}"
       status: "pending"
       source: "original"
       gap_details: null
       evidence_files: []
     ```
   - Use YAML flow-style arrays for `source_hints` and `evidence_files` (e.g., `["web", "codebase"]` and `[]`) to stay within the 2-level nesting limit.
4. Write the entire file back to `.expedite/state.yml`

**IMPORTANT:** DA-level metadata (name, depth calibration, readiness criterion) lives ONLY in SCOPE.md. It does NOT go into state.yml. This respects the flat structure constraint (max 2 nesting levels). Only question-level data goes in state.yml.

Display: "Scope artifacts written. Running gate evaluation..."

Proceed to Step 9.
```

**Critical implementation notes:**
- This content is APPENDED after the existing "Proceed to Step 6." line. Do not modify anything above that line.
- The `references/prompt-scope-questioning.md` file MUST be read before generating the question plan. This is a hard requirement -- the quality of the entire downstream lifecycle depends on it.
- Evidence requirements must be concrete and checkable. The questioning guide has GOOD/BAD examples that the LLM must follow.
- The review loop (step 6e) uses freeform prompts, not AskUserQuestion.
- SCOPE.md is the data plane (human-readable). state.yml is the control plane (machine-readable). Both are updated, but DA-level metadata lives only in SCOPE.md.
- Step 7 is a simplified read-only source display. Full edit capability is Phase 10 (SCOPE-06).
  </action>
  <verify>
    <automated>
FILE="skills/scope/SKILL.md"
test -f "$FILE" && \
grep -q 'Step 6: Question Plan Generation and Review' "$FILE" && \
grep -q 'Step 7: Source Configuration' "$FILE" && \
grep -q 'Step 8: Write Artifacts' "$FILE" && \
grep -q 'prompt-scope-questioning.md' "$FILE" && \
grep -q 'Question Plan Preview' "$FILE" && \
grep -q 'Readiness:' "$FILE" && \
grep -q 'Evidence needed:' "$FILE" && \
grep -q 'yes / modify / add questions' "$FILE" && \
grep -q 'SCOPE.md' "$FILE" && \
grep -q 'Decision Areas' "$FILE" && \
grep -q 'evidence_requirements' "$FILE" && \
grep -q 'readiness criterion' "$FILE" && \
grep -q 'depth calibration' "$FILE" && \
grep -q 'Deep.*Standard.*Light' "$FILE" && \
grep -q 'backup-before-write' "$FILE" && \
grep -q 'sources.yml' "$FILE" && \
grep -q 'Proceed to Step 9' "$FILE" && \
grep -q 'self-check' "$FILE" && \
! grep -qi '\.arc/' "$FILE" && \
! grep -qi '/arc:' "$FILE" && \
echo "PASS: SKILL.md Steps 6-8 valid"
    </automated>
  </verify>
  <done>
    - Steps 6-8 appended to skills/scope/SKILL.md
    - Step 6: Question plan generation with inline reference loading, self-check, exact preview format, and review loop
    - Step 7: Simplified source configuration display (read-only, edit deferred to Phase 10)
    - Step 8: SCOPE.md artifact writing with full DA metadata and state.yml questions array update
    - Evidence requirements enforced as concrete/checkable (GOOD/BAD examples referenced)
    - DA-level metadata (depth, readiness) in SCOPE.md only -- not state.yml (flat structure constraint)
    - Backup-before-write for state.yml updates
    - Review loop supports modify, add, and approve flows
    - Intent-specific content: Risks Addressed (product) vs Concerns Addressed (engineering)
    - All .expedite/ namespace, no .arc/ references
    - Ends at "Proceed to Step 9" ready for 04-03 to append G1 gate
  </done>
</task>

</tasks>

<verification>
- [ ] Steps 6-8 are present in skills/scope/SKILL.md after Steps 1-5
- [ ] Step 6 instructs LLM to read prompt-scope-questioning.md before generating plan
- [ ] Step 6 includes self-check against quality criteria
- [ ] Step 6 uses exact "--- Question Plan Preview ---" format
- [ ] Step 6 review loop handles yes/modify/add with re-presentation
- [ ] Step 7 reads sources.yml and displays as checklist (read-only)
- [ ] Step 8 writes SCOPE.md with DA metadata (readiness, depth), questions, evidence requirements
- [ ] Step 8 updates state.yml questions array using backup-before-write
- [ ] DA-level metadata is in SCOPE.md only (NOT in state.yml)
- [ ] Evidence requirements are enforced as concrete and checkable
- [ ] Each DA has depth calibration (Deep/Standard/Light)
- [ ] No references to .arc/ or /arc: anywhere in the file
- [ ] File ends at "Proceed to Step 9" ready for 04-03
</verification>

<success_criteria>
Steps 6-8 are appended to the scope SKILL.md. The question plan generation uses the inline reference for quality criteria, presents a Terraform plan-apply preview with evidence requirements visible per question, allows user modification in a review loop, and writes both SCOPE.md (data plane with DA metadata) and state.yml (control plane with question-level data). The contract chain is established: every question has typed evidence requirements that downstream research agents will receive as targets.
</success_criteria>

<output>
After completion, create `.planning/phases/04-scope-skill/04-02-SUMMARY.md`
</output>
