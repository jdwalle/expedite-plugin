---
phase: 08-plan-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/plan/SKILL.md
autonomous: true
requirements: [PLAN-01, PLAN-02, PLAN-04, PLAN-05, PLAN-06]

must_haves:
  truths:
    - "Plan skill checks prerequisite phase (design_complete) before proceeding"
    - "Plan skill reads DESIGN.md, SCOPE.md, state.yml, and optional override-context.md"
    - "Plan skill generates inline implementation plan with per-DA design decisions mapped to phases"
    - "Engineering intent produces wave-ordered format; product intent produces epic/story format"
    - "Every implementation phase includes a tactical decision table with resolved/needs-spike classification"
    - "Phase sizing is enforced at 2-5 tactical decisions and 3-8 tasks per phase"
    - "PLAN.md artifact is written to .expedite/plan/PLAN.md"
  artifacts:
    - path: "skills/plan/SKILL.md"
      provides: "Plan skill orchestration Steps 1-5"
      min_lines: 200
  key_links:
    - from: "skills/plan/SKILL.md"
      to: ".expedite/design/DESIGN.md"
      via: "Step 2 artifact read"
      pattern: "Read.*DESIGN\\.md"
    - from: "skills/plan/SKILL.md"
      to: ".expedite/scope/SCOPE.md"
      via: "Step 2 DA enumeration"
      pattern: "Read.*SCOPE\\.md"
    - from: "skills/plan/SKILL.md"
      to: "skills/plan/references/prompt-plan-guide.md"
      via: "Step 4 quality reference read"
      pattern: "prompt-plan-guide"
    - from: "skills/plan/SKILL.md"
      to: ".expedite/plan/PLAN.md"
      via: "Step 5 artifact write"
      pattern: "Write.*PLAN\\.md"
---

<objective>
Replace the plan SKILL.md stub with Steps 1-5: prerequisite check, artifact loading, state initialization, inline plan generation with tactical decision identification, and PLAN.md artifact writing.

Purpose: This is the core pipeline of the plan skill -- the fourth stage in the Expedite contract chain. It reads completed design artifacts, breaks the design into uniform-sized implementation phases with tactical decision classification (resolved vs needs-spike), and writes the PLAN.md artifact with intent-adaptive formatting (waves for engineering, epics/stories for product).

Output: skills/plan/SKILL.md with Steps 1-5 (~200-300 lines replacing the current 27-line stub)
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-plan-skill/08-RESEARCH.md

# Prior phase patterns (design skill is the primary model)
@.planning/phases/07-design-skill/07-01-SUMMARY.md

# Source files
@skills/plan/SKILL.md
@skills/plan/references/prompt-plan-guide.md
@skills/design/SKILL.md
@templates/state.yml.template

<interfaces>
<!-- Key patterns and contracts the executor needs from the design skill (the model to follow). -->

From skills/design/SKILL.md (Steps 1-5 structure to mirror):
- Step 1: Prerequisite check with Case A (design_complete), Case B (design_recycled + --override), Case C (anything else)
- Step 2: Read artifacts (SCOPE.md, SYNTHESIS.md, state.yml, optional override-context.md) with DA cross-reference
- Step 3: Initialize state (backup-before-write, set phase to design_in_progress)
- Step 4: Generate inline using reference template (prompt-design-guide.md), intent-conditional format, self-check before write
- Step 5: Write artifact (DESIGN.md) with DA count validation and post-write verification

From templates/state.yml.template (state transitions):
```yaml
# design_complete -> plan_in_progress
# plan_in_progress -> plan_complete | plan_recycled
# plan_recycled -> plan_in_progress
```

From skills/plan/references/prompt-plan-guide.md (format reference):
- Engineering: Wave-ordered tasks with "Design decisions covered" per wave, task format with Design decision, Files, Acceptance criteria (parenthetical DA trace), Estimated effort, Dependencies
- Product: Epic/Story format with Given/When/Then acceptance criteria, Priority, Sizing
- Quality gate: every design decision mapped to task, every criterion traces to DA

From 08-RESEARCH.md (novel elements NOT in prompt-plan-guide.md):
- Tactical decision tables per phase: ID, Decision, Classification (resolved/needs-spike), Source
- Phase sizing bounds: 2-5 tactical decisions, 3-8 tasks per phase
- Override context consumption (read design override-context.md) and production (write plan override-context.md)
- Plan header format: title, Generated, Intent, Source, Phases/Tasks/TD summary line
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace plan SKILL.md stub with Steps 1-3 (prerequisite, artifact loading, state init)</name>
  <files>skills/plan/SKILL.md</files>
  <action>
Replace the entire plan SKILL.md body (preserve the existing frontmatter and !cat injection line exactly) with the plan skill orchestration script. Write Steps 1-3:

**Preamble (before Step 1):**
- Role paragraph: "You are the Expedite plan orchestrator. Your job is to break a design document into uniform-sized implementation phases..." Emphasize the contract chain position (4th stage: scope -> research -> design -> plan -> execute). Emphasize that every design decision must map to at least one implementation phase, every task traces to a design decision, and every phase identifies tactical decisions classified as resolved or needs-spike.
- Interaction model note: Use freeform prompts for revision feedback (same as design skill). Use AskUserQuestion only for structured choices.
- Auto-proceed instruction: "After completing each step, proceed to the next step automatically."

**Step 1: Prerequisite Check** (mirror design skill's Step 1 exactly, adapted for plan phase):
- Case A: Phase is "design_complete" -> Display "Starting plan phase..." -> Proceed to Step 2
- Case B: Phase is "design_recycled" AND --override flag present:
  1. Read `.expedite/design/override-context.md` (must exist). If missing, error and STOP.
  2. Record override entry in gate_history (backup-before-write): gate "G3-plan-entry", outcome "override", overridden true, notes "Entered plan via --override with design_recycled phase"
  3. Display warning about design gaps being flagged
  4. Proceed to Step 2
- Case C: Anything else -> Error with current phase, suggest /expedite:design first. If phase is "design_recycled", suggest --override flag. STOP.

**Step 2: Read Design + Scope Artifacts** (mirror design skill's Step 2, adapted):
Read the following files:
1. `.expedite/design/DESIGN.md` -- Extract per-DA design decisions, confidence levels, open questions
2. `.expedite/scope/SCOPE.md` -- Extract full DA list (DA-1 through DA-N) with names, depth calibration
3. `.expedite/state.yml` -- Extract project_name, intent, lifecycle_id
4. If `.expedite/design/override-context.md` exists -- Extract affected DAs and severity. Tasks tracing to overridden DAs will be annotated.

Display artifact loading summary (Project, Intent, Decision Areas count, override advisory if applicable).

Verify all DAs from SCOPE.md appear in DESIGN.md. If any DA has no design decision, note it -- the plan must still create tasks for it with an "insufficient design" flag.

DA cross-reference: Build list of all DA IDs and names. For each, track coverage status (full design decision, partial, missing from design). This becomes the coverage checklist for plan generation.

Error handling: If DESIGN.md or SCOPE.md cannot be read, display error suggesting /expedite:design. STOP.

**Step 3: Initialize Plan State** (mirror design skill's Step 3 exactly):
1. Read `.expedite/state.yml`
2. Backup: `cp .expedite/state.yml .expedite/state.yml.bak` (via Bash)
3. Set `phase` to `"plan_in_progress"`, set `last_modified` to ISO 8601 UTC
4. Write entire file back
5. Create output directory: `mkdir -p .expedite/plan/`
6. Display: "Plan phase initialized. Generating implementation plan..."
  </action>
  <verify>
Verify skills/plan/SKILL.md:
1. Frontmatter preserved (name, description, allowed-tools, argument-hint)
2. !cat injection line preserved
3. Preamble exists with role, interaction model, auto-proceed
4. Step 1 handles all 3 cases (design_complete, design_recycled+override, anything else)
5. Step 2 reads 4 files with DA cross-reference
6. Step 3 uses backup-before-write and creates .expedite/plan/
  </verify>
  <done>Steps 1-3 written to SKILL.md. Prerequisite check blocks on non-design_complete phase. Artifact loading reads DESIGN.md, SCOPE.md, state.yml, and optional override-context.md. State initialization transitions to plan_in_progress.</done>
</task>

<task type="auto">
  <name>Task 2: Append Steps 4-5 (plan generation with tactical decisions and PLAN.md writing)</name>
  <files>skills/plan/SKILL.md</files>
  <action>
Append Steps 4 and 5 to the plan SKILL.md after Step 3.

**Step 4: Generate Implementation Plan**

This is inline generation in the main session -- the plan is generated here, not dispatched to a subagent. Read `skills/plan/references/prompt-plan-guide.md` as a quality reference (use Glob with `**/prompt-plan-guide.md` if the direct path fails). The plan guide defines the required sections, quality criteria, and contract chain enforcement -- but is NOT dispatched as a subagent.

Generate the implementation plan following the correct format for the declared intent. The format is determined by the `intent` field from state.yml.

**If intent is "engineering"** -- Wave-ordered format:

For each wave:
- Wave heading: `## Wave {N}: {description}`
- Design decisions covered: list DA IDs
- Tactical Decisions table (NEW -- not in prompt-plan-guide.md):

```markdown
### Tactical Decisions
| ID | Decision | Classification | Source |
|----|----------|----------------|--------|
| TD-1 | {specific tactical decision} | resolved/needs-spike | DA-X: {brief source} |
```

- Tasks (t01, t02, etc.) with: Design decision (DA reference), Files, Acceptance criteria with parenthetical DA traceability `*(traces to DA-X decision: {brief})*`, Estimated effort (hours), Dependencies

Key rules from prompt-plan-guide.md:
- Tasks within a wave can execute independently (no intra-wave dependencies)
- Cross-wave dependencies explicitly listed
- Wave 1 has zero external dependencies
- Effort estimates 2-8 hours per task

**If intent is "product"** -- Epic/Story format:

For each epic:
- Epic heading: `## Epic {N}: {user-facing capability}`
- Design decisions covered: list DA IDs
- Tactical Decisions table (same format as engineering)
- Stories (1.1, 1.2, etc.) with: As a/I want/So that, Design decision, Acceptance criteria in Given/When/Then with parenthetical DA trace, Priority (P0/P1/P2), Sizing (S/M/L/XL)

**Tactical Decision Classification (the novel element):**
Provide clear guidance in the SKILL.md instructions for the LLM generating the plan:
- **Resolved:** The DESIGN.md decision provides enough specificity to implement directly. The design explicitly chose an approach, library, API, or pattern. No further investigation needed.
- **Needs-spike:** The DESIGN.md decision sets direction but leaves implementation details unresolved. There are at least two reasonable alternative approaches not fully resolved by the design. The spike skill will investigate these before execution.
- A tactical decision is something that: (a) is not fully resolved by the DESIGN.md decision text, (b) affects the implementation approach of at least one task, and (c) has at least two reasonable alternatives.
- TD IDs are phase-scoped (each wave/epic starts at TD-1). For cross-reference, use "Wave 2 TD-1" format.

**Phase Sizing Enforcement:**
- Each phase MUST contain 2-5 tactical decisions and 3-8 tasks/stories
- If a design decision requires 10+ tasks, split across 2 phases with logical grouping
- If a design decision requires only 1 task, group with related DAs in the same phase
- Self-check sizing before proceeding to Step 5

**Cross-cutting concerns:** Open questions from DESIGN.md's "Open Questions" section should become tactical decisions classified as "needs-spike" in the relevant phase. Cross-cutting concerns from DESIGN.md should be addressed as tasks within relevant phases or as a dedicated cross-cutting wave/epic.

**Contract chain enforcement (both intents):**
- Every DA from SCOPE.md MUST map to at least one phase's "Design decisions covered"
- Every task/story MUST cite a specific design decision
- Every acceptance criterion MUST include parenthetical DA traceability
- If evidence is insufficient for a DA (from override context), annotate tasks with advisory note

**Self-check before writing:** Before proceeding to Step 5, verify:
- [ ] Every DA from SCOPE.md has at least one phase covering it (count DAs in scope vs unique DAs in plan)
- [ ] Every phase has 2-5 tactical decisions and 3-8 tasks
- [ ] Every tactical decision is classified as resolved or needs-spike
- [ ] Every task/story cites a specific design decision
- [ ] Every acceptance criterion includes parenthetical DA reference
- [ ] Wave/epic ordering is logical (Wave 1/Epic 1 has no external dependencies)
- [ ] Override-affected DAs (if any) are annotated

If any check fails, revise the content before writing to disk.

**Step 5: Write PLAN.md**

Write the generated plan to `.expedite/plan/PLAN.md`.

Document header (both formats):
```markdown
# {Implementation Plan | Product Plan}: {project_name}
Generated: {ISO 8601 UTC timestamp}
Intent: {Engineering | Product}
Source: DESIGN.md + SCOPE.md
Phases: {N} | Tasks: {M} | Tactical Decisions: {K} ({resolved}/{needs-spike})
```

Summary footer (engineering):
```
--- {N} tasks across {M} waves ---
Design decisions covered: {count}/{total}
Total estimated effort: {hours}
Acceptance criteria: {count} total, all traced to design decisions
Tactical decisions: {resolved} resolved, {needs-spike} needs-spike
```

Summary footer (product):
```
--- {N} stories across {M} epics ---
Design decisions covered: {count}/{total}
Acceptance criteria: {count} total, all traced to design decisions
Tactical decisions: {resolved} resolved, {needs-spike} needs-spike
```

After writing, display plan summary (project, intent, phases/waves/epics count, tasks count, DA coverage, tactical decision breakdown).

If DA coverage is not complete, display WARNING with missing DAs.

**Post-write verification:** After writing PLAN.md, read it back and verify:
1. File exists and is non-empty
2. Header contains correct project_name, intent, timestamp
3. Count wave/epic sections and compare to expected
4. Count tasks/stories
5. Verify each phase has a Tactical Decisions table
6. Verify phase sizing (2-5 TDs, 3-8 tasks per phase)
7. Count DA coverage (unique DAs in all "Design decisions covered" lists vs SCOPE.md DA count)

If verification fails, display specific failure and fix before proceeding. Do NOT proceed to Step 6 with a malformed PLAN.md.
  </action>
  <verify>
Verify skills/plan/SKILL.md after Task 2 append:
1. Step 4 reads prompt-plan-guide.md with Glob fallback
2. Step 4 has intent-conditional format (engineering waves vs product epics)
3. Step 4 includes tactical decision table format with resolved/needs-spike
4. Step 4 includes tactical decision classification criteria (3 conditions)
5. Step 4 has phase sizing enforcement (2-5 TDs, 3-8 tasks)
6. Step 4 has self-check with 7 checklist items
7. Step 5 writes PLAN.md with header and footer formats
8. Step 5 has post-write verification (7 checks)
9. Both engineering and product header/footer variants are specified
  </verify>
  <done>Steps 4-5 appended to SKILL.md. Plan generation produces intent-adaptive format with tactical decision tables, phase sizing enforcement, self-check before write, and post-write verification. PLAN.md written to .expedite/plan/PLAN.md with complete traceability.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. skills/plan/SKILL.md has Steps 1-5 with frontmatter and !cat injection preserved
2. Line count is approximately 200-300 lines (replacing 27-line stub)
3. All 5 steps are present: prerequisite check, artifact loading, state init, plan generation, PLAN.md writing
4. Step 1 handles design_complete, design_recycled+override, and error cases
5. Step 4 includes both engineering (wave) and product (epic) formats
6. Step 4 includes tactical decision identification with classification criteria
7. Step 4 includes phase sizing bounds (2-5 TDs, 3-8 tasks)
8. Step 5 includes post-write verification
</verification>

<success_criteria>
- Plan SKILL.md stub replaced with Steps 1-5 orchestration script
- Prerequisite check blocks on non-design_complete phase (with --override path for design_recycled)
- Artifact loading reads DESIGN.md, SCOPE.md, state.yml, and optional override-context.md
- Plan generation is inline with prompt-plan-guide.md as quality reference
- Both intent formats specified (waves for engineering, epics for product)
- Tactical decision tables included per phase with resolved/needs-spike classification
- Phase sizing enforced (2-5 tactical decisions, 3-8 tasks)
- PLAN.md written to .expedite/plan/PLAN.md with header, footer, and traceability
- Self-check and post-write verification ensure quality before proceeding
</success_criteria>

<output>
After completion, create `.planning/phases/08-plan-skill/08-01-SUMMARY.md`
</output>
