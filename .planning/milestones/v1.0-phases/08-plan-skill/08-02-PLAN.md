---
phase: 08-plan-skill
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - skills/plan/SKILL.md
autonomous: false
requirements: [PLAN-03]

must_haves:
  truths:
    - "User can revise the plan via freeform feedback before gate evaluation"
    - "G4 gate structurally validates DA coverage, phase sizing, tactical decision presence, and acceptance criteria traceability"
    - "Gate outcomes (Go/Go-advisory/Recycle/Override) route correctly with escalating recycle messaging"
    - "Override writes .expedite/plan/override-context.md for spike/execute consumption"
    - "Plan completion transitions state to plan_complete"
  artifacts:
    - path: "skills/plan/SKILL.md"
      provides: "Complete plan skill orchestration Steps 1-9"
      min_lines: 350
  key_links:
    - from: "skills/plan/SKILL.md Step 7"
      to: ".expedite/plan/PLAN.md"
      via: "G4 gate reads PLAN.md for structural validation"
      pattern: "Read.*PLAN\\.md"
    - from: "skills/plan/SKILL.md Step 7"
      to: ".expedite/scope/SCOPE.md"
      via: "G4 gate reads SCOPE.md for DA reference"
      pattern: "Read.*SCOPE\\.md"
    - from: "skills/plan/SKILL.md Step 8"
      to: ".expedite/plan/override-context.md"
      via: "Override handler writes context for spike/execute"
      pattern: "override-context\\.md"
    - from: "skills/plan/SKILL.md Step 9"
      to: ".expedite/state.yml"
      via: "Completion sets phase to plan_complete"
      pattern: "plan_complete"
---

<objective>
Append Steps 6-9 to the plan SKILL.md: revision cycle, G4 structural gate evaluation, gate outcome handling with escalating recycle messaging and override context, and plan completion with state transition.

Purpose: This completes the plan skill with the quality gate and user interaction loop. G4 is structural (like G1), meaning all criteria are countable and deterministic -- no LLM judgment needed. The revision cycle follows the design skill's freeform pattern. Override context propagation ensures spike/execute receive gap information.

Output: Complete plan SKILL.md with all 9 steps (~350-450 lines)
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-plan-skill/08-RESEARCH.md
@.planning/phases/08-plan-skill/08-01-SUMMARY.md

# Pattern references
@skills/design/SKILL.md
@skills/scope/SKILL.md
@skills/research/references/ref-recycle-escalation.md
@skills/research/references/ref-override-handling.md
@templates/state.yml.template

<interfaces>
<!-- Key patterns from design skill Steps 7-10 and scope skill Step 9 that this plan mirrors. -->

From skills/design/SKILL.md Step 7 (revision cycle pattern to follow):
- Present "revise" or "proceed" options every iteration
- No round counter displayed, no hard limit
- "looks good", "done", "approved", "lgtm" = proceed
- On revise: parse feedback, apply changes, summarize changes, rewrite artifact, re-validate DA coverage, re-prompt
- On proceed: move to gate evaluation

From skills/scope/SKILL.md Step 9 (G1 structural gate pattern -- G4 mirrors this):
- Structural gate: all checks deterministic (counts, field existence, string matching)
- Do NOT apply LLM judgment
- For each criterion, state specific evidence from artifacts
- MUST criteria table with How to Check column
- SHOULD criteria table with How to Check column
- Gate outcomes: go, go_advisory, hold (scope uses hold; plan uses recycle per GATE-03)

From skills/design/SKILL.md Steps 8-10 (G3 gate outcome pattern):
- Record gate history in state.yml (backup-before-write)
- Go: proceed to completion
- Go-with-advisory: show SHOULD failures, offer proceed or revise
- Recycle: count previous recycles, escalate messaging (3 tiers from ref-recycle-escalation.md)
- Override: determine severity, write override-context.md, proceed to completion

From 08-RESEARCH.md (G4 criteria):
- M1: Every DA covered by at least one phase
- M2: Phase sizing within bounds (2-5 TDs, 3-8 tasks)
- M3: Tactical decisions listed per phase (each classified)
- M4: Acceptance criteria trace to design decisions (parenthetical DA references)
- M5: PLAN.md exists and is non-empty
- S1: Wave/epic ordering is logical
- S2: Effort estimates (engineering) or sizing (product) present
- S3: No orphan tasks (every task traces to DA)
- S4: Override-affected DAs flagged (auto-PASS if no override)

From templates/state.yml.template (state transitions):
```yaml
# plan_in_progress -> plan_complete | plan_recycled
# plan_recycled -> plan_in_progress
# plan_complete -> execute_in_progress
```

From templates/state.yml.template (execution state -- NOT populated by plan skill):
```yaml
tasks: []       # Populated by execute skill, not plan
current_wave: null  # Populated by execute skill, not plan
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Steps 6-9 (revision cycle, G4 gate, outcome handling, completion)</name>
  <files>skills/plan/SKILL.md</files>
  <action>
Append Steps 6-9 to the plan SKILL.md after Step 5.

**Step 6: Revision Cycle** (mirror design skill's Step 7 exactly, adapted for plan):

**6a: Present plan for review.** Display:
```
--- Plan Review ---

The implementation plan has been written to .expedite/plan/PLAN.md

Review the plan above. You can:
- **revise** -- describe changes (e.g., "move task t04 to Wave 1", "split Wave 3 into two", "reclassify TD-5 as resolved")
- **proceed** -- run G4 gate evaluation on the current plan

What would you like to do?
```

Wait for user input.

**6b: On "revise" (user provides feedback):**
1. Parse freeform feedback. Identify which phases, tasks, or tactical decisions they want changed.
2. Apply changes to the in-memory plan.
3. Summarize changes before rewriting:
```
Changes applied:
- Wave/Epic {N}: {brief description of change}
- {additional changes}
```
4. Rewrite `.expedite/plan/PLAN.md` with updated content.
5. Re-validate: verify DA coverage still complete (every DA from SCOPE.md in at least one phase). If any DA accidentally removed, display warning and restore.
6. Re-validate: verify phase sizing still within bounds (2-5 TDs, 3-8 tasks). If any phase out of bounds, display warning.
7. Return to 6a.

**6c: On "proceed":**
Display: "Proceeding to G4 gate evaluation..."
Proceed to Step 7.

**Key behaviors** (identical to design skill):
- No round counter. No "you have N revisions remaining" messaging.
- Every iteration presents both "revise" and "proceed" options.
- "looks good", "done", "approved", "no changes", "yes", "lgtm" = proceed
- "skip" or "skip to gate" = proceed
- Ambiguous feedback: ask for clarification rather than guessing.

Plan-specific revision types:
- Reorder tasks between waves/epics
- Split or merge phases
- Reclassify tactical decisions (resolved <-> needs-spike)
- Adjust phase boundaries (which DAs in which phase)
- Add/remove tasks or stories
- Modify acceptance criteria

**Step 7: G4 Gate Evaluation** (structural gate -- mirrors G1, NOT G3):

Read `.expedite/plan/PLAN.md` and `.expedite/scope/SCOPE.md` (for DA reference).

This is a **structural gate** -- all checks are deterministic (counts, field existence, string matching). Do NOT apply LLM judgment. For each criterion, state the specific evidence from the artifacts.

**MUST criteria (all must pass for Go):**

| # | Criterion | How to Check | Result |
|---|-----------|-------------|--------|
| M1 | Every DA covered by at least one phase | Count DAs in SCOPE.md, count unique DAs in all "Design decisions covered" lists in PLAN.md. State: "Found {N}/{M} DAs covered" | PASS/FAIL |
| M2 | Phase sizing within bounds | For each phase: count tactical decisions (expect 2-5) and tasks/stories (expect 3-8). State: "Phase {X}: {N} tactical decisions, {M} tasks" for any out-of-bounds phase | PASS/FAIL |
| M3 | Tactical decisions listed per phase | Each phase has a "Tactical Decisions" table with at least one entry, each classified as resolved or needs-spike. State: "{N}/{M} phases have tactical decision tables" | PASS/FAIL |
| M4 | Acceptance criteria trace to design decisions | Each acceptance criterion includes parenthetical DA reference matching `*(traces to DA-*`. State: "{N}/{M} criteria have traceability" | PASS/FAIL |
| M5 | PLAN.md exists and is non-empty | File exists with substantive content (not just headers). State: "PLAN.md: {line_count} lines" | PASS/FAIL |

**SHOULD criteria (failures produce advisory, do not block):**

| # | Criterion | How to Check | Result |
|---|-----------|-------------|--------|
| S1 | Wave/epic ordering is logical | Check that Wave 1 / Epic 1 has no external dependencies, subsequent phases build on prior ones. State reasoning. | PASS/ADVISORY |
| S2 | Effort estimates present (engineering) or sizing present (product) | Check each task/story has effort/sizing field. State: "{N}/{M} tasks have estimates" | PASS/ADVISORY |
| S3 | No orphan tasks | Every task/story traces to at least one DA via "Design decision" field. State: "{N} tasks without DA reference" | PASS/ADVISORY |
| S4 | Override-affected DAs flagged | If `.expedite/design/override-context.md` exists, check affected DAs are annotated in plan. Auto-PASS if no override context file exists. | PASS/ADVISORY |

Display gate results:
```
--- G4 Gate Evaluation ---
(Structural evaluation -- deterministic criteria)

MUST Criteria:
  M1: {PASS|FAIL} -- {evidence}
  M2: {PASS|FAIL} -- {evidence}
  M3: {PASS|FAIL} -- {evidence}
  M4: {PASS|FAIL} -- {evidence}
  M5: {PASS|FAIL} -- {evidence}

SHOULD Criteria:
  S1: {PASS|ADVISORY} -- {evidence}
  S2: {PASS|ADVISORY} -- {evidence}
  S3: {PASS|ADVISORY} -- {evidence}
  S4: {PASS|ADVISORY} -- {evidence}

Gate Outcome: {Go | Go-with-advisory | Recycle}
```

Gate outcomes:
- **Go**: All MUST pass AND all SHOULD pass
- **Go-with-advisory**: All MUST pass, one or more SHOULD failures
- **Recycle**: Any MUST criterion fails
- **Override**: Only when user explicitly requests (not auto-determined)

Proceed to Step 8.

**Step 8: Gate Outcome Handling** (mirror design skill's Step 9, adapted for plan):

**8a: Record gate history.** Append to gate_history in state.yml (backup-before-write):
```yaml
- gate: "G4"
  timestamp: "{ISO 8601 UTC}"
  outcome: "{go|go_advisory|recycle|override}"
  must_passed: {count}
  must_failed: {count}
  should_passed: {count}
  should_failed: {count}
  notes: "{brief summary}"
  overridden: false
```

**8b: Route by outcome:**

**Go** -- Display "G4 gate passed. Plan is ready for execution." Proceed to Step 9.

**Go-with-advisory** -- Show which SHOULD criteria failed. Present via freeform prompt: "1. Proceed with advisory | 2. Revise to address advisories". If proceed, proceed to Step 9. If revise, return to Step 6 (revision cycle).

**Recycle** -- Count previous G4 recycle outcomes in gate_history (entries where gate = "G4" AND outcome = "recycle").

- **1st Recycle (recycle_count == 0):** Informational tone. Display which MUST criteria failed with specific evidence. Offer via freeform prompt: "1. Revise (fix the issues above) | 2. Override (proceed with documented gaps)". If revise, return to Step 6 with gate feedback visible. If override, proceed to 8c.

- **2nd Recycle (recycle_count == 1):** Suggest adjustment. Display: "This is the second G4 recycle. Consider whether the plan structure needs adjustment." Show persistently failing criteria. Offer: "1. Revise | 2. Override (recommended if same criteria keep failing)". If revise, return to Step 6. If override, proceed to 8c.

- **3rd+ Recycle (recycle_count >= 2):** Recommend override. Display: "This is recycle #{recycle_count + 1}. Recommend overriding the gate. Remaining issues may require scope adjustment rather than plan revision." Offer: "1. Override (recommended) | 2. One more attempt (not recommended)". If override, proceed to 8c. If attempt, return to Step 6.

**8c: Override handling.**
1. Update gate_history entry: set overridden to true, update outcome to "override".
2. Determine severity: low (0 MUST failures -- user-initiated), medium (1 MUST failure), high (2+ MUST failures).
3. Write `.expedite/plan/override-context.md`:
```markdown
# G4 Override Context

Timestamp: {ISO 8601 UTC}
Severity: {low|medium|high}
Recycle count: {N}

## Overridden Issues

{For each failed MUST criterion:}
- {criterion_id}: {criterion description}
  Evidence: {what the gate found}
  Impact: {which phases/DAs are affected}

## Spike/Execute Phase Advisory

The following plan quality issues were overridden. The spike and execute
phases should account for these gaps.
{List affected criteria and recommendations}
```
4. Proceed to Step 9.

**Step 9: Plan Completion** (mirror design skill's Step 10):

Update state.yml (backup-before-write): set phase to "plan_complete", update last_modified.

NOTE: Do NOT populate the `tasks` array or `current_wave` field in state.yml. These fields exist in the schema but are populated by the execute skill (Phase 9 of the build), not the plan skill. The plan skill produces PLAN.md as its artifact; the execute skill reads PLAN.md and creates task-tracking state.

Display plan completion summary:
```
## Plan Complete

Project: {project_name}
Intent: {intent}

### Artifacts Produced
- Plan: .expedite/plan/PLAN.md
{If override:} - Override context: .expedite/plan/override-context.md

### Gate Results
G4 outcome: {outcome}
MUST: {passed}/{total} | SHOULD: {passed}/{total}
{If advisory:} Advisories: {list SHOULD failures}
{If override:} Override severity: {severity}

### Plan Summary
{intent == engineering: Waves | intent == product: Epics}: {count}
Tasks: {count}
Tactical decisions: {resolved} resolved, {needs-spike} needs-spike

### Contract Chain
Scope -> Research -> Design -> Plan (complete)
Decision Areas: {N} planned | Design decisions traced: {count}

### Next Step
Run `/expedite:spike <phase>` to investigate tactical decisions, or `/expedite:execute <phase>` to begin implementation.
```
  </action>
  <verify>
Verify skills/plan/SKILL.md after append:
1. Step 6 has revision cycle with revise/proceed options
2. Step 6 interprets "lgtm", "done", etc. as proceed
3. Step 6 re-validates DA coverage and sizing after each revision
4. Step 7 is explicitly structural ("Do NOT apply LLM judgment")
5. Step 7 has 5 MUST criteria (M1-M5) with How to Check column
6. Step 7 has 4 SHOULD criteria (S1-S4) with How to Check column
7. Step 8 records gate_history with backup-before-write
8. Step 8 has 3-tier recycle escalation (1st informational, 2nd suggest, 3rd recommend)
9. Step 8 override writes .expedite/plan/override-context.md
10. Step 9 sets phase to plan_complete
11. Step 9 does NOT populate tasks or current_wave
12. Step 9 displays completion summary with next step suggesting /expedite:spike or /expedite:execute
13. Total SKILL.md line count approximately 350-450
  </verify>
  <done>Steps 6-9 appended to SKILL.md completing the 9-step plan skill. Revision cycle follows design skill pattern. G4 is structural with 5 MUST + 4 SHOULD criteria. Recycle escalation follows 3-tier pattern. Override context flows to spike/execute. Plan completion transitions to plan_complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete plan skill</name>
  <files>skills/plan/SKILL.md</files>
  <action>
Human reviews complete 9-step plan SKILL.md orchestration covering the full plan lifecycle: prerequisite check, artifact loading, state initialization, inline plan generation with tactical decision identification (resolved/needs-spike), PLAN.md artifact writing, freeform revision cycle, G4 structural gate (5 MUST + 4 SHOULD), gate outcome handling with 3-tier recycle escalation and override context propagation, and plan completion with state transition.
  </action>
  <verify>
1. Open skills/plan/SKILL.md and read through all 9 steps end-to-end
2. Verify contract chain: Step 2 reads DESIGN.md + SCOPE.md, Step 4 maps every DA to phases, Step 7 validates coverage
3. Verify tactical decision format: each phase has a TD table with resolved/needs-spike classification
4. Verify G4 is structural (no LLM judgment) with deterministic criteria
5. Verify phase sizing bounds (2-5 TDs, 3-8 tasks) in both Step 4 self-check and Step 7 M2 criterion
6. Verify both intent formats are specified (engineering waves, product epics)
7. Verify override context flow: reads design override (Step 2), writes plan override (Step 8c)
8. Verify revision cycle has no round limit and interprets "lgtm"/"done" as proceed
9. Confirm SKILL.md reads as a coherent orchestration script from Step 1 through Step 9
  </verify>
  <done>Human approves complete plan SKILL.md as coherent orchestration script</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. skills/plan/SKILL.md is a complete 9-step orchestration script (~350-450 lines)
2. Frontmatter and !cat injection preserved from original stub
3. Steps 1-5 from Plan 01 are intact
4. Steps 6-9 from Plan 02 are appended
5. G4 gate is structural (deterministic, no LLM judgment)
6. Revision cycle follows design skill pattern
7. Override context propagation works bidirectionally (reads design, writes plan)
8. State transitions are correct: plan_in_progress -> plan_complete | plan_recycled
9. tasks/current_wave NOT populated (deferred to execute skill)
</verification>

<success_criteria>
- Revision cycle follows design skill freeform pattern with no round limit
- G4 gate is structural with 5 MUST + 4 SHOULD criteria (all deterministic, countable)
- Gate outcomes correctly route: Go, Go-with-advisory, Recycle (with escalation), Override
- Recycle escalation uses 3-tier messaging (informational, suggest, recommend)
- Override writes .expedite/plan/override-context.md with severity and affected phases
- Plan completion transitions state to plan_complete
- tasks and current_wave in state.yml are NOT populated (reserved for execute skill)
- Human verifies complete SKILL.md reads coherently end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/08-plan-skill/08-02-SUMMARY.md`
</output>
