---
phase: 01-plugin-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - skills/scope/references/.gitkeep
  - skills/research/references/.gitkeep
  - skills/design/references/.gitkeep
  - skills/plan/references/.gitkeep
  - skills/execute/references/.gitkeep
  - skills/status/.gitkeep
  - templates/.gitkeep
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03

must_haves:
  truths:
    - "Plugin manifest exists at .claude-plugin/plugin.json with name, version, description, author fields"
    - "Directory structure matches Claude Code plugin conventions: .claude-plugin/, skills/{name}/, skills/{name}/references/"
    - "All 6 skill directories exist: scope, research, design, plan, execute, status"
    - "templates/ directory exists at plugin root for future state/config templates"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin identity and metadata for Claude Code auto-discovery"
      contains: "\"name\": \"expedite\""
    - path: "skills/scope/references/.gitkeep"
      provides: "Scope skill references directory"
    - path: "skills/research/references/.gitkeep"
      provides: "Research skill references directory"
    - path: "skills/design/references/.gitkeep"
      provides: "Design skill references directory"
    - path: "skills/plan/references/.gitkeep"
      provides: "Plan skill references directory"
    - path: "skills/execute/references/.gitkeep"
      provides: "Execute skill references directory"
    - path: "templates/.gitkeep"
      provides: "Templates directory for state/config templates"
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "Claude Code plugin loader"
      via: "Convention-based auto-discovery"
      pattern: "\"name\":\\s*\"expedite\""
---

<objective>
Create the plugin manifest and full directory structure for the Expedite plugin.

Purpose: Claude Code discovers plugins via `.claude-plugin/plugin.json` manifest. Without this file in the correct location, no skills will be recognized. The directory structure must follow Claude Code conventions exactly.

Output: `.claude-plugin/plugin.json` manifest + complete directory tree with all skill subdirectories and references/ placeholders.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-scaffolding/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin manifest and directory structure</name>
  <files>
    .claude-plugin/plugin.json
    skills/scope/references/.gitkeep
    skills/research/references/.gitkeep
    skills/design/references/.gitkeep
    skills/plan/references/.gitkeep
    skills/execute/references/.gitkeep
    skills/status/.gitkeep
    templates/.gitkeep
  </files>
  <action>
Create the complete plugin directory structure with manifest.

**Step 1: Create `.claude-plugin/plugin.json`** with exactly this content:
```json
{
  "name": "expedite",
  "version": "0.1.0",
  "description": "Research-driven development lifecycle: Scope, Research, Design, Plan, Execute",
  "author": {
    "name": "Jared Wallenfels"
  }
}
```

Key constraints:
- `name` MUST be "expedite" (kebab-case, starts with letter) — this determines the `/expedite:` namespace
- `name` validation regex: `/^[a-z][a-z0-9]*(-[a-z0-9]+)*$/`
- `version` follows semver, starting at `0.1.0` for initial development
- `description` is 50-200 characters, active voice
- `author` uses object format with name field
- Do NOT include `commands`, `agents`, `hooks`, or `mcpServers` fields — not needed for this phase
- The manifest MUST be at `.claude-plugin/plugin.json`, NOT at the root. Placing it at root will prevent discovery.

**Step 2: Create skill directories with references/ subdirectories.**

Create these directories (use .gitkeep files to ensure git tracks empty directories):
- `skills/scope/references/`
- `skills/research/references/`
- `skills/design/references/`
- `skills/plan/references/`
- `skills/execute/references/`
- `skills/status/` (status has no references/ — it only reads state, no supplemental docs)

**Step 3: Create templates/ directory at plugin root.**

Create `templates/.gitkeep` — this directory will hold `state.yml.template`, `sources.yml.template`, and `gitignore.template` in Phase 2.

**Anti-patterns to avoid:**
- Do NOT nest skills/ inside `.claude-plugin/` — skills MUST be at the plugin root level
- Do NOT create any files besides plugin.json inside `.claude-plugin/`
- Do NOT use "arc" anywhere — the project is named "expedite"
- Do NOT create hooks.json — hook field naming is LOW confidence, deferred to Phase 2
  </action>
  <verify>
    <automated>
test -f .claude-plugin/plugin.json && \
python3 -c "import json; d=json.load(open('.claude-plugin/plugin.json')); assert d['name']=='expedite'; assert d['version']=='0.1.0'; assert 'description' in d; assert 'author' in d; print('PASS: plugin.json valid')" && \
test -d skills/scope/references && \
test -d skills/research/references && \
test -d skills/design/references && \
test -d skills/plan/references && \
test -d skills/execute/references && \
test -d skills/status && \
test -d templates && \
echo "PASS: All directories exist"
    </automated>
  </verify>
  <done>
    - .claude-plugin/plugin.json exists with name="expedite", version="0.1.0", description, and author fields
    - All 6 skill directories exist under skills/ (scope, research, design, plan, execute, status)
    - 5 of 6 skill directories have references/ subdirectories (status excluded)
    - templates/ directory exists at plugin root
    - No files exist inside .claude-plugin/ other than plugin.json
    - No "arc" references anywhere in created files
  </done>
</task>

</tasks>

<verification>
- [ ] `.claude-plugin/plugin.json` is valid JSON with all 4 required fields (name, version, description, author)
- [ ] Plugin name is exactly "expedite" (lowercase, no quotes issues)
- [ ] Directory structure matches the architecture pattern from research: `.claude-plugin/` + `skills/{name}/` + `skills/{name}/references/` + `templates/`
- [ ] No files or directories created inside `.claude-plugin/` except `plugin.json`
- [ ] No references to "arc" in any created file
</verification>

<success_criteria>
The plugin manifest and directory tree are complete. A subsequent plan can create SKILL.md files directly inside the skill directories without needing to create any directories.
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-scaffolding/01-01-SUMMARY.md`
</output>
