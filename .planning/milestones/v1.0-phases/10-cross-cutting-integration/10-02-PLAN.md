---
phase: 10-cross-cutting-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/execute/SKILL.md
  - skills/spike/SKILL.md
autonomous: true
requirements:
  - INTNT-01
  - INTNT-02
  - INTNT-03
  - GATE-05
  - ARTF-03

must_haves:
  truths:
    - "Execute PROGRESS.md entries use 'Epic' for product intent and 'Wave' for engineering intent"
    - "Completed lifecycles can be archived via a prompt in execute Step 7"
    - "Archival moves all .expedite/* except archive/, sources.yml, log.yml, .gitignore to .expedite/archive/{slug}/"
    - "G5 spike override writes advisory context that execute can consume via SPIKE.md"
    - "Override-context.md format is consistent across G2, G3, G4"
    - "Intent flows consistently from scope declaration through every downstream skill"
  artifacts:
    - path: "skills/execute/SKILL.md"
      provides: "Fixed intent terminology in PROGRESS.md entries + archival flow in Step 7"
      contains: "Archive this completed lifecycle"
  key_links:
    - from: "skills/execute/SKILL.md Step 7c"
      to: "archival flow"
      via: "freeform prompt after lifecycle completion summary"
      pattern: "Archive this completed lifecycle"
    - from: "skills/execute/SKILL.md Step 5e"
      to: "PROGRESS.md"
      via: "cat >> with intent-appropriate terminology"
      pattern: "Wave.*Epic"
---

<objective>
Polish intent flow consistency, add lifecycle archival to execute Step 7, and verify gate escalation override handling across all gates.

Purpose: Complete INTNT-01/02/03 (intent flows end-to-end), ARTF-03 (archival from completed lifecycle), and GATE-05 (override severity + gap context injection).

Output: Updated execute SKILL.md with archival flow and intent terminology fixes. Verified GATE-05 consistency.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cross-cutting-integration/10-RESEARCH.md
@skills/execute/SKILL.md
@skills/spike/SKILL.md

<interfaces>
<!-- Execute SKILL.md Step 5e PROGRESS.md append pattern (line ~278-311) -->
<!-- Currently uses "Wave: {wave_number}" for BOTH spiked and unspiked modes -->
<!-- Should use intent-appropriate: "Wave: {N}" for engineering, "Epic: {N}" for product -->

<!-- Execute SKILL.md Step 7 (line ~437-505) - lifecycle completion -->
<!-- Currently ends with "Note: Do NOT archive the lifecycle. Archival is Phase 10 functionality (ARTF-03)." -->
<!-- This note must be REPLACED with the actual archival flow -->

<!-- Scope SKILL.md archival flow (line ~92-111) - existing pattern to replicate -->
<!-- Uses: mkdir -p, for loop moving files, excludes archive/, sources.yml, log.yml, .gitignore -->

<!-- Override-context.md standardized format from research: -->
<!-- Severity: low (0 MUST fail), medium (1 MUST fail), high (2+ MUST fail) -->
<!-- Sections: Overridden Issues, {Next Phase} Advisory -->

<!-- G5 spike override (spike SKILL.md line ~305-311): -->
<!-- Override treated as go-with-advisory, recorded in SPIKE.md G5 Gate line -->
<!-- No override-context.md written -- advisory flows via SPIKE.md content -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix intent terminology in execute PROGRESS.md entries and verify GATE-05</name>
  <files>skills/execute/SKILL.md, skills/spike/SKILL.md</files>
  <action>
**Part A: Fix intent terminology in execute SKILL.md Step 5e PROGRESS.md entries.**

Read execute SKILL.md. Find Step 5e (PROGRESS.md append). Currently, both spiked and unspiked mode templates use:
```
- Wave: {wave_number}
```

Change both templates to use intent-appropriate terminology:
```
- {Wave/Epic}: {wave_number}
```

This matches the existing `{Wave/Epic}` pattern already used throughout the file in display messages (e.g., Step 5a "--- Task {task_id}: {task_title} ({current}/{total}) ---", Step 6 phase completion). The `{Wave/Epic}` placeholder means "use 'Wave' if engineering intent, 'Epic' if product intent" -- this is already the convention in the file.

Also check Step 6b PROGRESS.md phase completion append:
```
## Phase Complete: {Wave/Epic} {N}
```
This already uses `{Wave/Epic}` -- verify it is correct. No change needed if so.

Also check Step 7b PROGRESS.md lifecycle completion append -- verify it uses intent-appropriate language. Currently it says "All phases executed successfully" which is intent-neutral. This is correct.

**Part B: Verify GATE-05 override-context.md consistency.**

Read these files to verify override handling:
1. `skills/research/references/ref-override-handling.md` -- G2 override, writes `.expedite/research/override-context.md`
2. `skills/design/SKILL.md` -- search for "override-context" to find G3 override handling
3. `skills/plan/SKILL.md` -- search for "override-context" to find G4 override handling
4. `skills/spike/SKILL.md` -- search for "override" to verify G5 handling

Verify:
- G2 (research): writes override-context.md with severity. Design reads it in Step 2. EXPECTED: Already complete.
- G3 (design): writes override-context.md with severity. Plan reads it in Step 2. EXPECTED: Already complete.
- G4 (plan): writes override-context.md with severity. Execute/spike read it in Step 2/3. EXPECTED: Already complete.
- G5 (spike): Override treated as go-with-advisory. Advisory flows via SPIKE.md content (the G5 Gate line and advisory list in the output). Execute reads SPIKE.md. EXPECTED: This is correct -- G5 does not need a separate override-context.md because SPIKE.md IS the downstream artifact and it contains the advisory.

If any gate is missing severity or gap context injection, fix it. If all are consistent, document in the SUMMARY that GATE-05 is verified as already implemented across all gates.

**Part C: Verify INTNT-01/02/03 intent flow.**

The research found intent is 90%+ implemented. Verify by checking these specific points:
1. Execute SKILL.md Step 5e: NOW FIXED in Part A above
2. All `{Wave/Epic}` placeholders throughout execute SKILL.md: scan for any remaining hardcoded "Wave" that should be `{Wave/Epic}`
3. Spike SKILL.md: verify `{Wave/Epic}` usage is consistent

If any hardcoded "Wave" found where it should be intent-conditional, fix it.
  </action>
  <verify>
1. Grep for `"- Wave:"` in skills/execute/SKILL.md (should NOT appear as a PROGRESS.md field -- should be `{Wave/Epic}`)
2. Grep for `"Wave/Epic"` in skills/execute/SKILL.md to confirm consistent usage
3. Grep for `override-context` across all skill files to verify GATE-05 pattern
4. Verify no hardcoded "Wave" where `{Wave/Epic}` is needed in execute and spike
  </verify>
  <done>
Execute PROGRESS.md entries use intent-appropriate {Wave/Epic} terminology. GATE-05 verified: G2/G3/G4 write override-context.md with severity, downstream skills read them. G5 advisory flows via SPIKE.md content. No override-context.md gap remaining. INTNT-01/02/03 verified as fully implemented across all skills.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add archival flow to execute Step 7 lifecycle completion</name>
  <files>skills/execute/SKILL.md</files>
  <action>
Read execute SKILL.md. Find Step 7 (Lifecycle Completion), specifically the note at the end:

```
Note: Do NOT archive the lifecycle. Archival is Phase 10 functionality (ARTF-03).
```

REMOVE this note entirely. Replace it with a new Step 7d section that adds archival after the lifecycle completion summary (7c).

**Insert after Step 7c (the lifecycle completion summary display), BEFORE the final "STOP" instruction:**

```
**7d: Offer archival.**

After displaying the lifecycle completion summary, prompt the user:

```
Archive this completed lifecycle? Archiving moves all lifecycle artifacts to
.expedite/archive/{slug}/ while preserving sources.yml, log.yml, and .gitignore.

> yes / no
```

Handle responses:
- **"yes"**, **"archive"**: Execute the archival flow below.
- **"no"**, **"keep"**, **"skip"**: Display "Lifecycle artifacts preserved in .expedite/. Run `/expedite:scope` to start a new lifecycle." Then STOP.

**Archival flow:**

1. Generate a slug from the project_name: lowercase, replace spaces and special characters with hyphens, append the date in YYYYMMDD format. Example: "Auth Redesign" becomes "auth-redesign-20260308".
2. Create the archive directory:
   ```bash
   mkdir -p .expedite/archive/{slug}
   ```
3. Move all contents of `.expedite/` to the archive directory EXCEPT: `archive/` itself, `sources.yml`, `log.yml`, `.gitignore`. Use Bash:
   ```bash
   for item in .expedite/*; do
     base=$(basename "$item")
     if [ "$base" != "archive" ] && [ "$base" != "sources.yml" ] && [ "$base" != "log.yml" ] && [ "$base" != ".gitignore" ]; then
       mv "$item" ".expedite/archive/{slug}/"
     fi
   done
   ```
4. If any move command fails, warn but proceed: "Warning: Could not archive some files."
5. Display: "Lifecycle archived to `.expedite/archive/{slug}/`. Run `/expedite:scope` to start a new lifecycle."
```

This replicates the exact same archival pattern from scope SKILL.md Step 1 (the "start over" path). The only difference is that scope archives THEN starts a new lifecycle, while execute archives and STOPS.

**Also update the "Next Steps" section in Step 7c** to include archival as an option:
Find the line:
```
- Run `/expedite:scope` to start a new lifecycle
```
And update to:
```
- Archive this lifecycle (prompted next) or run `/expedite:scope` to start a new lifecycle
```

Ensure the STOP instruction comes AFTER the archival flow handling, not before it.
  </action>
  <verify>
1. Grep for "Archive this completed lifecycle" in skills/execute/SKILL.md -- should find the archival prompt
2. Grep for "ARTF-03" in skills/execute/SKILL.md -- should NOT find the old "Phase 10 functionality" note
3. Grep for "mkdir -p .expedite/archive" in skills/execute/SKILL.md -- should find the archive directory creation
4. Verify the archival flow excludes archive/, sources.yml, log.yml, .gitignore
  </verify>
  <done>
Execute Step 7 now offers archival after lifecycle completion. Archival flow replicates scope Step 1 pattern: slug generation, mkdir, selective move excluding archive/sources.yml/log.yml/.gitignore. User can archive or keep artifacts. The old "Do NOT archive" note is removed.
  </done>
</task>

</tasks>

<verification>
1. Execute SKILL.md uses `{Wave/Epic}` consistently in all PROGRESS.md entries and display messages
2. Execute Step 7 offers archival after lifecycle completion with correct exclusion list
3. Override-context.md pattern verified across G2, G3, G4, G5
4. No remaining intent-hardcoded terminology issues
</verification>

<success_criteria>
- Execute PROGRESS.md entries use {Wave/Epic} for intent-appropriate terminology (INTNT-01/02/03)
- Execute Step 7d offers archival with slug-based archive directory (ARTF-03)
- Archival excludes archive/, sources.yml, log.yml, .gitignore
- GATE-05 override handling verified consistent across all gates
- No hardcoded "Wave" where {Wave/Epic} is needed
</success_criteria>

<output>
After completion, create `.planning/phases/10-cross-cutting-integration/10-02-SUMMARY.md`
</output>
