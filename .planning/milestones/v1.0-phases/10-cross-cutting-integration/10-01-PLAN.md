---
phase: 10-cross-cutting-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/scope/SKILL.md
autonomous: true
requirements:
  - SCOPE-12
  - SCOPE-06

must_haves:
  truths:
    - "Scope skill generates additive codebase-routed questions per DA after the question plan is approved"
    - "Codebase-routed questions are not counted against the 15-question budget"
    - "Codebase-routed questions have source_hints: ['codebase'] automatically"
    - "Greenfield projects with no relevant code get graceful skip-per-DA handling"
    - "All step cross-references in scope SKILL.md are correct after renumbering"
    - "Source configuration step still works correctly at its new step number"
  artifacts:
    - path: "skills/scope/SKILL.md"
      provides: "New Step 7 (codebase analysis) + renumbered Steps 8-10"
      contains: "Step 7: Codebase Analysis"
  key_links:
    - from: "skills/scope/SKILL.md Step 6e review loop"
      to: "Step 7 (codebase analysis)"
      via: "approval routing"
      pattern: "Proceed to Step 7"
    - from: "skills/scope/SKILL.md Step 7 (codebase analysis)"
      to: "Step 8 (source configuration)"
      via: "step progression"
      pattern: "Proceed to Step 8"
    - from: "skills/scope/SKILL.md Step 8 (source configuration)"
      to: "Step 9 (write artifacts)"
      via: "step progression"
      pattern: "Proceed to Step 9"
    - from: "skills/scope/SKILL.md Step 9 (write artifacts)"
      to: "Step 10 (G1 gate)"
      via: "step progression"
      pattern: "Proceed to Step 10"
---

<objective>
Add codebase-routed question generation to the scope skill (SCOPE-12) by inserting a new Step 7 that analyzes the user's codebase per Decision Area and generates additive questions routed to the codebase researcher.

Purpose: Users get targeted questions about existing code patterns relevant to each DA, improving evidence quality for design decisions that must account for existing implementation.

Output: Updated scope SKILL.md with new Step 7 (codebase analysis) and renumbered Steps 8-10.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cross-cutting-integration/10-RESEARCH.md
@skills/scope/SKILL.md

<interfaces>
<!-- Key patterns the executor needs to understand -->

From skills/scope/SKILL.md current step numbering:
- Step 6: Question plan generation and review (Step 6e review loop approves plan)
- Step 7: Source configuration (SCOPE-06) -- becomes Step 8
- Step 8: Write artifacts -- becomes Step 9
- Step 9: Gate G1 Evaluation -- becomes Step 10

Step 6e approval routing currently says: "Proceed to Step 7."
Step 7 approval routing currently says: "Proceed to Step 8."
Step 8 completion says: "Proceed to Step 9."
All of these need updating after renumbering.

From skills/scope/SKILL.md Step 8b (becomes Step 9b), questions written to state.yml:
```yaml
- id: "q01"
  text: "{question text}"
  priority: "P0"
  decision_area: "{DA name}"
  source_hints: ["web", "codebase"]
  evidence_requirements: "{specific requirements}"
  status: "pending"
  source: "original"
  gap_details: null
  evidence_files: []
```

Codebase-routed questions follow the same schema but with:
- IDs continuing sequentially from last approved question (q16, q17, etc.)
- source: "codebase-routed" (distinguishes from "original" and "discovered-round-{N}")
- source_hints: ["codebase"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert codebase analysis step and renumber scope skill</name>
  <files>skills/scope/SKILL.md</files>
  <action>
Read the full scope SKILL.md. Insert a NEW Step 7 between current Step 6 and current Step 7. Then renumber current Steps 7-9 to Steps 8-10. Finally, fix ALL step cross-references throughout the entire file.

**New Step 7 content to insert (after Step 6e and before current Step 7):**

```
### Step 7: Codebase Analysis (Additive Questions)

This step generates additive codebase-routed questions that help the research phase understand existing patterns in the user's codebase. These questions are NOT counted against the 15-question budget and have no cap -- generate as many as needed per DA.

**7a. Determine relevance.**

If the user described this as a greenfield project with no existing codebase (no project directory, or the project directory is empty/contains only scaffolding), skip this step entirely:

Display: "No existing codebase detected. Skipping codebase analysis."

Proceed to Step 8.

**7b. Analyze codebase per DA.**

For each Decision Area in the approved question plan:

1. Determine relevant codebase search patterns based on the DA name and the user's project description.
2. Use Grep, Glob, and Read to scan the project codebase for existing patterns:
   - Directory structure (Glob for relevant directories)
   - File patterns (Glob for relevant file types)
   - Code patterns (Grep for imports, function signatures, configuration, conventions)
3. Generate codebase-routed questions based on findings. Each question:
   - Is specific to patterns found (or notably absent) in the codebase
   - Has `source_hints: ["codebase"]` (always codebase-only)
   - Is NOT counted against the 15-question budget
   - Focuses on "how does the existing code handle X" rather than general knowledge
   - Gets an evidence requirement like any other question (concrete and checkable)
4. If no relevant patterns found for a DA, skip that DA with a note.

Assign IDs continuing sequentially from the last approved question ID. If the approved plan has questions q01-q12, codebase-routed questions start at q13.

**7c. Present codebase-routed questions.**

Display all generated codebase-routed questions:

```
--- Codebase-Routed Questions ---

DA-1: {Name}
  [CB] q{N}: {question about existing pattern} (codebase)
    Evidence needed: {specific codebase evidence}
  [CB] q{N+1}: {question about existing pattern} (codebase)
    Evidence needed: {specific codebase evidence}

DA-3: {Name}
  [CB] q{N+2}: {question about existing pattern} (codebase)
    Evidence needed: {specific codebase evidence}

DA-2: {Name}
  (No relevant codebase patterns detected)

--- {N} codebase questions across {M} DAs ---
Approve? (approve all / select specific / skip all)
```

**7d. Review.**

Wait for user response:
- **"approve all"** or **"yes"**: Accept all codebase-routed questions. Proceed to Step 8.
- **"select specific"** or **"modify"**: User indicates which questions to keep. Remove unselected ones. Proceed to Step 8.
- **"skip all"** or **"skip"**: Discard all codebase-routed questions. Proceed to Step 8.

Note: Codebase-routed questions will be written to state.yml alongside the original questions in Step 9 (Write Artifacts). They use `source: "codebase-routed"` to distinguish them from original questions.
```

**Renumbering changes (apply throughout the ENTIRE file):**
- Current "Step 7" (Source Configuration) becomes "Step 8"
- Current "Step 8" (Write Artifacts) becomes "Step 9"
- Current "Step 9" (Gate G1 Evaluation) becomes "Step 10"

**Cross-reference fixes (search the ENTIRE file for these):**
1. Step 6e review loop: "Proceed to Step 7." -- this is CORRECT (now routes to new codebase analysis step)
2. Old Step 7 heading: "### Step 7: Source Configuration" -> "### Step 8: Source Configuration"
3. Old Step 7 body: "Proceed to Step 8." -> "Proceed to Step 9."
4. Old Step 8 heading: "### Step 8: Write Artifacts" -> "### Step 9: Write Artifacts"
5. Old Step 8 body: "Proceed to Step 9." -> "Proceed to Step 10."
6. Old Step 9 heading: "### Step 9: Gate G1 Evaluation" -> "### Step 10: Gate G1 Evaluation"
7. Step 1 Case B resume routing: "If questions exist, skip to Step 6. If intent and description are set but no questions, skip to Step 5. If only project_name is set, skip to Step 4." -- these reference Steps 4-6 which are UNCHANGED, so no fix needed.
8. Step 6e AskUserQuestion options: description says "Accept the plan and proceed to source configuration" -- no step number, so no fix needed.
9. Anywhere "Step 8" appears in the context of artifact writing: update to "Step 9"
10. Anywhere "Step 9" appears in the context of gate evaluation: update to "Step 10"
11. Step 9 (now Step 10) completion message: check "Next step: Run `/expedite:research`" -- no step numbers, so no fix needed.

**Also in the new Step 9 (Write Artifacts, formerly Step 8):**
In section 9b (formerly 8b), the questions array write must also include any approved codebase-routed questions. Add a note after the existing question entry schema:

After the existing `source: "original"` example, add:
```
     # For codebase-routed questions from Step 7:
     # - source: "codebase-routed"
     # - source_hints: ["codebase"]
     # Include these in the same questions array alongside original questions.
```

**SCOPE-06 verification:** Source configuration (now Step 8) is already implemented. No content changes needed for SCOPE-06 -- only the step number change.
  </action>
  <verify>
Run these checks:
1. Grep for "Step 7" in skills/scope/SKILL.md -- should find the NEW codebase analysis step, NOT source configuration
2. Grep for "Step 8" -- should find source configuration
3. Grep for "Step 9" -- should find write artifacts
4. Grep for "Step 10" -- should find gate G1 evaluation
5. Grep for "Proceed to Step 7" -- should appear in Step 6e (correct: routes to codebase analysis)
6. Grep for "Proceed to Step 8" -- should appear in Step 7d (codebase analysis -> source config)
7. Grep for "Proceed to Step 9" -- should appear in Step 8 (source config -> write artifacts)
8. Grep for "Proceed to Step 10" -- should appear in Step 9 (write artifacts -> gate)
9. Verify no orphaned references to old step numbers remain
10. Count total steps: should be 10 (was 9)
  </verify>
  <done>
Scope SKILL.md has 10 steps. New Step 7 (Codebase Analysis) generates additive codebase-routed questions per DA. Steps 8-10 are correctly renumbered. All cross-references updated. Codebase-routed questions use sequential IDs, source: "codebase-routed", and source_hints: ["codebase"]. Greenfield projects get graceful skip. Write Artifacts step includes codebase-routed questions in state.yml.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "### Step" skills/scope/SKILL.md` shows Steps 1-10 in order
2. `grep -n "Proceed to Step" skills/scope/SKILL.md` shows correct routing chain
3. `grep -n "codebase-routed" skills/scope/SKILL.md` shows the new step and the state.yml write instructions
4. No references to old step numbers remain in wrong context
</verification>

<success_criteria>
- Scope SKILL.md has exactly 10 steps (was 9)
- New Step 7 generates codebase-routed questions per DA with Grep/Glob/Read analysis
- Codebase questions use sequential IDs continuing from approved plan
- Codebase questions marked with source: "codebase-routed" in state.yml
- All step cross-references are correct after renumbering
- Source configuration (SCOPE-06) is preserved at Step 8
</success_criteria>

<output>
After completion, create `.planning/phases/10-cross-cutting-integration/10-01-SUMMARY.md`
</output>
