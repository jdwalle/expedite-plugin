---
phase: 02-state-management-and-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/state.yml.template
  - templates/gitignore.template
  - templates/sources.yml.template
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
  - STATE-06

must_haves:
  truths:
    - "state.yml.template exists in templates/ with all fields from PRODUCT-DESIGN.md schema"
    - "state.yml.template has max 2 nesting levels -- questions and gate_history are flat lists of flat objects"
    - "state.yml.template includes version field set to '1' as first field after comment header"
    - "state.yml.template enumerates ALL valid phase values in a YAML comment"
    - "state.yml.template includes v2-reserved fields (imported_from, constraints) with null/empty values and a comment marking them as reserved"
    - "gitignore.template exists with patterns for log.yml and any runtime-only files"
    - "sources.yml.template exists with default source configuration structure"
    - "Backup-before-write pattern is documented in a comment within state.yml.template"
    - "No references to 'arc' in any created file -- all references use 'expedite'"
  artifacts:
    - path: "templates/state.yml.template"
      provides: "Initial state.yml content copied to .expedite/state.yml during /expedite:scope"
      contains: "version: \"1\""
    - path: "templates/gitignore.template"
      provides: ".gitignore content copied to .expedite/.gitignore during /expedite:scope"
      contains: "log.yml"
    - path: "templates/sources.yml.template"
      provides: "Default source configuration copied to .expedite/sources.yml during /expedite:scope"
      contains: "web"
  key_links:
    - from: "templates/state.yml.template"
      to: "/expedite:scope (Phase 4)"
      via: "Template copied to .expedite/state.yml at lifecycle initialization"
      pattern: "version.*1"
    - from: "templates/gitignore.template"
      to: "/expedite:scope (Phase 4)"
      via: "Template copied to .expedite/.gitignore at lifecycle initialization"
      pattern: "log\\.yml"
---

<objective>
Create the three template files that define the state schema, gitignore rules, and default source configuration for the Expedite lifecycle.

Purpose: These templates are the schema definition for all lifecycle state. Every subsequent phase reads/writes state.yml according to this schema. The templates are NOT instantiated until Phase 4 (`/expedite:scope` copies them to `.expedite/`), but defining the schema now ensures all Phase 2 work (status skill, hook script) references the correct field names and structure. The state.yml.template is the single source of truth for the state file format.

Output: 3 template files in `templates/` directory. The `.gitkeep` file in `templates/` is replaced by actual content.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-management-and-context/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create state.yml.template with complete schema</name>
  <files>
    templates/state.yml.template
  </files>
  <action>
Create `templates/state.yml.template` with the EXACT content below. This file defines the state schema that all skills read and write. The template is copied to `.expedite/state.yml` during `/expedite:scope` initialization.

```yaml
# Expedite Lifecycle State
# Machine-readable. Complete-rewrite on every update.
# WRITE PATTERN: (1) read current, (2) cp state.yml state.yml.bak, (3) modify in-memory, (4) write entire file
version: "1"
last_modified: null

# Lifecycle identity
project_name: null
intent: null
lifecycle_id: null

# Current position
# Valid phases (forward-only transitions):
#   scope_in_progress -> scope_complete
#   scope_complete -> research_in_progress
#   research_in_progress -> research_complete | research_recycled
#   research_recycled -> research_in_progress
#   research_complete -> design_in_progress
#   design_in_progress -> design_complete | design_recycled
#   design_recycled -> design_in_progress
#   design_complete -> plan_in_progress
#   plan_in_progress -> plan_complete | plan_recycled
#   plan_recycled -> plan_in_progress
#   plan_complete -> execute_in_progress
#   execute_in_progress -> complete
#   (any) -> archived
phase: "scope_in_progress"
current_task: null

# Reserved for v2 -- cross-lifecycle import (IMPORT-01 through IMPORT-04)
imported_from: null
constraints: []

# Research tracking
research_rounds: 0

# Questions (flat list -- each item is a flat object, max 2 nesting levels)
questions: []
# Question item schema:
#   id: "q01"
#   text: "How should X be implemented?"
#   priority: "P0"           # P0 | P1 | P2
#   decision_area: "area-name"
#   source_hints: ["web", "codebase"]
#   status: "pending"        # pending | covered | partial | not_covered | unavailable_source
#   source: "original"       # original | discovered-round-{N}
#   gap_details: null
#   evidence_files: []

# Gate history (flat list -- each item is a flat object, max 2 nesting levels)
gate_history: []
# Gate history item schema:
#   gate: "G1"
#   timestamp: "2026-01-01T00:00:00Z"
#   outcome: "go"            # go | go_advisory | recycle | override
#   must_passed: 0
#   must_failed: 0
#   should_passed: 0
#   should_failed: 0
#   notes: null
#   overridden: false

# Execution state (populated by plan skill)
tasks: []
current_wave: null
```

**Critical constraints:**
- `version` MUST be the string `"1"` (quoted), not integer 1
- `phase` starts at `"scope_in_progress"` -- this is the initial value when `/expedite:scope` creates the file
- `questions` and `gate_history` are the ONLY nested structures -- both are flat lists of flat objects (max 2 nesting levels)
- `imported_from` and `constraints` are reserved for v2 -- include them with null/empty values
- The comment block documenting the write pattern is essential -- it reminds every skill of the backup protocol
- The comment block listing valid phase transitions is essential -- it serves as inline documentation for phase validation
- `source_hints` uses YAML flow-style arrays `["web", "codebase"]` to stay within the 2-level nesting limit
- Do NOT use "arc" anywhere -- all references are "expedite"
- Remove the existing `.gitkeep` file from `templates/` after creating this file
  </action>
  <verify>
    <automated>
test -f templates/state.yml.template && \
grep -q 'version: "1"' templates/state.yml.template && \
grep -q 'phase: "scope_in_progress"' templates/state.yml.template && \
grep -q 'project_name: null' templates/state.yml.template && \
grep -q 'intent: null' templates/state.yml.template && \
grep -q 'lifecycle_id: null' templates/state.yml.template && \
grep -q 'questions: \[\]' templates/state.yml.template && \
grep -q 'gate_history: \[\]' templates/state.yml.template && \
grep -q 'tasks: \[\]' templates/state.yml.template && \
grep -q 'research_rounds: 0' templates/state.yml.template && \
grep -q 'imported_from: null' templates/state.yml.template && \
grep -q 'constraints: \[\]' templates/state.yml.template && \
grep -q 'last_modified: null' templates/state.yml.template && \
grep -q 'current_task: null' templates/state.yml.template && \
grep -q 'current_wave: null' templates/state.yml.template && \
grep -q 'WRITE PATTERN' templates/state.yml.template && \
grep -q 'state.yml.bak' templates/state.yml.template && \
grep -q 'scope_in_progress -> scope_complete' templates/state.yml.template && \
grep -q 'execute_in_progress -> complete' templates/state.yml.template && \
! grep -qi 'arc' templates/state.yml.template && \
echo "PASS: state.yml.template valid"
    </automated>
  </verify>
  <done>
    - templates/state.yml.template exists with complete schema
    - All fields from PRODUCT-DESIGN.md are present: version, last_modified, project_name, intent, lifecycle_id, phase, current_task, imported_from, constraints, research_rounds, questions, gate_history, tasks, current_wave
    - Version field is "1" (string)
    - Phase starts at "scope_in_progress"
    - Max 2 nesting levels confirmed (questions and gate_history are flat lists)
    - v2-reserved fields present with null/empty values
    - Write pattern documented in comment header
    - Phase transition diagram documented in comments
    - Question and gate_history item schemas documented in comments
    - No "arc" references
  </done>
</task>

<task type="auto">
  <name>Task 2: Create gitignore.template and sources.yml.template</name>
  <files>
    templates/gitignore.template
    templates/sources.yml.template
  </files>
  <action>
Create two additional template files. These are simpler companion files to state.yml.template.

**Create `templates/gitignore.template`** with exactly this content:
```
# Expedite lifecycle runtime files
# This file is copied to .expedite/.gitignore during lifecycle initialization

# Telemetry log (append-only, persists across lifecycles)
log.yml

# State backup (recreated on every state write)
state.yml.bak
```

This file is copied to `.expedite/.gitignore` when `/expedite:scope` initializes a new lifecycle. It ensures:
- `log.yml` is gitignored (Decision D5: single-user workflow, less commit noise)
- `state.yml.bak` is gitignored (runtime backup, not useful in version control)

**Create `templates/sources.yml.template`** with exactly this content:
```yaml
# Expedite Source Configuration
# Persists across lifecycles. Edit via /expedite:scope source configuration step.
sources:
  web:
    enabled: true
    tools: ["WebSearch", "WebFetch"]
  codebase:
    enabled: true
    tools: ["Grep", "Read", "Glob", "Bash"]
  # Add MCP sources below. Example:
  # github:
  #   enabled: true
  #   tools: ["mcp__github__*"]
  #   note: "Requires GitHub MCP server configured"
```

This file is copied to `.expedite/sources.yml` when `/expedite:scope` initializes a new lifecycle (only if sources.yml does not already exist -- it persists across lifecycles).

**After creating both files, remove the `.gitkeep` from templates/:**
```bash
rm templates/.gitkeep
```

**Constraints:**
- gitignore.template MUST include log.yml (Decision D5)
- gitignore.template MUST include state.yml.bak
- sources.yml.template uses YAML flow-style arrays for tool lists to match state.yml conventions
- Do NOT use "arc" anywhere
  </action>
  <verify>
    <automated>
test -f templates/gitignore.template && \
grep -q 'log.yml' templates/gitignore.template && \
grep -q 'state.yml.bak' templates/gitignore.template && \
test -f templates/sources.yml.template && \
grep -q 'web:' templates/sources.yml.template && \
grep -q 'codebase:' templates/sources.yml.template && \
grep -q 'enabled: true' templates/sources.yml.template && \
! test -f templates/.gitkeep && \
! grep -rqi 'arc' templates/ && \
echo "PASS: gitignore.template and sources.yml.template valid"
    </automated>
  </verify>
  <done>
    - templates/gitignore.template exists with log.yml and state.yml.bak patterns
    - templates/sources.yml.template exists with web and codebase sources enabled by default
    - templates/.gitkeep removed (replaced by actual template files)
    - No "arc" references in any template file
  </done>
</task>

</tasks>

<verification>
- [ ] `templates/state.yml.template` contains all 14 fields from PRODUCT-DESIGN.md schema
- [ ] `templates/state.yml.template` has max 2 nesting levels (no sub-objects within question/gate items)
- [ ] `templates/state.yml.template` includes version "1", phase "scope_in_progress", write pattern comment, and phase transition diagram
- [ ] `templates/gitignore.template` includes log.yml and state.yml.bak
- [ ] `templates/sources.yml.template` includes web and codebase sources with correct tool lists
- [ ] `templates/.gitkeep` has been removed
- [ ] No references to "arc" in any template file
- [ ] All template files are valid YAML (where applicable) or valid gitignore syntax
</verification>

<success_criteria>
The three template files define the complete schema for Expedite lifecycle state, gitignore rules, and source configuration. The state.yml.template serves as the single source of truth for the state file format that all skills reference. These templates are ready to be copied to `.expedite/` by `/expedite:scope` in Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/02-state-management-and-context/02-01-SUMMARY.md`
</output>
