---
phase: 18-gate-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/plan/SKILL.md
  - skills/spike/SKILL.md
autonomous: true
requirements:
  - GATE-03
  - GATE-04

must_haves:
  truths:
    - "G4 gate evaluation checks that task coverage accounts for DA depth calibration (Deep vs Light)"
    - "G4 S5 failure produces advisory, not Recycle (SHOULD criterion)"
    - "G5 gate evaluation checks that spike resolution addresses the specific ambiguity identified in each TD"
    - "G5 S4 failure produces advisory, not Recycle (SHOULD criterion)"
    - "Previously passing G4 and G5 gates continue to pass (no regression)"
  artifacts:
    - path: "skills/plan/SKILL.md"
      provides: "G4 gate with S5 depth calibration criterion"
      contains: "S5"
    - path: "skills/spike/SKILL.md"
      provides: "G5 gate with S4 ambiguity resolution criterion"
      contains: "S4"
  key_links:
    - from: "skills/plan/SKILL.md G4 S5"
      to: "SCOPE.md DA depth levels + PLAN.md task counts"
      via: "Compare task counts per DA grouped by depth level"
      pattern: "depth.*calibration|Deep.*Light"
    - from: "skills/spike/SKILL.md G5 S4"
      to: "PLAN.md TD descriptions + Step 5 resolution rationales"
      via: "Check resolution rationale addresses the specific ambiguity in TD description"
      pattern: "ambiguity|resolution.*address"
---

<objective>
Add SHOULD criteria S5 to G4 (plan) and S4 to G5 (spike) quality gates for DA depth awareness and ambiguity resolution enforcement.

Purpose: Surface advisories when plan task coverage does not reflect DA depth or when spike resolutions do not address the specific ambiguity, without blocking progress (SHOULD, not MUST).
Output: Updated plan/SKILL.md (G4 S5) and spike/SKILL.md (G5 S4)
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-gate-enforcement/18-RESEARCH.md

Key source files:
@skills/plan/SKILL.md (G4 gate at Step 7, lines 415-489)
@skills/spike/SKILL.md (G5 gate at Step 7, lines 316-419)
@skills/scope/SKILL.md (DA depth calibration definition, lines 333-337)
</context>

<interfaces>
<!-- Current G4 gate criteria (plan/SKILL.md lines 428-445) - table format: -->

MUST:
| M1 | Every DA covered by at least one phase | ... | PASS/FAIL |
| M2 | Phase sizing within bounds | ... | PASS/FAIL |
| M3 | Tactical decisions listed per phase | ... | PASS/FAIL |
| M4 | Acceptance criteria trace to design decisions | ... | PASS/FAIL |
| M5 | PLAN.md exists and is non-empty | ... | PASS/FAIL |

SHOULD:
| S1 | Wave/epic ordering is logical | ... | PASS/ADVISORY |
| S2 | Effort estimates present | ... | PASS/ADVISORY |
| S3 | No orphan tasks | ... | PASS/ADVISORY |
| S4 | Override-affected DAs flagged | ... | PASS/ADVISORY |

<!-- Current G5 gate criteria (spike/SKILL.md lines 320-335) - table format (no Result column): -->

MUST:
| M1 | Every "needs-spike" TD resolved | ... |
| M2 | Every implementation step traces to a TD or DA | ... |
| M3 | Every resolved TD has a recorded rationale | ... |
| M4 | Step count within phase sizing bounds | ... |

SHOULD:
| S1 | Implementation steps add spike-specific guidance | ... |
| S2 | Full task coverage | ... |
| S3 | File lists are specific paths | ... |

<!-- DA depth calibration from scope/SKILL.md line 336: -->
Deep (high-stakes, hard to reverse) | Standard (moderate reversibility) | Light (low-stakes, easily reversible)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add S5 depth calibration criterion to G4 gate (plan/SKILL.md)</name>
  <files>skills/plan/SKILL.md</files>
  <action>
In `skills/plan/SKILL.md`, locate Step 7 G4 gate section (around line 415).

1. **Add S5 row to the SHOULD criteria table** after the S4 row (line 445). Insert a new table row:

```markdown
| S5 | Task coverage reflects DA depth calibration | Read SCOPE.md DA depth levels (Deep/Standard/Light). For each depth level, count how many tasks or acceptance criteria cover DAs at that level. Check that no Deep DA has fewer tasks or acceptance criteria than any Light DA. State: "Deep DAs: {DA-list with task counts}. Light DAs: {DA-list with task counts}. Depth-proportional: {yes|advisory}" | PASS/ADVISORY |
```

2. **Add a clarifying note** after the SHOULD criteria table (after the new S5 row, before the display format block). Insert:

```
Note: S5 uses task count as a heuristic for implementation depth. A Deep DA with fewer tasks than a Light DA is suspicious but may be valid (e.g., one complex task vs many simple ones). The advisory acknowledges this: "This may be intentional if the Deep DA's tasks are individually complex."
```

3. **Update the display format block** (lines 449-465). Add S5 line in the SHOULD section:

```
  S5: {PASS|ADVISORY} -- {evidence}
```

This goes after the S4 display line and before the closing of the SHOULD section.

IMPORTANT: G4 is a structural gate. S5 uses moderate judgment (consistent with S1 "wave ordering is logical" which already uses judgment). Do NOT convert G4 into an LLM-judgment gate. The check is a simple comparison: count tasks per DA, group by depth, flag if any Deep DA has fewer tasks than any Light DA.

IMPORTANT: G4 already reads SCOPE.md (line 424: "Read `.expedite/plan/PLAN.md` and `.expedite/scope/SCOPE.md`") so no new file read instruction needed.

Pitfall avoidance (from research P4): S5 is SHOULD (advisory only) to avoid punishing valid plan structures where a Deep DA has one complex task. The advisory text explicitly acknowledges this heuristic limitation.
  </action>
  <verify>
Read skills/plan/SKILL.md lines 438-470. Confirm:
- S5 row exists in SHOULD criteria table after S4
- S5 references SCOPE.md depth levels and task count comparison
- S5 result column says PASS/ADVISORY (not PASS/FAIL)
- Clarifying note explains the heuristic limitation
- Display format block includes S5 line
- Existing M1-M5 and S1-S4 rows are unchanged
- Gate outcomes section is unchanged
- Telemetry logging section is unchanged
  </verify>
  <done>
G4 gate has 5 MUST criteria (M1-M5) and 5 SHOULD criteria (S1-S5). S5 checks task coverage reflects DA depth calibration. Failure produces advisory. Existing criteria and gate infrastructure untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add S4 ambiguity resolution criterion to G5 gate (spike/SKILL.md)</name>
  <files>skills/spike/SKILL.md</files>
  <action>
In `skills/spike/SKILL.md`, locate Step 7 G5 gate section (around line 316).

1. **Add S4 row to the SHOULD criteria table** after the S3 row (line 335). Insert a new table row:

```markdown
| S4 | Spike resolution addresses specific ambiguity | For each needs-spike TD, read the ambiguity described in the TD (from PLAN.md). Check the resolution rationale (from Step 5) explains how it resolves that specific ambiguity -- not just picks an option, but explains why the chosen approach resolves the uncertainty. Clear-cut resolutions with brief but ambiguity-specific rationale are adequate. State: "{N}/{M} spike resolutions address their specific ambiguity{. TD-2 picks an option without explaining why the ambiguity is resolved}" |
```

NOTE: G5's SHOULD table has 3 columns (no Result column), unlike G3/G4 which have 4 columns. Match the existing 3-column format exactly: `| # | Criterion | How to Check |`

2. **Add a clarifying note** after the SHOULD criteria table (after the new S4 row, before the telemetry logging block). Insert:

```
Note: S4 distinguishes between resolutions that address ambiguity and those that merely pick an option. A rationale of "chose LRU" is insufficient. A rationale of "chose LRU because the access pattern is temporal with hot keys" addresses the ambiguity. Clear-cut TDs (resolved directly without extensive research) may have brief rationale -- this is acceptable as long as the rationale mentions the specific ambiguity.
```

3. **Update the "On Recycle" section** (lines 406-421). The existing recycle guidance lists specific fix paths for M1-M4 failures. S4 is a SHOULD criterion so it does NOT trigger Recycle -- but add S4 to the Go-with-advisory display logic. Find the Go-with-advisory display block (line 394-398) and confirm it already says "List the SHOULD failures as advisories." S4 will naturally appear in this list. No change needed to the display logic.

4. **Verify no change needed to gate evaluation logic.** The existing gate evaluation block (lines 386-421) routes by MUST pass/fail and SHOULD pass/fail. S4 as a new SHOULD criterion is automatically included. Confirm the should_passed/should_failed telemetry counts (line 349) will naturally include S4 without modification.

IMPORTANT: G5 is a structural gate. S4 adds a semantic check as SHOULD (advisory only), consistent with S1 which already uses judgment ("implementation steps add spike-specific guidance"). Do NOT change the structural gate characterization.

Pitfall avoidance (from research P5): S4 explicitly allows brief rationale for clear-cut TDs. Only flags when the rationale does not mention the ambiguity at all.
  </action>
  <verify>
Read skills/spike/SKILL.md lines 329-355. Confirm:
- S4 row exists in SHOULD criteria table after S3
- S4 uses the 3-column format matching S1-S3 (no Result column)
- S4 text references ambiguity in TD description and resolution rationale from Step 5
- Clarifying note explains clear-cut TD tolerance
- Existing M1-M4 and S1-S3 rows are unchanged
- Gate evaluation logic is unchanged (S4 naturally included in SHOULD pass/fail)
- Telemetry logging format is unchanged (should_passed/should_failed counts include S4 naturally)
- Recycle guidance is unchanged (S4 does not trigger Recycle)
  </verify>
  <done>
G5 gate has 4 MUST criteria (M1-M4) and 4 SHOULD criteria (S1-S4). S4 checks spike resolution addresses the specific ambiguity identified. Failure produces advisory. Existing criteria and gate infrastructure untouched.
  </done>
</task>

</tasks>

<verification>
1. Read skills/plan/SKILL.md -- G4 section has M1-M5 MUST + S1-S5 SHOULD
2. Read skills/spike/SKILL.md -- G5 section has M1-M4 MUST + S1-S4 SHOULD
3. No other sections of either SKILL.md have been modified
4. Gate outcome routing (Go/Go-with-advisory/Recycle/Override) is unchanged in both files
5. Telemetry logging format is unchanged in both files (should_passed/should_failed counts naturally include new criteria)
6. Gate history schema is unchanged (counts update naturally)
7. G5 SHOULD table uses 3-column format (no Result column), matching existing S1-S3
8. G4 SHOULD table uses 4-column format (with Result column), matching existing S1-S4
</verification>

<success_criteria>
- G4 gate in plan/SKILL.md evaluates S5: depth calibration task coverage check
- G5 gate in spike/SKILL.md evaluates S4: ambiguity resolution specificity check
- Both new criteria are SHOULD (failure produces advisory, not Recycle)
- Clarifying notes prevent over-penalization (P4: valid plan structures, P5: clear-cut TDs)
- No regressions: existing criteria, display format, outcome routing, telemetry, and gate history are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/18-gate-enforcement/18-02-SUMMARY.md`
</output>
