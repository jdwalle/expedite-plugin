---
phase: 14-quick-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - .gitignore
  - .planning/PROJECT.md
autonomous: true
requirements:
  - HSKP-01
  - HSKP-02
  - HSKP-03

must_haves:
  truths:
    - "plugin.json version field reads 1.0.0"
    - ".DS_Store files are gitignored at all directory depths"
    - "No .DS_Store files exist in the git index"
    - "PROJECT.md Key Decisions table documents sufficiency evaluator architecture divergence"
    - "PROJECT.md Validated requirements section accurately reflects Task()-dispatched (not inline) sufficiency assessment"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin metadata with correct version"
      contains: '"version": "1.0.0"'
    - path: ".gitignore"
      provides: "Root gitignore excluding .DS_Store recursively"
      contains: ".DS_Store"
    - path: ".planning/PROJECT.md"
      provides: "Architecture decision documentation"
      contains: "Sufficiency evaluator as Task() subagent"
  key_links: []
---

<objective>
Commit three housekeeping fixes: version bump to 1.0.0, root .gitignore, and sufficiency evaluator documentation consistency.

Purpose: Plugin metadata and repository hygiene match a shipped v1.0 product. These are zero-risk, zero-functional-impact changes that close audit items FIX-1, FIX-2, FIX-3.
Output: Three committed changes bringing plugin.json, .gitignore, and PROJECT.md into production-ready state.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-quick-fixes/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and stage plugin.json version bump and root .gitignore</name>
  <files>.claude-plugin/plugin.json, .gitignore</files>
  <action>
HSKP-01 (plugin.json version):
The working tree already has the correct change -- `"version": "1.0.0"` replacing `"version": "0.1.0"`. Verify the working tree content matches the requirement by reading `.claude-plugin/plugin.json` and confirming the version field reads `"1.0.0"`. Then stage the file with `git add .claude-plugin/plugin.json`.

HSKP-02 (.gitignore):
The working tree already has a root `.gitignore` containing `.DS_Store` (untracked). Verify its content by reading `.gitignore`. A single `.DS_Store` line is correct -- git matches filename patterns recursively by default (no `/` prefix means all depths). Then verify no .DS_Store files are tracked: run `git ls-files '*.DS_Store'` and confirm empty output. Stage with `git add .gitignore`.

Do NOT add any additional patterns to .gitignore beyond `.DS_Store` -- the requirement is minimal and other patterns (log.yml, state.yml.bak) are already handled by `.expedite/.gitignore` template.
  </action>
  <verify>
    <automated>git diff --cached .claude-plugin/plugin.json | grep '"version": "1.0.0"' && git diff --cached .gitignore | grep '.DS_Store' && git ls-files '*.DS_Store' | wc -l | grep '0'</automated>
  </verify>
  <done>plugin.json shows version 1.0.0, .gitignore is staged with .DS_Store pattern, zero .DS_Store files in git index</done>
</task>

<task type="auto">
  <name>Task 2: Fix PROJECT.md sufficiency evaluator wording inconsistency</name>
  <files>.planning/PROJECT.md</files>
  <action>
HSKP-03 (sufficiency evaluator documentation):
The Key Decisions table in PROJECT.md (line ~105) already correctly documents the divergence: "Sufficiency evaluator as Task() subagent (not inline)". This entry is committed and meets the HSKP-03 requirement. Do NOT add a duplicate entry.

However, the Validated requirements section (line ~20) still says "inline sufficiency assessment" which is factually incorrect -- the implementation uses Task()-dispatched subagents, not inline evaluation. Fix this inconsistency:

Change line 20 from:
`- Research skill: source-affinity batching, parallel Task() subagents, dynamic question discovery, inline sufficiency assessment -- v1.0`

To:
`- Research skill: source-affinity batching, parallel Task() subagents, dynamic question discovery, Task()-dispatched sufficiency assessment -- v1.0`

This is a documentation accuracy fix, not a functional change. Stage with `git add .planning/PROJECT.md`.
  </action>
  <verify>
    <automated>grep "Task()-dispatched sufficiency assessment" .planning/PROJECT.md && grep -c "inline sufficiency" .planning/PROJECT.md | grep '0' && grep -A2 "Sufficiency evaluator" .planning/PROJECT.md | grep "Task() subagent"</automated>
  </verify>
  <done>"inline sufficiency assessment" replaced with "Task()-dispatched sufficiency assessment" on line 20. Key Decisions table entry unchanged. Both references in PROJECT.md now consistently describe the Task() subagent approach.</done>
</task>

</tasks>

<verification>
All three HSKP requirements verified with these commands:

1. HSKP-01: `grep '"version"' .claude-plugin/plugin.json` shows `"version": "1.0.0"`
2. HSKP-02a: `git ls-files .gitignore` returns `.gitignore` (tracked)
3. HSKP-02b: `git ls-files '*.DS_Store'` returns empty (no tracked .DS_Store)
4. HSKP-02c: `cat .gitignore` contains `.DS_Store`
5. HSKP-03a: `grep -A2 "Sufficiency evaluator" .planning/PROJECT.md` shows Key Decisions entry
6. HSKP-03b: `grep "inline sufficiency" .planning/PROJECT.md` returns no matches (inconsistency fixed)
</verification>

<success_criteria>
- plugin.json version reads "1.0.0"
- .gitignore exists in git with .DS_Store pattern
- Zero .DS_Store files in git index
- PROJECT.md Key Decisions table documents sufficiency evaluator architecture divergence
- PROJECT.md Validated requirements section says "Task()-dispatched sufficiency assessment" (not "inline")
</success_criteria>

<output>
After completion, create `.planning/phases/14-quick-fixes/14-01-SUMMARY.md`
</output>
