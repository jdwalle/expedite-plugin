---
phase: 15-step-level-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/scope/SKILL.md
  - skills/research/SKILL.md
autonomous: true
requirements:
  - STEP-03
  - STEP-04

must_haves:
  truths:
    - "Every numbered step in scope SKILL.md (Steps 1-10) writes current_step on entry"
    - "Every numbered step in research SKILL.md (Steps 1-18) writes current_step on entry"
    - "Steps that already write state.yml fold current_step into existing writes (no double I/O)"
    - "Steps that do not already write state.yml add a standalone backup-before-write cycle"
    - "Scope Step 10 and Research Step 18 clear current_step to null on completion"
    - "Labels in current_step match the exact step heading text from each SKILL.md"
  artifacts:
    - path: "skills/scope/SKILL.md"
      provides: "Step tracking writes for all 10 scope steps"
      contains: "current_step"
    - path: "skills/research/SKILL.md"
      provides: "Step tracking writes for all 18 research steps"
      contains: "current_step"
  key_links:
    - from: "skills/scope/SKILL.md"
      to: ".expedite/state.yml"
      via: "Each step writes current_step field on entry"
      pattern: "current_step"
    - from: "skills/research/SKILL.md"
      to: ".expedite/state.yml"
      via: "Each step writes current_step field on entry"
      pattern: "current_step"
---

<objective>
Add step-level tracking to the scope and research skills so every numbered step writes `current_step` to state.yml on entry.

Purpose: These are the two largest skills (10 and 18 steps respectively). After this plan, scope and research always report their position, enabling users to see exactly where they are during the longest skill flows.

Output: Updated scope SKILL.md (10 step tracking writes) and research SKILL.md (18 step tracking writes).
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-step-level-tracking/15-RESEARCH.md
@skills/scope/SKILL.md
@skills/research/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add step tracking to scope SKILL.md (10 steps)</name>
  <files>skills/scope/SKILL.md</files>
  <action>
Add `current_step` tracking to EVERY numbered step in `skills/scope/SKILL.md`. Insert the tracking instruction at the VERY TOP of each step, immediately after the `### Step N:` heading line (before any existing content).

**Two patterns depending on whether the step already writes state.yml:**

### STANDALONE pattern (steps that do NOT already write state.yml: Steps 1, 2, 6, 7, 8)

Insert this block immediately after the `### Step N:` heading:

```markdown

**Step tracking:** Update `current_step` in state.yml (backup-before-write):
1. Read `.expedite/state.yml`
2. Backup: `cp .expedite/state.yml .expedite/state.yml.bak` (via Bash)
3. Set `current_step` to `{skill: "scope", step: N, label: "LABEL"}`
4. Set `last_modified` to current timestamp
5. Write the entire file back
```

### FOLD-IN pattern (steps that ALREADY write state.yml: Steps 3, 4, 5, 9, 10)

For these steps, do NOT add a separate backup-before-write cycle. Instead, find the existing state.yml write instructions and add ONE line to the existing in-memory update list:

```markdown
   - Set `current_step` to `{skill: "scope", step: N, label: "LABEL"}`
```

For Step 10 specifically (the completion step), ALSO add the clearing instruction. When the gate outcome is "go" or "go_advisory" and `phase` is set to `scope_complete`, the same write must ALSO:
```markdown
   - Set `current_step` to null
```

**IMPORTANT for Step 3 (Initialize Lifecycle):** Step 3 initializes state.yml from the template. Since the template already has `current_step: null`, fold the Step 3 tracking into the initialization by setting it as part of the "initial values set" list:
```markdown
   - `current_step` to `{skill: "scope", step: 3, label: "Initialize Lifecycle"}`
```

### Exact labels for each step (use EXACTLY these):

| Step | Label (from heading) |
|------|---------------------|
| 1 | Lifecycle Check |
| 2 | Connected Flow Import (v2) |
| 3 | Initialize Lifecycle |
| 4 | Interactive Questioning (Round 1: Context) |
| 5 | Interactive Questioning (Round 2: Adaptive Refinement) |
| 6 | Question Plan Generation and Review |
| 7 | Codebase Analysis (Additive Questions) |
| 8 | Source Configuration |
| 9 | Write Artifacts |
| 10 | Gate G1 Evaluation |

### Special cases:

- **Step 1 (Lifecycle Check):** This step may detect a resume case (existing state.yml). The standalone tracking block goes at the top regardless. If state.yml doesn't exist yet (fresh start), the step tracking write will fail silently because there's no state.yml to read. That's OK -- Step 3 will initialize state.yml with `current_step` set to Step 3. Add a note: "If `.expedite/state.yml` does not exist yet, skip step tracking (lifecycle not initialized)."

- **Step 2 (v2 placeholder):** Still add tracking. It's brief but the instruction should be there for consistency. Same conditional note as Step 1: skip if state.yml doesn't exist.

- **Step 10 (Gate):** On "go" or "go_advisory" outcome, the existing state write sets `phase: "scope_complete"`. In that SAME write, also set `current_step: null`. On "hold", do NOT clear current_step (the user is still in scope). On "recycle", do NOT clear (recycle means re-do).
  </action>
  <verify>
    <automated>grep -c "current_step" skills/scope/SKILL.md</automated>
  </verify>
  <done>
- All 10 steps have `current_step` tracking at step entry
- Steps 1, 2, 6, 7, 8 use standalone backup-before-write pattern
- Steps 3, 4, 5, 9, 10 fold into existing state writes
- Step 10 clears `current_step` to null on go/go_advisory outcomes
- Steps 1 and 2 have the "skip if state.yml doesn't exist" conditional
- All labels match the exact step heading text
- grep returns 15+ occurrences of "current_step" in the file
  </done>
</task>

<task type="auto">
  <name>Task 2: Add step tracking to research SKILL.md (18 steps)</name>
  <files>skills/research/SKILL.md</files>
  <action>
Add `current_step` tracking to EVERY numbered step in `skills/research/SKILL.md`. Insert the tracking instruction at the VERY TOP of each step, immediately after the `### Step N:` heading line (before any existing content).

**Two patterns depending on whether the step already writes state.yml:**

### STANDALONE pattern (steps that do NOT already write state.yml: Steps 1, 2, 4, 5, 6, 7, 8, 9, 11, 13, 14, 16, 17)

Insert this block immediately after the `### Step N:` heading:

```markdown

**Step tracking:** Update `current_step` in state.yml (backup-before-write):
1. Read `.expedite/state.yml`
2. Backup: `cp .expedite/state.yml .expedite/state.yml.bak` (via Bash)
3. Set `current_step` to `{skill: "research", step: N, label: "LABEL"}`
4. Set `last_modified` to current timestamp
5. Write the entire file back
```

### FOLD-IN pattern (steps that ALREADY write state.yml: Steps 3, 10, 12, 15, 18)

For these steps, find the existing state.yml write instructions and add ONE line to the existing in-memory update list:

```markdown
   - Set `current_step` to `{skill: "research", step: N, label: "LABEL"}`
```

For Step 18 (Research Completion), ALSO add:
```markdown
   - Set `current_step` to null
```

### Exact labels for each step (use EXACTLY these):

| Step | Label (from heading) |
|------|---------------------|
| 1 | Prerequisite Check |
| 2 | Read Scope Artifacts |
| 3 | Initialize Research State |
| 4 | Form Source-Affinity Batches |
| 5 | Validate DA Coverage |
| 6 | Present Batch Plan for Approval |
| 7 | Pre-Validate Sources |
| 8 | Assemble Prompt Templates |
| 9 | Dispatch Parallel Subagents |
| 10 | Collect Results and Update State |
| 11 | Research Completion Summary |
| 12 | Sufficiency Assessment |
| 13 | Dynamic Question Discovery |
| 14 | G2 Gate Evaluation |
| 15 | Gate Outcome Handling |
| 16 | Gap-Fill Dispatch |
| 17 | Synthesis Generation |
| 18 | Research Completion |

### Special cases:

- **Step 3 (Initialize Research State):** This step writes a phase transition (`research_in_progress`). Fold `current_step` into that same write.

- **Step 13 (Dynamic Question Discovery):** This step conditionally adds discovered questions to state.yml. The `current_step` write is STANDALONE at step entry (before conditional logic), because it must always fire regardless of whether new questions are discovered.

- **Step 15 (Gate Outcome Handling):** This step writes gate history. On "go" or "go_advisory", the flow continues to Steps 16-18. On "recycle", the user re-enters research. Do NOT clear `current_step` here -- it continues to Steps 17/18.

- **Step 16 (Gap-Fill Dispatch):** This step loops back to Step 12 for gap-fill rounds. The standalone tracking write fires at Step 16 entry, then Step 12's fold-in write overwrites it when the loop continues. This is correct behavior.

- **Step 18 (Research Completion):** This step writes `phase: "research_complete"`. In the SAME write, also set `current_step: null`.
  </action>
  <verify>
    <automated>grep -c "current_step" skills/research/SKILL.md</automated>
  </verify>
  <done>
- All 18 steps have `current_step` tracking at step entry
- Steps 1, 2, 4, 5, 6, 7, 8, 9, 11, 13, 14, 16, 17 use standalone backup-before-write pattern
- Steps 3, 10, 12, 15, 18 fold into existing state writes
- Step 18 clears `current_step` to null on completion
- All labels match the exact step heading text
- grep returns 25+ occurrences of "current_step" in the file
  </done>
</task>

</tasks>

<verification>
1. `grep -c "current_step" skills/scope/SKILL.md` returns 15+ (10 step tracking writes + clear + labels)
2. `grep -c "current_step" skills/research/SKILL.md` returns 25+ (18 step tracking writes + clear + labels)
3. Every `### Step` heading is followed by a tracking instruction (either standalone or folded)
4. Step 10 (scope) and Step 18 (research) set `current_step: null` on completion
5. No duplicate backup-before-write cycles in steps that already write state.yml
</verification>

<success_criteria>
- Every numbered step in scope (10 steps) writes `current_step` on entry with correct skill name and label
- Every numbered step in research (18 steps) writes `current_step` on entry with correct skill name and label
- Steps with existing state writes fold `current_step` in (no double I/O)
- Completion steps clear `current_step` to null
</success_criteria>

<output>
After completion, create `.planning/phases/15-step-level-tracking/15-02-SUMMARY.md`
</output>
