---
phase: 25-state-splitting-and-hook-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - skills/scope/SKILL.md
  - skills/research/SKILL.md
  - skills/design/SKILL.md
  - skills/plan/SKILL.md
  - skills/spike/SKILL.md
  - skills/execute/SKILL.md
  - skills/status/SKILL.md
  - skills/shared/ref-state-recovery.md
autonomous: true
requirements: [STATE-01]

must_haves:
  truths:
    - "Each skill's frontmatter injects only the state files it needs per the consumption matrix"
    - "Scope and research skills inject state.yml + checkpoint.yml + questions.yml"
    - "Execute skill injects state.yml + checkpoint.yml + tasks.yml"
    - "Gate evaluation and status skills inject state.yml + checkpoint.yml + gates.yml"
    - "Design, plan, and spike skills inject state.yml + checkpoint.yml only"
    - "State recovery protocol creates all 5 split files instead of one monolithic file"
  artifacts:
    - path: "skills/scope/SKILL.md"
      provides: "Scope skill with scoped frontmatter injection"
      contains: "questions.yml"
    - path: "skills/research/SKILL.md"
      provides: "Research skill with scoped frontmatter injection"
      contains: "questions.yml"
    - path: "skills/execute/SKILL.md"
      provides: "Execute skill with scoped frontmatter injection"
      contains: "tasks.yml"
    - path: "skills/status/SKILL.md"
      provides: "Status skill with scoped frontmatter injection"
      contains: "gates.yml"
    - path: "skills/shared/ref-state-recovery.md"
      provides: "Updated recovery protocol for 5-file split"
  key_links:
    - from: "skills/scope/SKILL.md"
      to: ".expedite/questions.yml"
      via: "frontmatter !cat injection"
      pattern: "cat .expedite/questions.yml"
    - from: "skills/execute/SKILL.md"
      to: ".expedite/tasks.yml"
      via: "frontmatter !cat injection"
      pattern: "cat .expedite/tasks.yml"
    - from: "skills/status/SKILL.md"
      to: ".expedite/gates.yml"
      via: "frontmatter !cat injection"
      pattern: "cat .expedite/gates.yml"
---

<objective>
Update all 7 skill frontmatter injections to use scoped state files per the consumption matrix, and update the state recovery protocol for the 5-file split.

Purpose: Currently every skill injects the entire monolithic state.yml (~500-2000+ tokens growing with lifecycle). After the split (Plan 01), each skill should inject only the state files it actually needs, reducing token waste by an estimated 60-80%. This is the core of STATE-01: scoped injection per consumption matrix.

Output: Updated SKILL.md files for all 7 skills with scoped frontmatter injection, and updated ref-state-recovery.md for the 5-file format.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-state-splitting-and-hook-infrastructure/25-01-SUMMARY.md
@.planning/research/agent-harness-architecture/design/PRODUCT-DESIGN.md (Consumption Patterns table)

<interfaces>
<!-- Consumption matrix from PRODUCT-DESIGN.md -->
| Consumer         | state.yml | checkpoint.yml | questions.yml | gates.yml | tasks.yml | session-summary.md |
|------------------|-----------|---------------|---------------|-----------|-----------|-------------------|
| All skills       | Frontmatter | Frontmatter | --           | --        | --        | Frontmatter       |
| Scope skill      | Frontmatter | Frontmatter | Frontmatter  | --        | --        | Frontmatter       |
| Research skill   | Frontmatter | Frontmatter | Frontmatter  | --        | --        | Frontmatter       |
| Design skill     | Frontmatter | Frontmatter | --           | --        | --        | Frontmatter       |
| Plan skill       | Frontmatter | Frontmatter | --           | --        | --        | Frontmatter       |
| Spike skill      | Frontmatter | Frontmatter | --           | --        | --        | Frontmatter       |
| Execute skill    | Frontmatter | Frontmatter | --           | --        | Frontmatter | Frontmatter     |
| Gate/Status skill| Frontmatter | Frontmatter | --           | Frontmatter | --      | --                |

Note: session-summary.md injection is Phase 29 work. For now, only inject the state files that exist.

<!-- Current frontmatter injection pattern (all skills use this) -->
```
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`
```

<!-- New frontmatter injection pattern (example for scope) -->
```
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`

Checkpoint:
!`cat .expedite/checkpoint.yml 2>/dev/null || echo "No checkpoint"`

Questions:
!`cat .expedite/questions.yml 2>/dev/null || echo "No questions"`
```

<!-- Current state.yml fields used by skills (for migration reference) -->
state.yml v1 contains: version, last_modified, project_name, intent, lifecycle_id, description,
  phase, current_task, current_step, imported_from, constraints, research_round,
  questions[], gate_history[], tasks[], current_wave

After split:
- state.yml v2: version, last_modified, project_name, intent, lifecycle_id, description, phase, started_at, phase_started_at
- checkpoint.yml: skill, step, label, substep, continuation_notes, inputs_hash, updated_at  (replaces current_step)
- questions.yml: research_round, questions[]  (replaces questions[] and research_round in state.yml)
- gates.yml: history[]  (replaces gate_history[] in state.yml)
- tasks.yml: current_wave, current_task, tasks[]  (replaces tasks[], current_wave, current_task in state.yml)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skill frontmatter to use scoped injection</name>
  <files>
    skills/scope/SKILL.md
    skills/research/SKILL.md
    skills/design/SKILL.md
    skills/plan/SKILL.md
    skills/spike/SKILL.md
    skills/execute/SKILL.md
    skills/status/SKILL.md
  </files>
  <action>
Update the frontmatter injection section of each skill's SKILL.md. Currently all skills have:

```
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`
```

Replace with skill-specific scoped injection per the consumption matrix. Each skill gets state.yml + checkpoint.yml (always), plus its specific additional files.

**Scope skill** (state.yml + checkpoint.yml + questions.yml):
```
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`

Checkpoint:
!`cat .expedite/checkpoint.yml 2>/dev/null || echo "No checkpoint"`

Questions:
!`cat .expedite/questions.yml 2>/dev/null || echo "No questions"`
```

**Research skill** (state.yml + checkpoint.yml + questions.yml):
Same as scope -- questions.yml contains the research questions and round tracking.

**Design skill** (state.yml + checkpoint.yml):
```
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`

Checkpoint:
!`cat .expedite/checkpoint.yml 2>/dev/null || echo "No checkpoint"`
```

**Plan skill** (state.yml + checkpoint.yml):
Same as design.

**Spike skill** (state.yml + checkpoint.yml):
Same as design.

**Execute skill** (state.yml + checkpoint.yml + tasks.yml):
```
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`

Checkpoint:
!`cat .expedite/checkpoint.yml 2>/dev/null || echo "No checkpoint"`

Tasks:
!`cat .expedite/tasks.yml 2>/dev/null || echo "No tasks"`
```

**Status skill** (state.yml + checkpoint.yml + gates.yml):
```
Current lifecycle state:
!`cat .expedite/state.yml 2>/dev/null || echo "No active lifecycle"`

Checkpoint:
!`cat .expedite/checkpoint.yml 2>/dev/null || echo "No checkpoint"`

Gates:
!`cat .expedite/gates.yml 2>/dev/null || echo "No gates"`
```

IMPORTANT: The injection section MUST remain between the frontmatter `---` block and the `# {Skill Name}` heading. This is where Claude Code's frontmatter dynamic content injection executes.

IMPORTANT: Each skill also needs its internal state read/write patterns updated. Skills currently read/write `current_step` to state.yml. After the split, `current_step` data moves to checkpoint.yml. Similarly:
- Skills that read/write `questions` need to read/write questions.yml instead of state.yml
- Skills that read/write `gate_history` need to read/write gates.yml instead of state.yml
- Skills that read/write `tasks`, `current_task`, `current_wave` need to read/write tasks.yml instead of state.yml
- Skills that read/write `research_round` need to read/write questions.yml instead of state.yml

However, the full internal state read/write refactoring is a much larger change (each skill is 500-860 lines with dozens of state.yml read/write operations). For this plan, ONLY update the frontmatter injection section. The internal refactoring is deferred -- skills will temporarily read from both injected frontmatter (new split files) and on-demand reads (state.yml for fields that haven't been migrated yet). This is safe because:
1. The frontmatter gives skills the new scoped data
2. Skills still `cat state.yml` on demand for fields not yet migrated
3. The old monolithic state.yml will still exist during the transition (the template change from Plan 01 only affects NEW lifecycles)

Add a comment at the top of each skill's instructions section noting the migration status:
```
<!-- v2.0 Migration: Frontmatter injection updated for split state files. Internal state read/write patterns still reference monolithic state.yml and will be updated in skill-thinning phase (M4). -->
```
  </action>
  <verify>
    <automated>cd /Users/jaredwallenfels/Desktop/Projects/expedite-plugin && node -e "
const fs = require('fs');
const expected = {
  scope: ['state.yml', 'checkpoint.yml', 'questions.yml'],
  research: ['state.yml', 'checkpoint.yml', 'questions.yml'],
  design: ['state.yml', 'checkpoint.yml'],
  plan: ['state.yml', 'checkpoint.yml'],
  spike: ['state.yml', 'checkpoint.yml'],
  execute: ['state.yml', 'checkpoint.yml', 'tasks.yml'],
  status: ['state.yml', 'checkpoint.yml', 'gates.yml']
};
let pass = true;
for (const [skill, files] of Object.entries(expected)) {
  const content = fs.readFileSync('skills/' + skill + '/SKILL.md', 'utf8');
  for (const f of files) {
    if (!content.includes('cat .expedite/' + f)) {
      console.log('MISSING: ' + skill + ' does not inject ' + f);
      pass = false;
    }
  }
  // Check that skills don't inject files they shouldn't
  const allFiles = ['state.yml', 'checkpoint.yml', 'questions.yml', 'gates.yml', 'tasks.yml'];
  for (const f of allFiles) {
    if (!files.includes(f) && content.includes('cat .expedite/' + f)) {
      console.log('EXTRA: ' + skill + ' injects ' + f + ' but should not');
      pass = false;
    }
  }
}
if (pass) console.log('All skills have correct scoped injection');
else { console.log('FAIL'); process.exit(1); }
" && echo "PASS"</automated>
  </verify>
  <done>All 7 skills inject exactly the state files specified in the consumption matrix -- no more, no less. Each skill has a migration comment noting the transition status.</done>
</task>

<task type="auto">
  <name>Task 2: Update state recovery protocol for 5-file split</name>
  <files>skills/shared/ref-state-recovery.md</files>
  <action>
Update `skills/shared/ref-state-recovery.md` to handle the 5-file state split. The recovery protocol currently only recreates state.yml. After the split, it needs to create all 5 files.

Changes needed:

1. **Reconstructed state.yml Template** section: Update the template to match the new slimmed-down v2 format:
```yaml
# Expedite Lifecycle State (v2)
# RECOVERED: state reconstructed from artifacts on {ISO 8601 UTC timestamp}
version: 2
last_modified: "{ISO 8601 UTC timestamp}"
project_name: "{extracted from artifact}"
intent: {extracted value or null}
lifecycle_id: {extracted value or null}
description: null
phase: "{inferred phase}"
started_at: null
phase_started_at: null
```
Note: version is 2 (number), not "1" (string). Fields current_step, current_task, current_wave, questions, gate_history, tasks, research_round, imported_from, constraints are GONE from state.yml.

2. **Add checkpoint.yml reconstruction** section. After writing state.yml, also write a minimal checkpoint.yml:
```yaml
# Recovered checkpoint
skill: null
step: null
label: null
substep: null
continuation_notes: "State recovered from artifacts. Resume position unknown."
inputs_hash: null
updated_at: "{ISO 8601 UTC timestamp}"
```
This signals to skills that no checkpoint exists and they should use artifact-based resume as fallback.

3. **Add questions.yml reconstruction** section. If the recovery source is SCOPE.md and it contains questions, attempt to reconstruct questions.yml from SCOPE.md's question tables. Otherwise create an empty template:
```yaml
research_round: 0
questions: []
```

4. **Add gates.yml reconstruction** section. Create an empty template:
```yaml
history: []
```

5. **Add tasks.yml reconstruction** section. If the recovery source is PLAN.md, attempt to reconstruct tasks from the plan structure. Otherwise create an empty template:
```yaml
current_wave: null
current_task: null
tasks: []
```

6. **Update the "Post-Recovery Instructions"** section to note that all 5 files are now created during recovery.

Keep the existing artifact scanning logic (Step 1-4) unchanged -- it correctly scans PLAN.md > DESIGN.md > SYNTHESIS.md > SCOPE.md in reverse lifecycle order. Only the reconstruction output changes.
  </action>
  <verify>
    <automated>cd /Users/jaredwallenfels/Desktop/Projects/expedite-plugin && node -e "
const fs = require('fs');
const content = fs.readFileSync('skills/shared/ref-state-recovery.md', 'utf8');
const checks = [
  ['version: 2', 'v2 state format'],
  ['checkpoint.yml', 'checkpoint recovery mentioned'],
  ['questions.yml', 'questions recovery mentioned'],
  ['gates.yml', 'gates recovery mentioned'],
  ['tasks.yml', 'tasks recovery mentioned'],
];
let pass = true;
for (const [pattern, label] of checks) {
  if (!content.includes(pattern)) {
    console.log('MISSING: ' + label);
    pass = false;
  }
}
// Verify old monolithic fields are NOT in the template
const oldFields = ['current_step:', 'current_task:', 'questions: []', 'gate_history: []'];
for (const field of oldFields) {
  // Only check within the state.yml template section, not the questions.yml section
  const stateTemplate = content.split('checkpoint.yml')[0]; // rough section before checkpoint
  if (stateTemplate.includes(field) && field !== 'questions: []') {
    console.log('OLD FIELD still in state template: ' + field);
    // Don't fail on this -- just warn
  }
}
if (pass) console.log('Recovery protocol updated for 5-file split');
else { console.log('FAIL'); process.exit(1); }
" && echo "PASS"</automated>
  </verify>
  <done>State recovery protocol creates all 5 split files during recovery. state.yml template uses v2 format. checkpoint.yml, questions.yml, gates.yml, tasks.yml are all created with safe defaults.</done>
</task>

</tasks>

<verification>
1. Each skill injects exactly the state files from the consumption matrix
2. No skill injects state files it does not need
3. State recovery protocol creates all 5 split files
4. Recovery state.yml uses v2 format (version: 2, no monolithic fields)
5. Migration comments present in all skill files
</verification>

<success_criteria>
- Scope and research skills inject state.yml + checkpoint.yml + questions.yml
- Execute skill injects state.yml + checkpoint.yml + tasks.yml
- Status skill injects state.yml + checkpoint.yml + gates.yml
- Design, plan, spike skills inject state.yml + checkpoint.yml only
- State recovery protocol creates all 5 files with safe defaults
- No skill injects files outside its consumption matrix row
</success_criteria>

<output>
After completion, create `.planning/phases/25-state-splitting-and-hook-infrastructure/25-03-SUMMARY.md`
</output>
