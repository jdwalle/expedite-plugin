---
phase: 28-checkpoint-based-resume
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - skills/scope/SKILL.md
  - skills/research/SKILL.md
  - skills/design/SKILL.md
  - skills/plan/SKILL.md
  - skills/spike/SKILL.md
  - skills/execute/SKILL.md
autonomous: true
requirements: [RESM-01, RESM-04]

must_haves:
  truths:
    - "User resumes any skill and lands on the exact step recorded in checkpoint.yml"
    - "checkpoint.skill must match the invoked skill for checkpoint-based resume to activate"
    - "Artifact-existence checking is secondary fallback only when checkpoint is missing or skill mismatches"
    - "Cross-reference: if checkpoint says step N but state.yml says skill_complete, state wins"
  artifacts:
    - path: "skills/scope/SKILL.md"
      provides: "Checkpoint-based resume logic in Step 1 Case B"
      contains: "checkpoint.skill"
    - path: "skills/research/SKILL.md"
      provides: "Checkpoint-based resume logic in Step 1 Case B replacing artifact-existence heuristics"
      contains: "checkpoint.skill"
    - path: "skills/design/SKILL.md"
      provides: "Checkpoint-based resume logic in Step 1 Case B2 replacing artifact-existence heuristics"
      contains: "checkpoint.skill"
    - path: "skills/plan/SKILL.md"
      provides: "Checkpoint-based resume logic in Step 1 Case B2 replacing artifact-existence heuristics"
      contains: "checkpoint.skill"
    - path: "skills/spike/SKILL.md"
      provides: "No resume case needed (spike has no _in_progress phase)"
      contains: "checkpoint"
    - path: "skills/execute/SKILL.md"
      provides: "Checkpoint-based resume logic in Step 1 Case B replacing per-phase checkpoint heuristics"
      contains: "checkpoint.skill"
  key_links:
    - from: "skills/*/SKILL.md resume logic"
      to: ".expedite/checkpoint.yml"
      via: "Frontmatter injection reads checkpoint at skill start"
      pattern: "checkpoint\\.skill.*matches"
    - from: "resume logic"
      to: ".expedite/state.yml"
      via: "Cross-reference: state phase wins over checkpoint"
      pattern: "state.*complete.*wins"
---

<objective>
Rewrite the resume logic in each skill's prerequisite check (Step 1) to use checkpoint.yml as the primary resume mechanism, with artifact-existence as a secondary fallback.

Purpose: Currently, each skill's resume case (Case B / Case B2) uses artifact-existence heuristics to determine the resume point. This is fragile -- partial artifacts confuse the heuristic, and the LLM interprets state differently on resume. Checkpoint-based resume is deterministic: same checkpoint always produces the same resume point.

Output: All skills with in_progress resume cases have deterministic checkpoint-based resume logic.
</objective>

<execution_context>
@/Users/jaredwallenfels/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaredwallenfels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/28-checkpoint-based-resume/28-01-SUMMARY.md

@.planning/research/agent-harness-architecture/design/PRODUCT-DESIGN.md (lines 690-713, Resume Flow)
</context>

<interfaces>
<!-- Resume flow from PRODUCT-DESIGN.md (the authoritative design) -->

```
On skill invocation (or context reset mid-skill):

1. Read .expedite/checkpoint.yml (via frontmatter injection, already in context)
2. Read .expedite/state.yml (via frontmatter injection, already in context)
3. If checkpoint exists and skill matches the invoked skill:
   a. Read step and label
   b. Verify artifacts expected from completed steps exist (secondary check)
   c. Report to developer: "Resuming [skill] from step [N]: [label]"
   d. Jump to step N. If substep exists, use continuation_notes as context.
4. Cross-reference: if checkpoint says "research, step 7" but state says "research_complete", the state wins
5. If checkpoint does not match or no checkpoint: start from step 1
```

<!-- checkpoint.yml schema fields available for resume -->
```yaml
skill: "research"          # Must match invoked skill name
step: 7                    # Step number to resume at
label: "Pre-Validate Sources"  # Human-readable step name
substep: "waiting_for_agents"  # Sub-state for mid-step resume
continuation_notes: "3 of 5 web researchers dispatched..."  # Context
inputs_hash: "sha256:..."  # Not used for resume, used for idempotency
updated_at: "2026-03-12T12:45:00Z"
```

<!-- Skills with resume cases that need updating -->
- scope: Case B (scope_in_progress) -- currently uses questions/intent/project_name presence heuristics
- research: Case B (research_in_progress) -- currently uses artifact-existence decision tree
- design: Case B2 (design_in_progress) -- currently uses DESIGN.md/HANDOFF.md existence
- plan: Case B2 (plan_in_progress) -- currently uses PLAN.md existence
- spike: No resume case (operates within plan_complete or execute_in_progress, no spike_in_progress phase)
- execute: Case B (execute_in_progress) -- currently uses per-phase checkpoint.yml (different checkpoint)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite resume logic for scope and research skills</name>
  <files>
    skills/scope/SKILL.md
    skills/research/SKILL.md
  </files>
  <action>
Rewrite the resume logic in each skill's Step 1 prerequisite check to use checkpoint.yml as the primary resume mechanism.

**scope/SKILL.md -- Case B (scope_in_progress):**

Replace the current Case B resume logic (which checks questions count, intent presence, project_name presence to infer resume point) with checkpoint-based logic:

```
**Case B: Active lifecycle with `phase: "scope_in_progress"` AND `project_name` is set (not null)**

This is a resume scenario. Check the injected checkpoint above for deterministic resume.

**Checkpoint-based resume (primary):**
If the injected checkpoint shows actual values (not "No checkpoint" and not all-null) AND `checkpoint.skill` is "scope":
  1. Read `checkpoint.step` and `checkpoint.label`
  2. Cross-reference: if state.yml says scope_complete but checkpoint says scope step N, state wins -- treat as Case C instead
  3. Display:
     ```
     Found in-progress scope for "{project_name}" ({intent} intent).
     Checkpoint: step {checkpoint.step} -- {checkpoint.label}
     {If checkpoint.continuation_notes is not null: "Context: {continuation_notes}"}
     ```
  4. Use AskUserQuestion (same options as current: Continue / Start over)
  5. If Continue: Jump to Step {checkpoint.step}. Do NOT re-run earlier steps.
  6. If Start over: Execute archival flow, then proceed to Step 3.

**Artifact-based fallback (secondary):**
If checkpoint is missing, all-null, or checkpoint.skill is not "scope":
  {Keep the existing artifact-existence logic as fallback, prefixed with:}
  "Checkpoint unavailable or mismatched. Using artifact heuristic for resume."
  {Then the existing logic: If questions exist skip to Step 6, if intent/description skip to Step 5, etc.}
```

**research/SKILL.md -- Case B (research_in_progress):**

Replace the current Case B resume logic (the artifact-existence decision tree with research_round checks, evidence file counts, sufficiency-results.yml checks, SYNTHESIS.md checks) with checkpoint-based logic:

```
**Case B: Phase is "research_in_progress"**

This is a resume scenario. Check the injected checkpoint above for deterministic resume.

**Checkpoint-based resume (primary):**
If the injected checkpoint shows actual values AND `checkpoint.skill` is "research":
  1. Read `checkpoint.step` and `checkpoint.label`
  2. Cross-reference: if state.yml says research_complete but checkpoint says research step N, state wins -- the skill finished. Display: "Research already complete." Then STOP.
  3. Display progress summary:
     ```
     Found in-progress research for "{project_name}".

     Checkpoint: step {checkpoint.step} of 18 -- {checkpoint.label}
     {If checkpoint.substep: "Sub-state: {substep}"}
     {If checkpoint.continuation_notes: "Context: {continuation_notes}"}
     Research round: {research_round from state.yml}

     Continue from checkpoint?
     ```
  4. Use AskUserQuestion (same options as current: Continue / Start over)
  5. If Continue: Jump to Step {checkpoint.step}. If substep is populated, use continuation_notes as context for resuming mid-step work (e.g., if substep is "dispatching_agents" and notes say "3 of 5 dispatched", resume dispatching the remaining 2). Do NOT re-run Step 3 (state already research_in_progress).
  6. If Start over: Reset research_round to 0, clear question statuses, proceed to Step 2.

**Artifact-based fallback (secondary):**
If checkpoint is missing, all-null, or checkpoint.skill is not "research":
  Display: "Checkpoint unavailable or mismatched. Using artifact heuristic for resume."
  {Keep the existing artifact-existence decision tree as fallback -- the research_round checks, evidence file counts, sufficiency-results.yml, SYNTHESIS.md existence checks}
```

Important notes:
- Do NOT change the structure of Case A or Case C in either skill
- The AskUserQuestion structure (Continue / Start over) remains the same
- The cross-reference rule (state.yml phase wins over checkpoint) is critical -- prevents impossible states
- For research, the mid-step resume via substep/continuation_notes is a key value-add over the old heuristic approach
  </action>
  <verify>
    grep -c "checkpoint.skill" skills/scope/SKILL.md && grep -c "checkpoint.skill" skills/research/SKILL.md && grep "artifact.*heuristic\|artifact.*fallback" skills/scope/SKILL.md skills/research/SKILL.md | wc -l
    Expected: scope >= 1 mention of checkpoint.skill, research >= 1, both have artifact fallback mentions
  </verify>
  <done>Scope and research skills use checkpoint.yml as primary resume mechanism. Artifact-existence is secondary fallback. Cross-reference rule implemented. Mid-step resume context works for research agent dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite resume logic for design, plan, and execute skills</name>
  <files>
    skills/design/SKILL.md
    skills/plan/SKILL.md
    skills/execute/SKILL.md
  </files>
  <action>
Rewrite the resume logic for design (Case B2), plan (Case B2), and execute (Case B).

**design/SKILL.md -- Case B2 (design_in_progress, no --override):**

Replace the current Case B2 artifact-existence checks (DESIGN.md + HANDOFF.md presence) with checkpoint-based logic:

```
**Case B2: Phase is "design_in_progress" AND `--override` flag is NOT present**

This is a resume scenario. Check the injected checkpoint above for deterministic resume.

1. First, check gate_history for G2 recycle entries (same as current -- keep this check unchanged).

   {Keep the existing G2 recycle display and routing logic unchanged.}

2. **Checkpoint-based resume (primary):**
   If the injected checkpoint shows actual values AND `checkpoint.skill` is "design":
     a. Read `checkpoint.step` and `checkpoint.label`
     b. Cross-reference: if state.yml says design_complete but checkpoint says design step N, state wins -- treat as post-design. Display: "Design already complete." Then STOP.
     c. Display:
        ```
        Found in-progress design for "{project_name}".

        Checkpoint: step {checkpoint.step} of 10 -- {checkpoint.label}
        {If checkpoint.continuation_notes: "Context: {continuation_notes}"}
        Resume point: Step {checkpoint.step}
        ```
     d. Proceed directly to Step {checkpoint.step}. Do NOT re-run Step 3 (state already design_in_progress).

   **Artifact-based fallback (secondary):**
   If checkpoint is missing, all-null, or checkpoint.skill is not "design":
     Display: "Checkpoint unavailable. Using artifact heuristic for resume."
     {Keep existing artifact-existence checks: DESIGN.md exists + HANDOFF.md exists logic}
```

**plan/SKILL.md -- Case B2 (plan_in_progress, no --override):**

Same pattern as design. Replace Case B2 artifact check (PLAN.md existence) with checkpoint-based logic:

```
**Case B2: Phase is "plan_in_progress" AND `--override` flag is NOT present**

This is a resume scenario. Check the injected checkpoint above for deterministic resume.

1. First, check gate_history for G3 recycle entries (same as current -- keep unchanged).

2. **Checkpoint-based resume (primary):**
   If the injected checkpoint shows actual values AND `checkpoint.skill` is "plan":
     a. Read `checkpoint.step` and `checkpoint.label`
     b. Cross-reference: if state.yml says plan_complete but checkpoint says plan step N, state wins.
     c. Display:
        ```
        Found in-progress plan for "{project_name}".

        Checkpoint: step {checkpoint.step} of 9 -- {checkpoint.label}
        {If checkpoint.continuation_notes: "Context: {continuation_notes}"}
        Resume point: Step {checkpoint.step}
        ```
     d. Proceed directly to Step {checkpoint.step}. Do NOT re-run Step 3.

   **Artifact-based fallback (secondary):**
   If checkpoint is missing, all-null, or checkpoint.skill is not "plan":
     Display: "Checkpoint unavailable. Using artifact heuristic for resume."
     {Keep existing: PLAN.md exists -> Step 6, else -> Step 2}
```

**execute/SKILL.md -- Case B (execute_in_progress):**

The execute skill is special: it has BOTH a top-level checkpoint (.expedite/checkpoint.yml, tracking execute skill steps 1-7) AND a per-phase checkpoint (.expedite/plan/phases/{slug}/checkpoint.yml, tracking tasks within a phase). The resume logic must use both:

```
**Case B: Phase is "execute_in_progress"**

This is a resume scenario.

1. Parse the phase argument first (same logic as Step 2's argument parsing).
2. Determine the slug for the requested phase.

3. **Checkpoint-based resume (primary):**
   If the injected checkpoint (top-level .expedite/checkpoint.yml) shows actual values AND `checkpoint.skill` is "execute":
     a. Read `checkpoint.step` and `checkpoint.label`
     b. If checkpoint.substep starts with "executing_task_": this was mid-task execution. Resume at Step 5 (Task Execution Loop) for the task indicated in substep. Use continuation_notes for context.
     c. Otherwise: Resume at Step {checkpoint.step}.
     d. Display: "Resuming execution of {Wave/Epic} {N} from checkpoint (step {checkpoint.step}: {checkpoint.label})."
     e. If checkpoint.step is 5 (Task Execution Loop): Also read the per-phase checkpoint at `.expedite/plan/phases/{slug}/checkpoint.yml` for task-level resume context (current_task, tasks_completed).
     f. Proceed to Step 2 (to load all artifacts), then skip Step 3 and go to the checkpoint step.

   **Per-phase checkpoint fallback (secondary):**
   If top-level checkpoint is missing, all-null, or checkpoint.skill is not "execute":
     Display: "Top-level checkpoint unavailable. Using per-phase checkpoint for resume."
     {Keep existing per-phase checkpoint logic: read .expedite/plan/phases/{slug}/checkpoint.yml for current_task}
     4. If per-phase checkpoint exists AND status is "paused" or "in_progress": display "Resuming execution of {Wave/Epic} {N} from per-phase checkpoint..." Proceed to Step 2, skip Step 3, go to Step 4.
     5. If no checkpoint for this phase: display "Starting execution of {Wave/Epic} {N}..." Proceed to Step 2.
```

**spike/SKILL.md -- No resume case modification needed.**
Spike has no `spike_in_progress` phase. It operates within `plan_complete` or `execute_in_progress`. However, add a brief note to the spike skill's Step 1 that if checkpoint.skill is "spike" and the user re-invokes `/expedite:spike`, the checkpoint can orient them:

In the spike skill preamble (before Case routing), add:
```
**Checkpoint context:** If the injected checkpoint shows `checkpoint.skill: "spike"`, display: "Previous spike was interrupted at step {checkpoint.step}: {checkpoint.label}. {If continuation_notes: continuation_notes}" This is informational only -- spike always re-runs from the phase argument.
```

Important notes:
- Case A (normal entry) and Case C (error) remain unchanged in all skills
- Case B for design/plan (the --override entry path) remains unchanged
- The per-phase checkpoint mechanism in execute is preserved as a secondary layer
- Cross-reference rule (state.yml phase wins) is implemented in all skills
  </action>
  <verify>
    grep -c "checkpoint.skill" skills/design/SKILL.md && grep -c "checkpoint.skill" skills/plan/SKILL.md && grep -c "checkpoint.skill" skills/execute/SKILL.md && grep -c "checkpoint.skill" skills/spike/SKILL.md && grep "artifact.*fallback\|artifact.*heuristic\|per-phase.*checkpoint.*fallback" skills/design/SKILL.md skills/plan/SKILL.md skills/execute/SKILL.md | wc -l
    Expected: design >= 1, plan >= 1, execute >= 1, spike >= 1, all with fallback mentions
  </verify>
  <done>Design, plan, and execute skills use checkpoint.yml as primary resume mechanism. Artifact-existence is secondary fallback. Execute skill correctly layers top-level checkpoint (skill steps) with per-phase checkpoint (task tracking). Cross-reference rule implemented. Spike shows checkpoint context informationally.</done>
</task>

</tasks>

<verification>
1. All skills with resume cases (scope, research, design, plan, execute) use checkpoint.skill matching as primary resume mechanism
2. All resume cases have artifact-existence as a secondary fallback, not primary
3. Cross-reference rule implemented: state.yml phase (e.g., research_complete) wins over checkpoint step
4. Execute skill correctly uses both top-level checkpoint (skill steps) and per-phase checkpoint (task tracking)
5. Mid-step resume context (substep + continuation_notes) is used by research (agent dispatch) and execute (task loop)
6. Spike shows checkpoint context informationally without changing its re-run-from-scratch behavior
</verification>

<success_criteria>
- User resumes any skill and lands on the exact step recorded in checkpoint.yml (RESM-01)
- Artifact-existence checking is secondary fallback only when checkpoint.yml is missing or skill mismatches (RESM-04)
- Cross-reference prevents impossible states (checkpoint says in-progress but state says complete)
</success_criteria>

<output>
After completion, create `.planning/phases/28-checkpoint-based-resume/28-02-SUMMARY.md`
</output>
